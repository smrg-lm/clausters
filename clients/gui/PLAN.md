# Clausters GUI track - implementation plan

Milestones for the graphical-element system: a scriptable set of GUI widgets driven from a dynamic language (Python now, JavaScript later), covering an audio editor, navigable waveform/spectrogram views, custom instrument panels, a live node-tree view, shader canvases, editable music notation, and - later - an editable sequencer. This file is both the staged plan and the design rationale behind it; a settled, published summary of that rationale lives in the server book's `docs/clients.md` ("The GUI host: a scriptable peer"). Like the `PLAN.md` roadmaps, milestone labels (`Gx`) live only here, never in published docs or docstrings.

## What `clausters-gui` is (the naming)

`clausters-gui` is **two roles in one process**, and the plan only makes sense once both are explicit:

- **A GUI client of the audio server.** It talks to `clausters-server` over OSC exactly the way `clausters-python` does - same encoding, same transports (UDP/TCP/shared-memory ring/WebSocket), same `osc::decode_packet` door. It reads buffers, control buses, the node tree, and sends control messages. Anything `clausters-python` can do against the server, `clausters-gui` can do too.
- **A GUI server (host) for the languages.** It owns the windows, the widgets and the GPU, and exposes a widget protocol that the language clients drive. `clausters-python` and the web client send it a serialized JSON document - a **GuiDef**, built the same way a `SynthDef`/`GraphDef` is - to construct a window/widget tree, then drive live updates and receive interaction events.

So the topology has three legs:

```
  clausters-python / JS  ──GuiDef + control──▶  clausters-gui  (windows, widgets, GPU)
        │                                            │   ▲
        │                                            │   │ (a widget bound to the server
        └────────────── OSC ─────────────────▶  clausters-server   bypasses the script)
                                                     ▲   │
                                          OSC (gui is also a client)
```

1. **`clausters-python` <-> `clausters-server`** - the existing audio control path, unchanged.
2. **`clausters-python` <-> `clausters-gui`** - the new widget protocol: the script builds windows/widgets with a serialized JSON GuiDef and drives/reads them.
3. **`clausters-gui` <-> `clausters-server`** - the gui acting as an audio-server client (reading buffers/buses/node tree, sending control).

**The script can be bypassed.** When a widget is *bound* to a server destination (`/gui_bind`), its value flows straight from `clausters-gui` to `clausters-server` with no round-trip through `clausters-python` - the low-latency interactive path. Unbound widgets keep emitting events back to the script.

**Standalone is a first-class mode.** Because a GuiDef is a persisted document just like a `GraphDef`, a saved GuiDef can be paired with GraphDefs and run by `clausters-gui` against an embedded audio server - a self-contained application with **no separate language client running at all**.

## Architecture (converged)

The earlier sketch proposed a separate JSON-over-WebSocket protocol with one OSC message per widget. That was wrong on two counts; the corrected model, consistent with the existing server, is:

- **OSC is the single encoding everywhere.** A whole window/widget tree rides **as JSON inside an OSC argument**, exactly as a `SynthDef`/`GraphDef` does today through `/def_send synth` (`serde_json::from_slice` over an `OscType::String` or `OscType::Blob`; the server already accepts both - `src/osc/translate`). "JSON vs OSC" is a false dichotomy: JSON is the payload, OSC is the framing. serde's number handling distinguishes integers from floats, so ids stay `i32` and control values stay `f32` across the wire, the same rule the audio server relies on.
- **Construction is declarative; updates are incremental.** A GUI is a *def*: `/gui_def <id> <json tree>` builds the whole tree in one message (like a `SynthDef`), `/gui_set <id> ...` updates one live widget (like `/node_set`), `/gui_free <id>` frees a subtree. No per-widget construction chatter.
- **The GUI host is a sibling OSC front, not code in the audio server.** It reuses the existing transport layer (UDP/TCP/shared-memory ring/WebSocket, the `osc::decode_packet` door, `ClientId`) with a widget/GPU command interpreter in place of the audio engine. The audio server stays headless and RT-safe and is untouched by any of this.
- **The GUI host is also a client of the audio server.** It reads buffers, sends bound-widget values, mirrors the node tree, and - the payoff of the shared-memory IPC - reads the control buses **directly from the shared segment every frame**, so meters/scopes update with zero messages.
- **WebSocket is one more transport, primarily for the browser.** A browser cannot map shared memory or open raw UDP; it can only do WebSocket. The desktop host reuses the existing transports; the browser host uses `WsHub` (already in the core, see G1) - the same OSC encoding through the same decode door, not a new protocol.
- **Heavy widgets are the existing `wgpu` renderers.** The waveform and spectrogram are written once against `wgpu`/WGSL (the `TimelineView` trait) and run natively today and under WebGPU in a browser unchanged.

The widget tree is a hierarchy with client-allocated integer ids and subtree freeing - the shape of a DOM / scene graph (and, incidentally, the shape of the server's group/node tree), reached as a declarative document rather than mutated node by node.

### Crate / packaging boundary

`clausters-gui` is its **own crate**, kept out of the root `clausters` workspace so it can never break the core server build (the `clients/gui` crate already validates this). For end users it ships as a **separate package/binary** from the audio server and the Python client, because the GPU/windowing stack (`wgpu`, `winit`, font/shader assets) is large and should not bloat a headless server install or a `pip` wheel. The two move and version independently.

### Why a web-capable rendering substrate

The varied uses (editor, notation, instrument panels) pull toward a web-capable rendering substrate, for three concrete reasons, not fashion:

1. **Scriptability from JS is free** if the host *is* a web surface; Python drives the same surface over the same protocol (WebSocket).
2. **Music notation = Verovio.** Verovio is a C++ engraving library that renders MEI/MusicXML to **SVG**, and ships a **WebAssembly/JS** build. MuseScore (a whole Qt app) is not embeddable, so what we actually want - its notation capability - is Verovio's engraving. *(This originally read as an argument for a web surface: drop the JS build in and let the browser make the SVG interactive. G31 found the better seam - the SVG is parsed into a display list on the **client** side and drawn as ordinary mesh geometry, so notation costs the host no dependency and reaches the browser like every other widget. The argument for the substrate stands on 1 and 3; this point no longer carries it.)*
3. **One GPU stack, two targets.** `wgpu` *is* the WebGPU implementation. The heavy widgets (waveform, spectrogram) are custom GPU rendering either way; written against `wgpu`/WGSL they run natively today and under WebGPU in a browser unchanged. The prototype in this crate exists to prove that seam.

That substrate gives **one frontend and one GPU stack across all the targets**, which is why the browser staging (G11-G17) is incremental rather than a fork: the native desktop host comes first (fastest to iterate), the browser/WebGPU target is reached by swapping the native surface for a `<canvas>` while the renderers run unchanged, and an optional Tauri wrapper repackages the same web frontend as a native app.

What we explicitly reject: betting the whole thing on a single native Rust toolkit (egui/iced/Vizia/Makepad) as the scriptable layer. That would force us to invent a widget protocol *and* solve Verovio over FFI *and* give up the web - paying all three costs. Those toolkits are excellent for a monolithic Rust app, which is not what this is.

### Generic on the wire, typed in the renderer

The wire form is deliberately *generic* - any `{id, type, props, children}` - so the protocol never changes when a widget type is added. The renderer is the other half of that rule: it turns the generic tree into a *typed* model it knows how to lay out and draw, where adding a type is a new variant plus a handler, not a wire change. An unrecognized type is not an error - it is laid out (reserving its space) but not painted, so a host built today renders the parts of a newer GuiDef it understands and ignores the rest. That keeps the catalog extensible from both ends: the script may send types this host predates, and this host may render types a future script does not yet use.

### The host's two fronts and the windowing thread

The host has a headless protocol front (for tests, automation and no-display machines) and a windowed front; both run the same protocol logic, which is transport- and GPU-agnostic and *returns* its effects (replies, window open/close, repaint) rather than performing I/O, so it is unit-testable without a socket or a GPU. Windowing forces a threading shape: the GPU toolkit (winit) must own the main thread, so the OSC transport runs on a background thread and hands each datagram to the main thread through the event loop's proxy. All host state then lives on one thread, lock-free, mirroring the audio server's single-threaded command loop; replies go back out the shared socket. The per-window *render* is itself one shared, platform-agnostic function (`host::frame::render`): both fronts feed it a tree plus the window's GPU resources, so the browser front (a `<canvas>` WebGPU surface) is pixel-faithful by construction, not a parallel renderer - it differs from the desktop only in the surface, the loop driver and where the live inputs come from (the shared-memory bus natively; the server's `/bus_stream` bus snapshots over WebSocket in the browser, filling the same `BusSource` seam). The *interaction* is shared the same way (`host::interact`: hit-test, value/toggle/menu mutation), so a turned knob decides bound-vs-event identically on both.

The typed widget tree is the **single source of truth**, held by the host: the windowed front renders and hit-tests from it and writes interaction results straight back into it, so a script's `/gui_set` and a user's drag are the same edit to one tree and the next frame shows both. A control's value flows out as `/gui_event <id> <value>` to the script that built the window (whose address is captured at `/gui_def`), and a user-closed window as `/gui_closed <id>`; the heavy views send their navigation out the same way.

That "transport- and GPU-agnostic logic" claim is made structural by a **platform seam** (G11): the host is split into a platform-agnostic core (the widget tree, layout, protocol dispatch, the light-widget drawing) that compiles for the browser (`wasm32`) unchanged, and a native I/O shell behind four small traits - `Transport` (send OSC to the audio server), `BusSource` (a control bus, read from shared memory natively), `BulkLoader` (resolve a waveform/plot's local file to samples or a peak pyramid), and `DefStore` (persist named GuiDefs). The browser fills the same seams over its only carrier (WebSocket) and over `fetch`, reusing every line of the agnostic core; `check-wasm.sh` compiles the core for `wasm32` so it can never silently re-couple to native I/O. The in-process embedded server is, *for now*, the one explicitly native-only path - a browser host drives a *separate* audio server over WebSocket. That scope boundary is not a law: the "in-browser audio engine" track below relaxes it through the very same `Transport`/`ServerLink` seam.

### One drawing primitive for the light widgets

The heavy views (`waveform`, the spectrogram/scopes) own custom GPU pipelines; everything else - panel chrome, the control widgets, and the text of labels and values - is built from a single primitive: a batch of flat-colored triangles (rect/quad/line/disc) drawn by one pipeline. A knob is a disc plus a swept pointer; text is a compact embedded bitmap font emitted as one small quad per lit pixel into that same batch - no glyph texture, no second pipeline. So adding a control is composition, not new GPU code, and the heavy-view machinery stays the only place with bespoke shaders. Proportional/large text (a real font rasterizer) is a deliberate later refinement; the fixed-cell font is enough for instrument-panel labels and read-outs.

## Guiding principle: serve the server and its clients, and reuse what exists

Two constraints sit above every milestone:

- **A GUI must never pull focus from the audio server and its clients.** The point of the project is a headless, RT-safe server driven by OSC clients; `clausters-gui` is one more client and one more front, *in service of* that model, not a parallel universe. So the GUI adopts the server's semantics rather than inventing its own (OSC framing, the def model, the node-tree/id-allocation/subtree-free shape, the flat-primitives-at-the-boundary rule, the single `decode_packet` door), the audio server stays untouched and RT-safe, and no GUI convenience is allowed to fork the wire protocol or the client model. When a feature could live in the server/clients or in the GUI, it lives where the rest of the system already keeps it.
- **Stand on the existing crates; do not reimplement.** A large amount of what the GUI needs already exists in the server and the complementary crates and is meant to be reused. The default answer to "where does this behaviour come from?" is an existing module, behind the FFI when it is DSP.

Concrete reuse map (the things already implemented that the GUI builds on):

| Source | What it already provides | How the GUI reuses it |
|---|---|---|
| `clausters-core::osc` | UDP/TCP/WS/shared-ring transports, `decode_packet`, `ClientId`, OSC encode/bundle assembly, `WsHub` | Both legs: the host's server front (script -> gui) and its client leg (gui -> audio server) ride the *same* transports and decode door; no new protocol. |
| `clausters-core` def model (`SynthDef`/`GraphDef`/`d_recv`/defstore) | JSON-in-OSC defs, serde int/float distinction, on-disk persistence | The GuiDef *is* this pattern; `/gui_def` mirrors `/def_send synth`, and GuiDef persistence reuses the def-store machinery (the standalone mode, G10). |
| `clausters::embed` + shared-memory ring (the embed ABI) | In-process server, control-bus shared segment, garbage/command FIFOs | Zero-message meters/scopes read the shared buses each frame (G5); the standalone app embeds the server (G10). |
| `clausters-ffi` (C ABI, flat data) | `clausters_ws_*` client, timing/sample conversion, seeded RNG, numeric builtins | The host's audio-server-client leg and any DSP it needs go through the FFI, exactly as `clausters-python` does - one implementation, not two. |
| `clausters-midi` | the MIDI path | Feeds the future `timeline`/DAW sequencing view. |
| Python client (`session.Server`, `responders` `OscFunc`/`MidiFunc`, `ipc`, `_native`) | the reference client model and the encoder over the FFI | The script drives `clausters-gui` the same way it drives the server; the GUI's client leg reuses the client encoder rather than a parallel one; events mirror the responder model. |
| node-tree / group-node semantics (scsynth model) | client-allocated ids, add-actions, subtree free, `/g_query`-style introspection | The widget tree reuses this shape verbatim; the `nodetree` view (G8) reads the server's real tree through its existing query/notification path. |
| `clients/gui` crate renderers (`viewport`/`peaks`/`Stft`/`TimelineView`/`bytes`) | the heavy GPU views and resolution-matched analysis with a cache | The heavy widgets; the analysis is a candidate to migrate behind the FFI so signal code lives once (G7). |

## The OSC vocabulary (generic terminology)

The protocol is deliberately generic - not "windows". The root of a GuiDef may be a window, but it may equally be an embeddable panel, so the address family is `/gui_*`, not `/win_*`. (`/ui_*` is the recorded alternative if `/gui_*` reads worse in practice; the point is *not* a window-specific verb.)

### Commands (script -> gui host)

| Address | Args | Meaning |
|---|---|---|
| `/gui_def` | `id, json` | Build a whole GUI tree (window/panel + widgets) from a JSON GuiDef, in one message. The `SynthDef`/`/def_send synth` analogue. |
| `/gui_set` | `id, k, v, ...` | Update live properties of one widget (value, range, label, color, data ref, ...). The `/node_set` analogue. |
| `/gui_bind` | `id, target...` | Bind this widget's value to forward straight to the audio server, bypassing the script (see below). |
| `/gui_free` | `id` | Destroy a widget and its subtree (or a whole def by its root id). The group-free analogue. |
| `/gui_query` | `id` | Request a `/gui_info` reply describing a widget's current state. |
| `/gui_load` | `name` | Load a persisted GuiDef from the def store by name (standalone / saved-app path). |

### Events and replies (gui host -> script)

| Address | Args | Meaning |
|---|---|---|
| `/gui_event` | `id, value...` | A widget was interacted with (knob turned, button pressed, region selected, point dragged). |
| `/gui_info` | `id, type, k, v...` | Reply to `/gui_query`. |
| `/gui_closed` | `id` | A window was closed by the user. |

Property values are OSC primitives (int/float/string/blob) - the binding technology never leaks across the wire, in line with the project's "flat primitives at the boundary" rule.

### Bindings: the value can bypass the script

`/gui_bind` lets a widget's value flow **straight to the audio server** without a round-trip through the script - the same idea already used for MIDI in this project, where a control source is bound to a server-side destination instead of being polled. A knob bound to a synth control sends an OSC `/node_set` (or equivalent) to the audio server itself on every change; an unbound knob just emits `/gui_event` back to the script. This keeps interactive control low-latency while leaving scripted/computed widgets fully in the script's hands.

### Example session

```
# script -> gui host
/gui_def  1  { "type":"window", "title":"Filter", "w":480, "h":240, "layout":"col",
               "children":[
                 {"id":10,"type":"knob",  "label":"cutoff","min":20.0,"max":20000.0,"value":800.0},
                 {"id":11,"type":"slider","label":"res",   "min":0.0, "max":1.0,    "value":0.2},
                 {"id":12,"type":"waveform","buffer":0}
               ] }
/gui_bind 10 "server" "/node_set" 1000 "cutoff"   # knob 10 drives node 1000's cutoff directly

# gui host -> script  (only for the unbound slider; the knob talks to the server itself)
/gui_event 11 0.35
```

## The widget catalog (the JSON elements)

A GuiDef is a tree of nodes; each node is `{ "id": int, "type": str, <props...>, "children": [...] }`. Every widget type combines freely inside that tree. The catalog is meant to be **extensible** - adding a type is a new renderer/handler in the host, not a protocol change.

| Type | Category | Role |
|---|---|---|
| `window` | container | Top-level root container (title, size, layout). |
| `panel` / `box` | container | Nestable container; `layout` = `row` / `col` / `grid` / `free`. |
| `label` | control | Static text. |
| `slider` | control | Continuous value with range. |
| `knob` | control | Rotary continuous value. |
| `button` | control | Momentary action. |
| `toggle` | control | Boolean state. |
| `number` | control | Numeric entry field (int or float). |
| `text` | control | Text entry field. |
| `menu` | control | Dropdown / option list. |
| `waveform` | heavy GPU view | Editor-grade min/max peak waveform of a buffer/file/blob: multichannel lanes (stacked or overlaid), LOD-crossfaded zoom, adaptive rulers, selection, playhead, amplitude zoom/pan, linked navigation groups via `link` (G20-G20d); the measured layers — RMS, true peak, loudness — planned in the A track. |
| `spectrogram` | heavy GPU view | Editor-grade STFT time-frequency view, same sources and chrome as `waveform` plus a Hz ruler and frequency zoom/pan, linked groups via `link` (G20-G20d); a constant-Q analysis mode experimental (G20f). |
| `scope` | heavy GPU view | Time-domain scope over `channels` adjacent buses from `bus`, at `rate` (audio by default, control for a bus history): the audio-rate form is a triggered oscilloscope — lanes or `overlay` traces, ms/value rulers, a trigger-level mark and a lock/free read-out (G28, G33). |
| `phasescope` | view | Phase/goniometer (Lissajous) view of the audio bus pair `bus`/`bus + 1`, with a correlation readout (G19, G33). |
| `meter` | heavy GPU view | Level meter reading a control bus directly from shared memory. |
| `spectrum` | view | Live FFT magnitude curves (spectroscope), one per channel over `channels` adjacent audio buses from `bus`: adjustable dB window, linear/log/mel/bark frequency axis with Hz/dB rulers, averaging + peak-hold (G19, G28, G33). |
| `plot` | view | Static plot of an NRT-generated signal/file or a numeric sequence: multichannel lanes, x/y rulers, hover readout, signal/spectrum views (extensible). |
| `nodetree` | view | Live text/graphic view of the audio server's node tree and parameters, updated in real time. |
| `canvas` | view | A surface that runs a supplied WGSL shader, driven by OSC params or server audio - custom visuals. |
| `score` | view | Engraved music-notation page: the client engraves (Verovio) into a **display list** of glyph outlines and primitives in page units, which the host tessellates into the same mesh as the rest of the chrome. Scrolls/zooms in a `scroll`, carries a playback cursor over the score's own timemap, and reports the element under the cursor - a click as `"element"`, a drag as a `"transpose"` in diatonic steps (G31). |
| `timeline` | view | DAW-style tracks + MIDI/OSC sequencing - **future**. |
| `bpf` | view | Drawable break-point-function envelope: breakpoints + per-segment shapes using the server's own `EnvGen` shape numbers (evaluated through `clausters-core`), any `[min, max]` range (bipolar/unipolar/on-off via the hold shape), optional exponential display scale; edits flow back per the edit-back pattern (G21). |

Heavy views never reimplement DSP the server already owns: when a widget needs analysis/processing not already provided by `clausters-server` (peaks, STFT, FFT, resampling), `clausters-gui` reaches for `clausters-ffi`/`libclausters` rather than duplicating signal code. The `clients/gui` crate's own `peaks`/`spectrogram` modules are the prototype of that shared machinery and are a candidate to migrate behind the FFI.

## Heavy widgets: the rendering strategy

An editor-grade waveform or spectrogram cannot draw every sample, and does not need to: it only needs to resolve the signal to the *rendered resolution*. The work is therefore driven by `samples_per_px` (visible length / render width in device pixels), never by buffer length.

### Reusable machinery (the modules)

The navigation and analysis are factored out so the waveform and the spectrogram share them. Core, windowing-agnostic (web-portable) modules:

- `viewport::View` - the visible window in sample units (`f64`), with `zoom`/`pan`/`clamp` and `samples_per_px`. Pure, unit-tested, renderer-agnostic. The spectrogram reuses it verbatim.
- `peaks::Pyramid` - the resolution-matched **min/max peak analysis** (waveform): level 0 summarizes every `base_bucket` samples into a `(min, max)` pair, each higher level halves the resolution, and `level_for(samples_per_px)` selects the level whose bucket matches the zoom so each pixel column reads ~one bucket.
- `spectrogram::Stft` - the **STFT analysis** (spectrogram): `n_frames` x `n_bins` magnitudes from a windowed FFT, the time-domain analogue of the peak pyramid.
- `view::TimelineView` - the trait both views implement (`total_samples`, `upload`, `draw`), so one harness drives either.
- `bytes` - shared little-endian (de)serialization for the caches.
- `waveform`/`spectrogram` renderers - the GPU pieces built on the above.

Native-only helpers (excluded from wasm): `native` (a winit + wgpu windowing harness generic over `TimelineView`) and `demo` (the synthetic test signal). A web build swaps `native` for a `<canvas>` surface and keeps everything else.

### The analysis is a cache (memory or temp file)

Computing the analysis for a long file is the one expensive pass, so both `Pyramid` and `Stft` are treated as caches, the way audio editors keep an overview/peak file beside the audio: each lives in memory and serializes to/from a flat byte buffer (`to_bytes`/`from_bytes`) or a temp/cache file (`write_cache`/`read_cache`) via the shared `bytes` module. The layout is a flat sequence of `f32` arrays so a production build can **memory-map** it instead of reading it into RAM. The peak pyramid is ~2x its level-0 size (a small constant fraction of the source); the STFT is `n_frames * n_bins` floats.

### Bulk data moves through local shared resources, not the wire

Two decisions follow once the data gets large. **First, where the analysis lives.** The peak pyramid and the forward FFT are not GUI-private: a pyramid is general client functionality (any client with a waveform view), and the FFT is shared with the server's `FFT`/`IFFT` UGens. So they live **once** in `clausters-core` (`peaks`, `fft` over `microfft`) and are reached from non-Rust clients through the FFI; this crate's `peaks` is a re-export and the spectrogram calls the core FFT. **Second, how the bytes move.** A multi-megabyte buffer cannot ride an OSC datagram (the ~64 KB cap), and chunking it over `/buffer_getRange` re-traverses the network asynchronously for data already in local RAM. So large payloads move as **memory-mapped files**: a `waveform` names a `path` (raw `f32`) or a `cache` (a prebuilt pyramid) the host maps read-only and reads zero-copy, and a server buffer reaches the same path via `/buffer_export` (a raw dump to a local file) rather than `/buffer_getRange`. The network reads stay the async fallback, and the browser - which can map neither shared memory nor files - is exactly that fallback's client: `path`/`cache` become URLs fetched against the page origin (the pyramid for raw fetches built in wasm from the same core), and a server buffer rides chunked `/buffer_getRange` over WebSocket through the shared fetch machine (`host::fetch`); the synchronous `BulkLoader` trait stays the native (mmap) fill, since a fetch cannot block. This is the same move the control buses made in the audio path (read from the shared segment, zero messages), generalized to bulk audio.

### Three render regimes (no wasted work, and never "by samples" naively)

Per frame the waveform renderer picks one by `samples_per_px`, so it is always bounded by the screen:

- **Line** (`samples_per_px <= 2`): few enough samples are visible that individual ones matter - draw a polyline through the raw samples in range (vertex count bounded by window width). This is the only regime that touches raw samples directly, and only when it is cheap to.
- **Raw columns** (`2 < samples_per_px < base_bucket`): one min/max column per pixel computed directly from raw samples - exact, and bounded because we are below `base_bucket` samples/px.
- **Pyramid columns** (`samples_per_px >= base_bucket`): one min/max column per pixel read from the peak level matching the zoom.

### The spectrogram: same navigation, constant render cost

The spectrogram is the time-frequency analogue and deliberately reuses the navigation machinery. The STFT magnitudes are uploaded once as a 2D texture (x = time/frame, y = frequency bin). Rendering is a single full-screen quad whose fragment shader samples that texture; `viewport::View` only reshapes the sampled time slice, so the GPU cost is **constant regardless of zoom** (it is bounded by screen pixels, and the one-time analysis is the cache). The GPU's linear filtering gives resolution-matched down-sampling on zoom-out. So the waveform bounds work by *picking a resolution-matched LOD per frame*, while the spectrogram bounds it *structurally* (one texture sample per pixel) - two expressions of the same "never resolve finer than the screen" rule. Display controls (linear/log/mel/bark frequency axis, dB window/contrast, colormap) live entirely in the fragment shader as cheap uniforms, so they change live without re-analyzing; the analysis parameters that *do* require recomputation - window size, hop, sample rate - are arguments to `Stft::compute`.

Notation (`"score"` widget) is a third shape, and the cheapest: **all geometry, no analysis**. An engraved page is a fixed drawing - glyph outlines and strokes in page units - so there is nothing to resolve per frame and no LOD to pick; the page is tessellated into the flat `paint::Mesh` the light widgets use, and zoom is the containing `scroll`'s affine. What makes it fit here at all is that the *engraving* (the expensive, format-reading part) happens on the client, once per edit, exactly as the peak pyramid and the STFT are computed once per source - the same "the host draws, the client analyzes" split, with the display list as its cache (G31).

### Open questions in the rendering strategy

Design-level questions the heavy views still leave open, distinct from the staged milestones above (interpolating between adjacent pyramid levels for smoother zoom-out and the edit-back-to-data pattern have since landed - the LOD crossfade in G20 and the `bpf` editor in G21):

- **Cache lifecycle**: a cache key (source path + mtime + analysis params) and memory-mapping the cache file instead of reading it into RAM.
- **Spectrogram scaling**: time-axis mipmaps or tiling for buffers wider than the max texture size; a smoother (interpolating) log resample.
- **Migrating the rest of the `Stft` machinery behind `clausters-ffi`/`libclausters`** so the signal code lives once: done for `peaks` and the forward FFT (both now in `clausters-core`, the pyramid reachable over the FFI); the `Stft` windowing/normalization and the inverse FFT (for resynthesis UGens) remain, the latter waiting on the server's `FFT`/`IFFT` UGens.

## Status: foundation in place

The `clients/gui` crate (an independent workspace, so it can never break the core build) already validates the heavy-rendering path - the `Gx` work below builds the protocol/host around it. The prototype layout:

```
src/lib.rs           module index
src/viewport.rs      View + zoom/pan (unit-tested)
src/peaks.rs         Pyramid peak analysis + cache (unit-tested)
src/spectrogram.rs   Stft analysis + FFT + cache + renderer (unit-tested)
src/spectrogram.wgsl full-screen quad, texture sample, viridis colormap
src/waveform.rs      WaveformData + WaveformRenderer (3 regimes)
src/waveform.wgsl    passthrough shader (columns + line pipelines)
src/view.rs          TimelineView trait (incl. optional char/vertical hooks)
src/bytes.rs         shared little-endian cache (de)serialization
src/native.rs        winit + wgpu harness driving any TimelineView
src/demo.rs          synthetic test signal
src/bin/waveform.rs      waveform binary
src/bin/spectrogram.rs   spectrogram binary
```

`cargo test` exercises the reusable machinery without a GPU: navigation math, peak correctness vs brute force, FFT correctness (impulse -> flat spectrum, cosine -> peak at its bin), STFT frequency localization, and cache round-trips (memory and temp file). `cargo run --bin waveform` and `cargo run --bin spectrogram` open the two windows (need a display and a Vulkan/Metal/DX12/GL adapter). The split is the point: the renderers take a `wgpu::Device`/`Queue` and a target format and own nothing windowing-specific, so `native` is just a driver, swappable for a `<canvas>` WebGPU surface; adding a view is a new module implementing `TimelineView` plus a one-screen binary, no new windowing or input code. This validates the load-bearing claim: the heavy, custom, GPU-bound widgets can be written once against `wgpu`/WGSL and run both natively and on the web, while the *composition* of widgets is a scripted protocol, not compiled Rust.

And on the core/server side, the transport the web direction needs is already done (G1, below).

## Completed milestones (foundation + desktop host, G1-G10)

Each landed with its detail in the git history; the design rationale that outlived them is in this file's design sections above and `docs/decisions.md`.

- ✅ **G1 - WsHub transport**: a WebSocket transport carrying the existing OSC encoding (`src/osc/ws.rs`, `ClientId::Ws`, the `clausters_ws_*` FFI client), one OSC packet per binary frame through `osc::decode_packet`.
- ✅ **G2 - GUI host skeleton** (`clausters-gui` binary): the dual-role process, headless first — links `clausters-core` (not the server crate) for the shared OSC seam and owns a thin UDP transport front; a generic GuiDef node, the widget registry (node-tree shape), the transport-agnostic command loop, and the Python driver `clausters.gui`.
- ✅ **G3 - `/gui_def` + JSON widget tree + a real window**: the typed widget schema (renderer's interpretation of the generic node), a pure layout engine, and the winit-on-main-thread windowed front rendering the heavy `waveform` with zoom/pan; `--headless` for no-display runs.
- ✅ **G4 - Standard control widgets + `/gui_set` + events**: `slider`/`knob`/`number`/`button`/`toggle`/`menu`/`text` over one mesh `Painter` + a bitmap font (no new GPU code); the typed tree as single source of truth so a live `/gui_set` and a user drag are one edit; interaction emits `/gui_event`/`/gui_closed`.
- ✅ **G5 - GUI as a client of the audio server + shared-memory meters/scopes**: a bidirectional client leg; a read-only `SharedSegment` mmapping the `--shm` segment (versioned ABI checked on attach) so `meter`/`scope` read a control bus each frame with zero OSC; server-buffer waveforms fetched over `/buffer_getRange`. The GUI mirrors the segment's binary ABI rather than linking the server crate.
- ✅ **G6 - Bindings (`/gui_bind`): bypass the script**: a `widget id -> Binding` map so a bound widget forwards its value straight to the audio server; the unbound case keeps emitting `/gui_event`; bindings pruned on free/redefine.
- ✅ **G7 - Bulk data path + shared DSP**: heavy data moves between processes via local shared resources (mmap files, exported server buffers), never re-encoded over OSC (the network reads stay the async fallback); the forward FFT (`microfft`) and the peak pyramid live once in `clausters-core`, peaks reachable over the FFI so a client builds the identical cache the host maps.
- ✅ **G8 - Node-tree view + NRT plots**: `nodetree` (a pure parser of `/group_queryTree.reply`, refreshed off `/server_notify`) and `plot` (a lightweight decimated static view), both cheap flat-geometry views added by extension, the server untouched.
- ✅ **G9 - Canvas + shaders**: a `canvas` widget running a script-supplied WGSL shader over its area, params driven from OSC and from a control bus read out of shared memory each frame; a failed compile is caught, not fatal.
- ✅ **G10 - Standalone GuiDef + GraphDef bundles**: a bundle (a named GuiDef beside its GraphDefs) boots as a self-contained instrument via `--standalone <name>` against an embedded server, no language client — GuiDefs persist like the server's defs, a root `boot` list and widget `bind` props make a saved tree self-driving. The `standalone` feature links `clausters` (`embed,realtime`); `ServerLink::{Udp,Embed}`.

## Browser / WebGPU target (G11-G17)

The earlier single milestone framed the web host as "swap the winit surface for a `<canvas>` surface; the renderers run unchanged" - true, but it describes the small part. The host is largely native-only I/O glue against a smaller core of genuinely portable renderers/analysis and *pure* host logic (the typed tree, layout, GuiDef parse, the mesh `Painter`/`font`, the registry, the node-tree/scope models, bindings) that only sat behind the `wasm32` exclusion because it was never separated from the I/O glue. So the browser target is not a rewrite: it is **factor the host along the platform seam, then write one new I/O impl per native coupling, reusing everything else** - the protocol, the decode door, the analysis, the renderers and the whole pure core shared verbatim; only the platform shell (transport, bus source, bulk loader, GPU/surface bring-up) gets a second, web impl.

- ✅ **G11 - Host platform seam** (agnostic core + `Platform` traits, wasm build kept green): `pub mod host` made unconditional; only the I/O shell stays `#[cfg(not(wasm32))]`, behind small traits (`Transport`, `DefStore`, `BulkLoader`, `BusSource`). `check-wasm.sh` is the build gate so no later milestone re-couples the core to native I/O. **Decision:** the browser host always talks to a *separate* audio server over WebSocket (no in-process engine in the browser) — a scope boundary the "in-browser audio engine" track below relaxes, and the `Transport`/`ServerLink` seam is shaped to take the wasm-`Embed` variant without a protocol change.
- ✅ **G12 - Web surface**: `<canvas>` WebGPU + async GPU bring-up (no `block_on`) + render loop; the per-window render factored into a shared `host::frame::render` so the browser is pixel-faithful by construction. `web/build.sh` + `web/index.html`.
- ✅ **G13 - Web transport**: drive the browser host live over WebSocket — the browser front runs the real `Host` (shared protocol dispatch + a shared `host::interact`); a wasm-bindgen `GuiBridge` (`feed`/`def`/`poll`/`connect_server`) is the in-page binding surface, and `ServerLink::Ws` gives a bound widget the server bypass. The full TypeScript client stays a separate `clients/web` track — the harness here only tested the host; its W2 has since grown the product driver (`GuiHost` + the GuiDef builders) over this same binding surface.
- ✅ **G14 - Browser meters/scopes**: control buses over the wire — the server grew `/bus_stream periodMs bus...` (one subscription per client, transport-agnostic), feeding a message-based `BusSource` (`StreamedBuses`) so the same meter/scope drawing runs from streamed values. Fixed a `/server_notify`/stream leak on WS/TCP disconnect.
- ✅ **G15 - Browser bulk data**: fetch/blob and the `/buffer_getRange` fallback — the native buffer-fetch state machine extracted into a pure `host::fetch`; on the web `path`/`cache` resolve as URLs (`window.fetch`), the pyramid built in wasm from `clausters_core::peaks`. Fetch fills the `BulkLoader` seam through the event loop (it cannot block).
- ✅ **G16 - Packaging and native/browser parity**: `web/build.sh` + the pinned wasm-bindgen CLI (no wasm-pack/trunk); `web/index.html` the documented harness and `web/parity.html` the scripted headless parity pass; browser quick-start in `docs/clients.md`.
- ✅ **G17 - WebGL2 fallback**: browser reach where WebGPU is disabled — the wasm build enables wgpu's `webgl` backend alongside `webgpu` and picks per-runtime via `new_instance_with_webgpu_detection` (WebGPU where truly supported, WebGL2 otherwise, ~99% reach). Cheap because the crate already avoids compute shaders/storage buffers, runs FFT/peaks on the CPU, and lets naga translate WGSL to GLSL ES.

## Widget deepening (G18-G21): scopes, editor-grade views, edit-back

With the host complete on both platforms, the next arc deepens the **graphical elements themselves** - the catalog entries once marked *future* (`scope` as a real oscilloscope, `phasescope`, `spectrum`, `bpf`) plus the editor-grade refinement of the two heavy views and the edit-back-to-data pattern. The ordering criterion: every one of these is **driven from the Python client as it stands** (new `clausters.gui` builders over the unchanged `/gui_*` protocol - widgets are added by extension, never by protocol change), so each milestone lists its Python leg explicitly. The per-widget domain knowledge (trigger algorithms, goniometer geometry, LOD crossfade, envelope-shape math) lives in the `gui-widgets` skill; this plan stages the work. Packaging (Tauri) and the in-browser audio engine stay deliberately last - see "Future directions".

**Recurring analysis (every milestone in this arc, the G7b rule):** each new compute function gets an explicit placement decision, recorded with the milestone - **general** (useful to another client from Python, or to a future server feature) goes to `clausters-core`, with a `clausters-ffi` export when a non-Rust client consumes it; **display-only** (hit-testing, tick spacing, trigger alignment of a drawn window) stays in the gui crate. Peaks, the forward FFT and the windows already took the core path; of the known candidates below, the correlation metric (G19), the multichannel peak cache (G20) and the envelope shape math (G21, `clausters_core::envshape` with the server's `EnvGen` delegating; its FFI export deferred until a client evaluates envelopes client-side — the tap-reader precedent) have since landed core-side, and the G20c tick/label layout was analyzed display-only (gui-side) — leaving the per-bucket RMS descriptor (G20e, since folded into the A track's A1/A2 along with the rest of the loudness family) and the constant-Q transform (G20f, experimental).

- ✅ **G18 - Server audio tap + a real oscilloscope**: tap rings in the versioned `--shm` segment (ABI v2 → v3), created by a `/bus_tap tapIndex bus` command (not a UGen), with the browser sibling `/bus_tapStream` → `/bus_tapStream.reply` (a windowed blob per period). The `scope` gained an audio-rate mode (`tap`/`rate:"audio"`) with a hysteresis level trigger. The trigger search is display-only (gui-side); the ring reader's core move is deferred until a client needs to map-read taps.
- ✅ **G19 - Phasescope + live spectrum**: two tap-consuming views added by extension — `phasescope` (the 45°-rotated Lissajous with an age-faded trail + a Pearson-r bar) and `spectrum` (one forward FFT per frame, dB with adjustable window, log/linear axis, averaging + peak-hold). The new general functions — correlation and Lissajous geometry — landed in `clausters_core::measure` with FFI exports (ABI v6 → v7); display stays gui-side. Closes the catalog's four *future* scope entries with G18.
- ✅ **G20 - Editor-grade waveform + spectrogram**: the two heavy views at editor depth and the `spectrogram` finally host-wired. **Multichannel** as one cache file (`MultiPyramid`, CLPK v2, FFI CORE_ABI v7 → v8) so the client builds the identical cache; lanes stacked or overlaid. **LOD crossfade** (blend the two adjacent pyramid levels). **Rulers** (adaptive 1-2-5 time axis; the spectrogram's Hz ruler by inverting the shader's display→bin mapping). **Selection + playhead + readout** as shared `EditorProps` on both kinds, drawn in a second overlay pass through the one `frame::render` (plain drag selects, Shift+drag pans).
- ✅ **G20b - Configurable rulers: units per axis + side strips**: every axis is a configurable ruler in **its own strip** (bottom for x, left for y, independently optional). Time axis gains `"beats"` (`bar:beat` on the client's grid); the waveform amplitude axis gains `"db"`/`"bits"`/`"percent"`; `log_freq` grew into `freq_scale` = linear/log/mel/bark (new shader mappings). All live `/gui_set`. **Placement:** hertz↔mel/bark → `clausters_core::scale`, the bar/beat reads → `clausters_core::tempoclock`, six exported over FFI (CORE_ABI v8 → v9); tick spacing and the ladders stay gui-side.
- ✅ **G20c - Editor views: adaptive ruler layout + vertical zoom/pan**: the fixed pixel constants in `host/ruler.rs` gave way to a layout that **measures its labels** (`font::width`/`height` at the ruler scale, device pixels): each unit's ladder (1-2-5 decimal, musical, dB rungs, decades) is tried against its *own* formatted labels and the smallest colliding-free step wins (property test: no two labels overlap for any window/zoom/unit/strip size). Every tick generator now lays out over the **visible sub-range** of its axis, and the heavy views gained that vertical window: `y_start`/`y_len` in normalized display units on `EditorProps` (live `/gui_set`, `"view_y"` events; non-positive `y_len` resets) — the waveform maps its geometry through the visible amplitude slice, the host `spectrogram` drives `SpectrogramView`'s display-coordinate frequency window, so the cursor anchor holds across linear/log/mel/bark and the browser keeps display + `/gui_set` parity while gestures stay native (wheel on the y-ruler strip zooms, dragging it pans, `R` resets — see `docs/decisions.md`). **Placement:** tick spacing and label fitting are pixel concerns — display-only, gui-side.

- ✅ **G20d - Linked views: shared navigation groups, composable editor items**: the per-widget navigation/selection/playhead state (formerly a per-slot `View` in each front) extracted into one **group model owned by the host core** (`host/timeline.rs`: horizontal `View` + selection + playhead anchor per `GroupKey`); the GPU slots keep only pixels, per-widget state keeps only the y axis (G20c). Grouping is an **explicit `link` (int) prop** (decision recorded: an item may want *unlinked* views of one file, and a link may span sources), live via `/gui_set` (negative = unlink, carrying the current view along; joining adopts the group). A gesture on any member mutates the group and every member window repaints; `/gui_set` of `view_start`/`view_len`/`sel_*`/`playhead_at` on any member applies group-wide (the interception lives in the core `on_set`, so the browser gets it for free); `"view"`/`"selection"` events emit **once**, carrying the **interacted member's id** (decision: a designated group id is not a widget id and would break the `/gui_event` shape). Selection/playhead were **mirrored** group→members' `EditorProps` (single writer, no drift) — retired in E3, which reads the group directly; shared chrome stays plain composition - a linked lane under a ruled sibling sets `ruler:"off"`, no auto strip, no new container. The group navigates in **session-timeline units** with its length aggregated (max) over member extents and membership spanning windows - deliberately the seat of the future multitrack view, whose clip items become members with a per-member *placement* (offset), the one designed extension point (all members at offset 0 today). Placement: navigation/group state is protocol/display logic - gui-side host core, nothing for `clausters-core`. Python: `link=` on both heavy builders; `examples/gui_linked.py` composes a waveform + spectrogram of one NRT render in lockstep, single event stream, live re-linking. See `docs/decisions.md`.

- ✅ **G21 - BPF envelope editor + the edit-back-to-data pattern**: the first widget that *writes data back*, and the pattern it establishes (folding in the former "Edit-back-to-data" and "Automation / BPF view" future directions). The **`bpf` widget**: breakpoints `(time, value)` plus a per-segment shape/curve **using the server's own envelope shape numbers**, evaluated once per pixel column (painter geometry); interaction hit-tests points before segments — drag to move (times clamped monotonic), drag a segment vertically for curvature, Ctrl+click adds a point / removes the one under it; the widget is deliberately the future automation-lane shape — any `[min, max]` range (bipolar/unipolar; an on/off lane is the hold shape — SC's step jumps to the target at segment start) with an optional exponential display scale (`exp`), times over `[0, duration]` (0 = fit). **Placement (recorded):** the shape evaluation relocated to `clausters_core::envshape` with the server's `EnvGen` delegating to it (the FFT move); its FFI export is **deferred** until a client actually evaluates envelopes client-side (the tap-reader precedent — the Python leg maps breakpoints to `Env`, it does not evaluate curves). Breakpoint model/hit-testing/drag stay gui-side (`host/bpf.rs`, pure and unit-tested). **The edit-back pattern (recorded):** edited data flows back **to the script** as `/gui_event <id> "points" <t v shape curve ...>` — flat OSC primitives, ints kept int, new event *payloads* rather than new addresses (bulk data would ride one blob in the `samples_to_blob` layout) — and **to the server** through the binding path, `Host::forward_args`/`Binding::message_args` generalizing the bound widget-value forward to a flat list after the fixed prefix. The host's mapped resources stay **read-only**; editing gestures are native-only today with display + `/gui_set` parity in the browser (the editor-view posture). Python: the `bpf` builder plus `env_to_points`/`points_to_env` mapping to/from `clausters.defs.Env`; `examples/gui_bpf.py` draws an envelope and hears an `EnvGen` play it. See `docs/decisions.md`.

### G20e - RMS waveform layer — **superseded by the A track (A1 + A2)**

*Superseded 2026-08-12: the milestone's two halves are A1 (the per-bucket mean square in the pyramid, with its cache bump) and A2 (the drawn body, generalized into the signal element's `measure` prop and into the first layer of the stack). Its decisions were kept verbatim there — energy-exact aggregation, and the crossfade rather than a hard-off below `base_bucket`. The scope below stands as the record of what was designed here.*

The third classic waveform layer: the RMS body drawn inside the min/max envelope, the editor convention that separates dense peaks from perceived level.

**Scope:**

- **Placement analysis (the G7b rule, decided up front):** per-bucket RMS is a **general audio descriptor** - the Python client analyzing arrays/chunks headlessly and a future server analysis UGen/command both plausibly consume it, exactly the user-noted case - so it lands in `clausters-core`: an `rms` measure over slices/chunks (beside `measure::correlation`) and the peak pyramid growing per-bucket energy - CLPK v2 → v3 (v1/v2 caches still parse), the FFI exports bumped so `clausters.gui.peaks_cache_file` keeps building the identical cache the host maps. Only the drawing (the body layer, its color, the regime blend) stays gui-side.
- **Energy-exact aggregation (decision - record it):** store mean-square (or sum-of-squares) per bucket internally and expose RMS, so higher pyramid levels combine exactly (the RMS of a union is the root of the mean of the mean-squares) and the LOD crossfade blends without bias.
- **Rendering:** a second column layer inside the min/max envelope (±rms about zero, the editor convention) with its own color, LOD-crossfaded exactly like min/max; an `rms` prop toggling it live via `/gui_set`. **Decision (record it):** the zoomed-in Line regime (raw samples on screen) - fade the body out with the existing crossfade weight vs. hard-off below `base_bucket`.
- Python: the cache builder and `waveform` builder grow the RMS surface; the descriptor exposed through the `_native` wrappers for headless use; an example (or `gui_editor.py` extended).

**Acceptance:** the waveform draws the RMS body inside the min/max envelope with no pop across LOD switches; a Python-built v3 cache and a host-built pyramid draw identically; core RMS matches brute force on random chunks; v1/v2 caches still load.

### G20f - Constant-Q spectrogram - **experimental**

An exploratory alternative analysis for the `spectrogram` widget: a constant-Q transform (geometrically spaced center frequencies, window length inversely proportional to frequency, so Q = f/Δf is constant) gives uniform per-octave resolution - the musically faithful time-frequency picture the log-scaled STFT only approximates (an STFT's low bins are linearly spaced, so the log display stretches a handful of coarse bins across the bottom octaves). Experimental: it lands behind a prop, is evaluated for quality/cost against the STFT views, and is promoted or dropped on that evidence - the STFT stays the default either way.

**Scope:**

- **The analysis**: a CQT over the same sources, parameterized by `bins_per_octave` (12-48 typical), `fmin` and the source's Nyquist. **Decision (record it):** the algorithm - the Brown/Puckette FFT-based sparse-kernel CQT vs. the multi-rate hybrid (one STFT per octave, window/hop halving per octave, resampled or restacked) - decided by measured compute cost on editor-length files and by wasm friendliness (CPU-only, no threads assumed in the browser). Long-file capping follows the existing posture (`hop_capped`'s role) so the magnitude grid fits the 8192-wide texture.
- **Placement analysis (the G7b rule):** the transform is general audio analysis (Python chunk analysis and any future server spectral feature are plausible consumers, exactly like the forward FFT) - it lands in `clausters-core` beside `fft`/`window`, with the `Stft` pipeline shape (compute → magnitude grid → cache/texture) and an FFI export **deferred until a client actually consumes it** (the tap-reader precedent: record the decision, move on evidence). Display mapping, ruler ticks and props stay gui-side.
- **The widget surface**: a prop on the existing `spectrogram` kind (`analysis: "stft"` default / `"cqt"` plus the CQT params), extension by props - no new widget kind, no protocol change. Rendering reuses the full-screen-quad pipeline unchanged: the magnitude grid uploads as the same 2D texture; the rows are already geometrically spaced, so the shader's display→bin mapping is the identity on the log axis (and `freq_scale` interacts accordingly - **decision:** which scales stay meaningful under CQT rows, likely log/identity only). The frequency ruler inverts the bin→Hz law `f(k) = fmin·2^(k/bpo)`; with bins on the equal-tempered grid a note-name unit (`"note"`: C4, A4...) becomes nearly free - **decision (record it):** whether the note ruler ships with this milestone or waits for promotion.
- Python: the `spectrogram` builder grows the analysis props; an example comparing STFT and CQT views of the same harmonic element.

**Acceptance (experimental bar):** a CQT view of a bass-register arpeggio resolves the individual partials the log-STFT smears, with correct ruler/readout frequencies across zoom (G20c) and linked navigation (G20d); compute cost on an editor-length file is measured and recorded; the promote-or-drop decision is written down with the evidence.

## Multitrack timeline view (G22): the DAW-style track editor

The "DAW / timeline view" future direction, now firming up. It builds on exactly the seat G20d designed and named: the navigation group already navigates in **timeline sample units** with its length aggregated over member extents, and its own doc block declared "a clip item there is a member with a *placement* (offset) on the same shared timeline — G20d ships every member at offset 0; the per-member placement is the one designed extension point." G22 activates that point and draws on top of it, reusing the group model, the `EditorProps` chrome, the second overlay pass of `host::frame::render`, the `host::interact` hit-test, the heavy views as clip bodies, and the G21 edit-back pattern — no protocol change (a widget is added by extension). Staged in incremental, separately-shippable parts, each with its Python leg. **Placement analysis (the G7b rule, decided up front):** clip/track geometry, the offset arithmetic, hit-testing and snapping are protocol/display logic — **gui-side** (host core + `interact`), nothing for `clausters-core`, the same call G20d made for navigation state.

- ✅ **G22a - Per-member placement (the offset)**: the extension point G20d declared, activated as pure host-core plumbing. An `offset` (timeline samples) on `EditorProps` — per-member (unlike the group-wide `link`/`sel_*`/`view_*`), so a view's own data sample 0 sits at timeline position `offset`; a placed member lengthens its group timeline to `offset + extent` (`timeline_total` aggregates the *placed* extents). A change routes through the group model (`set_timeline_offset`: writes the one widget's offset, re-clamps the shared window with the same full-follows-growth rule as `set_timeline_total`, repaints every member) and rides `/gui_set offset` as a timeline key. The GPU body upload draws through the **placed** window (`frame::placed_nav` shifts the group window by `-offset`); the time ruler and the selection/playhead overlay keep the timeline-unit window. At `offset = 0` (the un-placed default) everything is the identity, so no existing view regresses. Tests: the placement lengthens the group and re-clamps a zoomed window; `placed_nav` shifts the body window; the offset parses/clamps.
- ✅ **G22b - `track`/`clip` widgets + the graphic-unit overlay** (the visible milestone): a new `WidgetKind::Track` lane container carrying `clip` children, and `WidgetKind::Clip` — a placed rectangle spanning `[offset, offset + dur]` in timeline sample units (the graphic unit, length = duration) with an optional inline decimated body. Drawn by the new `host::track` module (pure over a `Mesh`, unit-tested without a GPU, the static-view posture): a left header names the track, the lane field frames it, and each clip is a framed rectangle with its body and label. The window's tracks share **one time axis** (aligned lanes) — the frame renderer computes the common span (the longest clip end) and maps every lane through the same `View`, so a clip at a given offset lines up across tracks. `clip_x_range` is the pure placement helper (offset/dur → clamped x range). Scalar vertical (a `track` of events → piano-roll) defers to G22d; shared-group zoom/pan of the axis (vs. today's full-span static view) is a later refinement. Python: `track`/`clip` builders; `examples/gui_multitrack.py` composes three aligned lanes.
- ✅ **G22c - Clip edit-back (drag/resize)**: `host::interact` grows clip hit-testing on the overlay (`clip_hit` reconstructs the same `track::window_nav` the renderer drew, so pixels map identically) — drag the body → move `offset`; drag an edge → resize `dur` (edges before body, as `bpf` prioritizes points over segments), each snapped to the track's `snap` grid (`interact::snap`; 0 = whole samples). The native front grows a `Drag::Clip` (a press-time snapshot so a clamped edge never drifts); `clip_set` writes the placement, `emit_clip` delivers it as `/gui_event <id> "clip" <offset> <dur>` — flat OSC primitives, a new event *payload* not a new address, through `Host::forward_args`, exactly the G21 pattern; the widget tree stays the source of truth. Editing is native-only with display + `/gui_set` parity in the browser (the `bpf` posture); the bound path (forward to the server to re-schedule) and the arrangement re-render are the Fase 3 driver's job. Tests: `clip_hit` finds the clip and body/edge part, `snap`/`clip_part` are pure, `clip_set`/`clip_event_args` move and report. Python: `snap=` on the `track` builder; `examples/gui_multitrack.py` prints the `"clip"` edit-back stream.
- ✅ **G22d - Events track → piano-roll**: the scalar-vertical clip body, reusing the clip placement/edit-back whole. A `clip` with `notes` (a flat `[start, dur, pitch, …]` list, the `bpf`-points convention) draws a **piano-roll** instead of a waveform body: each note a bar placed in time on the same shared axis (`track::draw_piano_roll`, so the whole roll moves when the clip is dragged) and in pitch over the clip's `[min, max]` range. New geometry, deliberately the reused-body sibling of `draw_clip_body` — the clip is the graphic unit either way, so move/resize/edit-back and the aligned time axis come for free. Pitch→y is linear here; a note-name ruler would read `clausters_core::scale` (G20b), deferred until it earns its place. Python: `notes=` on the `clip` builder (an iterable of `(start, dur, pitch)`); `gui_multitrack.py`'s lead track is a piano-roll. Tests: a clip parses its notes (dropping a trailing partial triple); a piano-roll clip draws its note geometry.

- ✅ **G22e - Lane playhead + time ruler**: the lane grows the time chrome the heavy views already had, so a composition can be *watched* playing over its clips. A `track` carries an `EditorProps` of its own — the same struct, parsing and `/gui_set` keys the timeline views use, of which a lane reads the time `ruler` (a strip reserved under the lane by `track::lane_body`, **off** by default so an un-rulered multitrack keeps its layout; the tick math is `frame::draw_time_ruler`, unchanged) and `playhead_at` (the sample-clock value at timeline position 0, so the line lands at `sample_clock - playhead_at` on the shared clip axis — `track::playhead_x`, drawn into the overlay over the clips). A lane joined **no** navigation group then (its axis was the window's clip span), so those keys applied to the widget itself rather than routing through the group model — G22g made every lane a group member, and they route, and the animation demand now sees a lane, so the window animates. The hit-test reconstructs the same ruler-reserved body, so a clip is grabbed by the pixels it is drawn on. Python: `ruler=`/`playhead_at=`/`tempo=`/`sample_rate=` on the `track` builder; `gui_multitrack.py` rules its bottom lane in beats and rolls the playhead from `/clock_query`. Tests: the ruler strip is reserved (and not when off), `playhead_x` maps the clock onto the axis, a lane parses and live-`/gui_set`s its chrome.
- ✅ **G22f - Long takes in a clip (the bulk body)**: a real take is minutes long, so it must never ride the wire as JSON — a `clip` now resolves its waveform body exactly the ways the heavy `waveform` view does, in the same precedence order: `cache` (a prebuilt peak-pyramid file), `path` (raw little-endian `f32`, mapped and its pyramid built/cached on load), `buffer` (a server buffer over the client leg's `/buffer_query` + chunked `/buffer_getRange`), then inline `data`/`blob` for a sketch. **Placement analysis (the G7b rule):** no new decimator — the min/max summary *is* `clausters_core::peaks` (the pyramid every client already builds), read through `WaveformData::column`, the same LOD source and crossfade the waveform view draws from; the host only maps the resource, through the existing `BulkLoader` seam (`MmapLoader` natively, `fetch` in the browser). A loaded take lands in the **tree** (like a plot's samples), not on the GPU: a lane body is flat geometry decimated to the clip's pixel width, so a minutes-long clip costs a screen's worth of columns — the one graphics rule, again. Python: `path=`/`cache=`/`buffer=`/`channels=`/`base_bucket=` on the `clip` builder; `gui_multitrack.py`'s audio lanes map real take files. Tests: a clip parses its bulk sources; a loaded take draws decimated to the clip's width, not to its sample count.

- ✅ **G22g - Zoom/pan of the shared axis**: the multitrack's time axis stops being a static full span and becomes navigable — and it does so by **joining the navigation group model**, not by growing an axis of its own. A `track` is now a timeline widget (`is_timeline`), so it is a group member like any heavy view: the lanes of a window are **linked by default** (keyed by the window root, since aligned lanes are the multitrack's whole promise; an explicit `link` splits them or joins lanes *across* windows), the group's length aggregates each lane's extent — the end of its last clip, registered by `sync_track_totals` on def, on a `/gui_set` of a clip and after a drag, so pulling a clip past the end lengthens the timeline — and the group's window is what the renderer and the hit-test both draw through, so a zoomed axis grabs clips where it looks. The gestures are the ones the editor already had: wheel zooms (anchored at the cursor, over the lane body), Shift+drag pans (plain drag still grabs a clip), `r` resets, and `/gui_set view_start`/`view_len` navigates from a script — all group-wide. Tests: the lanes of a window share one axis spanning the composition, zoom/pan on one lane moves all of them, a clip moved past the end lengthens the shared axis.

- ✅ **G22h - The automation clip (a curve as the body, edited in place)**: the third body of the same graphic unit, and the sibling G22d's piano-roll promised. A `clip` with `points` draws a **break-point curve** instead of a waveform or notes — the `bpf` editor's model and shape math (`clausters_core::envshape`, the very function the server's `EnvGen` plays, so what is drawn is what is heard), placed on a lane: times are timeline units relative to the clip, values live over `[min, max]` (with the `exp` display scale), and the curve maps through the shared axis exactly as the piano-roll's notes do, so it moves with the clip and holds up under zoom. It is **editable in place**: a break-point wins over the clip body (as it wins over a segment in the `bpf` view), dragging it moves it, Ctrl+click adds one or removes the one under the cursor, and the edit leaves as the **same flat `"points"` payload** the `bpf` view sends — one payload, so an `Automation` consumes an edit without caring which view drew it. The reuse is structural, not by copy: `bpf::place_point`/`insert_point` were extracted as the mapping-free core both the widget-local and the clip-placed editors call, and `track::clip_draw` became the one place the typed tree becomes a drawable clip, so renderer and hit-test cannot disagree. Python: `points=`/`exp=` on the `clip` builder; `Editor` renders any element wrapping a `clausters.seq.Automation` as a curve clip and writes a `"points"` edit back onto its `Env` (times converted back to beats) — the curve you draw is the curve the next rendering plays; `gui_composer.py` sweeps a filter from a lane. Tests: the curve draws instead of a body, a point is hit where it is drawn and maps back through the axis, and the edit-back lands on the automation in beats.

- ✅ **G22i - Locate: the multitrack's cursor**: the one gesture a timeline view cannot do without. A press on a lane's **time ruler** — or on its empty space, where no clip is — locates the transport: the position under the cursor becomes the group's **static cursor** (drawn at once on every lane, so the click lands where you see it) and leaves as `/gui_event <id> "locate" <position>`, which the script seeks its playhead to. That needed a second kind of playhead, and the distinction is the point: `playhead_at` **anchors the line to the engine clock** so it sweeps with the audio, while the new `playhead` prop is a **cursor that stands still** — a located, stopped transport has a position, and it must not drift with the clock. Both are group-wide keys (one cursor across the lanes) and both are drawn by the lanes and the heavy views alike. Python: `Editor` grows the transport this implies — `position`, `play`/`pause`/`stop`/`locate` — and consumes the `"locate"` event as a *transport* action, not an edit (it does not mark the arrangement changed). Seeking while playing re-renders from there, so a seek also picks up any pending edit.

- ✅ **G22k - Layered clip bodies**: a clip's bodies stop being exclusive and **stack**, back to front: the take, the events over it, the envelope over both. An automation drawn on top of the element it shapes is *one* clip, not two hiding each other — which is what makes an envelope attachable to its event. They do not share a value axis (a roll's `min`/`max` are pitches, a curve's are its parameter's units), so the curve carries its own `points_min`/`points_max`, and its editing (drag a point, Ctrl+click) maps through that. Python: the `clip` builder grows them; the `Editor` fuses a **simultaneous** group into one layered clip (below).

## G23: the patcher view (the logical group)

- ✅ **G23 - `patch`: a patcher view of a bus-wired node graph**: the arrangement's *logical* grouping — members that relate by **processing**, not by time — finally has the shape it always needed, and it is not a lane: it is a **server patch** (level 1, in the P track's later two-level vocabulary — this milestone built its view). The new `patch` widget (`host::patch`, pure over a `Mesh` and unit-tested without a GPU, the static-view posture) draws a GraphDef as it actually is: a **member box** per node (its def name, and a port per control that names a bus), a **bus node** per internal bus (`OUT`, the hardware, among them), and a **wire** per `(member, control) ↔ bus` pair. **Decision (recorded):** the view is deliberately **bipartite** rather than a directed source→sink patch, because a GraphDef only knows that a control *touches* a bus — which end writes and which reads is the server's own analysis (it sorts the graph topologically), and guessing it from a control's name would be a lie the renderer tells; a bus is therefore a first-class node, not an edge, and several members writing one bus is a sum on that node. **(This bus-as-node decision was reversed 2026-07-22** — it proved unreadable as a signal-flow diagram; the patcher turned directed and typed, see `docs/decisions.md`, "The patcher is a directed, typed graph", and the P track below. This milestone's widget stands as the record of what shipped; it is reworked in the redefined P3.)** Rewiring is therefore well-defined: drag a port onto a bus to point that control at it, onto empty space to unwire it (one wire per control, always), and the edit leaves as the flat `/gui_event <id> "wire" <member> <control> <bus>` — the clip/points edit-back pattern, third time. Python: the `patch` builder; the `Editor` renders a `Group{logical}` as a server patch (the same 1:1 mapping onto a GraphDef that rendering uses, drawn instead of sent) and a `"wire"` edit rewrites the member `Generator`'s controls, so the next rendering sends a GraphDef wired the way the patch is drawn. Tests: the server patch lays out and hit-tests, a wire dropped on a bus rewires and reports, one dropped on empty space unwires; a logical group draws as a server patch and its rewiring reaches the GraphDef spec.

## G24: the editor-grade piano-roll view

The multitrack (G22d) draws notes only as a **clip body** — bars, no keyboard, no velocity, no note editing, no OSC events. G24 is the dedicated, editor-grade `pianoroll`, the note view at the depth the heavy `waveform`/`spectrogram` reached: a keyboard gutter, an editable MIDI-note grid, a velocity lane and an OSC-event lane, with the same rulers, group navigation, selection and playhead. It contemplates the two message families a sequence carries — **MIDI notes** in the grid and **OSC events** in their lane. Staged in incremental parts; the first shippable slice is below.

- ✅ **G24a - The `pianoroll` widget + the shared note core**: a new `WidgetKind::PianoRoll`, architecturally a flat-geometry timeline widget (the `track` posture: `is_timeline`, `EditorProps`, group navigation — **not** a wgpu view; notes/keyboard/lanes are `Mesh` geometry, so it is browser-correct and wasm-clean by construction and unit-testable without a GPU). The note model, pitch↔y / time↔x mapping, drawing, hit-testing and editing live **once** in `host::pianoroll`, the reusable core **shared by two consumers** — the dedicated widget and the multitrack `clip` body (`track::draw_piano_roll` now delegates to `pianoroll::draw_notes`) — the same discipline G22h took extracting `bpf::place_point`. The `Note` gained `velocity`/`channel` (a real MIDI note), unified between the clip and the widget; the wire form is the flat quintuple `start dur pitch velocity channel` (a legacy triple list still parses). **Decision (OSC events, recorded):** notes draw in the pitch×time grid; OSC events (which have no pitch) draw as flags in a **separate lane** below it, not scattered in the grid. Editing is native (the browser keeps display + `/gui_set` parity, the editor-view posture): drag a note to move it in time/pitch, drag an edge to resize, Ctrl+click to add/remove, drag the velocity lane to set velocity, Ctrl+click/drag the OSC lane to add/remove/move an event; every edit rides the **edit-back pattern** — `/gui_event <id> "notes" <start dur pitch velocity channel …>` and `"osc" <time label …>`, flat payloads not new addresses, the fourth use of that pattern. **Placement (the G7b rule):** the one piece of general musical knowledge — the MIDI-note ↔ name / black-key spelling drawn on the keyboard — landed in `clausters_core::scale` (`note_name`/`pitch_class`/`is_black_key`, beside the perceptual scales), its FFI export **deferred** until a client evaluates it (the `envshape`/bus_tap-reader precedent); everything else (grid model, hit-test, keyboard/lane geometry, drag clamps) is display-only, gui-side. Python: the `pianoroll` builder (and `clip(notes=…)` grew velocity/channel); the `Editor` reads a note's velocity from the data (an explicit `velocity`, else `amp`), so a clip roll shows the events' dynamics; `examples/gui_pianoroll.py` draws notes + OSC events and plays them through a `Pbind`. Tests: the note core (hit-test edges-vs-body, insert/remove/resize clamps, pitch↔y, region layout), the widget parse (quintuples, OSC pairs, pitch window), the host edit-back (`pianoroll_hit`, `notes`/`osc` event args), and the note-name mapping in the core.

- ✅ **G24b - Pitch zoom/pan gestures + cursor readout**: the vertical axis becomes navigable by gesture, not only by `/gui_set`. The **keyboard gutter** is the piano-roll's y-axis surface (the counterpart of the heavy views' y-ruler strip): wheel over it zooms the pitch window (`y_start`/`y_len`, anchored at the cursor via `viewport::zoom_span`), drag pans it (`Drag::PanY`, the same absolute-from-snapshot pan), and `r` resets — reusing `zoom_timeline_y`/`set_y_view` verbatim, so nothing new in the group model. Wheel over the grid still zooms the shared time axis. A **cursor readout** (the note name under the cursor via `clausters_core::scale::note_name`, plus the time in the ruler's unit) draws in the grid corner, the piano-roll's answer to the heavy views' time+amplitude/frequency readout. All display-only, gui-side.

- ✅ **G24c - The Editor's dedicated piano-roll view**: `Editor.open_pianoroll(host, element=None)` opens one events element as the editor-grade `pianoroll` — the note view of an element, as opposed to `open`, where the same notes are only a clip body. `render` dispatches on the editor's view mode; the dedicated render draws the element's MIDI notes in the grid and its OSC (and raw MIDI) events in the event lane, frames the pitch window around the notes, snaps to the `quant` grid and rides the beats ruler; the roll registers as a lane too, so playhead/cursor/locate work unchanged. **Decision (recorded): per-note edit-back writes only to random-access element.** The `"notes"` payload rebuilds a `Track`'s editable `seq.Timeline` (times converted to beats, the OSC/MIDI events sharing the timeline preserved); a generator (`Pbind`/`Routine`) is forward-only, so its bounced notes show **read-only** (bounce it to a `Track` to edit) — the generated/generator distinction of the arrangement model doing its job at note granularity. OSC events are display-only for now: the `(time, label)` marker is a lossy view of the message, so writing it back would drop the args. Python-only (`gui/editor.py`); `examples/gui_composer.py` opens the melody `Track` this way beside the multitrack, sharing its timeline. Tests: the dedicated render (notes, OSC lane, pitch window), the timeline rewrite preserving the shared OSC event, the generator no-op.

- ✅ **G24d - Multi-note selection + block edits**: **Decision (recorded): the marquee is the shared time selection restricted in pitch.** A plain drag on the empty grid keeps driving the linked views' time selection exactly as before, and the notes inside the time × pitch rectangle become the widget's **selected set** — one gesture, no conflict with the heavy-view convention (Shift still pans, Ctrl still adds/removes); Alt+click toggles a note in/out (a non-rectangular selection, one note at a time). The set is **native view state** (`WidgetKind::PianoRoll::selected` — never parsed from the wire, cleared when the script replaces `notes` since the indices would dangle; the browser draws it but never builds it, the display-parity posture). Block edits: dragging a **selected** note's body moves the whole selection **rigidly** in time and pitch (the grabbed note snaps to the grid; the core clamps the block's delta as one, so an edge stops the block instead of folding it — grabbing an unselected note drops the selection and moves singly, and an edge drag still resizes just that note); Delete/Backspace over the roll removes the selection; dragging the velocity lane over a selected note nudges every selected velocity **relatively** from a press snapshot (each saturating 0–127 on its own, so reversing restores the spread). All pure block ops live in `host::pianoroll` beside the single-note edits (`notes_in_rect`, `move_notes_from`, `remove_notes`, `nudge_velocities_from`, `selection_after_removal`, `toggle_selected`); the gestures are thin `Drag::SelectNotes`/`NoteBlock`/`VelocityBlock` wrappers over them. **The wire did not grow**: every block edit emits the same flat `"notes"` payload, so the Python `Editor` (and any script) consumes it with zero changes. Tests: the marquee rect (reversed ranges, span edges), the rigid clamps (time zero, pitch top, a block wider than the pitch window), removal order/duplicates, the relative velocity nudge with per-note saturation, and selection index maintenance across a single removal and toggling.

- ✅ **G24e - Quantization, on both surfaces**: **the GUI runs standalone too, so the gesture and the client-side helper are complementary, not alternatives.** In the host, `q` over the roll quantizes the **selected** notes' onsets (all of them when nothing is selected) to the widget's `snap` grid — the same grid a drag snaps to — through the pure `pianoroll::quantize_notes` (durations kept, no-op without a grid), emitting the usual flat `"notes"` payload; that is the whole quantize story for a standalone host wired straight to the server. In the client, `seq.Timeline.quantize(grid)` snaps every placement to the beat grid **in the client** (the same rounding, in beats), for programmatic/NRT use and for elements never opened in a view. No wire change either way. Tests: the core op (selection vs all, durations kept, movement reported, gridless no-op) and the timeline method (snap, re-sort, gridless no-op).

- ✅ **G24f - The clip roll's note-name pitch ruler**: the multitrack `clip`'s roll body has no keyboard gutter, so it gets the **compact** form of the same knowledge — `pianoroll::draw_pitch_labels` names each C at the clip's left edge (via `clausters_core::scale::note_name`, the G7b placement again), drawn **only when a semitone row is tall enough to read** (the LOD discipline: a collapsed lane shows bars, an expanded one gains the names). Display-only, shared-core drawing, no wire change; tested by the row-height guard (a readable window draws, a squeezed one stays silent).

- ✅ **G24g - Live MIDI input painting notes, on both surfaces** (the standalone rule again): **host-side**, a `pianoroll` with the new `midi_in` prop makes the native host open a virtual MIDI input port ("clausters-gui", held open while any such roll is; dropped when the last closes) through the shared `clausters-midi` crate — whose `live::Input` gained a **safe Rust face** (`open`/`poll`, the C ABI now delegating to it) and a shared channel-voice **note parser** (`parse_note`: on / off / zero-velocity-on-is-off) — drained at the frame cadence behind the gui's new default-on `midi` feature (target-gated off wasm; the browser keeps display parity). A note-on paints a **held** note — at the **running playhead** (live recording; note-off then writes the real held duration) or at the **step cursor** when the transport is stopped (step entry: notes land on the snap grid, a chord shares one step, the last key up advances it) — and every paint emits the usual flat `"notes"` payload. **Client-side**, the same loop in script: `examples/gui_pianoroll.py` grew a `record_midi()` cell hanging a `MidiFunc` note-on/off pair off the client's own virtual port and painting via `/gui_set`, timed by the session clock. Python: the `pianoroll(midi_in=…)` builder kwarg. Tests: the shared parser (on/off/velocity-0/ignored types) and the `midi_in` parse/default.

- ✅ **G24h - Cut/copy/paste**: Ctrl+C copies the selected notes to a **host-wide clipboard** (normalized to the block's first onset, pitches absolute — so a block travels between rolls and windows), Ctrl+X also removes them, and Ctrl+V pastes with the block's first onset at the **mouse cursor's time** (snapped to the note grid), original spread and pitches kept. The pasted block lands **selected**, ready to drag into place — paste and adjust is one motion. Pure ops (`pianoroll::copy_notes`/`paste_notes`) beside the block edits; cut and paste emit the usual flat `"notes"` payload (copy is silent — nothing changed). Tests: normalization, paste placement + returned selection, the negative-time clamp, the empty copy.

With G24h the follow-ups G24 opened are all closed; further piano-roll work (e.g. per-channel coloring, a controller lane beyond velocity) gets numbered when a design converges.

## Done outside the numbered tracks

- **Shared config + config-driven standalone.** Server, GUI host and Python
  client read one TOML schema (`clausters-core::config`): a user file under
  `clausters.toml`/`config.toml` overridden by a project `clausters.toml`, then
  CLI flags. The GUI host reads `[gui]`/`[standalone]` as flag defaults, accepts
  `--config <path>`, and `--standalone` with no name falls back to
  `[standalone].gui`. Standalone now lets the embedded server load the whole
  bundle from the data directory (`Clausters::open_with_data_dir` →
  `attach_store`: SynthDefs, Faust defs, GraphDefs, bindings, `boot.json`),
  instead of replaying specs by hand; a `standalone-faust` feature pulls
  `clausters/faust` for Faust bundles. See `docs/configuration.md`.

- **The host's two long files became directories.** `gestures.rs` (4706 lines)
  and `frame.rs` (2514) each split along a seam they already had, so new work
  lands in a named place instead of at the end of a file:

  ```
  host/gestures/mod.rs      the machine: Drag + press/drag_to/release/wheel/tick
  host/gestures/effects.rs  what a gesture delivers (bound -> server, else event)
  host/gestures/nav.rs      what it reads and moves (hit-test, pan/zoom/selection)
  host/gestures/keys.rs     the keyboard half (text editing, block operations)
  host/gestures/tests.rs    the test module
  host/frame/mod.rs         the spine: GPU slots, FrameInputs, render
  host/frame/items.rs       the read half: per-widget snapshots + the tree walk
  host/frame/draw.rs        the write half: the three mesh passes
  ```

  Alongside it, three pieces of repetition got an address: the heavy-view slot
  walk both fronts had written separately (`frame::visit_elements` +
  `inline_slot` — the drift risk, since the browser half is not built on the
  native target), the five edit-back emitters (`gestures::effects::emit_read`),
  and the `window_def(id)?.find(id)?.kind` walk (`Host::widget_kind`/`_mut`,
  plus `Host::layout_window` for the layout call hit-testing shares with the
  renderer).

  **Two cuts deliberately not made**, so they are not re-proposed: splitting
  `drag_to`/`element_press` by widget family (the arms mutate `self.drag`, so
  the coupling would cross a file boundary rather than go away, and `drag_to`'s
  match is exhaustive over every `Drag` variant with no catch-all — that check
  is what guarantees a new drag kind gets driven), and folding the
  `{rect, clip, theme}` header the frame's item structs share into a nested
  struct (18 field triplets deleted, 102 sites made longer).

## G25: the host's TCP front (default command transport)

- ✅ **G25 — TCP server front for the `/gui_*` protocol** *(done 2026-07-12; pairs with server M25 and client C34)*: the host grows the TCP variant its `ClientId` seam anticipated — an acceptor plus one reader thread per connection handing whole length-prefixed frames to the single-threaded front, the audio server's `osc::tcp` pattern reused verbatim (the zero-length wake datagram is already in place in `host::transport`), with the same configurable frame ceiling as M25 (`--max-frame`, default 16 MiB). On **by default** beside UDP on 57210; `--no-tcp` opts out, and a standalone bundle (native or the future Tauri/mobile wrapper) that talks to its host in-process needs no network front at all. Python's `GuiHost` / `Session.gui` connect over TCP by default (`transport=` opts down to UDP), so a `/gui_def` JSON — the largest payload in the system, a whole editor tree with its blobs — is no longer bounded by a datagram, and neither are dense `notes`/`points` edit-backs on long elements. The browser front keeps WebSocket unchanged (it already frames without the datagram cap; it adopts the shared ceiling).

- ✅ **G25b — WebSocket server front (`--ws`), the browser's carrier into a native host** *(done 2026-07-18; pairs with the web client's W2 groundwork)*: the third leg of the script front, controlled exactly like the audio server's — `--ws [port]` (default 57220, clear of the host port since both bind a TCP listener), `ws = true`/a port under `[gui]` in the config, the shared `--max-frame` ceiling. `host/ws.rs` mirrors the server's `osc::ws` (one OSC packet per binary message — WebSocket frames, so no length prefix; tungstenite-enforced size ceiling; per-connection reply channels since a tungstenite socket owns both halves) with G25's sink generalization, so both fronts take it: the headless serve loop through a `WsHub` (mpsc + the zero-length wake datagram), the windowed front through the winit proxy. `ClientId::Ws(id)` joins the seam; replies route back on the originating connection. A page cannot open UDP or raw TCP, so this is what lets the TypeScript client drive a **desktop** host the way it drives a `clausters --ws` audio server; the e2e (`clients/web/tests/gui-host.test.ts`) runs `/gui_def` + `/gui_query` from the web client's `WsConnection` against a spawned `clausters-gui --headless --ws`.

- ✅ **G26 — The measuring plot: views, rulers, multichannel, readout + the free-standing `plot()`** *(done 2026-07-14)*: the G8 `plot` grows from a bare trace into a **measurement** view while staying static (no zoom/pan/edit — the heavy views own navigation). It gains **views** (`view` prop; `PlotView`, an enum on purpose so future forms — a histogram, a phase plot — extend it without touching the wiring): **signal** — value against time/index, every channel drawn (stacked lanes or `overlay` traces), the whole sequence always contributing (polyline or per-column min/max envelope: no visual aliasing), the value axis `[min, max]` with **either side omitted auto-fitting to the data** (the arbitrary-range sequence case; the string `"auto"` releases a side live) — and **spectrum** — the Welch-averaged magnitude spectrum per channel in dB over the four spectrogram scales (linear/log/mel/bark), analyzed **once at the widget's mutation points** (parse / bulk load / `/gui_set`) through `spectrogram::Stft` (the shared-core FFT, so it agrees with the spectrogram bin for bin) and drawn through the identical `display_to_hz` geometry the rulers use. New chrome, all shared math: a generic 1-2-5 **value ruler** (`ruler::value_ticks`, any range — not amplitude-tied), a **horizontal** frequency ruler (`ruler::hz_ticks_h`, width-fitted labels), the x ruler in samples or clock time, and a **hover readout** (hairline + dot + the exact sample's index/time and value, or the bin's frequency-per-scale and dB) in the overlay pass. The bulk paths keep every interleaved channel (native mmap and browser fetch alike). Python: the `guidef.plot` builder grows the props, and the new **free-standing `clausters.plot(obj, …)`** (the visual sibling of `play`) opens **one individual window per call** on the ambient GUI host (the session's, else one it boots lazily, no client leg) — dispatching a **def** (`SynthDef`/`FaustDef`/`GraphDef` + its `defs` members: an ephemeral NRT session renders `dur` seconds of its output), an **`Env`** (rendered through the engine's own `EnvGen`, gate-released at its sustain point), a **`Buffer`** (fetched live with its shape/rate), or any **iterable of numbers** (a `Pseq`/`Pwhite`/list, materialized up to `n`, range auto-fitted). Tests: tick fitting/collisions for the new rulers, the per-channel analysis peaks, decimation bounds, parse/apply round-trips, and the Python dispatch + NRT render paths against a fake host.

- ✅ **G27 — Shared gesture machine: one interaction state machine for both fronts**: retire the two divergent per-front drag machines (the native front's full one; the browser's `host/web/` mini-`Drag` covering only slider/knob/button) in favor of **one** press → drag → release → wheel interpreter in the agnostic core, so every editor gesture the native front has — selection, pan/zoom, BPF editing, clip placement, the whole piano-roll editing set — reaches the browser by rewiring, not by reimplementation. The machine (`host/gestures/`: the `Drag` state + `Gestures::press/drag_to/release/wheel/relative_motion` and the keyboard block operations) mutates the `Host` through the existing `interact`/model doors and returns `GestureEffect`s (emit/redraw/release-pointer) for the front's own sinks; the front supplies a per-call `GestureCtx` (framebuffer, modifiers, the GPU slots' lane counts — the one front-owned datum) and a pointer-grab callback at press (native pointer lock; a front without one returns false). Two legs:
  - ✅ **Leg 1 — extract + native rewire** *(done 2026-07-15)*: the machine extracted with **no behavior change**; `host/gui/input.rs` reduced to winit adapters (context snapshot + effect application), the piano-roll press/keyboard ops folded into the machine, MIDI painting kept native (`host/gui/midi.rs` — the virtual port is a device). The gestures became unit-testable without a window or GPU for the first time (press/drag/release/wheel dispatch tests over a JSON-built host).
  - ✅ **Leg 2 — browser rewire** *(done 2026-07-15)*: the `#[cfg(not(wasm32))]` lifted from `gestures` and from the `interact` gesture helpers it drives (they are pure; the cfg only reflected native-only use); `host/web/`'s mini-`Drag` deleted and its pointer/wheel/keyboard events routed through the same machine (`GestureEffect`s to the outbox/binding surface; the grab callback returns false — Pointer Lock stays a later refinement; the piano-roll clipboard is page-wide, modifiers tracked via `ModifiersChanged`), so the browser gained selection, pan/zoom, BPF/clip editing and the whole piano-roll editing set by rewiring, with zero new gesture logic. The browser posture re-stated in the docs (`docs/clients.md`, `docs/architecture.md`, `docs/decisions.md`): from "display + `/gui_set` parity" to full editing gestures. Verification: `check-wasm.sh` gates the lifted cfgs; a browser smoke (headless Chrome over CDP, real input events on the wasm bundle) drove a selection drag, a wheel zoom and a BPF edit end to end, each emitting its `/gui_event` payload.

- ✅ **G28 — Multichannel live scopes: one rule for `bus` + `channels`, instrument rulers, a visible trigger** *(done 2026-07-16)*: the C37 verb generalized so monitoring stereo/multichannel/HOA sets is one coherent rule — *the verb monitors `channels` consecutive buses from `bus`, each view presents them its way* — instead of three per-view special cases. The `scope` and `spectrum` widgets grew a `channels` prop reading that many **adjacent** tap rings from `tap` (the phasescope's existing adjacency convention, now the family's): the oscilloscope draws stacked lanes or color-coded `overlay` traces, searching the trigger in the **first** channel and applying the found alignment to all (true relative phase across channels); the spectrum folds one FFT state per channel and overlays one color-coded curve each. The trigger became **visible**: a faint level line in the trigger channel's lane and a `lock`/`free` corner read-out (`oscil::align` now reports whether it fired). Both widgets gained **axis rulers** as owned strips (the G20b posture — a ruler measures its widget's axis, it is not a free-standing element; recorded in the ruler-as-widget discussion): `ruler` (ms of the window / Hz on the active `freq_scale`, via the shared tick machinery) and `ruler_y` (value / dB), shown by default, `"off"` hides, all live via `/gui_set`. The tick's tap window became the typed `TapWindow` (interleaved channels + lock flag) shared by scope and phasescope on both fronts. The composite "analyzer" stays **client-side composition** (a GuiDef of menu + number + scope; deliberately no monolithic widget). Python: `channels`/`overlay`/`ruler`/`ruler_y` on the builders; the verb takes `channels=` (a `Bus` brings its own count; `view="phase"` is the fixed two-channel case, erroring on any other explicit count) and its docs read as a brief user manual. Tests: multichannel parse/apply and adjacent `taps_read`, the interleaved tick alignment on the first channel, per-channel spectrum states, and the Python channel semantics.

## G29: the virtual piano keyboard (`piano`)

- ✅ **G29 — The `piano` widget: a playable keyboard with real proportions, range navigation, and two instrument-mapping paths** *(done 2026-07-19)*: a new mesh-drawn widget (`host/piano.rs`, pure and window-free) whose keys follow the **physical instrument's ratios expressed only as proportions** — white keys 1 unit, black keys `13.7/23.5 ≈ 0.583` units wide and `0.6` of the white length, and the black keys *not* centered on the white boundaries: the C–E group's rear white segments are equal (`(3−2b)/3`) and F–B's are equal (`(4−3b)/4`), so the keyboard resizes freely and always reads like a piano. The visible `min`/`max` window (min snapped to a white key) pans over the whole MIDI range through the widget's own "ruler": an **overview strip** of all 128 keys with the window boxed — drag pans, wheel zooms anchored at the cursor (span clamped one octave..128), wheel over the keys pans by white keys — every change emitted as `"range" min max` and settable via `/gui_set` (browser parity), all gated off by `pan: 0`. Keys outside `active_min`/`active_max` draw **grayed and inert**, visualizing the mapped range. Playing emits the **MIDI-shaped** payload `"note" pitch velocity state channel` (ints; state 1/0; a later consumer translates 1:1 to note-on/off): velocity from the press height (front of the key = louder; a `velocity` prop fixes it), glissando on drag (off + on crossing keys), `pressed` as native view state (the `selected` posture). **Two instrument-mapping paths, both worked**: unbound, the events reach the script, which programs the voices (`examples/gui_piano.py` — the "programmable like a GuiDef" path; a bound piano instead forwards the flat payload); with `voice: "<def>"` the **host** manages one server voice per held key — `/synth_new <voice> <id> 0 0 freq <hz> amp <vel/127> gate 1 <voice_args…>` on press, `gate 0` on release, the def freeing itself (`FREE_SELF`) — over the existing `ServerLink`, so UDP, embed and the browser's in-page engine all work unchanged; the self-driving bundle `clients/web/examples/piano` plays with zero page JS (the graph-controls posture). Voice node ids come from a dedicated high window (`0x1000_0000`, recorded in `docs/decisions.md`); held voices release on widget free/redefine. Placement analysis: `midi_to_hz`/`hz_to_midi` landed in `clausters_core::scale` (general musical knowledge, beside the note spelling); the key geometry, overview math and velocity map stay gui-side (display-only). Python: the `piano` builder; tests cover the layout ratios and hit priority, pan/zoom clamps, velocity endpoints, parse/apply round trips, press/glissando/release/gray/pan-gating gestures and the voice bookkeeping. Future lines (unnumbered until a design converges): lighting keys from live MIDI-in (the G24g machinery) and a script-settable `pressed` display.

## G30: the editable text field (`text`)

- ✅ **G30 — The `text` widget becomes a real editable entry field**: the catalog's `text` was a display-only string (script-driven via `/gui_set`); G30 makes it *editable* — the general-widget track's overdue counterpart to the numeric `number`. Clicking focuses the field and drops the caret where the press lands; typing inserts, the arrows/Home/End/Ctrl+arrows move the caret (word-wise with Ctrl), Backspace/Delete edit, a drag or Shift+arrows select, Ctrl+A selects all. **The entered string is delivered exactly as a numeric control's value is — on *every* edit, never gated on Enter** (`emit_value` on each keystroke, so the bound path forwards it straight to the audio server and the unbound path emits `/gui_event <id> <string>`, the scalar form `text` already reported): a text field is a single-value control like a slider, so **no wire change** — no new address, no new tag. A `multiline` prop allows embedded newlines (Enter inserts one; single-line ignores it) and a growing field; the caret/selection are **native view state** (never parsed from or sent over the wire — the `PianoRoll::selected` precedent). **The editing logic is a std-only pure module** (`host/textedit.rs`, the sibling of `bpf.rs`/`pianoroll.rs`): the caret and selection are byte offsets kept on `char` boundaries via `str::char_indices` (no `unicode-segmentation` — `char` is the right granularity for the per-cell bitmap font), the ops mutate the widget's `value` in place so it stays the single source of truth, and it unit-tests without a window and compiles to wasm unchanged. Focus is host-wide state (`Host::focused_text`), so a keystroke edits the focused field instead of running the global editor shortcuts; both fronts translate their winit key into a neutral `TextKey` and route it through the shared gesture machine (`Gestures::text_key`), so native and browser edit identically by construction (the G27 posture). **Cut/Copy/Paste (recorded):** an **internal host-wide clipboard** (a `String`, zero new dependency — the G24h note-clipboard precedent; winit exposes no OS clipboard and libwgpu covers none), the same in-page clipboard on the web front for now; binding it to the browser's real OS clipboard (a `writeText` out plus a `paste`-event listener in — free via the DOM, no Rust dep) is the one recorded follow-up. The embedded 5×7 font grew the punctuation an OSC message and free text need (`_ = < > [ ] { } " ' ; ? ! * # & @ | ~ ^ $ \`); a real font atlas and true lowercase stay the deferred refinement L4 already named. **Placement analysis (the G7b rule):** all display/interaction logic — caret/selection model, hit-testing, focus, key routing, rendering — is **gui-side**, nothing for `clausters-core`; the one client-facing piece, tokenizing a typed string into an OSC message, is Python-client ergonomics (the shell layer), not shared core. Python: `multiline=` on the `text` builder, and `examples/gui_oscsend.py` — an OSC-message field whose typed `/node_set <id> freq <value>` sweeps a running synth's pitch live as you edit the number (the ungated string delivery made audible), parsed by a minimal `parse_osc` helper, plus a multiline scratch pad. Tests: the `textedit` model (insert/delete/word & vertical motion/selection/clipboard-free ops/boundary clamp), the `caret_at` hit layout, and the gesture integration (a press focuses, each keystroke emits the whole string ungated, a press elsewhere defocuses, Enter is a newline only when multiline, cut/paste move text through the clipboard).

## G31: music notation (the `score` widget)

The plan carried notation for years as "Verovio's SVG, hosted by the web surface, off the GPU path" — a whole second rendering substrate for one widget, and the strongest single argument for going web-first. G31 landed it the other way and the second substrate turned out to be unnecessary: **the SVG is a serialization, not a runtime**. The client engraves once and parses the result into a *display list* — a glyph-outline table keyed by SMuFL codepoint plus placed glyphs, staff lines, stems, beams, slurs, hairpins and text in page units — and the host tessellates that into the ordinary `paint::Mesh`, knowing nothing about MEI, MusicXML or notation. So notation costs the host **no dependency** (Verovio is an optional *client* dependency), draws in the one batch the rest of the chrome draws in, works in the browser by construction like every other widget, and needs no SVG renderer anywhere. The seam is the same one the heavy views already use — the client analyzes, the host draws — with the display list as the cache.

- ✅ **G31a — The display list and the `score` widget** *(done 2026-07-23)*: the spike (Verovio → SVG → geometry) grew into the widget. The engraving vocabulary is complete (glyphs, staff/stem/beam strokes, slurs and ties as filled curves, hairpins, text), a multi-system page lives in a `scroll` (pans on both axes, zooms anchored), and `vb` fits the page into the widget so the drawing is resolution-independent. `docs/decisions.md` records the finding that made the shape choosable at all: Verovio's `DeviceContext` is a viable extraction point, but the SVG output already carries everything the display list needs, so parsing it is the cheaper seam.
- ✅ **G31b — The playback cursor on the engine clock** *(done 2026-07-23)*: the engraving's third layer is a `cursors` track (the score's own timemap folded into `t` in ms → an `x` and a staff span), and the widget takes `playhead_at` — the sample-clock value score time 0 maps to — so the cursor **sweeps on its own**, one message per pass, the host reading the clock every frame exactly as the timeline views do. Negative means stopped, and the static `playhead` holds the cursor where the music was left, which is what makes pause look like pause.
- ✅ **G31c — The page is clickable: `"element"`** *(done 2026-07-23)*: every primitive carries the MEI `xml:id` it was engraved from, so a press hit-tests to an element and reports it. The id is the *client's own*, so it indexes the client's score directly — nothing but a string crosses the wire, and the host still reads no notation.
- ✅ **G31d — The edit round trip: drag a note, `"transpose"`** *(done 2026-07-23)*: a drag on an engraved element moves it up or down the staff in **whole diatonic steps**, drawn as it goes, and the release emits `"transpose" <xml:id> <steps>`; the client applies it, re-engraves, and replaces the drawing with one `/gui_set <id> display_list` (a new prop — until then a page could only change by redefining its window). Steps rather than a coordinate, because Verovio's coordinate-taking `drag` reads a page y in a frame that does not line up with the display list's, and its editor transposes by steps anyway; the quantum travels **with the page** (`step`, page units per diatonic step, measured from the staff-line spacing) since it follows the engraver's unit size, not the staff scale. Two things the drag revealed about what an "element" is: a sounding element **claims every primitive drawn inside it** (Verovio ids a note's stem and flag separately, which scattered one note across three draggable things), while a chord deliberately stays a container so its notes remain separately transposable. And the staff furniture a pitch implies — **ledger lines**, drawn per staff, so no per-element displacement can move them — is *re-derived* by the host from the staves it reads back out of the engraving, with the engraved ones over the notehead's column suppressed: that is the half that could not be faked, since a note dragged back onto the staff has to *lose* them. The host cannot re-engrave, so the preview stands after release until the new page arrives.
- ✅ **G31e — A reproducible Verovio build** *(done 2026-07-23)*: `third_party/verovio.pin` + `build-verovio.sh`, the arrangement `build-faust.sh` established (source uncommitted, reproducibility in two committed files). It pins a `develop` commit rather than a release tag on purpose: in 6.2.1 — the PyPI wheel included — the score editor is **dead in any build keeping Humdrum support**, a guard typo that makes every `Toolkit::Edit()` return false; upstream inverted it after 6.2.1. Recorded in `docs/decisions.md`; repin to 6.3.0 when it ships. The editing half of the widget therefore needs a Verovio whose editor works, which the docs and the smoke step both say.
- ✅ **G31f — Engrave the client's own data: `from_notes` / `from_timeline`** *(done 2026-07-24)*: the inverse of the score→sound flow. Until now a page could only be *typed* (ABC/PAE/MEI/MusicXML); now `clausters.gui.notation.from_notes` (a monophonic run of `Event`s) and `from_timeline` (a `Timeline`, simultaneous events → a chord, gaps → rests) turn the client's own `clausters.seq` data into **MEI** that flows into the same `engrave`/`Score` path — a third way in, beside typed text and the SVG adapter, with `Score.from_notes`/`from_timeline` as the generate-and-edit shortcut. MEI is the target because it spells pitch and value explicitly, sidestepping ABC's contextual traps, and it needs no ids (Verovio mints them on load, so id stability across editing is unchanged). The substance is the rhythm: durations decompose into tied note values (a dotted value when exact) and a note overrunning a barline splits and ties across it, all in exact 32nd-note ticks. Example `examples/gui_score_from_data.py` (a Timeline of chords + melody, engraved then played from the same timeline); tests cover the pure encoder (spelling, decomposition, cross-bar ties, chords/rests) without the engraver. v1 reads only the written `dur`; the refinements are the next line.
- ⬜ **G31g — Engraving refinements** *(no date)*: not permanent limits, the refinement pass on the same encoder, kept behind narrow seams (the `dur`→value and pitch-spelling helpers) so it extends rather than rewrites. **Written duration beyond `dur`** — read `sustain`/`legato`/`stretch` for performance nuance (staccato dots, a note drawn shorter than its slot); **tuplets/triplets** (v1 snaps off-grid durations to the 32nd grid and ties, never a tuplet); **full polyphony** (v1 chords are same-beat same-value stacks in one layer — the refinement adds voices/staves and mixed-duration simultaneity, **composing several `voice`s above the monophonic `voice_to_mei`, which stays the per-layer primitive** rather than being widened); **tonal spelling, grace notes, articulations/dynamics** from custom `Event` keys, and key-aware suppression of redundant accidentals. (Moving the encoder itself into the shared core is not here — it is part of G31h, the whole notation layer's move.)
- ✅ **G31h — The notation layer moves to `clausters-notation`/`core`/`ffi`** *(done 2026-07-24)*: the entire notation layer used to live in the Python client (`clausters.gui.notation`) — the `ctypes` binding to libverovio, `svg_to_display_list`, the editable `Score` model (`transpose`, `undo`/`redo` over its own MEI-snapshot stack, the edit→commit→reload cycle) and the `from_notes`/`from_timeline` encoder. The host is deliberately **dumb about the domain** (it draws the display list and reports gestures as requests, holding no score), which keeps it reusable across languages — but it pushes all the notation *logic* onto the client, so a second client (JS/wasm) would reimplement every line. Per the cross-client rule (`clients/python/PLAN.md`, "Build strategy") that logic moves down and each client rebinds it. **The planning pass settled the shape — this is now a build, not a design:**
  - **Where the native half lives.** libverovio is C++; `clausters-core` is deliberately `build.rs`-free and wasm-clean, so it cannot host the binding. A new **`clausters-notation`** crate (its own `build.rs`, the libverovio binding, the `Score` model) owns everything native, behind a `verovio` feature **default-off** for flexibility — the Python reference wheel turns it on (`build_native.py`), exactly as it already stages `libverovio.so`. `clausters-ffi` re-exports its `extern "C"` surface. Linking/staging mirror libfaust: `DT_RPATH` of `$ORIGIN`/`$ORIGIN/../_libs`/prefix, `CLAUSTERS_VEROVIO` to locate, the SMuFL resource dir resolved inside the Rust binding.
  - **Where the pure half lives.** The SVG→display-list walk (moving to Rust XML parsing via `roxmltree`, wasm-safe — no duplicate parser per client) and the MEI encoder go into **`clausters-core`** behind an opt-in `notation` feature, so they compile to wasm and a future JS/wasm client reuses them. The `Event`/`Timeline`→*voice* reduction reads client-native types and **stays in each client**; the core exposes `voice_to_mei(voice, meter, clef, key)` — that is where the agnostic/shell line falls.
  - **The producer stays the SVG walk, not the native `DeviceContext`.** Binding libverovio in Rust makes `RenderToDeviceContext` reachable, but verovio-wasm only emits SVG, so a native `DeviceContext` producer would force the wasm client onto a *different* producer that would have to agree with it bit-for-bit. The SVG walk is **one producer every client shares** (native libverovio→SVG and verovio-wasm→SVG both feed the same Rust walk), so cross-client parity is structural, not asserted. The native `DeviceContext` (no serialize round-trip, in-memory relayout) becomes a later *optional producer-swap*, decoupled here (`docs/decisions.md` updated to say so).
  - **The C-ABI** is a near 1:1 relabel of today's `Score`: an opaque `*mut Score` handle (`score_open`/`_free`/`_display_list`/`_mei`/`_transpose`/`_edit`/`_undo`/`_redo`/`_can_undo`/`_can_redo`), the one-shot `engrave`, and the pure `svg_to_display_list`/`voice_to_mei` — every JSON return *size-then-fill* into a caller buffer (the `clausters_core_patch_compile` pattern), every stateful call carrying the handle (the `Scheduler`/`SampleClockModel` pattern).
  - **Editing ports minimal and extensible.** The move carries the existing minimal edit (transpose = verovio `keyDown`+`commit`, whose `commit` **applies verovio's own autolayout** for the definitive placement — which is why the round trip re-sends a whole display list rather than nudging one primitive), our MEI-snapshot undo stack, and the raw `edit(action, …)` escape hatch that already passes any `EditorToolkitShared` action (`insert`/`delete`/`set`/…). Richer editing rides that same seam **later** — not in G31h.

  The build, in four steps:
  - ✅ **G31h-a — The `clausters-notation` crate + libverovio in Rust** *(done 2026-07-24)*: the new crate binds libverovio through its C wrapper (`vrvToolkit_*`) — a safe RAII `Toolkit` and a one-shot `engrave_svg` — behind the `verovio` feature **default-off** (the crate is empty without it, so `cargo build --workspace` links no libverovio). `build.rs` links it from `VEROVIO_PREFIX`/`~/.local`/`/usr/local` with the relocatable `DT_RPATH` libfaust uses, and bakes the build prefix's `share/verovio` in as the run-time resource fallback (overridable by `CLAUSTERS_VEROVIO`). An `ffi_lock` serializes the library (verovio's resource init is global, and the Rust harness runs tests in parallel). Verified **byte-for-byte geometry parity** against the Python client's engraving of the same phrase: identical once the per-load random `xml:id`s are normalized out. The SVG→display-list walk is still Python's — it moves to `core` in G31h-b.
  - ✅ **G31h-b — The pure half into `core`** *(done 2026-07-24)*: `svg_to_display_list` (over `roxmltree`, with `regex` parsing transforms/glyph refs exactly as the Python original) and `voice_to_mei` now live in `clausters_core::notation` behind the `notation` feature (**off by default** — a plain core pulls no XML/regex/JSON weight), and the module compiles to `wasm32-unknown-unknown`. The `Event`/`Timeline`->*voice* reduction stays per-client (it reads client-native types); the core takes the reduced [`Slot`] voice, which is where the agnostic/shell line falls. The pure-logic tests migrated to Rust — pitch spelling, tick decomposition, cross-bar ties, chords/rests, clef/key — and the SVG walk gained the unit tests Python never had (one primitive of each kind on a hand-built SVG: the note-owns-its-stem rule, the glyph flip, line-vs-fill, the deepest font size, the measured step, the codepoint scan). **Parity verified against the Python original**, both directions: `voice_to_mei` is **byte-identical** to `_mei_document` across five voices (melody, cross-bar tie, chord+rest, empty bar, alternate clef/key), and `svg_to_display_list` reproduces Python's display list **prim-for-prim in draw order** (plus vb/glyphs/step) on the engraved phrase's real verovio SVG. The Python client is untouched — it still runs its own copy; the ctypes rebind onto this core is G31h-d.
  - ✅ **G31h-c — The stateful `Score` + the full C-ABI** *(done 2026-07-24)*: `clausters_notation::Score` is the editable model — `open`/`display_list`/`mei`/`transpose`/`edit`/`undo`/`redo`/`can_undo`/`can_redo`, carrying the edit→commit→reload cycle, the MEI-snapshot undo stack (64 deep) and the never-drawn guard that would otherwise segfault the editor — and `clausters-ffi` exposes it behind two new features, `notation` (pure) and `verovio` (native, pulls `notation`), both **off by default**. `display_list` returns the whole `Page`: the drawing layers flattened together with `cursors` and `notes`, so one call carries what the host draws, where the playhead goes and what sounds — they must come from one engraving, since ids are minted per load. The **timemap→cursor fold moved to `core`** with the rest of the pure half (`cursor_track`, over the display list's own geometry), leaving only the timemap/MIDI reads native. The C ABI is `clausters_core_svg_to_display_list` / `_voice_to_mei` plus the opaque `clausters_score_*` handle, size-then-fill throughout; `CORE_ABI_VERSION` is 12. **The one-shot `engrave` is deliberately absent from the ABI**: size-then-fill needs the payload identical on both calls, and the engraver mints fresh `xml:id`s of varying length per load, so sizing and filling would engrave two different documents — a binding's one-shot is `score_open`→`score_display_list`→`score_free`, where the ids are minted once (recorded in `docs/decisions.md`). A `links = "verovio"` key publishes the build prefix to `clausters-ffi`'s own `build.rs`, so the shipped cdylib carries the same relocatable `DT_RPATH` the rlib does. The Python client is still untouched; the rebind is G31h-d.
  - ✅ **G31h-d — The Python rebind** *(done 2026-07-24)*: `clausters.gui.notation` is now a ctypes shell over the ABI — **public API unchanged** (`Score`, `engrave`, `page_json`, `from_notes`/`from_timeline`, `svg_to_display_list`, `score_view`), roughly 1120 lines down to ~380, with the verovio ctypes binding, the SVG walk, the MEI writer and the cursor/geometry folds all gone into the workspace. What stayed is what reads Python-native types: the `Event`/`Timeline`→*voice* reduction, `page_json` and `score_view`. Python owns the **handle's lifetime** (the score is freed with the object) and nothing else. `_native` gained `has_notation`/`has_engraver` (the symbols are bound only if present, so a cdylib built without the features still loads and every other entry point works) plus the `size_then_fill`/`as_u8` helpers the surface needs; `clausters_score_open` grew an `options` JSON argument so the client's `options={"unit": 6}` still reaches the engraver without the ABI growing a parameter per knob. `build_native.py` builds the FFI with `--features verovio`. **Verified against the pre-move implementation**: engraving the same phrase through both produces identical `vb`/`step`/`glyphs`/`prims`/`cursors`/`notes` (ids normalized), and the client suite passes unchanged (381 tests) — its pitch-spelling and tick-decomposition unit tests were dropped, having migrated to Rust with the encoder. Docs updated: `docs/architecture.md` (the layer is shared, not per-client), `docs/decisions.md` (the `DeviceContext` entry now says the move happened and kept the SVG producer), `docs/clients.md`, `docs/gui-protocol.md` and `BUILD.md` (libverovio is behind a Cargo feature now, not "Python-side only").

Open lines (unnumbered until a design converges):

- **Per-element edit properties.** A widget-level `editable` flag now gates the whole page — off by default (a `score` is a read-only view; a drag does nothing), on for an editor that applies the `"transpose"` round trip, since the host cannot fulfil an edit the driver will not apply. That is the coarse cut; the finer one remains open: *which element* allows *which* edit is a per-element property (a notehead transposes, a barline does not), deliberately not gated yet since Verovio's editor is still developing and the obvious next wants (lengthening a stem, which autolayout normally does) are edits, not restrictions.
- **More edit verbs.** The payload is a tag plus flat primitives, so duration, accidentals, articulation and stem direction extend it without touching the protocol — each is a Verovio editor action plus a client method, the `"transpose"` shape reused.
- **Notation as a composition surface.** The `score` is a view of a *score*; binding it to the arrangement (`clausters.form`) the way `track`/`clip` are bound is the open question the multitrack and piano-roll views already answered for their own material.

## G32: host gaps the editor example turned up

Three of them, found while porting `gui_editor.py` to the browser. The first
two are **platform-independent** — they live in the shared widget/render code,
so the native front has them too — and are about a view that is *driven* rather
than merely opened; the third is the browser front's. The first is closed; the
other two remain open (unnumbered until their design converges).

- ✅ **G32c — `timeruler`: the axis ruled once, above the lanes** *(done
  2026-07-28)*: a ruler over a multitrack belongs to the **axis**, not to any
  one lane, and a `track`'s `ruler` strip is reserved out of *that lane's*
  height — so ruling a stack meant electing one lane to carry the strip and to
  pay for it, and the strip then sat wherever that lane sat (between two lanes
  unless it was the last). The new `timeruler` widget owns its own box: the
  document places it above the lanes and no lane loses a pixel. It is a timeline
  widget like any other — it joins the group its `link` names, reads that
  group's window, and its ticks are indented by a lane's header width so they
  stand over the samples they label. Gestures are the DAW's: a press **locates**,
  Shift+drag pans, the wheel zooms. Deliberately **no sizing concept of its
  own** — its thickness is the `h` place prop, like every other widget's, with
  the default in the builders (`track`'s vestigial `height` is read by nobody,
  which is the counter-example). Python and TypeScript builders, both catalog
  tables, and `composer.html` now rules from the top. Tests: the ruler and the
  lanes resolve to one group key, it follows the lanes' window across a zoom, and
  it contributes no extent of its own.

- ✅ **G32d — A roll's axis exists before its notes, and follows them**: the
  gap the client-side MIDI painting turned up (`gui_pianoroll_midi.py`), the
  piano-roll's version of G32b. A `pianoroll` opened *empty* registered extent
  0, so its window was the single sample `View::full` floors to and every note
  written into it by `/gui_set` landed outside — the roll stayed blank however
  much was painted. Two halves, both the DAW's: the roll now navigates an
  **empty grid** it has before it has content (`EMPTY_BEATS`, sixteen beats read
  off its own `tempo`/`sample_rate`; a floor on the *axis*, never on the extent,
  so nothing that measures material sees it) and the authoring headroom that
  used to be the lane's alone; and a `/gui_set` of `notes`/`osc` re-registers
  the extent **keeping the window** (a take grows under a still axis instead of
  zooming it out from under the notes just drawn) and **pages the axis forward**
  when the take passes the right edge. Tests: an empty roll spans its grid and
  survives its first note; a take written past the edge pages one window on at
  the same zoom.

  **Open — unify the follow rules.** Three places now decide how a window
  follows content it does not contain: this paging, G32b's edge auto-scroll
  during a clip drag, and the notation cursor's follow. They share one
  principle — *at constant zoom, the window goes where the writing is* — and
  differ only in what triggers them (content arriving, a held cursor, a moving
  playhead). Factor the principle into one place rather than letting three
  copies drift; unnumbered until the shape is clear.

- ✅ **G32b — A clip dragged to the edge pulls the view** *(done 2026-07-28)*:
  the companion gap to G22's "empty time to zoom out into". The headroom let a
  lane be zoomed *out* past its content, but a drag still mapped the cursor
  through the **press-time** window and nothing scrolled while it was in
  flight — so a clip could never travel more than one visible window per
  gesture, and holding it against the lane's edge did nothing, because a
  standing pointer sends no events. Zoomed in to place something precisely,
  that was a sliver. Now the drag maps through the **current** window (which is
  the whole mechanism: panning under a standing cursor moves the sample beneath
  it and the clip follows), and `Gestures::tick` pans while the cursor is held
  within `EDGE_MARGIN` of a lane's edge, at a rate that is a **fraction of the
  visible window per second** rather than a pixel rate, so it behaves the same
  at every zoom (rationale in `docs/decisions.md`). The tick is each front's
  existing frame timer — the one already driving meters, scopes and the
  playhead — so both fronts get it from the one shared method, and an
  otherwise-still window runs the timer only while such a drag is in flight.
  `ClipHit`/`Drag::Clip` carry the **lane** id now: a clip is not itself a
  navigation-group member, so everything reaching the shared axis asks through
  its lane. Tests: the right edge pulls the view and carries the clip (and
  reports both the `"clip"` and the lane's `"view"`), a cursor clear of the
  margins scrolls nothing, the scroll stops with the drag, and the left edge
  walks back and parks at the origin without the clip going negative.
  `clients/web/examples/composer.html` is where it was found and verified.

- ✅ **G32a — The playhead follows a loop** *(done 2026-07-28)*: the swept line
  was one subtraction in `host::frame` (`sample_clock - playhead_at`), which is
  why following a transport costs **no messages** and is exactly right for a
  straight pass — but it had no notion of a loop, so a repeating region (an
  editor's "play selection", a looping clip) ran off the view instead of being
  followed. Shape **(a)** landed, the explicit region: `playhead_loop_start` /
  `playhead_loop_len` on the shared `EditorProps`, the swept position becoming
  `start + ((sample_clock - playhead_at - start) mod len)` and `len <= 0`
  keeping the straight sweep. It beat the `playhead_loop`-flag-over-the-
  selection alternative because it does not tie playback to a selection a
  gesture can change under it, and because a client may loop something that is
  not the selection (rationale in `docs/decisions.md`). The whole sweep rule
  moved into **one** place — `head_at` (and `swept_at`, which keeps the
  playing/stopped distinction the MIDI painting needs), on `EditorProps` then
  and on `GroupState` since E3 — so the four
  sites that each re-derived it (the timeline views, the piano-roll, the track
  lanes, the native MIDI cursor) now agree by construction; the `score` carries
  the same pair in **ms**, its own unit. Group-wide like the anchor, held
  by the group model, so linked views wrap at one place. Python and
  TypeScript builders and `docs/gui-protocol.md` carry the props;
  `clients/web/examples/editor.html` uses them — and documents the one real
  trap, that a looped pass anchors at `clock - start`, not `clock`. Tests: the
  wrap model (straight pass, static park, wrapping, a non-positive length) and
  the group-wide routing.

**A heavy view's source is fixed once sent.** `/gui_set` applies the chrome
(selection, playhead, rulers, the spectrogram's contrast) but not `buffer`,
`path`, `cache`, `data` or `blob`: `widget::apply` has no arm for them. So an
editor that opens a second file must close its window and send a new GuiDef,
losing the navigation, the selection and the group the user was working in. The
question to settle is whether a source swap is worth its cost — it drops the
peak pyramid and the STFT cache and re-fetches, which is most of the work of a
new def anyway — or whether the honest answer is that a view *is* its source
and the client should redefine. If it lands, the reset must be explicit about
what survives (the view window? the selection? the link group?), which is the
part that makes it a design and not a patch.

**A canvas starts at the host's size, not the page's.** The browser front
creates every surface with `CANVAS_SIZE` (480x420, `host::web`), overriding
whatever the element already measured — so a page that sizes its canvas before
the def goes out sees it reset. There *is* a way through, and it is the right
one: `GuiBridge::resize`, which is what a `<clausters-bundle>` drives from its
`ResizeObserver`; the web client now offers it to any script as
`guiHost().fit(defId, element)`, so this is no longer a wall. What remains is
the smaller correctness point: when the page supplies a canvas that already has
a size, the front should adopt it instead of stamping the default on top and
waiting to be corrected.

**Acceptance:** ✅ an editor plays a selected region in a loop and the line
sweeps that region, wrapping, with one message sent when playback starts and
none after — the sweep is shared code, so both fronts get it from the one
`head_at` (on `GroupState` since E3); a view fed a second buffer redraws from it without the
window being redefined, if the source swap lands; and a canvas supplied at a
size keeps it, with no `resize` needed to correct the default.

## G33: every live view names a bus and a rate

- ✅ **G33 — Every live view names a bus and a rate; the sample ring is the
  server's** *(done 2026-07-28)*: the four live data views took three different
  kinds of number — a `meter` a control bus, a `scope` either a control bus or a
  *tap* (an index into the segment's eight sample rings), the goniometer and the
  spectroscope only taps — so the surface claimed two kinds of signal where the
  server has one kind at two rates, and the ring index (which the caller had to
  allocate, route and release) leaked through the wire, the host, both clients
  and every example. Now each view takes **`bus`, `rate` and `channels`**
  adjacent buses (the SuperCollider model; audio is the default and bus 0 is the
  first hardware output, so a bare `scope()` is the console scope). `/bus_tap bus
  watch` replaces `/bus_tap tapIndex bus`: the **server** picks the ring and
  publishes the choice in a per-bus directory in the segment (ABI v3 → v4),
  counting watches so views share one ring and the last one to stop frees it;
  the **host** is what turns a widget into that command, diffing the audio buses
  its open documents read on every def/free/set; a `/bus_tapStream` subscription
  *is* a watch, so a browser client never sends it. A `meter` needed a path of
  its own — eight rings cannot back a mixer, and a meter wants one number per
  block, not samples — so the segment also carries a **per-bus level**, the
  block peak held with a 20 dB/s decay (correct for a reader a dozen blocks
  slower than the engine, and for several readers at once, which a
  reader-clears-the-max scheme is not). Ported to both clients in the same
  change; the Python and TypeScript tap allocators are gone. Rationale in
  `docs/decisions.md`.

## G34 — The multitrack editor reuses the widget set that grew past it

Found while reviewing `gui_composer.py`. `clausters.gui.Editor` composes its window the way it did when the lanes first landed (G22) and has not followed the widgets that landed after — so the Python multitrack view builds by hand what the catalog now provides, and the two multitrack views have **drifted apart**: the web's `composer.html` already rules from the top with the `timeruler` G32c added, while the client that is supposed to be the reference for the port does not know the widget exists. That inversion is the milestone's real subject; the individual gaps are small and mostly mechanical.

- **The ruler is elected, not placed.** `Editor.draw` walks the lanes backwards and mutates the last `track` dict (`clients/python/clausters/gui/editor.py:257-260`, `lane["ruler"] = "beats"`), which is exactly the pattern G32c replaced: the strip is reserved out of *that lane's* height, and because a patch lane is skipped when electing, it can land **between two lanes** rather than under the stack — the failure G32c named, still reachable from Python. Place a `timeruler` above the lanes instead.
- **The editor names no navigation group.** No lane carries `link`; the stack shares an axis only because `Widget::link_lanes` keys an un-linked lane by the window root (`clients/gui/src/host/widget/mod.rs:1327-1337`). A `timeruler` joins the group its `link` names, so it cannot join a group that has no name — which is why the ruler fix drags this one along, and why the web example writes `link: 1` explicitly.
- **The lanes are not told the bar.** `_lane` passes `sample_rate`, `tempo` and `snap` but not `quant` (`editor.py:713-720`), so a beat ruler labels beats with no bar grid; the web composer passes `quant: 4.0` to every lane and to its ruler.
- **Nothing sizes a lane.** The editor gives no height and could not: `track`'s `height` is vestigial (G32c: "read by nobody"). Every lane is therefore elastic weight 1 and any chrome a script hands to `extra` takes a lane's whole share — `gui_composer`'s transport did precisely that until it was given a fixed `h`. Decide it here: a lane sizes with the place props everything else uses (`h`/`weight`), and the dead `height` prop goes.
- **The window sets no `margin`/`gap`** (`editor.py:261`), so the lanes touch the frame and each other; the web composer uses 8 and 6.
- **The transport strip is copied, not built.** `Transport` is the shared *logic*, but the same four buttons are hand-written in `gui_composer.py`, `gui_score.py` and `gui_score_from_data.py`. A builder for the strip (named buttons, a fixed `h`) would end the copy and give the sizing one place to be right.

**Open — the stack does not scroll, and the vertical scroll view cannot simply wrap it.** Lanes are children of the window in a `col` layout, so a composition of many lanes divides the window instead of scrolling. The obvious fix — a `scroll(axis="y", zoom=False)` around them, the constrained form L3 fixes — collides with the gesture the lanes already own: `Gestures::wheel` gives a timeline view the wheel first (`clients/gui/src/host/gestures/mod.rs:1824`) and only falls through to the workspace when nothing claimed it (`gestures/mod.rs:1859-1862`, "a widget with its own wheel won above"), and a drag inside a lane is the clip's. A scrolled stack of full-width lanes would therefore scroll nowhere but over its own gaps. So the scrolling model is a decision, not an edit: lane heights plus a scrolled viewport, with the wheel arbitrated (a modifier, a header/gutter that yields it, or a visible scrollbar) — and whatever is decided holds for the piano-roll and notation views, which own the wheel the same way.

**Acceptance:** a composition of a dozen lanes is navigable in a window that shows four, with the ruler above the stack labelling bars, the lanes aligned on one named group, and a transport strip that stays a strip — checked by eye through `gui_composer.py`, and ported so the Python and web composers compose the same window.

## L track — the look: layout, sizing and themes for the light widgets

Section added 2026-07-19; ordered before the P track because the patcher's surfaces build on exactly these primitives — a positioned child, a themed accent, a sized label. This track delivers what the layout engine deliberately deferred ("children are evenly sized at this milestone") plus the customization the protocol has none of today: no color prop on any widget, no per-child size, no text size or wrap, margin/gap as compiled constants. The goal is versatility of *composition* — a script must be able to build a real application face (a menu bar, a working area, a status bar) from the same light elements — while the elements themselves stay simple and cheap to draw: flexibility lives in the layout and the theme, never in the widgets' drawing cost. Five design rules bound it, all in the direction of keeping the host light:

- **Parametric, never structural.** *How* a widget is drawn stays monolithic and compiled — one `Mesh` pass, the embedded font, fixed shapes — because that is what keeps a whole window one draw batch. Customization is a *parameter* of that fixed drawing (a color, a size, a wrap width), never a template, box-model or cascade system. There is no styling language; there are a few props and a theme.
- **The basics stay light, the particular stays particular.** The new props are the small general set every widget can share (`w`/`h`/`weight`, `x`/`y`, `color`, `text_size`, `wrap`, `align`, container `margin`/`gap`). Anything specific to one visualization remains that widget's own Rust prop, exactly as today — the spectrogram's `colormap` is the model, not the exception.
- **Color by function, not by part.** Every chrome color becomes a named **role** in one `Theme` (background, panel, field, text, dim text, accent, selection, …); every paint site names a role, no paint site names an RGBA literal. The roles themselves are **programmatically overridable at every level** — host-wide from the style file, per subtree through a container's `theme` prop (the theme group, L5), per widget through the single `color` prop (the shorthand for the accent role, the color that carries a widget's function: a meter's bar, a slider's handle, a trace). What stays closed is the *part*: a widget's pieces are only ever named by roles, never addressed individually — no selectors, no per-part styling.
- **Size by role, not by literal** *(added 2026-07-29, with L6–L8)*. The twin of the color rule, one step behind it: sizes become named roles in one **metrics** table — `pad`, `gap`, `control_h`, `track_thick`, `hit_slop`, `text_scale` — and no layout or paint site names a number, exactly as no paint site names an RGBA. The roles' defaults come from a single quantized modular scale over the font cell rather than from per-site invention, and the table is **constant**: the scale generates the defaults once, it is never arithmetic on a frame. A widget's own extent is a pure function of that table (its **natural size**), so density and HiDPI are one multiplier applied to one table, never a pass over the tree.
- **Monolithic per host, portable per def.** A host has one look, loaded from a **theme file** — a TOML role table through the shared config — and that uniformity is a feature. A GuiDef that needs its own look carries partial theme overlays in its tree, so a standalone bundle ships its appearance the way it ships its defs.

Out of scope, recorded so they are not relitigated per milestone: per-widget borders/radius/shadows and a real font atlas — each folds into a milestone only if a need firms up (the font is the likeliest candidate, see L4). The recursive theme overlay of L5 is deliberately the *only* cascade-like mechanism, and it cascades role tables, never per-part rules.

- ✅ **L1 — the `Theme`: color roles centralized + the host style file** *(done 2026-07-19)*: a pure refactor with zero visual change that creates the seam everything else styles through. Every RGBA literal scattered across `controls.rs`, `frame.rs`, `meters.rs` and the view painters moves into one `Theme` struct in the agnostic core (`host/theme.rs`), its fields named by **function**: `background`, `panel`, `field`, `track`, `text`, `text_dim`, `accent`, `accent_dim`, `hilite`, `border`, `ruler`, `selection`, `playhead`, `warn` (the clip/error red), and the **series palette** (the per-channel cycle the overlay traces and multichannel lanes already share informally). The default `Theme` reproduces today's exact values. The host loads a theme from `[gui.theme]` in the shared config (`role = "#rrggbb[aa]"`; a partial table overlays the default, so a style file names only what it changes) and from `--theme <file>` for a free-standing style file; the web front takes the same table through the `GuiBridge`, so both fronts render one look from one source. Tests: hex parse round trips, partial-overlay semantics, and the default-equals-today guard (the frame snapshot tests, where they exist, unchanged).

- ✅ **L2 — the layout props: per-child size, weight, flow and free positioning** *(done 2026-07-19; sizes are device pixels, like every other pixel in the host — the roadmap's "logical px, HiDPI-scaled by the front" is deferred until the frame as a whole scales, text and margins included)*: today's even split becomes the *default case* of a richer rule that stays **one pass, no measurement, no constraint solver** — a deliberate boundary: when a layout needs negotiation, the answer is explicit sizes, not a solver. Along a `row`/`col` main axis a child with `w`/`h` is **fixed** (logical px, HiDPI-scaled by the front); the remaining children share the remainder by `weight` (default 1, so every existing GuiDef lays out pixel-identically); the cross axis keeps filling. `grid` gains `cols` (absent, today's near-square). Containers gain `margin` and `gap` props (defaults the current 6). `free` becomes real free placement: a child with `x`/`y` (+ optional `w`/`h`) positions absolutely inside the parent, and a child without them keeps the full-area overlay — the current behavior, preserved. All pure geometry in `layout.rs`, unit-tested without a window; the Python builders grow the props with the same defaults. The acceptance shape is the **application shell**: a `col` window holding a fixed-height menu-bar row (`menu`/`button` children), a `weight`-ed content area and a fixed-height status-bar row (`label`s a script live-updates via `/gui_set`) — versatility from *composition and flow*, with every element staying a handful of flat-colored quads in the one mesh batch, so a full application chrome costs what a panel of sliders costs.

- ✅ **L3 — the 2D workspace: a scrolling, zooming container, general first, constrained by configuration** *(done 2026-07-19; two departures from the sketch below, both recorded here: the view is **one uniform scale plus a pan offset per axis** rather than a `viewport::View` per axis — the two axes share a scale so the plane never distorts, and an independent per-axis clamp would de-couple them, so the anchor-preserving pivot math is reused on the scale factor (`host/scroll.rs`) instead of on two `View`s; and the clip is **geometric** (a Sutherland-Hodgman clip inside `paint::Mesh`) for the flat chrome, with a pass scissor only for the heavy views' own pipelines — which keeps the whole window one mesh upload and one draw call, the L-track cost rule, where a per-widget scissor would have split the batch)*: a new light container, `scroll`, whose children live in a **virtual content area** (its size from the free-placement extents of L2, or explicit `content_w`/`content_h`) seen through a 2D window — one `viewport::View` per axis, so the anchor-preserving zoom/pan math, already pure and unit-tested, is reused rather than rewritten. Deliberately **general first**: the default is the full 2D workspace (pan both axes, wheel zoom anchored at the cursor), and the *constrained* scroll views degrade from it by configuration — `axis: "y"` with `zoom: 0` *is* a plain vertical scroll view, `axis: "x"` a horizontal strip — one widget, one gesture path, the restricted forms costing nothing extra; if a constrained variant later earns an optimization, it specializes from the general one, not beside it. View state (`view_x`/`view_y`/`view_zoom`) is settable via `/gui_set` and emitted as the existing `"view"`-family payloads; gestures ride the shared `gestures` machine (browser parity by rewiring, the G27 posture); drawing stays the one mesh batch, clipped by the widget's scissor rect exactly as every widget already clips to its viewport. This is also the seat the P track's patch canvas (P1) sits on: the patcher's pan/zoom becomes this container's, not its own machinery. A further horizon this generality deliberately keeps open (recorded under Future directions, not opened here): the workspace's x `View` joining the window's **navigation group**, which turns the plane into a time-axed surface — the free arrangement plane. Pure tests: content extents, per-axis clamp/zoom, hit-testing through the transform, the constrained degenerations.

- ✅ **L4 — text: size, wrap and alignment on the light widgets** *(done 2026-07-19; `text_size` is clamped to `[1, 16]`, and the ellipsis is a dedicated single-cell glyph so a clipped string costs one cell, not three)*: `text_size` (a scale over the embedded 5×7 font; today's fixed 2.0 becomes its default) on every text-bearing light widget — `label`, `button`, `toggle`, `text`, `number`, `menu` and the control labels; `label` additionally gains `wrap` (word wrap on the font's fixed advance — a cheap width computation, no shaping) and `align` (`start`/`center`/`end`). Single-line text that overflows its rect clips with an ellipsis instead of bleeding into the neighbor. The font itself deliberately stays the embedded bitmap: weight, zero dependencies and wasm parity all argue for it until a real need is demonstrated — a crisper font (an atlas) is the future direction this track explicitly does not open.

- ✅ **L5 — theme groups: the recursive role overlay + the `color` prop** *(done 2026-07-19; the `color` prop re-seeds the whole function family — accent/dim/lit by the default theme's own ratios, the trace pair, `series_1` and the placed-object pair — so one hex behaves like the default green does)*: the customization surface reaches the wire, in one recursive mechanism. Any **container** (window, panel, scroll, track) may carry a `theme` prop — a partial role table (JSON object, role → `"#rrggbb[aa]"`), the same shape as the TOML file — that overlays its parent's theme for its whole subtree: a **theme group**, a style scoped to the *function of a set of widgets* (the transport bar dimmed, the recording strip warm, the analysis pane cold) rather than to any individual part. The concept is recursive by construction and cheap by construction: overlays **resolve at mutation points** (define / `/gui_set`), each widget ending up with one resolved theme reference, so the per-frame drawing path reads exactly one theme per widget and pays nothing — the less-overhead alternative to selector/cascade systems, recorded as such. The leaf case is the single `color` prop: a per-widget overlay of just the **accent** role — the meter's bar, the slider/knob handle and fill, the button/toggle face, a scope/spectrum/plot trace (re-seeding the series cycle for its multichannel siblings), the bpf curve, a clip body. A `theme` on the GuiDef root persists with a named def, so a standalone bundle keeps its look with zero host configuration; everything is live via `/gui_set`. Python: `color=` on every builder, `theme=` on the container builders; example `examples/gui_style.py` (one panel under the default theme, a file theme and a nested theme group, plus per-widget accents); docs: the props join the catalog in `docs/gui-protocol.md`, the theme file lands in `docs/configuration.md`.

**L6–L8 added 2026-07-29 — the half of the look L1–L5 did not cover: how big things are.** L1 centralized color and L2–L5 gave composition its props, but *size* stayed where it always was: every widget fills the cell the layout hands it, and the numbers around it are per-module constants invented at their own sites — `PAD` three times over (`controls.rs`, `meters.rs`, `track.rs`), `MARGIN`/`GAP` in `layout.rs`, `TRACK_THICK 4`/`HANDLE_THICK 8`/`HANDLE_GRIP 18`, `HEADER_W 96`, `RULER_H 18`, `LABEL_GAP_PX 14`, three separate text scales. Two consequences, both visible. **Nothing has a size of its own**, so a `button` alone in a `col` is as tall as its share of the window — a slab; the shipped workaround (`controls::button` filling its whole cell) fixed the opposite failure, a box shrunk to the text height floating in dead space, and the two failures are the same missing concept seen from either side. And **4, 6, 8, 14, 18, 24, 96 belong to no series**, so nesting one widget in another lands proportions nobody chose. The three milestones follow L1's order — the pure refactor that creates the seam first, then what it makes possible — and none of them adds a dependency, a measurement pass, a solver or a second draw call: the whole track's cost rule (one mesh upload, one draw call per window) is unchanged, and the per-frame path gets *cheaper*, since resolved metrics replace arithmetic now done at paint sites.

- ✅ **L6 — the metrics table: one sizing scale, no literals** *(done 2026-07-29; four departures from the sketch below, all recorded here. The **grid is 2 px, not 4**: the shipped spacing pair is 6 and every shipped chrome extent is even, so a 4-px grid would have moved them and cost the refactor its zero-visual-change guarantee — the generator quantizes extents onto 2 px, hairlines (`divider_w`, `tick_gap`) onto whole pixels. **Text scales quantize to half-steps**, not integers: `caption_scale` ships at 1.5 (the ruler and clip captions) and the bitmap cell has no rung between 1 and 2, so the integer rule holds for the whole-text roles while the reduced caption keeps its half-step — revisit when the font atlas L4 named lands. **The table holds the shared sizing vocabulary, not a widget's own structural geometry**: the patcher's box/port series (`patch.rs`'s own `PAD 8`/`HEAD_H`/`ROW_H`/`STRIP_H`, which interlock in its bounds math), the piano roll's key gutter and velocity/OSC lane heights, the score's staff step and the waveform's amp margin stay in their modules — a role earns its place when more than one widget means the same thing by it, which is what keeps the table readable. Sub-pad optical nudges (a 1–3 px lift on a label baseline, a knob's inner insets) likewise stay literal. And the **density multiplier covers this table, not the wire's own `text_size` prop**, which stays the number the script sent — the wire becoming scalable units is L8's business. Plus the roles the sketch did not name but the constants needed: `ruler_w` (the vertical-ruler strip, 46), `box_side` (a square marker, 24), `indent` (a tree's nesting step, 14), `label_gap`/`tick_gap` (the ruler ladders' collision floors, 14/7), `trace_w` (a drawn trace's weight, 1.5) and `micro_scale` (1.0, the densest in-view mark: a key's octave label).)*: `host/metrics.rs`, the peer of `host/theme.rs` in the agnostic core, same macro shape, same partial-overlay semantics, same config path (`[gui.metrics]` in the shared config beside `[gui.theme]`). Roles named by **function**, not by widget: the spacing family (`pad`, `gap`, `margin`), the control family (`control_h`, `row_h`, `track_thick`, `handle_thick`, `handle_grip`, `knob_d` — its own role, 5–10% over the square nominal, because a disc reads smaller than a box of the same bounding rect), the chrome family (`ruler_h`, `header_w`, `divider_w`, `point_radius`, `hit_slop`) and the text family (`text_scale`, `label_scale`, `caption_scale`). Every constant listed above moves into it and every site reads a role. The **defaults come from one quantized modular scale**: the base is the font cell (`font::GLYPH_H` at scale 2 = 14 px, not a round decimal, because every readable widget is text plus padding and the cell is what makes a button, a number field and a menu line up unaided); spacings step on a 4-unit grid; **text scales are integers only** — the embedded font is a bitmap, so a fractional scale makes a glyph's own pixels unequal, which reads as ragged rather than soft, and that is why the ratio governs spacing and control heights but never the font. The scale is a **generator for the table's defaults, not a runtime computation** — the resolved table is constant data. One `scale` multiplier in the config gives compact/comfortable density for free, which is deliberately the *whole* density surface: no `xs`/`sm`/`md` token props on the wire, no per-widget density, since a host has one density the way it has one look. Tests: role parse and partial overlay (mirroring L1's), the grid/integer quantization invariants, and a default-equals-today guard so the refactor is provably invisible. Wasm-clean by construction (`check-wasm.sh`), so both fronts inherit it with no web-side work. **How it is threaded** (the shape L7/L8 build on): the table lives on the `Host` beside the theme, reaches the render through `FrameInputs::metrics` (so every draw pass that already had the inputs has it), reaches `layout::layout`/`scroll_content` and the per-widget draw/hit functions as one `&Metrics` parameter beside their `&Theme`, and reaches the hit-test/gesture side as `host.metrics` — passed, never global, because L8 resolves one table per window. The browser gets the sibling setter `GuiBridge::metrics(json)` next to `theme(json)`, so a page restyles sizes the way the config does.

- ✅ **L7 — intrinsic size: a widget knows how big it wants to be** *(done 2026-07-29; the visible change, and it redrew every existing GuiDef as planned. Three notes on what shipped. The natural size lives in its own wire pass, `host/widget/size.rs`, beside `build` and `apply` — the third arm-per-kind pass, in the other direction. The eye pass at the window found L6's `knob_d` too small — L7 is its **first reader**, so the role's value only became visible now: at 26 px (a toggle box plus 8%) the disc drew as a 22-px marker floating in a sidebar column. A dial is read by its angle, so the role is now **two lines of control** plus the same 8% (48 px), and the natural height follows it. A **knob is intrinsic in height only**: its disc sizes itself to the shorter side of its body and centres there, so extra width is slack it absorbs, while a natural width would pack a row of knobs to the left and hand the leftover to whatever elastic sibling sat next to them; height was the axis that actually needed fixing. And the eye pass over the examples found the break in exactly one shape — a **single control alone in a window** (`gui_bind.py`, `gui_standalone.py`), which now takes `weight=1.0`, the escape hatch used for what it is for; a row panel of menus that a comment already called compact (`gui_rulers.py`) took the `h` it had always needed, since a container still does not hug its content. Every other example improved untouched (a caption over a view stopped taking half the pane) and the web examples needed nothing: their controls already sit in rows and strips with explicit heights. The `<clausters-bundle>` component builds no widgets, so nothing there to size.)*: each `WidgetKind` gains a **natural size** — a pure function of the L6 table returning an optional extent per axis, `None` meaning elastic — and the main axis of a `row`/`col` resolves in one order: explicit `w`/`h` → **explicit `weight`** → natural size → a share of the leftover. Explicit `weight` beating the natural size is the escape hatch that keeps "stretch this button" expressible, so L2's vocabulary is extended, not replaced; the cross axis keeps filling, and the pass stays **one pass with no measurement and no constraint solver** — the boundary L2 fixed, which is exactly why a natural size must be a function of the metrics and never of the widget's *data* (a scope's height must not follow its sample count, or a `/gui_set` would relayout the window on every message). The split is content vs surface: content whose extent the widget itself knows is intrinsic (`label`, `button`, `toggle`, `number`, `menu`, single-line `text`, a control's label strip), a surface whose extent is the caller's stays elastic (`panel`, `scroll`, `patch`, `track`, the heavy views, `plot`, `nodetree`, `canvas`), and mixed is normal rather than exceptional (a `slider` has a natural thickness across its track and is elastic along it). Hit-testing follows for free — `interact.rs` reads the rects the layout produced — but `hit_slop` is what keeps a small target clickable once it stops being a slab. **This is the one-time break:** every `clients/python/examples/gui_*.py`, every `clients/web/examples/*.html` and the `<clausters-bundle>` component get an eye pass, and the commit says so plainly. Tests: pure layout cases per resolution branch (fixed, weighted, natural, mixed, all-natural with leftover), and the natural sizes themselves against the metrics table.

- ✅ **L8 — logical units and HiDPI: one scale per window, resolved once** *(done 2026-07-30; three notes on what shipped. **Resolution at scale 1 is the identity**, quantizers included: an ordinary display paints the number the config declared (a role set by hand to an odd 5 px stays 5), which is what let the whole track keep its zero-visual-change guarantee and let every existing layout test stand unchanged. **Text is scaled, not re-quantized**: the resolved table's own text roles quantize to half-steps as L6 fixed, while a widget's `text_size` prop times the window's scale stays the number the script sent — the same line L6 drew for the density, and the reason the integer-scale rule in the sketch below applies to the table rather than to the wire. And the **snapping the sketch called "integral rects" is snapping at resolution**, not a pass over the placed tree: the resolved roles and every declared length land on whole pixels (`metrics::snap_px`), while an *elastic* share of a fractional leftover stays exact — rounding those independently would open one-pixel seams between neighbours, and the chrome that must be crisp is metric-driven, not share-driven. Plus what the units seam turned out to need beyond the sketch: the placement `Space` in `layout.rs`, which is what makes "chrome is logical, a navigable plane is physical" a rule the pass carries rather than a convention — a `scroll` workspace's content plane keeps its own units (its `content_w`/`content_h`, its `view_x`/`view_y` and its children's placements), because it has a zoom of its own and its pan is written in the pixels the pointer moved. It follows, and is recorded rather than fixed: a widget's module-local structural geometry (the patcher's box/port series, the roll's key gutter and lanes, the score's staff step) lives in that physical space too, so a plane's interior grows with its zoom and not with the display's density.)*: the wire's `w`/`h`/`x`/`y`/`margin`/`gap`/`text_size` become **logical** units and the host resolves them, paying the deferral L2 wrote down ("logical px, HiDPI-scaled by the front… deferred until the frame as a whole scales, text and margins included") now that the frame as a whole *can* scale. The mechanism is one `ui_scale` per window — **written by the shell, never read by the core**, which is both the platform-seam rule and what `check-wasm.sh` enforces: natively from winit's `scale_factor()`, re-armed on `ScaleFactorChanged` (whose `inner_size_writer` is answered, not ignored) with a re-resolve, a re-layout and a redraw; in the browser from the page. The efficiency shape is the point: the metrics table is resolved to **whole physical pixels once per scale change**, so layout and painting are byte-for-byte the code L6/L7 left and the per-frame cost of HiDPI is zero — which matters most on a page holding forty canvases. Snapping is part of resolution (integral rects, integer text scale ≥ 1) because the chrome is hairlines: dividers, track edges and font pixels are all one unit, and a fractional position turns a crisp line into a two-pixel grey smear. **The heavy views do not move**: `render_width_px` stays physical and "never resolve the signal finer than the screen" is untouched — the two pixel spaces coexist, and that is the invariant to state rather than a detail to remember.
  - **The web half is a real change, not a port.** `GuiBridge::resize(defId, w, h)` takes device pixels *only*: the page multiplies its box by `devicePixelRatio` in `devicePixelBox()` (`clients/web/src/gui/page.ts`) and hands over the product, so **the ratio is destroyed at the boundary** and the browser host has no way to recover it — by design, since "the host never reads the DOM: the element owns its box and reports the pixels". So the binding grows the scale (`resize` carrying it, or a `set_scale` beside it), `devicePixelBox` returns the logical box and the ratio separately instead of collapsing them, and `fit()` and the `<clausters-bundle>` component follow. That reshapes the wasm-bindgen surface rather than extending it; it ships in this milestone's commit and is named there.
  - **Docs:** all three books — `docs/gui-protocol.md` (the units the wire declares), the Python book's builders, the web book's regenerated TypeDoc — plus a `docs/decisions.md` entry covering both halves at once: why the wire is logical while the heavy views stay physical, and why the scale is an input the shell writes rather than something the host detects.

- ✅ **L9 — a plane starts at the density it is drawn on** *(done 2026-07-30; found by eye on `gui_patch1.py` at 2x, right after L8: the chrome scaled and the patcher's boxes did not, so they read half-size against everything around them.)*: a `scroll`'s `view_zoom` becomes `Option<f64>` and resolves through `ScrollView::zoom(&Metrics)` to the window's `ui_scale` when nothing names one — physical pixels per content unit is unchanged as the *meaning*, so the pan, the clamp, the gestures and the hit math are untouched; only the default moves. A named zoom (the tree's, or the wheel's first turn) is literal from then on. The rule this settles, and the reason it is not a patch on the patcher: **a navigable view's default zoom belongs to its content unit** — a display unit (the plane's) takes the density, a data unit (a `waveform`'s sample, a roll's semitone, a `score`'s staff step) keeps its own window and must never see the scale, since a denser screen is more detail over the same span. Zoom-to-fit was rejected as the plane's default (a box's apparent size would follow how many boxes there are, and every edit would re-zoom) and stays a command. Scaling the patcher's own box/port series was rejected too: same result, 35 call sites, and two multipliers competing inside one space. **Two bugs the eye pass caught, both older than the default that exposed them.** `layout::scroll_content` compared a content extent against the *pane's pixels*, which at zoom 1 was the same number and at any other zoom put a graph that should centre itself into the corner by exactly that factor — every extent there is a content unit now, the viewport included. And a **natural size was measured at `text_size` while the drawing draws at `text_size * scale`** (the placement's zoom): the box promised room for a 14-pixel line and got a 28-pixel one, so text spilled into the next row and a knob — which reserves strips for its label and its read-out — had nothing left for its disc, which collapsed to a dot. `natural_size` takes the placement scale now, so a box is measured where its own text lands. Both were reachable before by wheel-zooming a workspace; nothing but the default made them the first thing you see. A third, same shape, cost the wheel its anchor over a **graph-sized** plane: its content extent is "the graph, never smaller than the viewport", and converting that viewport with the *live* zoom made the extent shrink as the zoom grew — the plane the pivot math holds a point in was moving, so the graph slid under the cursor (invisible on a plane with an explicit `content_w`, which is why the older anchor test passed). The conversion uses the plane's natural scale, so the extent is constant under zooming. **And the deepest one, which the eye pass named exactly**: only *text* scaled with a plane's zoom, while every metric role (padding, a track, a knob's diameter, the gap between rows) stayed at the window's table — the old "patcher posture", tolerable while the default zoom was 1 and plainly broken proportions at any other. A placement now carries the table it is drawn with (`Metrics::at`, resolved at the placement's scale) and the layout measures with that same table, so a zoomed box is an enlargement of itself. `layout::Space` grew from two spaces to **three** to make it exact — the window, a plane's content units, the inside of a scrolled child at the accumulated zoom — which also stopped a plane's default margin being counted twice. The default also needed a way *back*: a wire that can only name numbers cannot ask for a default that has none, so `/gui_set view_zoom 0` (or any non-number, the shape an empty `theme` already uses) **clears** the zoom and returns the plane to its density — found through `gui_workspace.py`'s reset button, which pinned `1.0` and so halved a 2x plane.

## P track — the patcher: from view to Max-style editing (graphs and defs)

Section added 2026-07-19; **reconceived 2026-07-22.** G23 gave the logical group a *view*; this track grows it into an **editing and creation surface** in the spirit of a Max/MSP patcher. Said crisply, the two abstraction levels are one idea at two scales: **level 1 is visual programming of a `GraphDef`; level 2 is visual programming of a `SynthDef`/`FaustDef`.** Both are **one directed, typed cord patcher**. The earlier *bus-as-node*, undirected view (built, then set aside on the `patcher-bus-as-node` branch) proved unreadable as a signal-flow diagram — a patch's one job — and the cause was drawing the connection **without direction**. Direction is not a guess: it is **structural** — a def control that feeds an `In` is an **inlet**, one that feeds an `Out` is an **outlet**, one that feeds neither is a **value**, not a port. So the patch is directed and typed, and the rationale is recorded in `docs/decisions.md` ("The patcher is a directed, typed graph").

**Re-sequenced 2026-07-22 (representation before editing).** The track splits into two phases. **Phase A builds the read-only *representation* of a def as an autonomous view**; **phase B adds editing on top of the closed view.** The reason: a patcher's editing affordances are far easier to get right when they sit on a faithful drawing of a structure that already exists — the def — than when view and editing grow together. So level 1 (a `GraphDef` drawn as a patch) and level 2 (a `SynthDef`/`FaustDef` drawn as its internal UGen graph) are both delivered **as views first, decoded from the def's own structure**, before a box can be created or a cord rewired. The shipped P3a–P3d stand as the record of what was built; P3d's edit-back *code* (a driver rewriting the arrangement — shipped, but not a proven editing capability yet; see the current-state note below) is the **seed of the editing surface**, not part of the representation. **Build order.** Phase A — representation, the forward path: **P3** (level 1, the signal-flow patch — done through P3h) → **P4** (level 2, the read-only Def-view). Phase B is deliberately **consolidated into one milestone still to plan**, not sprinkled through the middle where it bred confusion: **P5 — the editing & authoring surface** (structural creation, rewiring, parameter/value/buffer boxes, the live loop, level-2 compile). Then **P6** — the patch as a persisted document. The rationale is recorded in `docs/decisions.md` ("Representation before editing: the patcher's Def-views are autonomous, decoded client-side").

- **A cord is a bus the user never numbers.** A box has **inlets** (top edge) and **outlets** (bottom), each **typed** — audio (`ar`) or control (`kr`). A cord runs outlet → inlet and *is* a bus of the inlet's type; **the inlet defines the bus** (Max's rule) and several cords into one inlet **sum** on it. The user draws cords, never bus indices: the **client names one bus per net of cords** and writes it into the def/GraphDef, and **the server does the rest it already does** — it allocates the bus numbers and **auto-orders** the members topologically, considering the buses. No client-side ordering, no new server logic.
- **Level 1 — visual programming of a `GraphDef`.** Boxes are **whole synthesis nodes** — defs of all three families, synths, groups — and buffers; a cord is a **server bus** between them. The graphic is deliberately **explicit and restricted**: a signal goes exactly where a cord connects it. The power to route arbitrarily lives in the *server's* node/bus architecture, not in this canvas — arbitrary, cyclic routing is something you build in code (nodes and groups inside a control cycle), never drawn (see the scope boundary).
- **Level 2 — visual programming of a `SynthDef`/`FaustDef`.** Boxes are **UGens**, with the def's controls and `In`/`Out`/hardware as edge boxes; a cord is an **internal wire** of the single def it compiles to. This is Max proper: patch objects, get a DSP program. It adds `ir` (a third cord type) over level 1's `ar`/`kr`.
- **One grammar, different objects.** Both levels are directed typed patchers over the *same* client cord→bus pass; they differ only in what a box **is** (a whole node vs a UGen) and what the patch **compiles to** (a `GraphDef` the server wires and orders vs a single def). Build the engine once and it serves both — the old "which level first" question dissolves into one path.

**Scope boundary — the graphic is a DAG.** Feedback (a node reading a bus a later node writes) is a **code** construction — nodes and groups inside a control cycle, a one-block delay across the loop — not something the acyclic patch drawing expresses. The patcher draws the directed *acyclic* routing; the server's own ordering resolves it. A genuine cycle stays code, not a cord.

**Ownership stance (the edit-back pattern, unmoved).** The host never owns patch semantics: it draws boxes and directed cords, runs the gestures, and emits flat `/gui_event` edits (the directed `"wire"`, plus `"move"`/`"make"`/`"free"`), exactly as the clips and the bpf do. The **driver** — Python first, per the reference-client rule — owns the patch model and its one small pass (cords → a bus named per net, summing fan-in), translates it (level 1 → a `GraphDef` spec with those bus names, which the server allocates and orders; level 2 → the existing SynthDef builder / Faust box API, never a second compiler), and re-renders. **The server and the Python client are barely touched — this is a GUI track:** the server already allocates buses and auto-orders a `GraphDef`, and the Python arrangement model (`Group`/`Generator` → `GraphDef`) already exists; what is new lives in the GUI (the directed `patch` widget) and the shared cord→bus pass. The **patch document schema *and* that pass** are language-agnostic and land in `clausters-core`, so later clients rebind them rather than reimplement them.

**Current state: no editing is realized yet — a state of the development, not a premise.** As the track stands, no surface actually edits: the level-1 edit-back (P3d) shipped as *code* but is unproven in the window, and nothing else is built — so every surface the track has today is, in practice, a representation. This is **not** a claim that views cannot edit; editing is precisely what phase B builds (driver-side, over the edit-back seam the ownership stance describes — the widget emits gestures, the driver applies them). Representation-first simply matches where the track already is: make the views complete and faithful first (decoded from the defs), then add the editing on top.

**Autonomy stance (a Def-view is a peer of the heavy views; the editor only orchestrates).** The graphic of a def is an *autonomous visualization*, exactly like the waveform, spectrogram, oscilloscope, timeline, bpf and env views: a host widget pure over its `Mesh`/data, fed by a **headless decomposition of the def's structure** (`defs`/`clausters-core`, no window needed), and openable **on its own** — one window per def through the free-standing opener, the `clausters.plot` posture (G26). The **`Editor` does not own it.** The editor is the **gui-side representation of the arrangement model** (`clausters.form`): `form` is pure and transport-agnostic and never imports the GUI (its own docstring names `clausters.gui.editor` as its view), so the editor depends on `form`, never the reverse, and belongs in `gui`, **not** in `form` — the model must not know its view. Its job here is to **orchestrate**: when a composition holds a logical group it embeds the autonomous Def-view as a lane, the same way it embeds the piano-roll — it does not carry the `Group → patch` decomposition monolithically. That is the concrete correction the re-sequencing makes to P3d, which put that mapping inside `editor.py`; P3h lifts it into `defs` as `GraphDef → GraphPatch`, leaving the editor a thin consumer.

**Retrieval — client-side, from the def in memory (the server is not extended).** A box's directed typed ports come from the def itself: the client that **built** the def (or the box API) already knows which controls feed an `In` (inlets), which feed an `Out` (outlets), and which are values; for UGens the rates come from P2's `/ugen_query`. Level 2 goes one step deeper into the *same* structure: a `DefPatch` reads a `SynthDef`/`FaustDef`'s **internal UGen graph from the def object in memory** — the DFS `synthdef_ports` already walks (every UGen a box, every input a cord), not a new query. So the common case needs **no new server verb**. This covers the authoring case: a def you built or loaded through the client. Inspecting a def the server holds but no client in this process built would need the server to report each def's internal UGen graph over `/def_query`; that is **deliberately not done** (recorded 2026-07-22) — it would add per-def storage and processing to the server for something the arrangement model does not do for its other abstractions either, and it stays an explicit later decision, never a prerequisite. (`/def_query` already lists a def's control surface; it *could* later also report each control's **role** — input-bus / output-bus / value — but that too is an optional refinement.) No new node/def semantics are added anywhere in this track. "Tables" need no new object class: a table **is** a buffer (filled by `/buffer_gen`), so the patcher shows buffers, optionally with their generator recipe as a label.

**Scope boundary (what this is not).** Not a Max clone: no message scheduling, no `bang`s, no UI objects inside the patch (control surfaces are what the GuiDef already is — a patch and a panel compose side by side), and never a third def family: a def patched visually is byte-for-byte a def the code API could have built, auditioned and freed like any other.

- ✅ **P1 — the patch canvas: free placement, navigation, selection** *(done 2026-07-19; as anticipated, the canvas grew no navigation of its own — a `patch` inside a `scroll` pans/zooms through the container, the boxes/wires/text scaling by the placement's workspace zoom (the L4 `Placed::scale`), and the wire/box/marquee gestures ride the shared machine)*: the `patch` widget sheds its fixed two-column layout without losing it — members and buses gain optional `x`/`y` props (absent, today's stacked layout auto-places them, and stays the default so every existing GuiDef renders unchanged); boxes are **draggable** (a `Drag::Box` in the shared `gestures` machine, so the browser gets it by rewiring), each move emitted as `/gui_event <id> "move" <kind> <index> <x> <y>` and settable back via `/gui_set` (the round trip that lets the driver own geometry); the canvas gains **pan/zoom** (the L track's 2D workspace container (L3) — a `viewport::View` per axis, the anchor-preserving math reused — wires and text scale with it; if P1 opens before L3, the canvas grows the viewport pair directly and L3 later hoists it) and **click/box selection** with the selected set as view state (the `selected` posture). Pure over the `Mesh`, window-free tests for placement, hit under transform, and the move round trip. Python: `x`/`y` on the `patch` builder's members/buses; the `Editor` persists geometry beside the logical group so a patch reopens as it was left.

- ✅ **P2 — the introspection verbs** *(done 2026-07-19; numbered on the server track as **M30**)*: the retrieval surface the palettes need, three queries in the `/server_query` mold. **`/def_query`** → `/def_query.reply` replies listing the loaded defs: name, family (`synth`/`faust`/`graph`), and per def its control surface (names, defaults, rates; a GraphDef reports its **ports**, scaling included — the named surface is what a patcher wires); with a name argument it details one def. **`/buffer_query` with no argument** (or `/b_list`) → the allocated buffer numbers, each with the `/buffer_query.reply` shape (frames, channels, sample rate) so the patcher can show real buffers as objects. **`/ugen_query`** → the UGen catalog straight from `src/dsp/registry.rs` descriptors (kind, inputs with defaults, static fields, rate rules) — the level-2 palette derives from the server's truth instead of a client-side copy that would drift (the schemas.md stance "a client never enumerates them" is deliberately relaxed into a typed verb; the catalog stays reference, the verb makes it discoverable). Faust primitives stay client-side knowledge (the box vocabulary is Faust's own, already in the client builders). Python: `server.defs()`, `server.buffers()`, `server.ugens()` (async, `/server_sync`-barrier friendly); docs in `schemas.md` beside the other queries. Shipped as M30 (the G25/M25 pairing pattern); the Python methods landed as `query_defs()`/`query_buffers()`/`query_ugens()`, joining the existing `query_*` family — a bare `buffers()` would have shadowed the server's `BufferAllocator` attribute. The UGen descriptors had no input names to report, so the registry grew them and the client tables became mirrors held in line by a contrast test (see `docs/decisions.md`).

- **P3 — the directed patch at level 1: typed inlets/outlets, cords, object creation** *(P3a–P3d and P3h shipped 2026-07-22; P3 is the level-1 signal-flow patch and its **read-only representation is complete**. The editing that used to sit here as sub-items P3e–P3g — structural creation, parameter/value boxes, buffers as value boxes — is **consolidated into the editing surface, P5**, so it no longer interleaves with the representation)*: the `patch` widget is **directed and typed** — a box draws inlet cells (top) / def name / outlet cells (bottom), each port typed (`ar` heavy, `kr` thin) and **derived from the def**; a **cord** is a drag outlet → inlet (a rate mismatch refused at the gesture, fan-in **sums**) and *is* a bus the user never numbers — the **client cord→bus pass** names one per net and the server allocates and orders them. The vision and its rationale are in the P-track intro above and `docs/decisions.md`; the milestone is broken into sub-items, each with its status:

  - ✅ **P3a — the cord→bus pass (`clausters_core::patch`) + FFI** *(2026-07-22)*: connected-components over the cord graph, one private bus per net (`b0`/`b1`/…), writers summing, no drawn hardware net (hardware is a terminal def). FFI `clausters_core_patch_compile` (ABI 11, JSON in/out, a bad cord on an error channel). Window-free tests.
  - ✅ **P3b — the directed `patch` widget + gestures + edit-back** *(2026-07-22)*: `host::patch` draws a box as three color-coded bands — inlet cells (top strip), the def name (middle), outlet cells (bottom strip) — cords typed by weight; `cord_between` normalizes and rate-checks a pair; the cord-drag / box-move / marquee gestures ride the shared machine; the four-place `"wire"` edit-back. The bus-as-node view is fully replaced.
  - ✅ **P3c — the programmatic model `clausters.defs.GraphPatch`** *(2026-07-22)*: build in code, `compile()` via the pass, `to_graphdef(name)` to send, `to_widget()` for the view; `synthdef_ports` derives a box's typed ports from its `SynthDef` graph (a control feeding an `In`/`InCtl` is an inlet, one feeding an `Out`/`OutCtl`/`ReplaceOut` an outlet) and `add(a_synthdef)` reads them off the def; `guidef.patch` emits the schema. *The GUI is a view of this*, usable headless. Example `examples/gui_patch1.py`.
  - ✅ **P3d — the directed Editor driver** *(2026-07-22; **the shipped seed of the editing surface, P5** — this is editing, and it shipped ahead of the representation work; its `Group → patch` mapping lives in `editor.py` and is superseded by P3h's headless decode, which leaves the editor a thin consumer)*: `Editor._logical_patch` maps a `Group{logical}`'s members + shared-bus controls to a `GraphPatch`, drawn as a directed `patch` in a `scroll`; the edit-back rewrites the arrangement — a `"wire"` points both members' controls at one bus (reusing one, or a fresh `w{n}` via `Group.declare_bus`, so the next render's GraphDef wires the way the cord is drawn), a `"move"` persists a box's canvas position (kept by group identity across redraws). A member wrapping a bare def *name* draws port-less; an `"OUT"` control is not a cord (that member reaches hardware itself).
  - ✅ **P3h — the level-1 Def-view: headless decode + autonomy** *(done 2026-07-22; phase A. Window-free tests throughout; the on-screen render is a manual smoke step, not asserted here)*: the level-1 representation stopped being the editor's and became an **autonomous view of a `GraphDef`**, the peer of the heavy views (the autonomy stance above). The three parts: **(1) the inverse decode `GraphDef → GraphPatch`** in `defs` — `GraphPatch.from_graphdef(gdef, defs)`, the reverse of `to_graphdef`: it reads each member's bus-valued controls back into cords, typing each box's ports from the `SynthDef` it was built from (`defs` maps a member's def name to that `SynthDef`; direction is never guessed, so a def not in `defs` draws port-less and grows no cords), plus a `GraphDef.members()` read accessor it walks. **(2) the `plot_def()` method** — `GraphDef.plot_def(defs=None)` decodes the def and opens its **structure** as a `patch` view in its own window on the ambient GUI host (one window per call, the `clausters.plot` posture, G26; the shared opener `clausters.plot._open_patch_view` + a `PatchWindow` handle), **scoped to the def as a method, not a global verb**: kept distinct from `clausters.plot(def)`, which renders the def's *sound* (its rendered waveform) where `plot_def` shows its *structure*. Lazy imports keep `defs` from coupling to the GUI. (`SynthDef`/`FaustDef` `plot_def`, the level-2 structure view, is P4.) **(3) the `Editor` reduced to a consumer** — `_logical_patch` is now a thin adapter: it renders the `Group{logical}` to a `GraphDef` (`Group.to_graphdef`), collects the members' `SynthDef`s, and calls `from_graphdef`; the mapping itself lives in `defs`. (The `synthdef_ports` use that remains in `editor.py` is the edit-back path `_outlet_rate` — the P3d seed of P5, not the representation decode.) Read-only: no cord drawn, no box created — that is the editing surface, P5.
  **Naming (recorded):** the level-2 counterpart is **`DefPatch`** (UGens → a `SynthDef`/`FaustDef`), beside this level-1 `GraphPatch` (defs → a `GraphDef`). The representation half is `DefPatch.from_synthdef` (P4), the same inverse-decode shape as `GraphPatch.from_graphdef` (P3h).

- ✅ **P4 — level 2, the read-only Def-view: a `SynthDef`/`FaustDef` as its internal UGen graph** *(done 2026-07-22; phase A — representation. Window-free tests throughout; the on-screen render is a manual smoke step, `examples/gui_patch2.py`)*: the **same directed widget and cord grammar P3 built**, now **representing** a def — **UGens as the boxes**, the def's controls and `In`/`Out`/hardware as edge boxes (the parameter surface as sources, the output as a sink), as in a real def. This is the level-2 half of phase A: a **decode `SynthDef → DefPatch`** (`DefPatch.from_synthdef`, the counterpart to P3h's `GraphDef → GraphPatch`) that reads the def's **in-memory UGen graph** (the DFS `synthdef_ports` already walks, one step deeper — every UGen a box, every input a cord), a widget that draws it, and the same `plot_def()` method (`synthdef.plot_def()` / `faustdef.plot_def()` open the level-2 structure view, as `graphdef.plot_def()` opens level 1). Faust boxes carry a `signature` from the client's own box vocabulary; UGen boxes from P2's `/ugen_query`. What level 2 adds over level 1 is `ir` (a third cord weight, dashed alongside `ar` heavy / `kr` thin) and the fact that a **cord is an internal wire, never an allocated server bus**. **Read-only** — no box created, no cord drawn (that is the editing surface, P5). The **patch document** is the one P3 defined in `clausters-core`, extended here with the UGen/Faust signatures — the schema is the deliverable as much as the widget. Window-free tests: the decode of a known `SynthDef` into its UGen boxes/cords, typed hit-tests over UGen signatures, the `DefPatch → SynthDef → DefPatch` round trip where it holds.
    **As shipped (the concrete shape):** the level-2 model is `clausters.defs.DefPatch`, a Python peer of `GraphPatch` (the pattern from level 1: the *model* is client-side; only a shared *pass* would live in `clausters-core`, and level 2 has none — a read-only decode has nothing to compile). So `clausters-core` gained exactly the shared vocabulary the widget needs — `Rate::Init` (the third cord type, `ar`/`kr`/`ir`) — and nothing more; the `DefPatch` document (boxes carrying each UGen's `kind`/`rate`/`op`/`label`/`static`, control boxes their name/default/rate, constants recorded per inlet) stays in Python, and its wire form is the same generic `{boxes, cords}` widget schema P3 defined, now with `rate:"init"` ports. A UGen box's inlets are **named from the client's own builder signatures** (`ugen_input_names` in `clausters.defs.ugens`, the same callables the `/ugen_query` contrast test keeps honest — no new server verb), falling back to positional for the generic op UGens and the wire-order-misaligned kinds (`EnvGen`, `Poll`, …). An unset UGen output rate defaults to **audio** (the honest headless heuristic — the exact per-kind default is the server's, not the client's). The `ir` cord draws **dashed** (`host::patch::draw_cord`). `to_synthdef` reconstructs the def from the boxes (dependency-ordered, one forward pass) and the round trip **reproduces the spec exactly**. Faust: the **signal-tree** form decodes node for node (every op a box, every operand a cord, a slider a source box); a **box-tree or source** def is opaque and draws as a single box (its internals are the compiler's, not reconstructable client-side). Examples consolidated to one per level — `examples/gui_patch1.py` (level 1) and `examples/gui_patch2.py` (level 2).
    **Refined after the on-screen reviews (also 2026-07-22):** the **node-positioning moved to the host** (`host::patch::auto_layout`) — the decode ships no coordinates, so every client rebinding the widget gets the layout for free (`GraphDef.plot_def` benefits too). Because a def is a **DAG** (not a single-root tree), the layout is a small **layered (Sugiyama-style)** graph drawing — layer by longest path to a sink (inputs land above their use, not piled in one top row — the mistake a first attempt made by pinning inputs to the top; Reingold–Tilford was rejected as it needs a single-root tree), insert a **dummy node** on every rank a long edge skips (so the wire bends through the gap instead of cutting across the boxes between its rows), **order** each layer by alternating up/down **barycenter** sweeps (crossings cut at every level, not just near the sinks — the earlier single simultaneous relaxation only settled the bottom row), **place** on the x axis by iterated barycenter separated with an **isotonic (pool-adjacent-violators)** fit so overlapping boxes spread around their shared centre (children align under parents, no left-margin pile-up). The scroll workspace **sizes its content to the graph's natural extent, never below the viewport** (`scroll_content` reads `patch::natural_size`) and the layout **centres** the block in that area — a small graph centres in the window, a large one fills the content and pans (the earlier Python guess over-sized the content and pushed the graph off-screen). The panel **frame hugs its boxes and their bent wires** instead of clipping a fixed rect; **constants became `const` value boxes** (a number box corded into the inlet, distinct `value_fill`) rather than inline defaults; every cord is **one width**, the **rate read by colour** — **contrasting primaries, audio red / control blue / init yellow+dashed** (a fat-vs-thin pair and the earlier green/blue both read badly); the box's **central band is white with black text**, framed by **dark port strips**; the per-box **role** is now only a drawing tag; and the panel **caption is the def kind** (`synthdef`/`faustdef`/`graphdef`), not the def name. The level-1 example was expanded to a four-box effect chain with the full canvas (placement, marquee, navigation, render/hear). See `docs/decisions.md`.

- **P5 — the editing & authoring surface (to plan)** *(phase B — one milestone, deliberately not designed yet; it earns editing on top of the closed level-1 and level-2 representations)*: **all** the patcher's editing lives here, consolidated so it stops interleaving with the representation. The shipped **P3d** edit-back (a `"wire"`/`"move"` rewriting the arrangement, still wired into `editor.py`'s `apply()` but unproven in the window) is its **seed**; this milestone makes it a real, verified authoring surface across both levels.

  **What the E track changed under this milestone** *(recorded 2026-08-09, after E16 closed; P5 was written before E9 moved the wire and the bullets below still spoke the old vocabulary)*. Three facts the design must start from rather than rediscover:

  - **There is no `patch` type any more.** E9 moved the wire onto the model: the patcher is a **`plane`** — a two-axis container in content units — **that has boxes**, and their presence is what makes it a patcher rather than a second type name. So every edit-back below is a `/gui_event` from a `plane`, and the cords are still a prop of the plane (a wire is between two children and belongs to neither). The Python and TS builders keep the name `patch` as a *shortcut* onto that node, which is why no example changed and why this entry's old prose read as if nothing had.
  - **The gestures ride the shared machine, and the container decides first.** E10 gave every container a **gesture plan** — an ordered list of steps per modifier, declared on the wire (`gestures`) and defaulted from what the container is — where `GestureStep::Element` is the point at which the thing under the cursor gets its turn, and a step that declines passes the press **outward**. A patcher's canvas is `Coords::Canvas`, so a marquee sweeps canvas units and a press that no box claims falls through to the plane under it. P5's new gestures are therefore arms of `gestures/press.rs` and `interact/edit.rs`, not a private handler; the box-drag and cord-drag already there (`Drag::Box`, `Drag::Wire`) are the pattern the rest follow.
  - **The eye pass is part of closing it.** E12 established what the whole E track had skipped: nothing in CI runs an example, so `gui_patch1` and `gui_patch2` are the manual test surface and a milestone is not closed until they have been opened and watched. P4's "the on-screen render is a manual smoke step, not asserted here" is the honest note; P5 inherits it as an obligation rather than a caveat.

  **And the one milestone the E track deliberately handed here** — `box` **as an element**. The catalog spent the name on a synonym for `panel`, E9's model wants it for the patcher's box, and E9's expand stage could not take it while both spellings parsed, so a plane's boxes are **still its `boxes` prop**. Turning them into child elements is behavior, not spelling: ids (a box becomes addressable on its own), layout (the plane places them instead of drawing them), per-box hit-testing (the chain reaches a box the way it reaches any child) and edit-back (a `"move"` becomes a child's `place`, not an indexed prop of the parent). It belongs here because P5 is what needs it — structural creation, per-box error state and a control panel per box all want a box to *be* something addressable — and it should be sequenced **first** within the milestone, since every other bullet reads differently depending on whether a box has an id. Note the ordering against the **K track**: K1 moves every *leaf* element behind `trait Element` and keeps containers in the enum, so a `box` that lands before K1 is an enum arm and one that lands after is a registered element. Neither is wrong; deciding which is cheaper is part of opening P5.

  What P5 must cover, each a design question to converge before opening:
  - **Structural creation** (`"make"`/`"free"`) — the drop-down/object-box gestures to add and remove boxes (`/gui_event <id> "make" <kind> <name> <x> <y>`, `"free" <kind> <index>`, cords dropping first). Graphically heavy (in-canvas text entry + filterable lists over P2's `/def_query`/`/ugen_query`) and the create round-trip is not yet defined — until it lands the patcher is the **view of the def-generators** a composition already holds, not an authoring-from-empty surface.
  - **Parameter/value boxes** — a box representing a **parameter as a value** (a number for `freq`, an envelope, a buffer) feeding a def box's parameter inlet; today the patcher wires def-to-def signal buses only and a def's parameters-as-values are undrawn.
  - **Buffers as a value box** — a buffer is **data, not a def-box** (one kind of value box). The arrangement model keeps a `Buffer` object (parity — the patch is a view of a part of the model), a cord carrying a buffer is *not* a signal bus; *visualizing* it (fetch its samples, draw like the audio editor's waveform) and *playing* it (a temporary synthdef) are facilities off the server buffer. See `docs/decisions.md`.
  - **The live patch** — the patch meets the running tree, closing the **drawn-vs-sounding gap**: rendered members light up (the driver correlates its instance with `/group_queryTree` + `/server_notify`, sets a per-member `live` flag via `/gui_set` — the host draws state, never queries), a box edited since its render reads **stale**, double-click opens a member's **control panel** (sliders over its controls from `/def_query.reply`, bindable via `/gui_bind` — Max's live number boxes with zero new widget code), and a `"render"`/`"stop"` affordance closes the create → wire → sound loop.
  - **Level-2 authoring → def** — the same directed edit-backs over UGen/Faust boxes, compiled to a def through the builders that already exist (a UGen patch through the SynthDef graph builder, a Faust patch through the box API — **never a third compiler**): a tree walk from the `Out` boxes; a patch the walk cannot serialize (a cycle without a delay, a dangling required inlet) fails client-side naming the offending box. The def is sent (`/def_send` + `/server_sync`) and auditioned live; a server `/fail` routes back as an `error` prop on the box. The translation lives beside the `clausters-core` document schema so it is portable. Examples when it lands: `examples/gui_patch1.py` (a level-1 effect chain from an empty canvas), `examples/gui_patch2.py` (a synth patched from UGen boxes).

- **P6 — the patch as a document: persistence and round trip** *(design to converge before opening)*: where a patch lives when the window closes. The original two options: the patch document persists **beside the def it produced** — a sidecar in the server's def store (the store already persists defs across sessions; a def compiled from a patch would reload *with* its patch, so any client can reopen the boxes behind a sound) — against a purely client-side document beside the arrangement. Needs a decision on identity (what happens when the def is re-sent from code and the patch goes stale) and lands with a `docs/decisions.md` entry either way; not opened until that converges.

  **A third option exists now, and it did not when this was written** *(recorded 2026-08-09)*. The GUI host grew its own persistence twice over while the P track was paused, and both are a better fit than a def-store sidecar for the thing a patch actually is:

  - **The host's named-GuiDef store** — `store::GuiStore` behind the `DefStore` seam, serving `/gui_load`: the host already saves a GuiDef by name and hands it back. A patcher window *is* a GuiDef, so the boxes and their canvas positions persist with no new format at all.
  - **The persisted bundle** — `host::bundle`, the boot the native standalone mode and the browser both replay: defs → graphdefs → boot preset → the GuiDef's `boot` messages, and, when the manifest declares a component contract, a **template** whose symbols are allocated and holes resolved at mount. That is precisely a patch's two halves travelling together — *the def it produced* and *the boxes behind it* — which is what the def-store sidecar was reaching for, in a container that already exists on all three legs.

  So the decision to converge is no longer "sidecar or client-side" but **which existing container owns it**, and the identity question sharpens rather than goes away: a bundle mounted twice is two instances of one patch, so a patch document has to say whether the boxes are the template's or the instance's. Two constraints to carry in. The bundle format is **version-bearing** — E9 moved it and that is what dragged the SemVer tier to 0.9.0 — so a patch inside one is a release decision under the `release-versioning` skill, not only a schema one. And a browser host **runs without a store** by design (`DefStore` is a seam the page simply does not fill), so anything built on `/gui_load` alone persists on one leg and not the other, which is a degradation of one build the K track's first constraint would otherwise forbid.

## E track — the element library: coordinate-system containers and composed elements

Section added 2026-08-07. The tracks above grew the catalog widget by widget, and it worked: each one shipped with its model, its tests and its books. What they did not grow is a **model of what a widget is**, so the catalog now carries the same idea in several hardcoded spellings — three implementations of min/max-per-column, two implementations of a visible window, five ad-hoc value axes, three clip bodies selected by precedence because a clip cannot contain anything. This track replaces the accumulated shapes with **one small model**, and it is a **refactor track**: almost nothing here is a new capability, and the capabilities that do fall out (a navigable plot, a live waterfall, envelopes over a clip, tabs) arrive because the model makes them free, not because they were built.

**The model.** An **axis** is a mapping from a domain to screen extent: a visible window, a unit, and a scale policy. A **container** owns 0, 1 or 2 axes and therefore a *coordinate system* — layout (0 axes, children flow by `row`/`col`/`grid`/`free`), plane (2 axes locked to a uniform scale, the patcher's canvas), or time/value (2 **independent** axes, x time and y a value). An **element** draws against the axes of the container holding it; it owns no navigation. Each child declares **how** it is placed: in axis coordinates, or pinned in layout coordinates. Coordinate systems **nest** — a clip inside a track is a time/value container whose local x is `[0, dur]`, placed at `offset` in its parent, one affine transform per level.

**Why this is the right decomposition** — it is what the existing widgets already are, once the accidents are removed. The heavy timeline views already have two independent axes (`View` in x through the group, `y_start`/`y_len` in y); the `track` already places children by time; the `scroll` already owns a plane. The model does not invent a mechanism, it **moves where the mechanism lives**: out of five per-widget implementations into one container property.

**Generality is the constraint, not the multitrack.** The DAW is one configuration of this library, never its shape. `track` and `clip` are not core concepts — they are time/value containers with particular chrome. A laboratory oscilloscope, a curve editor, a patcher and a multitrack must come out of the same pieces, or the pieces are wrong.

### The two efficiency facts this track is built on (measured 2026-08-07)

Both were measured before planning, on a 10M-frame × 2-channel buffer (`base_bucket` 256) at 1200/2400/3840 device pixels, release build. They are the reason two of the milestones exist and the reason a third one does not.

- **Geometry backend is not the cost; the pipeline count is.** Building the waveform's per-frame columns into the shared `Mesh` instead of its own vertex buffer costs **1.05–1.06×** in the realistic zoom regimes (0.370 ms vs 0.351 ms at 3840 px — 19 µs against a 16.6 ms frame), because **80–96% of that per-frame cost is the column computation itself** (the pyramid reads), which both paths share identically. The per-frame `Mesh::new()` costs 1.05–1.06×, i.e. nothing. **So the waveform is not moved to the mesh**: the real waste is that every `waveform` widget builds its own shader module + 2 render pipelines, and every `spectrogram` builds a shader + pipeline + texture **per channel**, so an eight-channel spectrogram compiles eight of each. Pipelines depend only on device and format; they belong to the window. This is E2. *(Corrected 2026-08-07, while opening E2: this bullet first read "a multitrack of 40 waveform clips is 80 pipelines", which is false — a `clip` body is CPU-side `WaveformData` drawn into the mesh by `track.rs` and allocates no GPU slot at all, so today's duplication is real but modest, bounded by the number of actual `waveform`/`spectrogram` widgets and a spectrogram's channel count. The claim describes where the track is **going**: E7 makes a clip body a signal element and E6 makes the signal element one thing, and from there the slot count scales with the composition. That is what makes E2 a prerequisite of those two rather than an optimization of today, and it is why it is ordered here.)*
- **Geometric clipping is the one real cost.** `Mesh::tri` runs Sutherland-Hodgman per triangle whenever a clip rect is active: **2.6–6.9×**. A column, a lane, a clip body and a note are axis-aligned rects, for which clipping is a clamp — the fast path measures **1.10–1.17×**. Today only `scroll` subtrees set a clip, so nothing pays it; the moment a timeline container goes inside a scroll (which is the point of this track) everything pays it. This is E1, and it is a prerequisite, not an optimization.

### Decisions taken before opening the track

- **Total migration of the wire, not presets** *(decided 2026-08-07)*. The alternative — keeping `waveform`/`plot`/`track`/`scroll`/`clip` as wire names that build the general form underneath — was rejected despite costing no migration. It buys compatibility and pays with two levels of naming: the reference would document 28 types over a model of six concepts, and a reader would never discover that a plot can navigate, because "plot" reads as a different thing. Since the model's whole value is that the general form is what a user learns, hiding it behind the old names defeats the refactor. The migration is not smeared through the track: it is **one milestone (E9)** that moves the wire, both builders, the three books, every example and the persisted bundles together, and carries the versioning consequence.
- **Navigability is a property of the axis, not of the source** *(decided 2026-08-07)*. The live views were nearly filed as "the ones without a time axis", which is wrong: a `scope`'s x *is* time (a triggered window in milliseconds), a `spectrum`'s x is frequency, a `phasescope` has no time at all. What actually separates them is that a tap is **forward-only** — the ring keeps the newest window and there is no addressable past — against the random-access sources (buffer, file, inline). That is the arrangement layer's own generated/generator distinction, and it settles the question cleanly: an axis is navigable when its domain is addressable. A live spectrum's **frequency** axis is addressable now, so zooming into part of the spectrum needs no retention and is available immediately; a live scope's **time** axis becomes addressable only if the host retains a history ring, so retention is a **policy on the axis**. The "missing waterfall" found while inventorying the catalog is therefore not a missing widget but a missing retention policy, which is why there is no milestone for it.
- **The container governs gestures; the element supplies defaults** *(decided 2026-08-07)*. Today `interact::hit` returns the deepest interactive widget and `press` matches on its kind across 15 arms, so that Shift+drag pans and plain drag selects is written inside the waveform's arm rather than belonging to the axis every timeline view shares. Under the model a hit produces a **chain** (container → … → element) and each level may claim a gesture or pass it down, which is what makes gestures configurable. **The chain is not new machinery**: it already exists implicitly, as 25 call sites of by-id lookups that each mean "find my container's state" (`nav` 12, `scroll_view` 4, `set_scroll_view` 3, `nav_of` 2, `note_at` 2, `clip_has_curve` 2), and `hit` already treats one container as a gesture surface — a `scroll`'s empty area is the pan target. So the split across E4/E5 is not mechanism-versus-policy with two mechanisms coexisting; it is **additive first, selective second**: E4 makes the chain explicit while every arm keeps its behaviour, and E5 lifts the six drags that are container-level (`Pan`, `PanY`, `Select`, `ScrollPan`, `Marquee`, `Clip`) out of the ~24 that exist, leaving the other eighteen where they belong.

### What actually constrains the track

Not the size of `gestures.rs` (3995 lines) and `interact.rs` (1414). Only 15 of their arms are press decisions, only six drags are container-level, and the container lookups the chain replaces already exist — so the conversion is additive rather than a rewrite (see the gesture decision above). The real constraint belongs to the project rather than to this track: **nothing in CI runs any example**, so a gesture regression is invisible to the build and is caught only by hand. That fixes the *shape* of the milestones rather than their size — E1 through E4 are behaviour-preserving and verifiable by running the examples each change touches and watching nothing move; E5 onward is where behaviour deliberately changes, so the manual checking concentrates there and each lifted gesture family is checked on its own.

- ✅ **E1 — Axis-aligned clipping is a clamp** *(done 2026-08-07)*: the fast path went into `Mesh::quad` rather than into `rect`, because `rect`, `line` and `border` all funnel through it — so a panel, a lane, a note body, a waveform column and every horizontal or vertical hairline take it, while a disc's fan, a slanted line and a glyph outline keep the general `tri` clipper untouched. Recognition is by **edge** (all four axis-parallel) rather than by corner order, so it holds whichever corner the quad starts from and whichever way it winds — `rect` and an axis-parallel `line` build different orders. It engages only when a clip is actually set; unclipped drawing is the code it always was. Equivalence is tested as **covered area** against the general clipper over ten placements (inside, over each edge, over a corner, flush, outside, touching one edge), which is the only comparison that means anything: the general path emits a fan of up to seven vertices where the clamp emits six. Two things the test surfaced and now documents: a sample landing exactly on the quad's interior **diagonal** is inside both, neither or one depending on rounding — the one place the paths may legitimately disagree, and a place the rasterizer never sees, since the two triangles share exact vertices and the clamp has no interior diagonal at all — and the general clipper **emits degenerate triangles** where the clamp emits nothing (a quad flush against a clip edge collapses to three equal vertices), which paint no pixel either way.

- ✅ **E2 — One pipeline per window, buffers per element** *(done 2026-08-07)*: `WaveformRenderer` and `SpectrogramRenderer` kept their names and became **window-owned**, holding only what a pipeline is a function of (the device and the target format) plus the waveform's build scratch, which is transient frame space rather than element state. What was left behind is genuinely per element and moved into new types beside the views: `WaveformGeometry` (vertex buffer, per-channel ranges, the regime they were built for, the trace palette) and `SpectrogramTexture` (the magnitude texture, its sampler and bind group, the uniforms). Both fronts, the `native` harness and the two prototype binaries hold a `view::Renderers` — the pair, the mirror of `Painter`, which has always had this shape — created once per window before the slots, since a spectrogram texture binds against the renderer's layout. Counted in `tests/gpu_slot_cost.rs`: two waveforms plus an eight-channel spectrogram allocated **22** shader/pipeline objects and now allocate **5**, and the number no longer follows the element count — which, at a single element, saves exactly nothing, and is the whole point at forty. The same test re-checks the measurement behind E1's other half, that 92-96% of a heavy view's per-frame cost is the peak-pyramid column computation both destinations share, so where the vertices are written stays not worth re-deciding. The heavy views stay GPU views and stay structurally identical to each other, which is the property the whole track needs — a waveform and a spectrogram must be placeable in a clip, a panel or a separate edit window by the same mechanism. **The point is not today's saving** (see the corrected measurement above: a `clip` allocates no slot, so the duplication is bounded by the widget count and a spectrogram's channels) **but the cost of a slot**, which E6 and E7 turn into the per-element cost of a composition. A bench in the crate records the before/after, since this is also the evidence for why the heavy views were not moved to the mesh.

- ✅ **E3 — The `Axis` primitive** *(done 2026-08-07)*: one type carrying a visible window (`viewport::View`'s `f64` sample-precise window, generalized past samples) plus the **bound it moves inside** — a domain with a unit and a policy — so clamping, panning, zooming and slicing are the axis's own arithmetic instead of four re-derivations of it. It took the vertical axes of both heavy views, `EditorProps::y_view` and both vertical gesture paths (outside `viewport.rs` nothing calls `clamp_span`/`zoom_span` any more), and then the **value ranges**: `meters::fraction`, `Range` and the linear branch of `bpf::value_fraction` were three copies of one mapping, and `interact::pitch_window` a fourth shape of it — a value domain sliced by a display window. `bpf` keeps only what is genuinely its own, the geometric branch. That corrects the count this milestone was written with: **three implementations plus a slice**, not "five ad-hoc value ranges", which counted widgets — `meters::fraction` was already shared by four of them. It also cost a regression on the way in: `Axis::clamp` bounded the window's start against zero rather than against the domain's origin, invisible for a counted or normalized axis and wrong for every value range (a roll over pitches 48..72 reported 0..24, so no note was ever under the cursor); a test pins it now.

  The third part is the one the milestone was for: **the navigation group is read, never copied**. Selection and playhead used to be mirrored group→members' `EditorProps` on every change, so that the frame path, the animation demand and the MIDI cursor could keep reading the widget tree they had always read. Every one of those readers now resolves the member's group key and reads the group — the same lookup `nav_for` already did per timeline item per frame, so it costs nothing new — and the mirror is gone: `mirror_timeline_group` and its five call sites, and with them one invariant that could only ever hold by discipline. The sweep rule (`head_at`/`swept_at`/`playhead_loop`) and the selection accessor moved off `EditorProps` onto `GroupState`, which is where the values live, so the type system no longer offers a stale copy to read. A member's own `sel_*`/`playhead*` props stay exactly one thing — the **def-time seed** a fresh group takes its values from — and `has_playhead` became a group-aware walk in `host::live`, since whether a window must animate is a fact about the group, not about the props a widget was parsed from. Behaviour-preserving throughout: the wire is unchanged (`docs/gui-protocol.md` never promised a mirror), and the widgets navigate exactly as before.

- ✅ **E4 — Containers own coordinate systems** *(done 2026-08-07)*: a hit-test used to throw away the containment it had just resolved — `interact::hit` walked the layout, returned the deepest widget under the cursor, and whoever needed the container around it laid the **whole window out again** to search for it. A wheel cost three passes, a press on a lane four (`hit`, then `clip_hit` twice, each rebuilding the lane's body and window from scratch), a press on a roll two. Now `Placed` keeps the parent index the recursion already had, and a hit carries the **chain**: the containers over the point, outermost first, the widget itself included when it is one, each naming the coordinate system it gives its contents (`Coords` — layout for `window`/`panel`, a plane for `scroll`, time for a `track` lane, which carries the body its samples map onto and the window they are seen through). `scroll_at` is gone, `clip_hit` and `pianoroll_hit` take the element the hit already found, and `interact::hit` is the only place a window is laid out to answer a pointer question. What deliberately did **not** move onto the chain is what a *live drag* reads: the chain is a snapshot of the press, and by the second step of a plane pan or a note drag the thing being dragged has moved, so those stay lookups of the current state and say so in place.

  Two departures from the sketch. The **placement mode is the container's `coords`, not a second enum beside it**: "flow" and "axis" would have been a second name for what layout-vs-time already says, and the third mode, `pin`, has no widget asking for it yet — it lands with the clip that carries state buttons (E7/E8), not before, so the variant is not built dead. And the **hit's tuple became a struct** rather than growing a fifth element: five positional fields at every call site is where a tuple stops reading, and the pattern matches got clearer, not longer. Two corners behave better rather than identically, both stated in their commits: a press falling through onto a widget that merely *overlaps* a `scroll` (a free layout, no containment) no longer pans that plane, and a lane whose navigation group is not registered yet pans the window's full span instead of refusing. Verified by hand on the workspace, patcher, multitrack, composer, piano and piano-roll examples — which is also how the piano-roll's vertical clamps surfaced (fixed in its own commit: a row leaving the view is cut, not slid back in).

- ✅ **E5 — The container's gestures move to the container, then become a table** *(done 2026-08-07)*: the six drags were not six lifts but **one**, because the chain first had to admit what a container is. `Coords::Time` stopped being the lane's and became every timeline view's — a `TimeAxis` carrying the body the renderer drew through, the navigation window it is seen at and the vertical `YAxis` beside it (its strip, its display window, the lane height one window's worth spans, and the value window where the axis measures something) — and `patch` became a `Canvas`. With the axis on the chain the five re-implementations collapsed on their own: `gestures.rs` no longer imports `track` or `frame` at all, because nothing in it rebuilds a body any more. A press now runs the containers' **plans** innermost-first — an ordered list of steps (`element`, `pan`, `select`, `locate`), each free to decline and pass the press outward — which is one mechanism where there were two: today's "nothing consumed it, so pan the enclosing plane" fallback *is* a plan's last step, and Shift on a patcher's empty canvas reaching the workspace around it is the same walk rather than a special case. The wheel went the same way, and `Select` absorbed the roll's marquee: one sweep, a time span that the linked views follow plus — on an axis that measures a value — the rectangle its elements are picked by.

  Then the plan table became the wire's (`gestures`, an object keyed by modifier, live via `/gui_set`, re-read from the kind's defaults each time so a table names only what it changes), with its two builders, `docs/gui-protocol.md` and an example that shows the same three views under two tables side by side. Two gestures deliberately stayed out of it: a press on a view's vertical strip always pans that axis, and the wheel always zooms — neither is ambiguous, so neither is a policy. Checked by hand on the gesture example (both tables, all four chords, the vertical strips and the wheel), on `gui_rulers` and on `gui_multitrack`; the remaining gesture examples — the patcher's marquee and its Shift fall-through, the workspace, the piano-roll's block edits, the score and the composer — are covered by the suite but were **not** re-run by eye, so that is the first thing to do if one of them looks wrong. Three corners behave better rather than identically, named here rather than left to be found: a press on a lane's **header** (beside the axis, naming no position) no longer locates to sample 0, a press that merely defocuses a text field no longer swallows the plane pan behind it, and the elements a container places are now looked up through the axis the hit resolved rather than through the kind that was matched.

- ✅ **E6 — The signal element** *(done 2026-08-07)*: `waveform`, `plot`, `scope`, `spectrum`, `spectrogram` and `phasescope` differ along exactly three axes and nothing else, so `host::signal` carries them as one element — a **presentation** (the trace, a magnitude spectrum, the time-frequency texture, the phase of a stereo pair) of a **source** (random-access samples: buffer/file/inline; or forward-only: a bus at a rate with its window/trigger/hold policy) with the **capabilities** the view offers over it — and `WidgetKind::Signal` is one arm where six were. The wire still names the six, each a **preset** naming a point of that product, which is the whole compatibility layer and the reason this is a refactor rather than the break: moving the wire onto the model is E9's, as decided when the track opened. What that bought immediately is that `build` and `apply` are one arm apiece, the frame's dispatch is *one* place that sorts an element to the renderer its presentation picks, and every collector that walks a tree — live buses, GPU slots, bulk sources, the gesture table, the lane count — asks the model instead of matching six variants.

  **The three min/max implementations collapsed first, in their own commit**, because that is where the milestone's other half lives: `signal::trace::Trace` is the one column source (an arm for raw interleaved samples, an arm for a peak pyramid) and `draw_channel` the mesh half of the renderer, so a clip body, a plot and the GPU waveform read their columns from one place and differ only in where the vertices land. That made the **renderer split explicit rather than implied**: an element costs a GPU slot only when it is a *navigable heavy* view, and a plot, a live curve and a clip's body all draw into the window's shared mesh — which is what E2's corrected measurement says should happen, and what E7 needs in order to put a signal element inside a clip without allocating per clip. Two corners of the trace behave better rather than identically: a clip body zoomed past the polyline threshold steps by sample instead of by pixel (nothing visible is dropped between one and two samples per pixel), and the plot's polyline and column branches now agree on one span mapping where they used to differ by a sample.

  Three seams the collapse settled rather than carried, each named because a reader will otherwise find them as surprises. A spectrogram had **two `sample_rate` fields** parsed from the same prop — its own and the editor's — and only the editor's took a `/gui_set`, so a rate set before a fetch landed was silently ignored by the analysis; there is one now. The **ruler strips are `EditorProps` for every presentation**, not only the navigable ones, so the live views' boolean spelling of `ruler`/`ruler_y` became `Ruler`/`RulerY` reading a boolean as off/on, and the strip's *unit* now follows the presentation, which is what it always did in practice. And a live view **accepts the editor keys it used to refuse** (`sample_rate`, the selection and playhead props): they mean nothing to it yet, but refusing them was an accident of six separate matches rather than a rule.

  The gaps the matrix exposes — a navigable live spectrum, a retained waterfall — are now expressible in the model (`caps.navigable` over a forward-only source; a retention policy on the axis) but are **not built**: they need the retention half of E3's policy wired to a live source, which no milestone has opened. Checked by hand on `gui_rulers` (both heavy views, all four axis units, the vertical zoom), `gui_scope`, `gui_analyzer`, `gui_plot`, `gui_bulk` (the raw/cache/buffer paths), `gui_multitrack`, `gui_linked` and `gui_standalone` (the persisted bundle still parses, since the wire did not move), plus a headless def holding all six names at once with a live `/gui_set` on each part of the model.

- ✅ **E7 — `clip` and `track` are containers** *(done 2026-08-07)*: a clip's three bodies were fourteen fields on `WidgetKind::Clip` that the *lane* knew how to draw; they are **children** now — a signal element, a piano-roll element, a break-point curve — placed by the layout on the clip's own axis and drawn by the elements themselves. What that bought is visible in what disappeared: `ClipDraw` and the copy-out behind it, the `Clip` arms of both bulk loaders and of both buffer-fetch legs (they fell into the `Signal` arms that already existed), and the take's own loader, since a clip's take is an ordinary `Data` source marked `bulk` — the flag that says "resolve as a peak pyramid, not as an array", which is a property of the source's size and not of the widget holding it. The bodies **layer** because that is the order the layout placed them in, so a Reaper-style envelope over a take is composition rather than a case in a match.

  **The clip's own axis came first**, in its own commit, because nothing else was possible until it did. Every body mapped its pixels through the *lane's* geometry — the body rect, the group's window, and the clip's offset subtracted back out at each of five call sites — so a body was not liftable anywhere. `track::clip_local_view` turns the lane's window into the slice of `[0, dur]` the clip's clamped rectangle actually shows, `Space::on_time` carries it down the subtree and `Placed::time` states it; from the clip inwards everything maps through `(rect, time)` and nothing else, which is exactly what makes a body an element. In the hit chain a clip contributes `Coords::Local` rather than `Coords::Time` — deliberately: a clip is not a navigation-group member, so a pan or a locate started over one still measures against the lane under it, and `time_of` keeps meaning the axis the groups move.

  Two things the split settled rather than carried. Each body now keeps **its own** value axis in fact and not only in the docs: `min`/`max` reach the bodies that measure with them (a take's amplitude, a roll's pitches) and the curve keeps `points_min`/`points_max`, where before one clip-wide pair fed both and a clip carrying notes drew its take against a pitch range. And a body prop arriving for a body the clip does not have **grows** that body (in layering order), which is how a script draws an envelope over a take without rebuilding the def — the wire has not moved, so a `/gui_set` still addresses the clip and `apply_clip_body` routes it to the child that owns it.

  **The header width was part of it** and landed as its own piece: what a container reserves before its axis started as three unrelated numbers (a lane's `header_w`, a roll's `KEYBOARD_W`, a heavy view's `ruler_w`), so stacking a lane, a roll and a ruler on one shared axis drew that axis at three different x. `timeline::group_indents` resolves one gutter per navigation group — the widest any member asks for — and the narrower chrome simply draws into the wider band. That is what made the lane header **sizeable**: `track::Header` carries a name, a mute, a solo and a level fader, presence-driven so a lane that asks for nothing is the name strip it always was, and a width that follows what it carries — and because the width is the *axis'*, one wide lane header moves the roll and the ruler with it. The controls edit back like everything else (`"mute"`/`"solo"`/`"level"`, the names of the props a driver would set).

  Found while checking by eye and fixed here: the multitrack, the composer and the `Editor` still ruled their axis with the bottom lane's own `ruler` strip, which is reserved out of *that lane's* height — the problem the free-standing `timeruler` was added to solve; they place one under the stack now. And a `timeruler` with no `link` joins the window's lanes, as an un-linked `track` already did, since a free-standing ruler exists to rule the lanes beside it. Every other timeline view stays out: a `waveform` among lanes is showing its own buffer, and joining it would be a guess.

  One piece of collateral, in its own commit: `test_gui_props.py` — the only thing standing between a prop and the three edits it costs — had been **red since E6**, because the six wire names stopped being six named schema arms and became one guarded arm over the preset table, so it reported ninety-six phantom divergences and hid whatever real one came next. It reads the guarded arm now, and the clip's bodies (whose props are read outside the clip's own arm, since they are children). With it reading again it found one real divergence, and the manifest was the stale side: the web builder's `plot` had always offered `buffer`/`cache` while the host read neither, and the signal element closed that without anyone noticing.

- ✅ **E8 — `stack`, and widgets that talk to each other** *(done 2026-08-07)*: a `stack` container showing one child at a time (an `index` prop), plus a `"widget"` target for `/gui_bind` alongside the existing `"server"` one. Together they cover alternating a waveform and a spectrogram from a control, tabs and a pager — with no script round-trip, so a persisted GuiDef becomes an autonomous application. Hidden pages skip layout and drawing, and because a heavy element's GPU slot and its bus watch are collected from the **tree** and not from the placements, a hidden one retains both: a tab switch re-uploads nothing, which is the property that made the "skip" a layout decision and nothing else. Tabs stayed composition (a control bound to a stack), so no `tabbar` was promoted.

  Three things the milestone settled rather than assumed. A binding is now an **enum of destinations**, not an address with a keyword in front of it — the wire's `"server"`/`"widget"` keyword was already there for exactly this, and what it bought is that a widget binding has no address, no prefix and no server to fail on. The **apply is the `/gui_set` mutation extracted as a method** (`Host::set_props`), so the two paths cannot drift and the no-cascade rule holds by construction rather than by a check: what a binding fires is that method, which never re-enters delivery. And the delivery seam (`gestures::deliver`) took a `&mut Host` and an effect sink, because an apply can ask for a repaint where a forward could only ask for a datagram — the one place the two destinations are genuinely different, and the reason it is a translation (`HostEffect::Redraw` → `GestureEffect::Redraw`) instead of a shared vector.

  Two smaller calls, both stated where a reader will look for them. A `stack` carries a `margin` rather than a whole `Flow`: it arranges nothing, so the `gap` between children and a `grid`'s column count would have been props the host reads and nothing means — the prop-parity check is what turned that from an opinion into a diff. And an `index` outside the children shows **nothing**, deliberately: a blank page rather than a clamped one, so a pager that runs off its end cannot silently show the wrong child. A multi-value edit-back payload aimed at a widget rides as the JSON string the array-valued props already take, so one curve drives another curve's `points` with no second carrier invented.

  Exercised through the new `gui_stack.py` — a waveform and a spectrogram of one take behind a bound picker, with a slider bound to `view_start` — and headless through `/gui_query` (the `stack` type and its `index` round-trip), plus a gesture-level test that presses a bound toggle and asserts the page moved, the window was asked to repaint and **nothing** left for the script. What that leaves for a human is the one thing none of it covers: clicking the picker and watching a page flip, and flipping back to see that the spectrogram does not rebuild.

  The example is also where the **shape of a second binding** got decided, after the first draft bound a slider to one page's value axis and the reviewer put it plainly: a control that only works on one of the pages has no business being under all of them. The y axis is per view on purpose — a waveform's is amplitude, a spectrogram's is frequency — so nothing vertical is shared; what the pages *do* share is time, through the navigation group. So the slider drives `view_start` on a linked pair, and moves the page nobody is looking at as well as the one on screen. Which is the same tree-not-placements property the GPU slot has, and now has a test of its own: a hidden page is still a member of its group.

### E9's target vocabulary, decided before the code moves

Written 2026-08-08, ahead of the milestone: E9 is a rename of everything at once, so what it renames *to* is the one thing that cannot be decided arm by arm while converting. This section is what the conversion is checked against; where a choice is genuinely open it says so rather than picking quietly.

**The shape of a node stays exactly what it is** — `{id, type, props, children}`, JSON inside one OSC argument, ids client-allocated, unknown types laid out but not painted. E9 changes the **vocabulary of `type` and the ownership of the props**, not the wire's form, and no `/gui_*` address moves.

#### The three kinds of node, and what each one is for

| Kind | What it is | `type` values |
|---|---|---|
| **Container** | owns 0, 1 or 2 **axes**, and therefore a coordinate system its children are placed in | `window`, `layout`, `plane`, `field` |
| **Element** | draws against the axes of the container holding it; owns no navigation | `signal`, `notes`, `curve`, `score`, `keys`, `nodes`, `meter`, `canvas`, `label`, and the controls |
| **Control** | an element with no axis and a value: already at the right altitude, so **it does not move** | `slider`, `knob`, `number`, `button`, `toggle`, `text`, `menu` |

The controls are called out because the temptation is to rename everything. A `knob` is not an accident of the catalog — it names what it is, its props are its own, and nothing about it duplicates another node. The migration's target is where the catalog says one thing several ways.

#### The container table

| New | Axes | Replaces | Notes |
|---|---|---|---|
| `window` | 0 | `window` | unchanged: a window is a root, not a coordinate system, and it is a `layout` that happens to be one |
| `layout` | 0 | `panel`, `box`, `stack` | children flow by `flow` (`row`/`col`/`grid`/`free`/**`stack`**). `stack` stops being a type and becomes the flow that shows one child — which is what it always was: a layout container with a selection instead of an arrangement. Its `index` rides along |
| `plane` | 2, **locked** to one scale | `scroll`, `patch` | a pannable/zoomable plane in content units. The patcher stops being a container type and becomes a `plane` holding `box` elements — the boxes and wires are what `patch` actually adds, and they are elements |
| `field` | 2, **independent** (x and y each their own domain) | `track`, `clip`, `timeruler`, and the container implicit in every heavy view | the time/value container. A lane is a `field` with lane chrome, a clip is a `field` nested in one, a free-standing ruler is a `field` with no elements in it |

**`field` is settled** *(2026-08-08)*, having been the name most open to a better one: it has to cover a lane, a clip and the inside of a plot without reading as any of them, and every alternative leaned toward one use — `region` *is* the clip in DAW vocabulary (Pro Tools, Logic), `chart` reads as the plot, `frame` is already taken twice in the crate (the renderer, an STFT column), and `axes` is the prop key the container carries. `field` is the one that names none of the three, which is what a container covering all three needs.

#### The element table

| New | Replaces | How the old name is reconstructed |
|---|---|---|
| `signal` | `waveform`, `spectrogram`, `plot`, `scope`, `spectrum`, `phasescope` | `view` (`trace`/`spectrum`/`spectrogram`/`phase`) × the source (`bus` = forward-only, `buffer`/`path`/`cache`/`data` = addressable) × `navigable`/`selectable`/`editable`. The table already exists in code as `signal::preset` — E9 deletes it by making the wire say the point directly |
| `notes` | `pianoroll`, a `clip`'s note body | one element, already one model (`host::pianoroll`) |
| `curve` | `bpf`, a `clip`'s automation body | idem (`host::bpf`) |
| `nodes` | `nodetree` | renamed only to stop reading as a *widget* named after a tree |
| `keys` | `piano` | a keyboard is an element; `piano` reads as an instrument |
| `box` | the patcher's boxes (today inside `patch`) | a `plane` places them; the wires stay a prop of the plane, since a wire is between two children and belongs to neither |
| `score`, `meter`, `canvas`, `label` | themselves | already one thing each |

A `clip`'s three bodies stop being props routed to children (E7's `apply_clip_body`) and become **children a script writes**, which is the migration's biggest simplification and the reason E7 shipped the containment first.

#### Where the props go

The migration moves props to whoever owns the thing they describe. This is the half that breaks scripts, so it is enumerated:

| Props | Today | Under the model |
|---|---|---|
| `ruler`, `tempo`, `beat_at`, `quant`, `sample_rate`, `link`, `view_start`, `view_len`, `sel_start`, `sel_len`, `playhead*` | on every timeline view (`EditorProps`) | the **x axis** of the container |
| `ruler_y`, `y_start`, `y_len`, `min`, `max`, `bit_depth` | idem, plus `min`/`max` on five widgets | the **y axis** of the container |
| `offset`, `dur` | on a `clip` | the child's **placement on the parent's x axis** |
| `w`, `h`, `weight`, `x`, `y` | any widget | unchanged — placement in a layout container |
| `data`, `buffer`, `path`, `cache`, `channels`, `base_bucket`, `bus`, `rate` | on each view | unchanged — the **source** is the element's |
| `fft_size`/`window_size`, `hop`, `db_floor`, `db_ceil`, `freq_scale`, `colormap` | on the spectral views | unchanged — the presentation's own parameters |
| `layout`, `margin`, `gap`, `cols`, `index` | containers | unchanged, on the container |

**The name collision this creates is `x`/`y`**, which are already the free-placement props. So the axes are declared under one key — `"axes": {"x": {…}, "y": {…}}` — rather than as bare `x`/`y` objects. That also gives an axis somewhere to grow a `unit` and a retention policy (the waterfall the model already allows and nothing builds).

#### How it lands without a half-migrated wire

E9 is written as one milestone because the wire, both builders, the three books, every example and the persisted bundles are ends of one cable. It still has to survive being interrupted, so it lands **expand → migrate → contract**, each stage green on its own:

1. ✅ **Expand** *(done 2026-08-08)*. The host learns the new vocabulary and keeps accepting all 29 current names as an internal alias into the same constructions. Nothing else moves; every example still runs. `docs/gui-protocol.md` documents the new reference and marks the old names as leaving.

   It landed as **one door rather than a second parse** (`host/widget/vocabulary.rs`): a node is rewritten into the catalog's type and flat props *before* `build` sees it, so there is one construction path, the alias layer is the only thing that knows two vocabularies exist, and stage 3 is a deletion. The rewrite is skipped entirely for a node the catalog already names, which is every node any current script writes.

   Three things the writing settled that the table had left implicit. **A `field` is told apart by what is on it** — a placement (`offset`/`dur`) makes it a clip, children make it a lane, neither makes it the bare ruler — which is the model's claim ("one container, three uses") turned into the only rule that could implement it. **A `plane` is a patcher when it has boxes**, for the same reason: what `patch` adds to a plane is its boxes and cords, so their presence is the distinction rather than a second type name. And the **capabilities became props of the signal element** (`navigable`/`selectable`/`editable` over the preset's), which is the part of the six-into-one collapse that actually buys something new: a trace that does not navigate used to require choosing a different widget.

   The one thing expand cannot do is `box`: the catalog spends it on a synonym of `panel` while the model wants it for a patcher's box, and both cannot hold while both spellings parse. It is deferred to stage 3, and a plane's boxes stay the `boxes` prop until then.
2. ✅ **Migrate** *(done 2026-08-08)*. Both builders emit the new names; with them the examples of the three directories, the Python `Editor`, the web composer, and the bundle path (`host::bundle`, `/gui_load`) — reading either spelling, writing the new one. At the end of this stage everything speaks the model and the old spelling still parses.

   **The builders keep their names.** `panel`, `waveform`, `track`, `clip`, `scope` and the rest stay, as *shortcuts* that build a model node with the props of one common case, and `layout`, `plane`, `field` and `signal` sit beside them for what no shortcut names. That is the one place the "total migration, not presets" decision does not reach, and deliberately: the decision is about the **wire** — the reference documenting 28 types over a model of six concepts — and the wire is now total. A client-side shortcut re-documents nothing: the protocol reference names the model, and the shortcut's entry says which point of it that name is. What it bought is the whole reason the stage landed in one pass: **no example changed**, in any of the three directories, because every call site was already writing a shortcut.

   The cost is that the *Python and TypeScript* surfaces are not total, and stage 3 has to decide whether the shortcuts stay. The argument for keeping them is the same one that keeps `Env.adsr` beside the general envelope; the argument against is that a reader who only ever types `waveform` never discovers that a plot is a trace that does not navigate.

   Four things the migration settled that only writing it could:

   - **A plot is `navigable: 0`, and that is not only a capability.** A trace that does not navigate also resolves its source as the sequence itself rather than as a take, and auto-fits a value axis nobody named — so the vocabulary layer resolves it to the whole `plot` construction rather than to a `waveform` with a flag off. The capability *is* the difference; it just implies more than a flag.
   - **An empty lane is a lane.** `field` was to be told apart by "children, or nothing at all" — but a multitrack opens empty lanes constantly, and reading one as a ruler is worse than any rule. So the ruler is the case that must say what it is: a bare strip of a given thickness `h`, nothing placed on it, no lane chrome.
   - **`flow` is every container's, not `layout`'s.** The model spends the word `layout` on the container type, so the arrangement needed the other word — on a `window` and a `plane` too, not only on the container named after it.
   - **The axis chrome is recorded flat.** `/gui_info` carries flat OSC arguments, so a structural prop cannot be answered — and moving the chrome into `axes` would have made a `/gui_query` stop reporting the ruler, the window and the value range, which is a real loss for a widget whose state a script reads back. So a def's axis pair is flattened at the door, before the registry records it, while the node's *type* is kept as written: a query answers in the vocabulary it was asked in, with the chrome the host itself reads.

   The parity instrument moved with them. `test_gui_props.py` reads all three surfaces by **model** type now, which meant fixing two readers that had been quietly wrong: both keyed a kind by assignment, so with several builders sharing one type (`waveform`, `plot` and `scope` all build a `signal`) the last one read won and the others vanished — and the web reader was attributing the generic `node` escape hatch's `type` parameter to whichever builder followed it in the file. Both now union per kind. The manifest's twelve `idiom` rows on `track` and `timeruler` became four on `field`, since the two are one container.
3. ✅ **Contract** *(done 2026-08-08)*. The alias table is deleted, `test_gui_props.py` is extended to the new surface, the `docs/decisions.md` entry for the axis/container model lands, and the versioning consequence is taken (the `release-versioning` skill; the persisted bundle format is what makes it breaking).

   The old names **stop parsing**: a `type: "waveform"` is an unknown type now, laid out and unpainted like any other. The alias module did not simply vanish, though — half of it was never scaffolding. `vocabulary::resolve` dissolved into the schema, where the discriminations became **guarded arms** of the model's own names (`"field" if it carries a placement => Clip`), and `signal::preset(name)` — the six-name table the whole track called the compatibility layer — became `signal::point(view, live, navigable)`, a function of the product's coordinates with no names in it. What remained is the axis pair and its flattening, which is a *prop* relocation and not an alias, so the file is now `widget/axes.rs` and says only that.

   The **client** surfaces keep their builder names, as decided: `waveform()`, `track()`, `panel()` and the rest are shortcuts onto the model, and `layout=` survives as the old spelling of `flow=` on the three containers that take one. Those are the only three rows the prop manifest gained; the host never sees either.

   The version moved to **0.9.0** — pre-1.0, so the minor is the breaking tier. Neither ABI counter moved: the GuiDef vocabulary is not the shm/embed boundary nor the core FFI, and a saved bundle in the old spelling fails as *content*, not as a version mismatch. Re-save it from a current client.

The transitional aliases are **not** the rejected "presets on the wire" decision reappearing: that decision is about the end state, and stage 3 is what makes the end state total. If stage 3 is not reached, the migration is not done — the alias table is scaffolding, and a release must not ship standing on it.

- ✅ **E9 — The migration** *(done 2026-08-08)*: the wire stops naming widgets and starts naming the model. One milestone moving together, per the packages-move-together rule: `docs/gui-protocol.md` rewritten around containers/axes/elements, `docs/architecture.md`'s widget map and module split, both builders (`clients/python/clausters/gui/guidef.py`, `clients/web/src/gui/guidef.ts`), every example in the three example directories, the Python `Editor` and the web composer, and the persisted bundles (`host::bundle`, `/gui_load`) — which is the piece that makes this a breaking change and drags the SemVer tier along (see the `release-versioning` skill). `clients/python/tests/test_gui_props.py` is extended to the new surface, since it is the only thing standing between a prop and the three edits it costs. The `docs/decisions.md` entry for the axis/container model lands here, when the model is proven rather than proposed. **The target vocabulary is written above** (the section before this one) rather than decided while converting, and the milestone landed expand → migrate → contract so an interruption never left a half-migrated wire.

  What it cost, in the end, was **not** the examples: the two clients' builder names survive as shortcuts onto the model, so every call site in the three example directories kept working untouched. What it did cost is the wire, the persisted bundles and the version (0.9.0). One name is left unclaimed on purpose — `box`, which the model wants for a patcher's box: a plane's boxes are still its `boxes` prop, because making them child elements changes behavior (ids, layout, per-box hit-testing and edit-back) rather than spelling. That is its own milestone, not a rename.

- ✅ **E10 — A clip's take is a presentation too** *(done 2026-08-08)*: `view` on a `clip`, so a take is drawn as the trace (the default) or as the time-frequency texture — the same signal element the `signal` view is, placed in time. The point is what it *stops* being: a spectral lane used to be a heavy view dropped into a lane, spanning the visible axis forever, with no offset, no duration and no drag; now it is a clip, and everything a clip does it does. Three seams moved. The **texture** is not geometry, so `frame` collects a time-frequency body apart from the mesh bodies and draws it in the GPU pass, uploaded against the clip's **own** axis (`Placed::time`) rather than the group's window — that upload is the whole difference between a spectral view of a file and a spectral clip of it. A slot is keyed by whatever *addresses* the widget, so `SignalElement::needs_gpu_slot` replaced `is_gpu_view` at the four collection points (the native window, the buffer-fetch leg, the browser's inline and bulk paths), a clip's body resolving under the clip's id the way its buffer fetch already did. And the analysis props stopped being read twice: `build::spectral_props` is one function for the element and for a take, so a spectral clip is tuned exactly like a spectral view, with the **display** half (dB window, frequency scale, colormap) live on the clip through `apply_clip_body` and the analysis half read when the texture is built. Ported in the same change: `docs/gui-protocol.md`, both builders, and `examples/gui_multitrack.py`, whose third lane is a spectral clip.

  A gesture bug the same example surfaced, open since E7 made a clip a container: the chain's innermost time container over a press on a clip **is the clip**, and its default table answered `element` for every chord — so Shift+drag grabbed the clip instead of panning the lane, and a busy arrangement could only be panned in the gaps between its clips. A clip now takes the plain drag and declines the rest (an **empty** plan, which walks outward, as against `none`, which consumes), because a clip is the container of its own local `[0, dur]` axis and a pan there would move the wrong window.

  Three more the example found, once it was a window someone actually used. **The wheel and Shift+drag off the lanes** did nothing: the gap between two lanes, the slack under the last one and a container's margin belong to the plane, which ate the gesture and — pinned, with nothing to scroll — did nothing with it. A plane that cannot move now passes it on — its wheel and its **pan step** alike (`interact::plane_can_pan`), since a press eaten by a pinned plane is what left Shift+drag dead everywhere but over a lane — and a window with exactly one navigation group answers for those pixels (`interact::sole_time_axis`): the wheel zooms the axis, Ctrl+wheel resizes its lanes, Shift+drag pans it, including on the bare window outside every container. **A free-standing `timeruler` had no `apply` arm at all**, so a `/gui_set` of its unit was recorded in the registry and never reached the drawing — the strip could not be switched between bars, seconds and samples after it was built. And a **`quant` in samples** (the example's own bug, but one the docs invited): `quant` is *beats per bar*, and given a sample count the ruler decides a bar is 24000 beats, finds no step whose labels fit as soon as you zoom out, and draws none — the wire reference and both builders now say the unit.

  Two drawing bugs the same example surfaced. A clip's **name** was drawn with its box, so the take's trace went over it and a spectral clip's texture — a GPU pass after every mesh — hid it outright; the box stays the base mesh's and the name moved to the **overlay**, where the playhead and the selection already are. And the group's gutter was applied **twice** to anything drawn inside a time container — the lane takes it out of its rect to make the body, and a member drawn in there took it out again. A clip's bodies never noticed (they carry no id, so they are in no group); a heavy view used as a lane's body landed a gutter's width right of the clips it shares an axis with, which reads as a desynchronized playhead. `layout::place` now stamps `indent = 0` inside a time container.

- ✅ **E11 — The menu opens** *(done 2026-08-08)*: a press opens the option list over the window and a press on a row picks it — what everyone who saw a menu bar already believed. What it took was not a widget but a **popup**: a thing on screen that is not a placement. It lives with the rest of the interaction state (`Gestures::menu_open`, beside the marquee), reaches the renderer through `FrameInputs` and draws into the **overlay**, last of everything, so it covers the heavy views the GPU pass paints between the two meshes. It is **modal at the door**: `Gestures::press` tests it before the tree and swallows the press either way — on a row it picks, elsewhere it only dismisses — which is one branch at the top of the press rather than a priority rule threaded through the hit-test. The geometry is one function used by the drawing and by the hit (`controls::menu_popup`/`menu_row_at`), so a click lands on the row it highlighted, and the list flips above its field when there is no room below. The hover highlight is read straight off the frame's cursor: a hover is not a gesture and needs no state. The wire did not move — same `options`, same `index`, same event, and a bound menu still forwards without a script — so neither client changed beyond its docstring. Out of scope, deliberately: keyboard navigation and submenus.

  What the milestone replaced was its own interim: the cycling press shipped with E10's marker pointing sideways, which now points **down**.

### Finishing the track (E12–E16)

*Added 2026-08-09, from the refactor audit written after E11 and from the generality audit that followed it.* E1–E11 replaced the catalog with a model, and the model is sound; what is left is **the files the model did not reach** and **the verification the track never got**. Nothing here is a capability (E16 excepted, and it is a capability the model already expresses), so the whole segment is checked the way E1–E4 were: the tests stay green and nothing moves on screen.

- ✅ **E12 — The eye pass and the web pass E1–E11 never got**: the track moved every drawing path in the host and every gesture in the machine, and **not one example was run**. Two verification holes, both named in the refactor audit rather than in a commit. Nothing in CI runs an example (the project rule: they *are* the manual test surface), and a `Draw::new` argument of the same type swapped for another compiles clean — so a pure move is low risk and not zero risk. And `clients/web` was never built or tested, which is the one thing that proves the web package works, and the split of `web/` is precisely what would break it. Order, because the second is expensive: `scripts/refresh-bin.sh gui_shell` first (the bundled binaries beat `target/`, so a stale copy would test the pre-refactor host), then the examples E5 recorded as *not* re-run by eye — `gui_patch1`, `gui_patch2`, `gui_workspace`, `gui_pianoroll`, `gui_score`, `gui_composer` — then `cd clients/web && ./build.sh && ./test.sh` (build first; `dist/` is git-ignored). Acceptance: the list above opened and watched, each divergence fixed in its own commit, and this entry recording what was actually looked at — so the next refactor inherits a list instead of a hole.

  **What was looked at (2026-08-09).** Binaries refreshed first, then the six
  opened and watched: `gui_patch1` (both patcher levels, auto-layout and box
  dragging), `gui_patch2` (the SynthDef and the FaustDef views, the spec's round
  trip), `gui_workspace` (plane pan/zoom, the view reported each frame),
  `gui_pianoroll` (scale edit and playback), `gui_score` (engraving, click,
  transpose drag, transport), `gui_composer` (clip drag and resize, the cursor
  moved from a lane's ruler). Every drawing path and every gesture the track
  moved came back unchanged on screen — **one** divergence, and it was not a
  drawing one: clicking a note in `gui_score` raised `no server to play on`,
  because a handler runs on the client's reply thread and the ambient session
  `play` resolves against is per-thread. Fixed in its own commit by naming the
  server; `pyright` clean over the package, the tests and both example
  directories. Then the web half: `./build.sh` staged, `./test.sh` green — 229
  unit tests and all 19 pages, the split of `web/` intact.

- ✅ **E13 — `interact.rs` splits by the question it answers** *(2036 lines, no submodules)*: the file has the same shape `gestures/mod.rs` had at 2149 before E5 cut it, and `web.rs` had at 2325 — hit-testing, the coordinate-system types, per-element value mutation and per-element data readers (`score_data`, `piano_state`, the `clip_*` and `graph_*` families) in one flat file, carrying the three surviving `allow(too_many_arguments)`. Into `interact/`: `coords.rs` (`Coords`/`TimeAxis`/`YAxis`/`Link`/`Hit` — the chain's vocabulary), `hit.rs` (the one layout pass that answers a pointer question, `chain_of`, `time_axis`, `view_of`), `edit.rs` (the write doors — `set_fraction`, the text field, `bpf_edit`, the scroll-view door) and `read.rs` (the per-element readers the frame and the gestures ask for). A pure move; the `allow`s go with the split rather than surviving it, since each one is a function that was carrying two files' worth of parameters.

  **What shipped.** The four children, and one rule that turned out to be worth
  more than the file sizes: **`coords.rs` never mentions the `Host`**. That is
  what makes it the vocabulary rather than a fourth door — types and arithmetic,
  testable on their own — and it decided every borderline case (the axis math,
  the clip-part split and the drag placement went there; anything that had to
  ask the tree did not). The other three follow the moment of the gesture:
  `hit.rs` answers *what is under the point*, `edit.rs` writes, `read.rs` says
  what an element holds and builds the payload that reports it. `mod.rs` is 68
  lines — the map, plus the re-exports the fronts use, which is deliberately
  *less* than the file held: a name used only inside the module is not
  re-exported, so the list is an inventory of the door rather than of the file.
  The three `allow(too_many_arguments)` retired the way the entry predicted, by
  naming what the scalars were: `CanvasAt` (the area, the workspace scale and
  the cursor — a patch canvas question) and `ClipAt` (the clip's rectangle, its
  own window and the cursor), each one value where four positional floats could
  be paired wrongly. Zero `allow`s left in the module. Green on 501 tests,
  clippy, rustdoc and `check-wasm.sh`; `docs/architecture.md` and the
  `gui-widgets` skill now name the children.

- ✅ **E14 — The schema file keeps the schema** *(`widget/mod.rs`, 3222 lines)*: `build`/`apply`/`size`/`parse`/`axes` are already siblings, so what is left is the enum plus roughly 600 lines that are not the enum — the prop bundles (`EditorProps`, `GestureMap`/`GesturePlan`, `ScrollView`, `Flow`, `Range`, `Place`) to `widget/props.rs`, and the per-kind predicates that are really a query pass (`is_timeline`, `event_value`, `live_bus`, `editor`, `link_lanes`) to `widget/query.rs` — the same third-arm-per-kind shape `size.rs` already is, in the same direction. What stays is `WidgetKind` and its documentation, which is where a reader looking for the model should land. Ordered after E13 because it is the smaller of the two and the one the K track's registry rewrites anyway.

  **What shipped, and the one thing the entry did not foresee.** `props.rs` and
  `query.rs` landed as described — the bundles and their vocabularies (the
  `Ruler`/`RulerY`/`Axis`/`GestureStep`/`Layout`/`Align`/`Rate` words go with the
  bundles they are built from, since they are the same detail), and the query
  pass took both halves: the per-kind readers *and* the **clip routing**
  (`signal_target`, `kind_or_body`, `clip_body`), which is the same question
  asked through a container and had no better home. But the file was 3222 lines
  and the enum plus the bundles plus the queries only account for half of it:
  **1466 lines were the test suite**, which no reading of the entry would have
  guessed and which is why the file did not shrink the way the arithmetic
  promised. It moved whole to `widget/tests.rs` — it is one suite with one
  `node()` helper, testing the schema through `from_node`, and splitting it
  three ways would have duplicated the helper without separating anything.
  `mod.rs` is 661 lines: the enum, the `Widget` node and the tree walk.
  One trap worth recording for the next move of this kind: **`pub(super)`
  changes meaning when an item moves down a level.** Three doors that `host` had
  reached (`EditorProps::parse` from `host::signal`) became invisible the moment
  they were a level deeper, and the compiler said "private" about code nobody
  had edited. They are `pub(crate)` now, which says what they always meant.

- ✅ **E15 — Notation gets a submodule, on both sides**: `host/score.rs` is 1664 flat lines, and unlike the other elements its growth is **semantic, not graphic** — the timemap, the identity of the element under the cursor, transposition in diatonic steps, the edit-back payloads, and everything G31g still owes (tuplets, voices, spelling, articulations). That is the argument that made `signal` a submodule, applied one element later. Host: `host/score/` — `list.rs` (display-list decode), `glyphs.rs` (the outline cache), `tess.rs` (the lyon fill and the painter's strokes), `cursor.rs` (timemap, playback cursor, element hit). Client: `clausters/gui/notation.py` (460 lines) becomes `clausters/gui/notation/` — `engrave.py` (verovio), `mei.py` (the encoder G31g extends, kept as the seam it already is), `timemap.py`, `edit.py`. The web client has **no** notation module at all, so per the packages-move-together rule this milestone leaves `clients/web/PLAN.md` naming the shape the port must follow rather than letting the two re-derive it. No wire change, no behavioral change; the host still never depends on verovio.

  **What shipped, and where the entry guessed wrong.** The host split landed as
  written: `score/list.rs`, `score/glyphs.rs`, `score/tess.rs`,
  `score/cursor.rs`, plus the 500-line suite in `score/tests.rs` and `mod.rs`
  left as the page itself — the geometry types, the primitive, and the
  `ScoreData` every one of those files is a method of. The client half is a
  package too, but with **three modules, not four**: `engraver` (the loaded
  document, the one-shot engrave, the SVG adapter, the page-replacement
  payload), `mei` (the encoder, kept as the seam it already is) and `view`
  (`score_view` + `transport`), beside a private `_abi` for the two shapes both
  native callers share. There is no `edit.py`, and the reason is worth
  recording: **editing is not a body of code here** — `transpose`, `edit`,
  `undo` and `redo` are four methods on the verovio handle, and lifting them
  off `Score` would invent a seam rather than find one. `timemap.py` did not
  survive contact either: the timemap is decoded in the core and consumed by
  `transport`, so it is a line of `view`, not a module. One name changed on the
  way: the submodule is `engraver`, because `notation.engrave` is already the
  function and a module of the same name would have shadowed it at runtime.
  530 Python tests green, `pyright` at zero, both score examples watched by eye
  (a click, a transpose drag, undo/redo, and a page engraved from a timeline).
  `clients/web/PLAN.md` now carries the shape the port must follow.

- ✅ **E16 — Retention is a policy on the axis** *(the one gap the model expresses and nobody built)*: E3 decided that an axis is navigable when its domain is addressable and that a forward-only source becomes addressable only if the host **retains** a history — and then built the clamp/zoom half and not the retention half. E6 confirmed it from the other side: a navigable live spectrum and a waterfall are reachable in the model and absent from the catalog. This wires the missing half: a retention ring per watched bus, sized by the axis's declared span, filled by the same live path that already reads the segment each frame, so `caps.navigable` over a forward-only source stops being a combination that parses and does nothing. Acceptance: a retained waterfall and a zoomable live spectrum in `gui_analyzer`, and the retention span live via `/gui_set`.

  **What shipped.** `retention`, in **seconds**, on the forward-only source —
  the unit the axis is declared in, so the same seconds are the same seconds
  whatever the frame rate, the `fft_size` or the `hop`. Two stores, keyed
  differently on purpose: a **history per watched bus** (`live::BusHistory` —
  one history however many views read it) and a **rolling transform per widget**
  (`waterfall::Waterfall` — two views of one bus may analyze it differently).
  The transform is rolling rather than recomputed: at 30 fps over an 8 s span
  a full re-analysis would be ~750 FFTs a tick to learn what two of them say, so
  each whole hop is analyzed once, through the very function the stored path
  uses (`spectrogram::column_into`, factored out for exactly this) — which is
  what makes a retained waterfall and an offline spectrogram of the same audio
  the same picture, and lets the renderer, the frequency ruler and the cursor
  readout stay one implementation.

  **Three things the entry did not foresee, all found by eye and all worth
  keeping.** *(1)* The append must be by **stream position**, not by window:
  two ticks read overlapping windows and the overlap follows the frame rate, so
  appending windows would stretch the history. `BusSource::read_bus_at` carries
  the position; a stretch the reader never saw is retained as **silence**, so a
  drop-out is visible instead of being compressed out of the time axis. *(2)* A
  retaining read must size itself by **what the source will serve**
  (`BusSource::window_limit` — half the tap ring): asking for more is refused,
  and a refusal is indistinguishable from a bus nobody writes, so the first
  build retained nothing and looked exactly like silence. *(3)* The one that
  actually took two rounds at the window: **a live axis has to register its
  length, and follow it.** Without the length the navigation window falls back
  to a span the size of the body in *samples* and the whole history draws as one
  stretched column; with the length but a kept view, the view walks off a
  history that slides underneath it and the picture stalls. The rule is
  `Host::set_live_timeline_total`: **follow the newest until you navigate it,
  then stay where you were put** — "untouched" being the view still spanning the
  whole axis, which a zoom or a pan stops being true of.

  **Not done, and it is a separate design rather than a leftover: the zoomable
  live `spectrum`.** The waterfall is a navigable live view and it is what the
  entry's body describes; the *curve*'s frequency-axis zoom is a different
  mechanism. A `spectrum` is not a timeline container — it has no `Coords::Time`,
  no navigation group, no x window — and its x domain is **frequency**, not
  time, so making `navigable` mean something over one means deciding what a
  navigation group is when its axis is not the window's time (today
  `signal::point` deliberately drops `navigable` for a live spectrum). The draw
  side is nearly free — `draw_spectrum` already maps every column through
  `ruler::display_to_hz`, so a normalized `[start, len]` sub-range is one
  remapping there and one in `hz_ticks_h`. It is the gesture and grouping half
  that needs a decision, and inventing one here would land it in the same voice
  as a staged design. It is **E17**, opened below.

**Not a milestone here — `box`.** The catalog spent the name on a synonym for `panel` and E9's model wants it for the patcher's box, but a plane's boxes are still its `boxes` prop. Turning them into child elements changes ids, layout, per-box hit-testing and edit-back — it is behavior, not spelling — so it belongs to **P5**, the patcher's editing surface, where it is now written down as that milestone's first item rather than only pointed at from here.

### The axis E16 left open, and what the GPU path still owes (E17–E19)

*Added 2026-08-09, from what closing E16 found; E19 added 2026-08-10, from the
by-eye pass that closed E17.* Retention made a forward-only source addressable
and proved the model end to end, and it surfaced two things that are genuinely
missing rather than merely unbuilt. Neither is a refactor: one is a capability
the acceptance line asked for and did not get, the other is a cost the first
landing knowingly paid. The third is older than both — a defect the examples
walked past for as long as there have been GPU windows, and that only a pass
with an eye on the drawing was ever going to catch.

- ✅ **E17 — A navigable axis whose domain is not time**: E16 shipped the
  retained waterfall and **not** the zoomable live `spectrum` its acceptance
  line also named, because the two are not the same mechanism and the second has
  no staged design. Everything navigable in the host today navigates *time*: a
  navigation group is keyed by `link` over a shared time axis, `Coords::Time`
  carries a `TimeAxis`, `is_timeline` is what joins a widget to a group, and the
  span is measured in samples. A `spectrum`'s x is **frequency** — a domain that
  is addressable without any retention at all (E3's own observation, and E6's) —
  so `navigable` over one is a combination the model expresses and the code has
  nowhere to put: `signal::point` deliberately drops the capability for a live
  spectrum rather than pretending.

  What the milestone has to decide is exactly one thing, and it is not a
  drawing question: **what a navigation group is when its axis is not the
  window's time.** Two shapes are open and the choice belongs here rather than
  in an implementation — a *second axis kind* beside `Coords::Time` (a
  `Coords::Domain` carrying its unit, so a frequency axis groups with other
  frequency axes and never with a lane), or the **element's own window**, no
  group at all, in the shape `EditorProps::y_view` already has for the vertical
  axis (a normalized `[start, len]` the element carries alone). The second is
  smaller and is probably right for a spectrum, which nothing else would ever
  share an axis with; the first is what a *pair* of spectra comparing two buses
  would want, and it is the one that generalizes to the arbitrary-domain plot.
  Decide it, record it in `docs/decisions.md`, then build.

  The draw side is nearly free once that is settled and is worth stating so the
  cost is not overestimated: `spectrum::draw_spectrum` already maps every screen
  column through `ruler::display_to_hz`, so a normalized sub-range is one
  remapping there and one in `ruler::hz_ticks_h` — the same inversion the
  spectrogram's frequency ruler and cursor readout already share. The work is
  the gesture and grouping half. Acceptance: a live spectrum in `gui_analyzer`
  zoomed and panned on its frequency axis, the window round-tripping through
  `/gui_set` and a `"view_x"`-shaped event, and the ruler revealing finer rungs
  as it zooms — the property L8 already fixed for every other axis.

  **The decision, and the argument that settled it.** It is the **element's own
  window**, and not because the diff is smaller: *frequency is already navigated
  that way in this host*. A spectrogram's frequency axis is its vertical one,
  and that axis is a normalized per-element window with no group behind it —
  because a display axis over `[0, Nyquist]` on a log/mel/bark scale is a
  display coordinate inverted through one shared mapping, and nothing else in a
  window measures in hertz to share it with. A grouped x would have given one
  domain two mechanisms depending on which way it happened to be drawn. So
  `EditorProps` grew `x_start`/`x_len` beside `y_start`/`y_len`, read through
  `x_view()` with the same clamp and the same read-at-use validation, reported
  as `"view_x"` beside `"view_y"`. The general case is not foreclosed: the
  per-element window stays where the answer is stored, so a future group would
  *write* it, exactly as a timeline group writes its members' view today.
  `docs/decisions.md` carries the record.

  **What shipped, and the three things the entry did not foresee.** The draw
  side was as cheap as promised — one remapping in `draw_spectrum`'s `bin_at`,
  one in `plot`'s (the stored spectrum navigates on the same window, for free)
  and a windowed `ruler::hz_ticks_h`. *(1)* That ruler needed more than a
  remapping to keep L8's property: the horizontal twin only walked decades, so
  zoomed past one it had nothing finer to show. It now picks between the two
  schemes its **vertical** twin already picked between — decades over a wide
  window, a 1-2-5 ladder in hertz fitted at the actual (nonlinear) positions
  over a narrow one — which is what makes zooming reveal finer round
  frequencies. *(2)* `navigable` had to become **opt-in for this one view**:
  the prop defaults to on everywhere else, and letting it default on here would
  have made every shipped `spectrum` start panning under a drag, unasked. *(3)*
  `is_nav_signal` had to stop meaning "navigable signal" and start meaning
  "navigates *time*" — otherwise a navigable spectrum joins `is_timeline`, and
  a curve over frequency lands in a navigation group measured in samples. That
  one line is the whole seam between the two axes. Green on 515 tests (the
  ruler property, the props round trip, and press/wheel/`R` over the new axis),
  clippy, `check-wasm.sh`, `pyright` at zero and the web suite; both builders,
  `docs/gui-protocol.md`, both books and `gui_analyzer` carry it.

- ✅ **E18 — The waterfall writes its new columns instead of re-uploading the
  texture** *(done 2026-08-10)*: E16's rolling analysis is incremental — a hop's worth of samples
  costs one FFT, not a re-analysis of the span — but the *upload* is not: every
  tick that lands a column rebuilds a `SpectrogramView`, which allocates a
  texture and writes the whole magnitude image. **The number, so this is a
  measured cost and not a worry**: the texture is `R8Unorm`, one byte a texel, so
  an 8-second span at hop 512 and `fft_size` 1024 is 750 columns × 512 bins =
  **384 KB per tick, ~11.5 MB/s at 30 fps**, plus one texture allocation per
  tick per waterfall. That is affordable — it is why the milestone landed this
  way — and it is also the wrong shape: it scales with the *span* where the work
  scales with the *hop*, so a minute of retention costs eight times as much to
  show the same two new columns.

  The fix is the classic one and it is what a ring texture is for: keep the
  texture allocated, `write_texture` the **new columns only**, and let the shader
  unwrap a write cursor (one more uniform beside the `time`/`freq`/`db` triple
  the pipeline already carries) so the oldest column reads as the leftmost.
  Its cost is a change to `spectrogram.wgsl`, which **every** spectrogram shares
  — the stored path included — so the invariant to hold is that a stored
  transform is the degenerate case (cursor 0, never advanced) and draws
  byte-identically to what it draws today. Ordered after E17 because it changes
  nothing a user can see and E17 changes something they asked for.

  **What shipped, and the one thing it did differently.** The ring is there and
  the numbers are the ones above (`tests/gpu_slot_cost.rs` states them: 384 000
  bytes a tick becomes 3 072, and eight times the retention leaves that
  unchanged) — but **`spectrogram.wgsl` was not touched**. The entry assumed the
  shader would unwrap a write cursor, and unwrapping is exactly what the shader
  must not do: the sampler filters linearly, so a coordinate that wraps blends
  the newest column into the oldest and sends a visible seam travelling through
  the picture. Storing the ring **twice**, back to back, makes the visible window
  one contiguous run of texels however far the cursor has advanced — and a
  contiguous sub-range is *precisely* what the `time` uniform already expressed.
  So the whole change is CPU-side: the invariant the entry asked for (a stored
  transform draws byte-identically) is not a property the diff has to preserve
  but one it cannot break, since a stored transform reaches the same shader
  through the same uniform. The doubling costs the texture twice the memory and
  halves the retained-column ceiling (`MAX_ROLLING_FRAMES`, 4096 — the pair still
  has to fit the 8192-texel dimension a plain device guarantees).

  Where the state landed follows from that: the **ring is the transform**, an
  `Stft` in rolling form (`capacity` + `head`, columns read through `column`),
  owned by the GPU view that draws it — so the renderer, the frequency ruler and
  the cursor readout still see one type and never learn which form they have.
  `host/waterfall.rs` correspondingly stops owning the retention: it analyzes and
  hands over the columns that landed (`take_pending`), and both fronts push them
  through one shared `frame::roll_into_slot`. A live `retention` change resizes
  the ring around its newest columns and rebuilds the texture once — one full
  upload per change, against one per tick before. Green on 521 tests (the ring's
  wrap, its contiguity in the texture, the stored degenerate case, the resize,
  and the property that a rolling transform *is* the stored one column for
  column), clippy, `check-wasm.sh`, the doc build, and `gui_analyzer` by eye
  including its live halving of the span.

- ✅ **E19 — A heavy view hanging off the window is cut, not squashed**
  *(done 2026-08-10; found by eye the same day, checking the trace-weight fix in
  `gui_multitrack`: pan a spectral clip until its lane pokes past the bottom of
  the window and the picture **flattens into what is left** instead of being
  cropped — the content behaves as if it were sized by the visible area rather
  than by the lane.)*

  The cause is one function. Every heavy view builds its geometry in NDC
  `[-1, 1]` and lets `set_viewport` place it, so the viewport is not a crop but
  a **scale**; and since wgpu refuses a viewport that leaves the attachment,
  `frame::clamp_viewport` shrinks the rectangle against the framebuffer. A view
  overhanging the bottom therefore gets its whole picture mapped into the
  remaining height, and one overhanging the top gets its origin clamped to 0 and
  slides instead of being cut. The scissor beside it (`frame::apply_scissor`) is
  right and its comment already says the viewport "positions but does not cut" —
  the sentence that is missing is that it also *scales*, which is where the bug
  lives. It dates from `277f79f`, the track's first GPU window.

  **Why only a spectral clip shows it today**, which is also why the milestone
  is small: every other clip body is drawn into the shared mesh and cut
  geometrically, and the one exception is a take whose `view` is the
  time-frequency texture — `frame/items.rs` collects it apart precisely because
  it is a texture rather than geometry. The navigable waveform and the `canvas`
  have the same defect and no example currently parks one against a window edge.

  The fix is to stop asking the viewport to do the framing: keep it legal
  (clamped, as now) and hand each pipeline the **visible sub-rectangle** as a
  scale + offset in NDC, one more uniform beside the ones each already carries,
  so the geometry is placed for the full rect and only the visible slice is
  rasterized. It touches the three heavy pipelines — waveform, spectrogram,
  `canvas` — and the `canvas` one is user-authored WGSL, so whatever the uniform
  is called becomes part of that contract and belongs in `docs/gui-protocol.md`.
  Acceptance: a lane dragged past each of the four window edges keeps its
  content at a fixed size and simply loses what is outside, in all three
  pipelines; the same view fully inside the window draws byte-identically to
  what it draws today.

  **What shipped, and the half of the bug the entry did not name.** The framing
  is a `Framing` in `src/view.rs` — a `scale * ndc + offset` a view applies to
  its own geometry — and the viewport keeps taking the visible part of the rect,
  as planned. But `frame::clamp_viewport` had to stop being a *clamp*: clamping
  the origin is what made an overhanging **top** slide, since the rect kept its
  full height and simply moved down into the window. It is the **intersection**
  now, and the framing is built from that pair.

  Where each pipeline takes it differs, and follows where its geometry is built.
  A **waveform** builds its columns on the CPU every frame, so the framing goes
  into the vertices, one per *lane* — each channel is drawn with its own
  viewport, so a stack can have its top lane whole and its bottom one cut. The
  **spectrogram** and the **canvas** draw one full-screen triangle from a
  uniform block, so each grew a `rect: vec4` and one line in its vertex stage;
  `uv` is taken from the *untransformed* corner, so the framing moves the picture
  without moving what each of its pixels samples.

  **The `canvas` contract did not change**, against what the entry anticipated:
  the vertex stage is the host's prelude, not the user's, so `shade(uv, frag)`
  sees exactly what it always saw and `docs/gui-protocol.md` is untouched. The
  added field is host-owned (`_rect`).

  One thing no test could have caught: the spectrogram's uniform was declared
  visible to the fragment stage only, so the first run died creating the
  pipeline. Nothing in the suite builds a GPU device — the example is the only
  thing that runs a shader, which is exactly why it is the manual test surface.
  Green on 526 tests (the identity, both vertical edges, the horizontal one, and
  the viewport's intersection), clippy, `check-wasm.sh`, the doc build, and
  `gui_multitrack` by eye at all four edges.

### What a pass with an eye on the drawing found (E20–E21)

*Added 2026-08-10, from the same session that opened E19.* Closing E17 and the
trace-weight fix meant opening four examples and watching them, and the pass
turned up two more things neither change caused. Both are older than both
changes, both are one rule away from right, and both are the kind of thing no
test was ever going to report — a label that runs out of room, and a gesture
that lands somewhere the reader did not point.

- ✅ **E20 — A ruler strip is as wide as its own labels** *(done 2026-08-10)*: the vertical strip's
  width is a fixed size role — `metrics.rs`'s `ruler_w`, `grid(5.0 *
  advance(caption_scale))`, five caption characters, sized for `-32768`, `20K`
  and `-INF`. But a label's width is a property of the **data**: a value axis
  over small numbers formats `0.0625` and `-0.0625`, six and seven characters,
  and the label is then clamped against the strip's edge and runs out of room.
  (Found on a spectrum's dB axis zoomed in; any auto-fitted `plot` over a small
  range does it too.)

  **Not a bigger constant.** That charges every view in the system the widest
  case a few axes have, and it would be a number picked to survive the examples
  rather than a rule. The rule is that the strip asks for what its own ticks
  need — and the negotiation for that already exists one level up: a navigation
  group's gutter is *the widest any member asks for* (`layout.rs`'s `indents`
  table, `Ctx::indent`), which is exactly this question asked across a group.
  So: the tick formatter reports the widest label it will draw, a view's
  vertical strip asks for that instead of the role, and the group table keeps
  reconciling members as it already does. The role stays as the **floor**, so
  an axis whose labels are narrow looks exactly as it looks today.

  Deliberately **not** in scope: the caption font and its advance. This is about
  how much room the strip asks for, not about the glyphs it asks for it in.
  Acceptance: a `plot` auto-fitted over `[-0.1, 0.1]` and a zoomed dB axis both
  label every tick in full, no clamping; a waveform's `-1.0 … 1.0` axis and a
  spectrogram's hertz axis are pixel-identical to today; two linked views with
  different label widths still share one gutter, the wider one's.

  **What shipped, and the one thing the entry underestimated.** `ruler` gained
  the measure (`ticks_width`, and the two floored forms `value_strip_w` /
  `amp_strip_w`), and it is asked in the two places a vertical strip is
  reserved. In the **local** ones — a `plot`'s, a `meter`'s, a `spectrum`'s —
  the body is right there, so the reservation just had to be reordered: the x
  strip takes height and the y strip takes width, so the *height* is settled
  first, because it is what decides how finely the axis steps and therefore how
  wide its labels are.

  The **group gutter** is where the entry was optimistic. `own_gutter` answers
  from the kind alone, and it has to: that is what lets the layout place a
  lane's clips before a single rectangle exists. But a label's width depends on
  the member's *height*, which is one pass too late — so `layout_on` measures
  the placed members and lays out **again**, and only when the measure asks for
  more than the roles reserved, which an ordinary window never does. The
  circularity is only apparent: the gutter moves a body's x and never its
  height, so the second pass reads a height the first one already settled.

  Two things the measure deliberately does not do. A **hertz** axis stays on the
  role — its labels are short and bounded (`20K`, `1.5k`, `440`) and the
  frequency they run to belongs to the analysis, not to the tree. And a member
  is measured as **one lane**: a stacked view's lanes are shorter and step more
  coarsely, so this asks for at most what a multichannel member needs and never
  for less. Green on 533 tests (the strip measure and its floor, a zoomed
  amplitude gutter, linked members sharing the wider one, the hertz axis
  unchanged, and the local dB and auto-fitted-plot strips each growing by
  exactly what their widest label needs), clippy, `check-wasm.sh` and the doc
  build; `docs/architecture.md` carries the second pass.

- ✅ **E21 — The wheel's fall-through is for pixels with nothing on them** *(done 2026-08-10)*:
  when nothing under the pointer claims the wheel and the window has exactly one
  navigation group, `gestures::wheel` treats those pixels as that axis and zooms
  it. The justification written beside it names *"a gap between lanes, the slack
  under the last one, a container's margin"* — pixels with nothing drawn on
  them, which is right. What the code actually tests is only whether the wheel
  was **claimed**, so it fires over an element that draws its own picture and
  simply has no wheel gesture: in `gui_analyzer`, turning the wheel over the
  goniometer zooms the waterfall underneath it. The reader pointed at one thing
  and another moved.

  The missing rule is the one already written in prose: the fall-through is for
  *empty* pixels. What makes it more than a one-line gate is that **the press
  path shares the mechanism and means it differently** — `pan_sole_axis` is what
  makes Shift+drag pan the axis from anywhere at all, over any element, and that
  is intended and documented. So the milestone has to separate the two: either
  the wheel's fall-through alone requires empty pixels (the press keeps its
  reach), or the distinction becomes a property a widget answers for — an
  element with a picture of its own against a label, a container, a gap — and
  each gesture says which it wants. Prefer the second if it stays small: it is
  the same question the gesture map answers everywhere else.
  Acceptance: the wheel over the goniometer (and over any element that has no
  wheel of its own) leaves the sole axis alone; over a gap, a margin and the
  slack under the last lane it still zooms it; Shift+drag still pans from every
  one of those places, including over an element. (E17 removed one instance of
  this by giving a navigable spectrum a wheel of its own — that is a coincidence
  of that milestone, not a fix of this one.)

  **What shipped**: the second shape, and it did stay small. `WidgetKind`
  answers `is_bare_surface` — a container's own surface, a label, a lane's empty
  space (and the strip that rules it, whose empty pixels *are* the axis), an
  unknown type this host does not paint — and the **wheel** asks it before
  falling through while the **press** does not, which is the whole separation.
  A one-line gate in `gestures::wheel`, a predicate beside the other kind
  queries, and the prose in `docs/gui-protocol.md` now says both halves out
  loud: the wheel needs the pixels to be empty, Shift+drag reaches from
  anywhere over any element. The regression test asserts both from the same
  pixel and fails without the gate.

### The hit-test pass: a control answers on what it draws (E22-E23)

*Added 2026-08-12, from a by-eye pass over the standard controls.* Every
pointer question in the host was answered by containment in the **placement
rectangle**, which is right for what fills its cell and wrong for everything
drawn smaller or rounder than the cell the layout handed it. A knob's dial is a
disc under a label strip and over a read-out; a slider is a thin groove in a
track area several times its thickness; a checkbox is a small box with a word
beside it in however wide a row it landed in. Each of those was acting on
presses that landed on blank space around the drawing, and the air around a
control belongs to the window, not to the control.

- ✅ **E22 — the seam, and the two dials on it** *(done 2026-08-12)*: `Element::hit_area`
  returns the shape the element answers the pointer on (`HitArea::Rect`, the
  default and the whole placement; `Disc`; `Ellipse`), and
  `gestures::element::press` is the **one** place it is applied, adding the
  metrics' `hit_slop` so no leaf decides how much air a small target deserves.
  A point off the shape reads exactly as a `Claim::Decline`, so the press falls
  back to the chain like any other; a leaf that declares nothing keeps the
  rectangle it always had, so nothing else in the catalog moved. The round
  geometry is `graphics::shape` (`in_disc`, `in_ellipse`, `dist2`), **squared
  throughout** — a hit-test compares a distance and never reports one, so the
  square root has no reader. Each shape is read off the same function the
  drawing places it with (`controls::knob_disc`, `controls::slider_groove`,
  `controls::toggle_hit`), so the two cannot disagree. The **overlay** route
  deliberately skips the filter: a modal is offered the press because it is
  outside as often as because it is inside (a click on the window closes a
  menu's list). What landed with it: the break-point grab dropped its `sqrt`,
  and the score's hit index carries a `HitShape` per entry so a **notehead** is
  the oval inside its box instead of the box.
  **Verified by eye** on `gui_panel`: the knob, the slider and the toggle now
  answer only where they are drawn. The toggle took a second pass and the miss
  is worth keeping: the first bound its width and returned the cell's whole
  **height**, and in a `panel` with `layout="row"` that cell is as tall as the
  tallest control in the row — so the air over and under the box still flipped
  it, and a half-bounded shape reads exactly like an unbounded one. A shape is
  two axes; bounding one of them is not a partial fix, it is none.

- ⬜ **E23 — the rest of the chrome on the same rule** *(open)*. Every remaining
  light widget states its drawn shape through `hit_area`, and each one is
  checked by eye on the example that shows it — the mechanism exists, so this is
  a pass over the catalog, not a design. What is in scope: `button`
  and `menu` (their drawn field, not the cell a row stretched), `number` (its
  field), `label` (a mark on somebody else's pixels — likely `Rect` of the text
  run, or nothing at all), the lane header's controls, and the patcher's ports
  and boxes (rectangles today, which is what they are drawn as — to be
  confirmed rather than assumed). Two questions the pass has to answer as it
  goes: whether **hover and the wheel** should read the same shape as the press
  (they still use the placement rectangle, which is why a wheel over a knob's
  corner still reaches the axis behind it), and what a **hugged** container
  should do when its child answers on less than it was given.

## K track — the widget API: a widget becomes an object

*Section opened 2026-08-09 from an audit of the crate read as a general UI library; **rewritten 2026-08-10** after the seam landed, one leaf ported and the next one stopped against the surface. The rewrite is not a change of goal — it is the same goal with the right diagnosis behind it, and the earlier entries `K1a`/`K1b` (added and superseded the same day) are folded into K3–K5 below.*

The track's purpose, in one line: **adding a widget should be writing one file, not editing eight** — and the way to get there is not to shorten the eight, it is to give a widget a self.

### The diagnosis: a widget has no self

The audit's first reading was that the crate is general everywhere around the element and closed in the element itself: no `trait Widget`, so a leaf is an arm in each of eight passes. That was true and it was the shallow half. Porting one leaf showed the deep half, and it is the finding the whole track now rests on.

**A widget's state does not live in the widget.** It cannot: a `WidgetKind` arm is data the host matches on, not an object that owns anything. So every piece of a widget that is not a plain prop has been rehoused somewhere in the host, and each rehousing needed its own routing:

| Where a widget's self ended up | How much | What it is |
|---|---|---|
| `WidgetKind::` matches across the crate | **389 mentions** | the eight passes: build, apply, size, the frame's draw, the query, the hit-test, the press, the tree collectors |
| `gestures::Drag` variants | **25** | the *geometry and snapshot* of a drag in flight — a slider's body, a knob's last y, a note's press-time span — held by the machine because the widget cannot hold it |
| `interact::edit` doors + `interact::read` readers | **27 + 12** | one mutation door and one payload reader per widget, routed by kind from outside |
| `FrameInputs` fields | **5 of 13** | `active_button`, `focused_text`, `menu_popup`, `marquee`, `wiring` — one widget's *interaction state*, fed back into the frame so that widget can draw itself mid-gesture |

Those four rows are one bug with four symptoms. A button is drawn pressed by passing `active_button` down the frame because the button does not know it is held. A slider's groove is remembered in `Drag::Slider` because the slider does not know it is being dragged. A menu's open list is a `FrameInputs` field and a `MenuOpen` in the gesture machine because the menu does not know it is open. Give the widget an object and all of it comes home — which is why this track is a *simplification* with an extension point falling out of it, and not an extension point bolted onto the existing shape.

The published claim "a widget is added by extension, never by protocol change" stays true **of the protocol** and is the opposite of true of the code. That gap is what closes here.

### What the API is: three things in, two things out

The temptation, once a leaf is an object, is one god-context carrying everything the host knows — which is the 389 matches with an indirection added. The boundary is deliberately narrow, and it is narrow because of what the diagnosis says: **most of what looks like "what a widget needs from the host" is the widget's own state coming back to it.** Once that state stays home, what genuinely crosses the boundary is small and it does not grow per widget.

**Three things in.**

1. **The roles** — `Draw`: the one mesh, the theme's colour roles and the placement's size table. Exists today, unchanged. No element names a literal number or an RGBA.
2. **The world** — read-only per-frame facts *no widget owns*: the bus source, the queried node trees, the sample rate, the sample clock, whether a server is attached, the pointer. One `&World`, identical for every element and every frame. This is K3.
3. **The container's coordinate system** — a leaf placed on a lane's time axis needs that lane's window; a leaf in a plane needs the plane's zoom. Already modelled as `interact::Coords` / `TimeAxis` on the hit chain and `Placed::time` on the placement; the trait takes it, it does not invent it.

**Two things out.**

4. **Effects** — `GestureEffect` (emit, redraw, release-pointer): what the element asks the front to do. Exists today.
5. **Declarations** — what the element *is* and what it *needs*: its value, its `/gui_query` fields, the buses it reads, the bulk it wants loaded, the GPU slot it claims. One `Needs` + `info` + `value`. This is what the tree collectors read instead of matching on a kind.

Everything else — the drag's geometry, the caret, the open list, the held key, the selection, the marquee — is **state, and state lives in the element**.

### One trait, not a family of them

`Element` stays a single object-safe trait with defaults on every method but two. Splitting it into `Element` + `Interactive` + `Fed` would force the schema to ask, at every pass, whether this element also implements that one — which `dyn` cannot answer without upcasting gymnastics, and which would put the branching back exactly where the track is removing it. **The smallest element is two methods** (`set`, `draw`); every capability past that is a method you may override, and not overriding it is the same as not having the arm today.

The surface, with the milestone each part lands in:

| The pass a built-in spells as an arm | The element spells as | Lands in |
|---|---|---|
| a `build.rs` arm | a registered `Constructor` keyed by the wire's `type` | ✅ K1 |
| an `apply.rs` arm | `set(&mut self, key, v) -> bool` | ✅ K1 |
| a `size.rs` arm | `natural(&self, m, scale) -> Natural` | ✅ K1 |
| `frame/items.rs` + `frame/draw.rs` | `draw(&self, d, ctx)`, or a declared GPU slot | ✅ K1 / K3 |
| the `is_bare_surface` query | `is_bare_surface(&self)` | ✅ K1 |
| `FrameInputs`' live reads | `ctx.world` — one read-only context | ✅ K3 |
| a `gestures/press.rs` arm + a `Drag` variant + a `drag.rs` arm + a `release` arm + the `interact` sub-hit | `press` / `drag` / `drag_relative` / `release` / `wheel`, with the drag's state **inside the element** | ✅ K4 |
| the `interact::read` payload readers | the gesture's own `Events` (K4); the non-gesture read has no consumer | ✅ K5 |
| the `gestures/keys.rs` arms + `focused_text` | `accepts_focus` / `key` | ✅ K6 |
| the tree collectors (live buses, taps, node groups, GPU slots) | `needs(&self) -> Needs` — the whole declaration | ✅ K3 (bulk with `signal`, ✅ K7) |
| a widget that draws **outside its rect** (a menu's open list) | `overlay_rect` + `overlay(&self, d, ctx)` | ✅ K4 |
| a container recognizing one of its **bodies** by matching the leaf's variant | `body_role(&self) -> Option<BodyRole>`, with the container's axis arriving in `Ctx`/`Input` | ✅ K5 |
| a workspace measuring a child's content by matching its variant | `content_size(&self) -> Option<(f32, f32)>` | ✅ K7 |

### What stays in the enum, and why that is not a compromise

`WidgetKind` keeps the **containers**: `window`, `layout` (with its stack flow), `plane`, `field` (the lane, the ruler strip and the clip). A container is not a drawing, it is a **coordinate system** — the layout pass has to know how it places into it, the hit chain has to know what system it gives its contents, and a gesture's plan (pan / select / locate) is a property of that system rather than of anything inside it. Promising a third-party coordinate system is a different and much larger piece of work, and pretending otherwise would make the trait carry a container's obligations for every leaf that has none.

So the boundary is: **a container arranges and defines a space; an element draws in one.** K8 makes that a test rather than a convention.

### Five constraints on every milestone

Unchanged from the track's opening, and none of them is relitigated per milestone.

- **wasm parity is not negotiable.** `check-wasm.sh` gates each milestone. Anything that cannot compile for `wasm32` goes behind the platform seam's traits or behind a Cargo feature the browser build turns off; a capability that exists natively and not in a page is a **degradation of one build**, never a fork of the core. The core stays single-threaded, so nothing here needs `Send`/`Sync` — which is what makes `Box<dyn Element>` cheap in the first place.
- **The GuiDef → HTML element path keeps working.** `<clausters-bundle>` mounting a GuiDef into a page (`clients/web/src/elements.ts`) is a shipped product surface, not a demo. Every milestone that reshapes the wasm-bindgen surface ships its web half **in the same commit**.
- **The cost rule is unchanged**: one mesh upload and one draw call per window, plus the heavy views' own pipelines. A capability that needs a second pipeline earns it **once per window** and only in the builds that compiled it in.
- **Weight is a feature, not a default.** The reason the GUI is a separate crate is that the GPU/windowing stack should not bloat a headless install; the same argument applies inside the crate, to a font rasterizer and to element families.
- **The packages move together.** A wire change ports `docs/gui-protocol.md`, both builders and the three books in its own commit; a Python signature change re-runs `pyright`; a web change re-runs `./build.sh && ./test.sh`.

### Build order, and the one lesson that reordered it

The original order was "seam, then port the leaves easiest-first, then the capabilities". Porting `label` — the easiest leaf there is — validated nothing, because the easiest leaf exercises the smallest possible surface: it draws from its own props and its press is a click. `meter` stopped on the world seam and `slider` on the drag seam, both immediately.

**So the surface is designed against the hardest leaves and landed against the easiest ones**, and those are two different orders that were previously conflated:

- **Designed against** `slider` (a drag with geometry), `meter` (a live world read), `text` (focus, a caret, a keyboard) and `patch` (a canvas, a multi-selection, two drags and a marquee). If the trait expresses those four it expresses everything; if it expresses `label`, it expresses `label`.
- **Landed against** the catalog in increasing order of what it touches, one leaf per commit, so a mistake in the surface is found on leaf three rather than leaf fifteen.

K3–K6 complete the surface, one seam each, each with the leaf that proves it. K7 is the port, against a finished surface. K8 locks the boundary. K9–K12 are the general capabilities, unchanged in substance from the original track and now cheaper: they land on one file per widget instead of eight passes. K13 and K16 make the whole thing someone else's; K14–K15 are the pass over where the code sits that Part A's port left owed.

One ordering detail worth stating because it was previously a known cost: **the keyboard (K6) comes before the port (K7)**, so `text` — 479 lines of real editing, today a parallel path through `Host::focused_text` — is ported once, into `key`, instead of being ported and then rewritten.

---

### Part A — the widget API

- ✅ **K1 — `trait Element`: the seam and the registry.** `host/widget/element.rs` carries the object-safe trait, `Needs`, `Claim` and a thread-local registry; `WidgetKind::Custom(Box<dyn Element>)` sits *beside* the enum and is wired through build, apply, size, the frame's flat draw, the query, the bus collector and the element step of the press; `clausters_gui::{register, unregister}` is the public door; `host/elements/` is where a ported leaf lives with its tests beside it, and `elements::builtin` is the table the schema consults **before** the registry.

  Three invariants, each now covered by a test. A built-in resolves first, so **a registration can never shadow one** or change what a shipped def means. A miss on both is `Unknown` — laid out, not painted — which is what an unrecognized type has always been, so **an element family compiled out of a build degrades exactly the way a host older than the def does**, with no new failure mode. And a container is not extensible here.

  `label` ported, and porting it corrected two things the paper design had wrong: `draw` takes the placement's `scale` (a widget's own `text_size` is a number no table resolves, so a label in a zoomed plane was measured at one size and drawn at another), and `is_bare_surface` is a trait method (whether the wheel falls through is a real per-element answer, `false` by default so a registered element never swallows the axis behind it by accident). **The port itself moved out to K7**, because the surface has to be finished before sixteen leaves are written against it.

- ✅ **K2 — A Rust API for building a tree.** `clausters_gui::tree` (`node`/`window`/`layout`/`plane`/`field`, chained `id`/`prop`/`child`/`children`) and `Host::define`/`define_with_blobs`, factored out of `on_def` so the wire and Rust share one definition path from the parsed node down: a tree built in Rust is recorded, rendered, bound and persisted by the same steps, and its JSON is *derived* rather than skipped, since that is what persistence, reload and `/gui_query` read. Parity is the suite — four builder-vs-parser document tests and two host-level ones.

  **The builder deliberately does not mirror the widget catalog** — a leaf is `node("knob").prop(…)`. The props already live in three surfaces `docs/gui-props.md` holds together, a fourth would drift, and a registered element's props no catalog in this crate can know; the reasoning is in `docs/decisions.md`.

- ✅ **K3 — The world: what a leaf reads that no widget owns.** `Element::draw` saw the mesh, the theme and the size table, which is everything a `label` needs and not enough for most of the catalog. The frame already assembled the answer — `FrameInputs` — and the milestone was to pass the *right part* of it and no more.

  The split is the whole design, and it came straight from the diagnosis. Of `FrameInputs`' thirteen fields, **five are one widget's own interaction state** (`active_button`, `focused_text`, `menu_popup`, `marquee`, `wiring`) and those are **not** in the world: they go home to the element in K4/K6 and the fields are deleted there. What was left is genuinely nobody's: the bus source, the queried node trees, `server_attached`, the sample rate, the sample clock, the pointer, and the timeline groups' shared window. That is `host/world.rs`'s `World`, read-only and identical for every element — and it carries the two bus-read rules (`control`, `level`) so no reader restates them.

  It reaches an element through a **`Ctx`**, not as a fourth argument, which was the one shape the paper design left open. `Ctx` carries the world *and the placement* (rect, scale, the resolved metrics), so K4's coordinate system lands as a field rather than as a signature change in every element ever written — and a method that gets no `Draw` (the slot's) still has the size table.

  **`Needs` became the whole declaration** a tree collector reads — control buses, the audio buses whose level is read, the audio buses whose samples must be tapped, the server groups whose node tree is drawn, whether the picture follows the clock, and the GPU slot a heavy view claims. Four walks that each matched on a kind now ask every widget the same question. **The bulk resources are the one part deliberately left out**: a bulk *declaration* is only half a seam — the other half is how the loaded data gets back into the element, which is shaped by `WaveformData`/pyramid types only `signal` uses — so it lands with `signal` in K7 rather than being invented against no consumer.

  The declaration also caught a bug the old walks had. An audio-rate `meter` reads a published level, which is neither a streamed bus nor a tap, so the liveness walk did not count it: a window holding only level meters never animated and the column sat still. It declares a level now, and the window follows the declaration.

  And the **GPU slot** got its shape, which settles what would otherwise be an open question about `canvas`. An element that cannot draw into the shared mesh — because it needs a texture, a vertex buffer or a shader of its own — *claims a `SlotKind`* in its `Needs` and is fed by `Element::slot(ctx)` each frame, and the frame maintains it keyed by widget id, exactly as it does for the built-in heavy views. The static claim is what a window allocates from; the per-frame call is where a param resolves from a bus, with the world in hand. **The set of slot kinds is closed and belongs to the frame** (which owns the device, the pipelines and the one-batch rule), so an element chooses among them and cannot invent one: a `canvas` takes the shader slot with its user WGSL — a *parameter* of that slot, not a new pipeline — so `canvas` is an ordinary element, not an exemption. The set ships with that one kind; `signal`'s geometry and texture kinds join it when `signal` ports, for the same reason bulk waits. Widening it is adding a pipeline to the frame, which the cost rule already prices at once per window and only in the builds that compiled it in; that is the same boundary as "a container is not extensible", drawn where the hardware actually is.

  **The three proving leaves ported with the seam**, one commit each after it: `meter` (the smallest thing that reads the world — one bus, and the rate picks the table), `nodes` (a queried document plus the `server_attached` placeholder) and `canvas` (the slot declaration carrying a shader). Each was an arm in build, apply, the frame's collect and the frame's draw, plus — for the first two — an item struct copied out of the tree for a draw that never needed the tree; each is now one file in `host/elements/` with its tests beside it, and `WidgetKind` is three variants shorter. The prop parsers they share with the schema widened to `pub(crate)`, which is what an element outside the `widget` module needs to read the same props the same way.

- ✅ **K4 — The element owns its interaction.** The largest single simplification in the track and the one that makes the API consistent: a widget's own geometry and press-time snapshot were parked in the gesture machine as `Drag` variants only because the widget could not hold them. What the machine keeps is the **sequence** — press → drag → release → wheel, the pointer grab — and the containers' navigation plans, which move a coordinate system rather than a widget. What the element gains is the **meaning**: `press` returns a `Claim` that holds the press, `drag` is handed the cursor and mutates *itself*, `drag_relative` is the same for a grabbed pointer, `release` delivers. Beside the plans sits `Drag::Element`, which carries no geometry at all.

  **The `Claim` says one thing, not two.** The design named two flags — grab the pointer, tick the edge scroll — and only the first landed. The grab is real and is the front's answer rather than the widget's wish (a page has no pointer lock, and the machine routes positions or deltas accordingly, which the element never learns). The edge scroll's only drag today is a `clip`'s, and a clip is a **container** that keeps its own drag; the flag lands with the first *element* that runs off an edge, which is also what will settle how the axis under it is panned. Adding it against no consumer would have been guesswork, the same call `Needs`' bulk half got in K3.

  **What an element reports is `Events`** — a list of messages, not a value — because the case that is not one value is a patcher's release reporting a `"move"` per box. The bound-vs-event decision stays where it was, on one restated rule: a single argument is a value and takes the value road, anything longer is a tagged edit-back payload.

  **An overlay is declared, not flagged.** A `menu`'s open list is the state that is not a value, and it used to exist twice — a `MenuOpen` in the machine so the next press could be tested against the rows, and a `FrameInputs` field so the renderer knew what to paint over the window. `overlay_rect` replaces both: the frame draws `overlay()` last into the overlay mesh, and a press routes to whoever declares one before the tree is consulted. Nothing is kept in step, and a def that replaces the tree takes its open lists with it.

  **The leaves that landed it**, one commit each after the seam: `slider` (absolute — a position in the groove *is* the value, and the groove is snapshotted because it may not move under the drag), `knob` and `number` (incremental — a delta re-anchored every step, so a control pinned at an end has no dead zone; the knob is what proves the grab), `button` and `toggle` (the click, and the held state that used to travel a full circle through the machine and back down through the frame), and `menu` (the overlay). The third family — snapshotted, a press-time origin plus a container's axis and a snap — belongs to the leaves placed on a time axis and lands with them in K7.

  What went with them: six `Drag` variants (`Slider`, `Vertical`, `Button`, twenty-five down to nineteen), `FrameInputs`' `active_button` and `menu_popup`, `Gestures::active_button`/`menu_open`/`close_menu`/`MenuOpen`, the frame's `MenuPopupItem` and its collect arm, `controls::draw`'s `active` parameter, and four `interact` doors that existed only to route a mutation in by kind (`set_fraction`, `fraction_of`, `flip_toggle`, `set_menu_index`) — for an element there is no outside. `WidgetKind::` is mentioned 305 times, from the 389 the track opened at.

  One behaviour is now uniform where it was special: **every taken press is held** until the button comes up, a toggle's included. It changes nothing a user can see and it removes the question of which claims are "clicks", which the machine has no way to answer and no longer has to. The release of an element repaints even when it reports nothing — an element that drew itself held has to be drawn let go.

  **A sub-hit is not on the trait.** The host's hit-test answers *which widget* — rect containment over the placements, generic — and *which part of itself* was hit is the element's own business, inside `press`, where it already is for a caret, a note, a break-point and a patch port. Putting a `Part` on the trait would make the host route by a type it cannot interpret, which is the routing this track is removing.

  A gesture takes an `Input`, not the `Ctx` a draw takes, and the reason is structural rather than stylistic: a gesture **mutates** the host tree while the `World` borrows out of it (the timeline groups, the queried node trees), so nothing can hold both. An element reads the outside when it draws and moves itself when it is dragged. `Input` carries the placement, the modifiers and the window size, and grows the container's coordinate system the same way `Ctx` does.

- ✅ **K5 — The body-role seam, and the `curve` port.** *(Rewritten on landing: the milestone opened as "the element owns its payload" and the payload half turned out to be already done — see the last paragraph.)*

  **The seam.** A `clip` layers its contents — the material, the events over it, the automation over both — and recognized each of them by matching the leaf's *variant*, in seven places: `empty_clip_body`'s candidate array and the `fn(&WidgetKind) -> bool` predicates threaded through it, `ensure_body`'s layering rank, `track::draw_body_widget`, `interact::clip_point_edit`/`clip_point_move`, the clip's hit-test, the frame's body collect and the point drag. A leaf behind the trait has no variant, so a curve could not move until the container had another way to know one of its bodies.

  `BodyRole` is that way, and it is shaped after the GPU slot because the boundary is the same one: **a closed set belonging to the container**, which an element *chooses* among and cannot invent. `Element::body_role` declares it, `WidgetKind::body_role` is the single door (a built-in answers from its variant, an element for itself), and the role's own declaration order **is** the layering order, back to front — so `ensure_body`'s rank stops being a hand-written table. The container keeps the layering, the axis and the edit doors, which are a coordinate system's business.

  It landed as a **method rather than a field of `Needs`**, which is the one place the proposal moved. `Needs` is what an element reads from *outside* itself, gathered by the tree collectors once per collect; a body role is what the element *is* inside a container, asked by that container on a hit and on a `/gui_set`. Same closed-set discipline, different question and a different rhythm — and asking it would have meant allocating four vectors per hit.

  **What the trait gained with it** is the third of the three things in: `Ctx`/`Input` carry the **container's coordinate system** (`TimeSpace` — the visible window and the span), which the design named from the start and nothing had yet filled. That is what makes one element two placements rather than two drawings.

  **The port.** `WidgetKind::Bpf` is gone, and with it a build arm, an apply arm, the frame's `BpfItem` and its collect arm, `interact::bpf_edit`, the whole clip half (`clip_curve`, `clip_point_edit`, `clip_point_move`, `ClipHit`'s `point`/`has_curve`), `read::bpf_event_args` with `emit_points`, three `Drag` variants (`BpfPoint`, `BpfCurve`, `ClipPoint`) and `track`'s four curve twins. `host/bpf.rs` stays the model, and the two display mappings it and `track` had written twice — the widget-local one and the clip-placed one — became one `bpf::Axes`: a field, the window of the domain it spans, the value axis. The element picks the window; everything else is shared.

  **The machine grew the address a body needs.** `Drag::Element` carries an `element::At` — which widget, or which *body of which container*, plus the placement and the axis it was placed on — and a body's address is its container's id plus its role, which is the routing a `/gui_set` of a body prop already took (a body carries no id; the script addresses the clip). The clip offers a press to its bodies, topmost first, before its own drag.

  **One rule the port had to state rather than inherit**, and it is the finding worth carrying to `notes` and the take: **a body claims its own parts and declines everywhere else.** It shares its rectangle with the container, whose own drag is what the rest of that rectangle means — so bending a *segment* stays the standalone view's gesture, or an automation clip stops being movable. Standing alone the whole field is the element's. A gesture test covers both halves, because the port nearly shipped without the second. (The segment drag itself is confirmed working and confirmed **unclear** — see "the editing gestures want affordances" under "Found by use".)

  Two things the shared mapping fixed on the way: a clip's curve now draws the vertical connector at a discontinuity (a hold-shape automation used to render its jumps as one-pixel slants), and a body's points and text follow the placement's own size table. `WidgetKind` is sixteen variants and `WidgetKind::` is mentioned **299** times, from the 305 K4 left and the 389 the track opened at.

  **The payload half was already spent, and that is the honest report.** K4 made a gesture return `Events`, so every reader whose payload the gesture *produces* went home with its leaf; the curve's `"points"` is produced by `press`/`drag`/`release` and needs no trait method. The non-gesture read the milestone was written around had **no consumer** as this landed: `/gui_query` answered from the registry's wire props rather than from the live tree, and the one binding that forwards on a `/gui_set` forwards a scalar. So `event()` was **not** added — it would have been a declaration with nothing calling it, the call `Needs`' bulk half and the `Claim`'s edge-scroll flag already got. `Element::value` stays the scalar door. (`Element::info` was in the same position and predates this — declared in K1, unread — and it is what the `/gui_query` fix recorded under "Found by use" then gave a consumer, which is also the answer to why `event()` is still not needed: a query wants the props' own vocabulary, keyed, not one anonymous payload.)

  Also fixed here, in its own commit because it predates the work: `clients/python/tests/test_gui_props.py` read the host's vocabulary out of the widget schema alone, so every leaf that moved into `host/elements/` had been reading as a "python and web only" divergence since K1 — eleven widgets, which is the shape of a real finding and therefore what made the instrument useless. It reads the element files too now.

- ✅ **K6 — Focus and the keyboard, generically** *(the original K4, moved ahead of the port; landed with the `text` port, which is why K7 no longer lists it)*.

  **The seam.** `Host::focused_text` was focus *of text* — one field, one parallel path, no tab order and no way for a script to ask where the keyboard is. It is now `focused: Option<(def_id, widget_id)>`, one focus per host because there is one keyboard, and the trait carries the two methods that make a leaf a keyboard reader: `accepts_focus(&self)` (whether it is a stop on the ring, and whether a press moves the focus there) and `key(&mut self, &Key, &mut KeyInput)` — `Some` consumes the key and its `Events` are delivered exactly as a drag's are, `None` lets it fall through to the front's own shortcuts, which are addressed to what is under the *cursor* rather than to what holds the focus. `KeyInput` carries the modifiers and the **host-wide clipboard**, which is not the element's: one string serves every field of every window, so a selection cut in one pastes into another.

  **The ring is asked, not remembered.** It is the layout pass' own order over the widgets that accept focus, recomputed per step (`host/gestures/focus.rs`), so there is nothing to keep in step with a freed widget, a redefined tree or a `stack` that turned the page. The wire gained `/gui_set <id> focus 1` and a `"focus"` event on both ends of every move **the user makes** — the widget that lost it as well as the one that gained it, since a script mirroring the focus needs both, while a `/gui_set focus` is not echoed, exactly as no other set is — and it is emitted as a **notification** even from a bound widget: a binding says where a widget's *value* goes, and this is not it. `focus` is deliberately **not a prop**: it is taken out before the document is written, so a `/gui_query` does not report it and a reloaded def does not restore a focus nobody asked for. The ring is drawn by the host, one `focus` colour role and one `focus_ring` metrics role, so being focused looks the same everywhere; what being focused means *inside* an element (a caret, a selection) arrives as `Ctx::focused`.

  **Tab leaves the tree, and that is the whole browser half** — which turned out to be smaller than the entry expected and in one place it did not. winit already gives its canvas a `tabindex` and forwards the keys, so no page-side plumbing was needed; what *was* needed is the exit, because winit also prevents the browser's default on every key it sees, so a wrapping ring would have made a mounted GuiDef an absolute keyboard trap. `GestureEffect::FocusOut` is the report — the browser shell blurs its canvas and the document's tab order carries on; a desktop window has nothing outside to hand the keyboard to, so nothing is focused and the next Tab enters the ring again. **The desktop deliberately does not wrap** — the shell could, and every desktop toolkit does, but the platform that *cannot* wrap sets the rule for both: one behaviour to learn, document and test beats saving a keystroke on one target. The rationale is in `docs/decisions.md`; IME and the system clipboard stay the page's, stated as a limit rather than left to be found.

  **The port.** `WidgetKind::Text` is gone, and with it a build arm, an apply arm, a size arm, two query arms, `controls::draw` entirely (its last two kinds had already moved), `interact::text_edit`, `interact::text_caret_at`, the `Drag::TextSelect` variant and its drag arm, and the 140-line `gestures::keys::text_key`. `host/elements/text.rs` holds the field, its caret and its `key`; `host/textedit.rs` stays the pure model and `controls::field` the drawing, which is the rule every ported leaf follows — **the element owns its state, not its geometry**. The drag's anchor is a field of the struct rather than a variant of the machine's `Drag`, which is what let the variant go. `WidgetKind` is fourteen variants and `WidgetKind::` is mentioned **281** times, from the 299 K5 left and the 389 the track opened at.

  **What the generalization bought immediately**, beyond `text`: a press now moves the focus by *asking the widget* instead of matching on `Text`, so a registered element is a stop on the ring like any other — the seam's suite covers exactly that, a third-party element that reads the keyboard in two methods. And a key an element declines still reaches the view under the cursor, which is what lets a field swallow `q` while a piano-roll behind it keeps quantizing on the same key when nothing is focused.

  - **Out of scope, recorded**: an accessibility tree (AccessKit). It is native-only with no canvas equivalent, and shipping half of it in one of two targets is worse than shipping none. Revisit if the desktop host becomes the product.

- ✅ **K7 — The port: the rest of the leaves onto a finished surface.** One leaf per commit, each with its tests moving beside it into `host/elements/`, each green on its own — `Custom` sits beside the enum, so the host is never half-migrated. The order is increasing surface, so a mistake in the trait is found early and cheap. **Twelve leaves have already landed with the seams that needed them** — `label` (K1), `meter`, `nodes` and `canvas` (K3), `slider`, `knob`, `number`, `button`, `toggle` and `menu` (K4), `curve` (K5), `text` (K6) — which leaves:

  1. ✅ **A payload** — `score`. The page (`host/score/`) was always the model; what the leaf owed the machine was routing, and that is what went: three `interact` doors (`score_select`, `score_drag`, `score_drag_end`), two payload readers, a `nav::score_steps` lookup and the `Drag::ScoreStep` variant, which existed only to carry the press-time origin the machine had nowhere else to put. It is a field of the element now. Two things landed with it. **`Needs` grew `clock`** — whether an element reads the engine sample clock — because a score's sweeping cursor is the one playhead that is the *widget's* rather than a navigation group's, and `live.rs` was asking that by matching on the variant; a front with the shared segment mapped reads the clock for free while a page has to poll for it, which is why it is a need of its own and not a flavour of `animated`. And **`Ctx` grew `clip`**, the placement's clip rectangle, for the element that narrows the clip itself: the frame holds that datum, so an element must not have to ask the mesh what state it was left in. A score also answers `/gui_query` with its `selected` now, which the built-in arm never did.
  2. **A GPU slot** — `signal` (one arm already, and it ports as one), which is also where `Needs`' bulk half and the geometry/texture slot kinds land — the two pieces K3 deliberately left for the consumer that shapes them. It is **four seams and then the port**, because a signal element is the only leaf that is fed from outside on that many different rhythms, and each of them was a walk of the tree matching on the kind:

     - ✅ **The tick** (`Element::tick`, a `Live` context). What a live view accumulates from a forward-only source — a rolling history, a triggered window, a per-channel analysis, a waterfall's transform — lived in **four front-side maps keyed by widget id**, filled by four collectors and read by a frame that looked each one up again. It is one `LiveState` on the element and one walk now, and the frame's live items borrow it (which is why the `id` came off all four: it existed only to find the data). The context is separate from `Ctx` on purpose — a tick mutates and runs at the front's steady rate, while a draw borrows the world out of the same tree — and the retained history of a **bus** stays outside it, since one history serves every view watching that bus. The browser gained `StreamedSource`, one `BusSource` over its two subscriptions, so both platforms now read through one door.
     - ✅ **The bulk half of `Needs`, and where a load lands.** An element says which resource it wants and in which form (`Bulk::{PeakCache, Peaks, Samples, StftCache, Stft, Buffer}`) and takes the answer home (`Element::bulk`). Four walks were deriving that from outside — `collect_slots` and `load_element_bulk` natively, `collect_bulk` in the page, and a third fork in each `serverleg`'s buffer reply — and the browser's `BulkRequest` was already exactly this taxonomy, so it is gone. **Where the answer goes is a declaration too**, which is what makes the fork disappear: a claimed slot is fed through the slot, everything else takes the data home. `Loaded::Raw` is the form that needed no fork at all — a server buffer's samples go home as they are and the element makes of them what it draws from, at its own bucket.
     - ✅ **The geometry and texture slot kinds** — `SlotKind::{Geometry { base_bucket }, Texture { window_size, hop, sample_rate }}`, their parameters riding with them because whoever fills a slot has to make the data fit it and only the element knows how. Both loaders route on them now, and the filling is the bullet below.
     - ✅ **The filling** (`Element::fill`, a `SlotFill`). A slot had two rhythms and only one door: `Element::slot` said what a claimed slot *draws* this frame, and what it is *fed* was still derived from outside, twice per front. The inline-data path matched on the presentation to decide whether an element's own samples became a pyramid or a set of analyses (`collect_timelines` natively, `build_inline_timelines` in the page), and the waterfall pass reached into `el.live.roll` for the columns of the tick. Both are one walk now — `frame::fill_slots`, which asks every widget the same question and uploads what comes back, keyed by the addressing rule (a clip's body is its container's id) it also states. `SlotFill` is the three shapes an element can hand over: `Geometry` (a pyramid), `Texture` (the analyses) and `Columns`, the rolling case, whose upload is what landed since the last fill rather than the whole span. **A fill is a taking**: the element marks itself clean because the frame kept what it gave, which is what keeps a still window at zero uploads — and the one thing it cannot know for itself, that a window's device was rebuilt and took the contents with it, arrives as `Element::slot_dropped` (a fresh native window, a re-attached page canvas). A **load** stays the loader's: what a mapped file, a fetch or a server buffer resolved is routed into the slot on the declared kind, which is why an element whose data is still out there hands back nothing rather than an empty picture that would be replaced the moment it lands.
     - ✅ **The tail the port had to answer.** With the four seams above, what was left was not data at all — each is a small door on the trait or on `Needs`, and none of them is a design question the way the four above were:
       - ✅ **The tap subscription** (`Element::tap_frames`, `Needs::retention`). Three collectors walked the tree building a per-kind read spec — a scope's window and trigger, a goniometer's pair, a spectrum's FFT size — only for `tap_stream_frames` to take the largest of the three and throw the specs away; they are gone, and each widget answers how wide a read of its taps has to be. The span is the other half and is data, so it is a `Needs` field: the history is the **bus's**, and a view now retains exactly the buses it declared it taps (which is also a small fix — a goniometer's span retains the pair it reads, where the walk had gone by `channels`).
       - ✅ **The lane count and the vertical anchor** (`Element::lanes`, `Element::centres_y_zoom`). Two halves of one answer met in the gesture machine: the front knows how many channels reached the card (only its GPU slots do), the widget knows what it *does* with them — so `GestureCtx` carries one `slot_channels` map where it carried a `wave_lanes` and a `spect_lanes` (the two existed only to pick which one to match against), and an overlaid trace answers one lane out of four channels. The anchor is the same shape: whether a y zoom holds the centre or the value under the pointer is a property of what the axis *measures* (amplitude is centred on zero in every lane; frequency and pitch say the same thing in each), and the widget states it instead of the wheel matching on a presentation.
       - ✅ **The axis chrome, the body draw and the default drag** (`Element::{gutter, measured_gutter, draw_body, texture_body, gesture_map}`). What a leaf placed on *somebody else's* axis owes it, in one pass: the band it reserves left of the shared body — asked twice, from the props before anything is placed (the layout needs it to place a lane's clips) and from the placement after, because an amplitude ruler's width is a property of the data and not of the kind; what it draws as a **clip's body**, chromeless and against the clip's own span, with `texture_body` for the one picture the frame has to route itself (a time-frequency take samples an uploaded texture, keyed by the clip's id since a body carries none); and the drag table a navigable view wants when the wire declares none, which is the axis' own selection and pan rather than the picture's.
       - ✅ **The frequency-axis reads** (`Element::{navigates_freq, freq_axis, freq_window_of, freq_min_span}`, and `FreqAxis` moved to the seam). The one axis in the host that is neither the window's shared time nor a container's coordinate system, and the machine held all of it: where the picture lies inside the rectangle (the element's own region split — it must be the *same* one the renderer drew through, or a zoom anchors at a hertz the reader is not pointing at), the window it shows, what it would show if asked for another, and the floor a zoom may not pass. All four are facts about the **analysis** — its resolution, its scale, its rate — and the machine only ever wanted the numbers.
       - ✅ **The frame's item fan-out**, as far as it goes without the port: the clip-body branch asks `texture_body` instead of matching the arm and reading `is_texture_view`. The rest of `frame::items::signal_item` — six item lists, one per renderer a presentation picks — is not a door and does not become one: those *are* the built-in's draw, and they go when the arm does.
     - ✅ **The arm itself.** `WidgetKind::Signal` is gone: every view of a signal is a registered element, built by a constructor the schema looks up and drawn through the trait. The draw split along the line that was already there — the four mesh presentations and the stored plot draw in `Element::draw` (the plot's hover readout in `Element::overlay`), and the two heavy ones describe their frame in `Element::slot`, which is what `SlotFrame::{Waveform, Spectrogram}` are: the body, the vertical window and the look, executed by the frame on the pipelines it owns. The horizontal window is deliberately *not* in them (it is the navigation group's, addressed by an id the element does not carry) and neither is the lane count (it is the slot's, being what reached the card). Three more doors fell out of it and are the ones a **later** leaf on a shared axis will want too: `Ctx::indent` (where the group's axis begins, the layout's answer and not a measurement), `editor`/`editor_mut` (what a group member is made of, read *and written* from outside), and `as_any`, defaulting to `None` — not for the passes, each of which has a door, but for an element's own tests. `frame::items::signal_item` and its five item lists are gone with it; the chrome around a heavy view stays the frame's, because rulers, playhead, selection and readout belong to the **axis** and a lane, a roll and a free-standing ruler draw the same ones.
     With that, `WidgetKind` is the containers plus `Custom` and `Unknown` — and the three leaves of item 3.
  3. ⬜ **Group-aware** — `keys`, `notes`, `patch`: the three that read a container's axis or hold a selection, last because they exercise every seam at once. **What the port needs, surveyed once item 2 was done** (56 match sites over 19 files; the order below is by how much of each is already a door):
     - ✅ **`keys`** (`host/elements/keys.rs`). Both drags moved into the element as the **snapshotted** family K4 named and left for the leaves: the press records what it needs (the strip and the range, or the key layout and the pitch under the cursor) and every motion is measured against that, so a drag pinned at an end never drifts. `Drag::PianoKey` and `Drag::PianoView` are gone from the machine, and with them the four `interact` doors that existed only to reach into the variant (`piano_press_key`, `piano_release_key`, `piano_set_range`, `piano_key_active`) and the two effects that wrapped them.
       **Voice mode became the seam it always was**: an element cannot reach the audio server, so it *asks* — a note carries a `Voice` beside what it reports and the host performs it, reading the def from `Element::voice`. That splits the two questions the variant had conflated: *what to play* is a prop a script sets (and can change mid-hold), *play it now* is a gesture's. `Host::piano_voice_on/off` are `voice_on/off` now and know nothing about keyboards.
       One rule the port had to state rather than inherit: **an element's wheel wins over the container it sits in**. The old `Piano` arm ran before the timeline and plane checks, and the generic element arm ran after them — so a keyboard inside a `plane` would have started scrolling the plane. The element is asked first now and `None` back hands the wheel on, which is what the fall-through already meant.
     - ✅ **`notes`** (`host/elements/notes.rs`). The two asks landed as named, and each of them turned out to be the *same* answer in a different direction. **The axis** is not snapshotted but **resolved per call** (`gestures::element::time_of`): a widget addressed directly reads its navigation group's window now, a body still takes the container's, and both arrive as `Ctx::time`/`Input::time` — which is what makes a drag survive an axis moving under it, and therefore what makes the edge-scroll flag work at all rather than freeze the note where the axis used to be. `TimeSpace` grew the other two facts of that same axis, its **shared selection** and where its **playhead** stands, because the roll draws them: its ruler sits under a grid with two strips between them, its selection band covers the grid alone and its readout names a pitch, so its chrome was never the heavy views' and stays with the element rather than going to the frame. **The request** is `Events::and_select`, beside `Voice`. And `Take::edge_scroll` landed on the same tick the clip's edge scroll already ran on, which is one arm rather than a second mechanism.
       Three more doors fell out, each replacing a match the machine was doing on its behalf: `Element::axis_body` (where the shared axis lies inside the rect — a roll's is its **grid**, not the rect minus its chrome, since the velocity and event strips are stacked under it and read the same time), `Element::content_span` (how far an **authored** surface reaches, which is what lets a roll being written into lengthen the group's timeline — it replaced a match in `sync_track_totals`, one in the ungrouped-view fallback, one in the roll-grid lookup and the `"notes"`/`"osc"` key list in `on_set`), and `Needs::midi` + `Element::midi` with the platform-neutral `MidiNote` (the `Key` posture: the front owns the device and the translation, the element owns what a note *does*) — which brought the held keys and the step cursor home from two maps on the native front.
       **The keyboard's second addressee is now a door rather than four methods.** `q`, Delete and Ctrl+C/X/V were front shortcuts matching on the kind; they are arms of `Notes::key`, reached by `Gestures::key_at_cursor` — the same call the focus makes, at a different address, which is exactly what K6 said out loud ("a key an element declines still reaches the view under the cursor"). The note clipboard went with them: there is **one** host-wide clipboard now, and a block of notes rides it as the JSON a `/gui_set notes` already accepts, which is the carrier every non-scalar uses.
       What went: `WidgetKind::PianoRoll`, six `Drag` variants (`Note`, `NoteBlock`, `Velocity`, `VelocityBlock`, `OscMark`, and `Select`'s whole `value` half), five `interact` doors (`pianoroll_notes_edit`, `pianoroll_state_edit`, `pianoroll_osc_edit`, `clear_element_selection`, `select_elements_in_rect`), the `pianoroll_hit`/`PianoRollHit`/`PrRegion` block with its two pickers, two payload readers, `YAxis::window` and `interact::value_at`, `frame`'s `PianoRollItem` + `draw_pianoroll_item` + `pitch_window`, `track::draw_piano_roll`, `nav::note_at`, four `gestures::keys` methods and their four front dispatches on both fronts. `WidgetKind::` is mentioned **198** times, from the 281 K6 left and the 389 the track opened at.
     - ✅ **`patch`** (`host/elements/patch.rs`). It needed nothing of its own, exactly as the survey said, and that is the finding: the last and least control-shaped leaf in the catalog — a cord that means something only where it is *let go*, a whole selection moved by a delta, a marquee that selects the element's own boxes — is three ordinary `press`/`drag`/`release` arms over three fields of one struct. Nothing was added to the trait for the drags, which is what "the drag shape is general" turns out to mean.
       **The marquee came home, and took a coordinate system with it.** `Coords::Canvas` existed for one reason: the machine had to know it was over a canvas so a plain drag could run the `Select` plan and sweep a rectangle nothing outside the widget could interpret. So it is gone, with `CanvasAt` and `Frame::scale` beside it — a patcher is a *leaf*, and the plane it is seen through is the workspace's, which the chain already carried. What the container keeps is what a container is for: Shift on the bare canvas is declined by the element and pans the workspace around it, which is the same rule `notes` stated for its own strips.
       **One door, and it is the opposite of an existing one.** `Element::content_size` is what a workspace sizes its scrollable content to, and it is the deliberate inverse of `natural`: a natural size is pure over the metrics and must **never** follow the data (it resolves in the layout's main axis, so data-driven would make a `/gui_set` relayout a window), while this *is* the data — the graph the host laid out — and it sizes a plane that scrolls rather than a slot that divides. Same shape as `content_span`, one axis more.
       One consequence stated rather than found: a `gestures` prop on a patcher node is now **inert**, because a table belongs to a container and a patcher is a leaf. That is what `docs/gui-protocol.md` already says ("any *container* may carry a `gestures` prop"), and the leaf's own answer — `Element::gesture_map` — is `None`, so it takes the default every element takes.
       **And the instrument came back green**, which is the **third** time this reader has gone blind on a restructuring (E6, K1, here) and therefore the last time it is left to infer where a leaf lives: `clients/python/tests/test_gui_props.py` read `elements::builtin` for a module *file*, so the two leaves that are not one — `signal`, written across `host/signal/`, and the patcher, built by the **schema** because its wire name `plane` means two constructions — had silently dropped out, reporting eleven props as a "python and web only" divergence. Both resolutions are now named in the reader rather than inferred, and two blind spots in its Rust scanner went with them (a helper reached by its module path, `widget::signal_element(props, blobs)`; a key passed after the thing it is read over, `spectral_props(props, el.spectral, "window_size")`). The failure predates this commit — it landed with item 2 — so it ships as its own.
       What went: `WidgetKind::Patch`, three `Drag` variants (`Wire`, `Box`, `Marquee`), `Gestures::marquee`/`wiring` and the four `interact` doors behind them (`graph_cord`, `graph_select`, `graph_move`, `graph_marquee`), `Coords::Canvas` with the `Select`-on-canvas press arm, `CanvasAt`, `Frame::scale`, the frame's patch draw arm and — the K3 diagnosis closed — the **last two `FrameInputs` fields that were one widget's interaction state**, `marquee` and `wiring`, with the cursor plumbing each front fed them. `FrameInputs` is three fields (metrics, world, focused), and `Gestures::release` is one arm. `WidgetKind::` is mentioned **187** times, from the 198 `notes` left and the 389 the track opened at.
     What item 2 already bought them: `Ctx::indent`, `editor`/`editor_mut`, `gutter`/`measured_gutter`, `body_role`, `draw_body`, `gesture_map`, `lanes` and `needs` are doors, so none of the three has to invent one — what is left is each leaf's **own interaction state**, which is where the remaining match sites are. `notes` and the take are the other two **body roles**, so they land on K5's seam — declaring a role and taking the container's axis, and following its rule that a body claims its own parts and declines everywhere else. `notes` is where the `Claim`'s edge-scroll flag lands, being the first element that runs off a lane's edge; `patch` is the one that proves the drag shape is general rather than control-shaped, with its two drags, its marquee and its multi-selection.

  What each commit deletes is the measure of the milestone: an arm from each of the eight passes, a `Drag` variant or three, an `interact` door and a `read` reader. The prize is not the eight edits — it is that **arm 26 can no longer quietly duplicate arm 12**, which is the pressure the `signal` collapse had to apply after the fact and which nothing structural prevented from recurring.

- ✅ **K8 — The enum is the containers, and a test says so.** `WidgetKind` holds `Window`, `Panel`, `Stack`, `Scroll`, `Track`, `TimeRuler`, `Clip`, `Custom` and `Unknown` — nine, from twenty-five.

  **The guard is two tests, because the rule has two halves.** One reads the enum out of its own source and pins the nine names, so adding a variant fails until whoever added it has read *why* — the failure message points at the test's own documentation, which states the boundary (a container arranges and defines a coordinate system, which is why they are a closed set; an element draws in one, which nothing outside it has to know) and where a leaf actually goes. The other asks the same rule of the widgets rather than of the source: a document builds one of each of the seven containers and every one of them answers the neutral value through every door — no element, no `Needs`, no focus, no body role, no overlay, no value, no axis of its own, no taps, no authored span. **A convention nobody checks is how the eight passes grew in the first place**, and a name list alone would pass while a container quietly grew an answer.

  **`query.rs` collapsed to one match.** The milestone expected "leftover per-kind helpers to fold into trait methods", and what the port had actually left was the opposite shape: twenty-odd methods each spelling `match self { Custom(el) => el.x(), _ => default }`, which is a pass over the enum written out to ask a question the enum does not answer. They are one line each now, through `as_element`/`as_element_mut` — so the *only* match left in a leaf's question is that one door, and the method beside it carries nothing but the **container's** answer. The five that stayed a match are the five where a container genuinely answers (`is_bare_surface`, `info`, `gutter`, `editor`, `editor_mut`). `WidgetKind::` is mentioned **165** times, from the 187 the port left and the 389 the track opened at.

  One fork closed on the way, found by the fold: `Widget::take_bulk` promised in its documentation to route a load to a clip's body and did not — **both fronts were walking that level themselves**, the same walk written twice. The routing is the door's now (`serverleg` calls it once on each side), and it takes a *maker* rather than a `Loaded`, since the payload is built only for the widget that takes it and never copied past one that declined.

  `interact` needed nothing: what is left there is the hit chain, the coordinate arithmetic and the **containers'** own doors — a scroll's view, a lane's header and thickness, a clip's placement, their three payloads — which is exactly the line the milestone drew. The per-widget doors left with the leaves that owned them, one milestone at a time.

  What is still lore rather than API is the widget author's checklist, which K16b documents: one file in `host/elements/`, one row in `elements::builtin`, and — unchanged, cross-package, and by design — a builder in Python, a builder in TypeScript and a row in `docs/gui-props.md`, which `clients/python/tests/test_gui_props.py` holds together.

### Part B — the general capabilities

*Unchanged in substance from the original track; each is cheaper after Part A, landing on one file per widget instead of on eight passes.*

- ✅ **K9 — The embedded font grows lowercase, accents and a real advance.** The 5×7 cell rendered lowercase as uppercase and had no accents, so `text` — an editable field of real editing — could not write "canción", and every user-visible string (a track name, a file path, a label a script wrote in Spanish) goes through it. The one limit no user of the library could work around, and fixable without a dependency.

  **The entry's own plan was the thing that had to move**, and the reason is worth keeping. It said the glyph box grows from 7 rows to 9 and `CELL` is pinned at 14 so the layout does not follow it. Pinning `CELL` is right and landed — but it only saves the *table*: with a 9-row box and the default scale, text would draw 18 px tall inside controls sized for 14 and clip. Growing the box moves the ink whatever the table says.

  So the split is one row lower down: **`GLYPH_H` is the body box** — the seven rows a capital fills, what `font::height` returns and every layout reserves, unchanged — and a glyph may put ink **outside** it, `ASCENT` rows above for a diacritic and `DESCENT` below for a tail, the way a real typeface overshoots its cap height. The drawing origin is the body box, so every capital lands on the exact pixel it landed on before (a test asserts it), and **nothing that was on screen moved**. What did change is `line_advance`, now the whole glyph rather than the box plus a row: two lines of wrapped text have to clear each other's tails and marks, and that is the one deliberate visual change — in the direction the milestone exists for.

  **Latin-1 is composed, not enumerated.** A mark is placed two rows above the base's *own* topmost ink, which resolves the space problem by itself: a lowercase letter's x-height leaves the box's top two rows free, so `á`/`ñ`/`ü` sit **inside** the body and cost no room at all, while only a capital's mark lifts into the ascent. `í` decomposes to the dotless base so the accent replaces the dot rather than stacking on it, the cedilla hangs in the descent row and the slash crosses the body. Ninety-six bitmaps became one table of fifty-eight rows plus seven marks, and a test walks every alphabetic Latin-1 code point asserting none of them falls back to the box.

  **`CELL` is declared, and that is the whole of the dependency direction.** It was `font::GLYPH_H * font::DEFAULT_SIZE`, which made the layout of every window a consequence of how the face was drawn; it is `14.0` with a test fixing it, and the cell is what the face is drawn to fit. `font::advance(scale)` is the face's own answer for the roles sized to hold text (`ruler_w`, `header_w`) and for the field's caret column, replacing both the metrics module's private restatement of the cell and the `font::width(" ", size)` the text field measured a column with. That is the seam K10 needs: a proportional face is asked for a string's width at the mutation point where the string changes, never in a layout pass.

  By eye through `gui_text.py`, which grew the panel the milestone is about: the alphabet in both cases, an accented line, and the same text in a field you can type into.

  **The direction of the dependency is the decision, and it is inverted here**: the cell is declared and the *font is rasterized to fit it*, never the other way round. That is what K10 then builds on — a `FontSource` is asked for a face at the pixel size whose line height lands in the declared cell — and it is what keeps the property the sizing model paid for: the metrics table is constant data resolved once per scale change, so **changing the typeface must never relayout a window**. What legitimately follows the font is the measurement of a *string* (`advance_of`), which happens at a mutation point, not in a layout pass: the cell says how tall a line is, the font says how wide this text is. It follows, and is recorded so it is not rediscovered while implementing: a user who wants larger type asks for it where density already lives — the `scale` multiplier of `[gui.metrics]`, resolved once — and never by choosing a different face.

- ✅ **K10 — A real font behind `font-atlas`.** A rasterizer (fontdue — pure Rust, wasm-safe, no system dependency), one atlas texture per window and a second, textured pipeline that exists only when the feature is compiled in. `text_size` is continuous with the feature on and quantized without it, which is the one place two builds of this host legitimately differ.

  **The entry planned an embedded face, and that is what moved.** A face is hundreds of kilobytes with a license of its own, in a repository that carries no other binary asset — and the platform has already answered the question: a desktop has faces installed, a page can fetch one. So what the feature compiles in is the **rasterizer**, and the face arrives through the seam the entry already named: `FontSource`, the fifth platform trait, filled by `fontfile::FontFile` natively (`--font <path>`, `[gui] font`, or one of the system's own when neither names one) and by `web::FetchedFace` in the page (`GuiBridge::font(bytes)`, what a `fetch` came back with). `docs/decisions.md` records the three costs.

  **Which makes the feature safe twice over**, and that is the shape worth keeping: the bitmap face is the **floor**, not a fallback to reason about. Every entry point in `host::font` asks `atlas::has_face()` first, so a host built with the feature and pointed at no face draws exactly what a host built without it draws — same measurements, same quads, same tests. There is no boot to block on a font and no error path to design; a machine with no fonts still draws every label.

  **The cell still declares and the face is still fitted to it.** A scale rasterizes at the pixel size whose *cap height* is the body box, so a capital through the atlas is exactly as tall as the one the bitmap drew and **no layout follows the typeface** — loading a face is a redraw, never a re-measure. What legitimately follows it is the width of a **string**, and the port was making that measurement real everywhere it had been a count of cells: the ellipsis cut, the word wrap (`wrap` takes a width now, not a column count), the field's caret, its selection band and its click-to-column hit-test. Each of them measures the glyphs it is actually drawing, which is the same arithmetic under a fixed-pitch face and correct under either. The field keeps its scroll window in *characters* — a caret moves by characters — and binds its visible run by width, since a field's text is clipped by nothing.

  **One batch survived, and one rule had to be stated to keep it.** The glyphs are a second vertex list *inside* the same `Mesh`, sharing its clip rectangle, uploaded with it and drawn immediately after it — so the atlas is one texture and one bind group per window, and text draws over the flat geometry of **its own** batch, which is where it always sat (the overlay batch still paints over both). A clipped glyph cuts its texture window with its quad, so half a letter at a `scroll`'s edge is half a letter rather than a squeezed whole one. The sheet is 1 MiB of coverage shelf-packed by `(character, pixel size)`; a document that overruns it repacks, which costs one frame of stale texels and no growing texture on every window.

  **What is on trust, and it is the tests' own limit**: the crate ships no face, so the atlas tests read one from the system and *skip* where there is none. They state that rather than hide it — the alternative was committing a megabyte of test data to assert what a machine with fonts already proves.

  By eye through `gui_text.py` against a host built with the feature (`cargo build --release --features font-atlas --bin clausters-gui`, then `CLAUSTERS_GUI_BIN=…`): the same window, the same layout, in a real typeface.

- ✅ **K11 — A container may hug its content.** The layout stays one pass, no measurement, no constraint solver — this widens what a natural size *is* without crossing that boundary. `hug` on a `window`, a `panel` or a `stack` makes its natural size the composition of its children's (`Widget::hug_size`), which is one bottom-up walk over functions that were already pure and already computed. Both workarounds are retired: `gui_rulers.py`'s row of menus is `hug=True` instead of the `h=60.0` a comment already called compact, and the single control alone in a window (`gui_bind.py`, `gui_standalone.py`) drops its `weight=1.0` because the **window** hugs — which is what that entry turned out to mean.

  **The rule the milestone was written around moved from "never the data" to "where the value is resolved"**, and that is the whole of its design. A prop settles at a *mutation point* — a `/gui_def`, a `/gui_set` — the same place the theme overlay already resolves; a **value** streams, one per pointer move, one per frame, one per keystroke. The failure L7 avoided is entirely on the value side (a window jumping while a control is turned, a relayout per message), so a menu's `options` may size it and the option it is *on* may not, a label's `text` may and a `number`'s reading may not.

  **Two mechanisms keep that structural rather than conventional**, and they are what let this land with no one-time break at all: the question is a **different method** (`Element::hug`, beside `natural`, defaulting to it), so the ordinary pass cannot read content by accident and every element that says nothing keeps its data-free answer; and it is only ever called **under a container carrying `hug`**. Four leaves override it — `label` (its line), `button` and `toggle` (their captions), `menu` (its widest option, never the one it is on) — and each measures what its own drawing lays out, so a hugged control never draws itself into an ellipsis. Every other element, every value and every signal answers the natural size unchanged, and the existing suite passed untouched.

  **What composes, and what propagates.** A `row` sums along its axis and takes the largest across it, a `col` the other way round, a `grid` counts its cells, a `free` container reaches its children's placements, and a `stack` takes the largest of **every** page rather than the shown one — a pager that resized when it flipped would be a worse answer than no hug. It is asked of the whole **subtree** (a plain panel nested in a hugging one is measured too, so a toolbar of grouped buttons needs the prop once), and `None` propagates: a container holding a plane, a lane or a heavy view hands that axis back to the layout, because a hug is not a promise that everything under it can be measured. `scroll` is deliberately not in the set — a workspace is a viewport onto a content area it already names with `content_w`/`content_h`.

  **The window half is the one asymmetry, and it is a platform truth rather than a fork**: `hug` on a root sizes the OS window to its content on the axes that settle, keeping the declared `w`/`h` on the others (`Host::window_size`, the one door both the shell and its tests read). A page has no window to size — the element owns its box and reports its pixels, which is what keeps the host from ever reading the DOM — so a mounted GuiDef lays out in the box it is given and only the containers inside it hug. The composition is identical in both builds.

  **What the eye pass added, and it is the milestone's own lesson.** Fitting `gui_bind`/`gui_standalone` to one knob exposed the two things a paper design does not: a **composite** control has to be fitted to *all* of itself, and a size measured in one table cannot be drawn in another. The knob is a label strip over a disc over a value read-out — one element, three drawings, one cell — so a hug that answered the disc's diameter ellipsized the name to `fr…` and pushed the number against the edge, which is parts of a widget being cut to fit the widget. Its `hug` is the widest of the three now, and the read-out's term is measured over `min`/`max` (`controls::readout_w`), never over the value, so the width is stable while the knob is being turned — the mutation-point rule again, on a control rather than on a menu. The knob's *natural* size stays elastic in width, which is not a contradiction and is the clearest statement of why the two functions are separate: elastic is the right answer to "how much of the row do you want" and the wrong one to "how big are you".

  The second was a **units** bug the rule this crate already carries would have predicted: a window is created before anything knows what display it landed on, so its size is measured in the logical table, while the resolved table snaps every role to a whole pixel — at 1.25 the two disagree by a pixel or two, which is exactly enough for a label measured one way and drawn the other to ellipsize inside the box that was supposed to fit it. So a hugging window is asked **twice**, the same shape the shared gutter already had: `Host::window_size` is the logical estimate the window is created with, and `Host::window_size_px` is the exact answer in physical pixels, asked by the shell as soon as it has written the scale and measured with the very table the layout will use. A window that declares its size answers `None` there, so nothing that did not ask to be fitted is ever resized under it.

  Ported in the same commit: `docs/gui-protocol.md` (the prop, and the three widget tables), both builders (`hug=` in Python, `hug?` in TypeScript — on `window`/`layout`/`panel`/`stack` and deliberately *not* on the `ContainerOptions` every plane shares), the Python book's layout section, `docs/architecture.md`, and a `docs/decisions.md` entry for the mutation-point rule. `clients/python/tests/test_gui_props.py` reads the prop on all three surfaces and stays at zero divergences.

- ✅ **K12 — Paint: the chrome capabilities, bounded.** Flat triangles with no antialiasing is the right decision for an oscilloscope and an absolute ceiling for application chrome, so this milestone takes the three that do not cost a batch — rounded corners (tessellated into the same mesh, the way the score's outlines already are), per-widget opacity (the mesh is already per-vertex RGBA; it needs a prop and a resolution rule, not a pipeline), and antialiasing as **MSAA on the window's pass**, one flag with a per-window cost and nothing per widget. Deliberately still out, and recorded so it is not re-argued: textures other than K10's atlas, gradients, drop shadows and per-layer compositing — each splits the batch, and the batch is the crate's whole performance argument.

  **The two props ride the mesh, which is what kept them off every draw function.** They apply to a *run* of triangles — everything one widget contributes — exactly as the clip rectangle already does, so `Mesh` carries an `Ink` (the resolved opacity and the corner radius), the frame sets one per placement beside the clip, and **no draw function grew a parameter and no element learned it is being faded**: every element written before this milestone honours both without knowing they exist. The radius reaches only the **box** primitive (`rect`, and the border built from it), never a line, a disc or a glyph, and each box clamps it to half its shorter side — which is what lets one number ride a whole widget, since a divider, a tick and a track edge have no room for an arc and come out byte-for-byte unchanged while the widget's own frame and its focus ring round.

  **The resolution rule the entry asked for turned out to be two, because the props inherit differently**, and that is the design rather than an inconsistency. `opacity` composes down the subtree like a theme group (a control at 0.5 inside a panel at 0.5 draws at 0.25) — a fade is a statement about a *group* of widgets — and it resolves at the same mutation point, in the same walk, which is why `resolve_themes` is `resolve_style` now. `radius` does not inherit at all: rounding is a statement about one box's shape, and a rounded panel says nothing about the controls in it. Both are style props on the K11 door, both are live, and a **negative** number clears either — the escape an empty `color` already is, since no number in either range means "say nothing".

  **What the fade does not do is stated rather than left to be discovered**: it is per-primitive alpha, not layer compositing, so two overlapping shapes inside a faded widget show through each other — compositing a subtree means a second target, which is the batch we refused to split. And it fades what the mesh carries; a heavy view's picture keeps its own opacity, because reaching it would be a uniform on the waveform, the spectrogram and every user `canvas` shader, three pipelines paying for one prop.

  **MSAA landed as one fact about the pass, and made the pass's contract explicit.** `crate::view::Target` (the format *and* the sample count) replaced the bare format every pipeline constructor took — a pipeline that disagrees with the attachment on either is rejected at draw time, so the two belong together — and `Gpu` owns the multisampled attachment, clamping the asked-for count to what the adapter reports for the surface format and falling back to 1 with a warning rather than refusing to open. It is a **host** setting on all three legs (`--msaa`/`[gui] msaa`, `GuiBridge::msaa`), never a prop: a widget cannot have its own sample count without its own pass, so offering it per widget would promise something the hardware does not do.

  Ported in the same commit: `docs/gui-protocol.md` (the two props and the host flag), `docs/gui-props.md`'s generic list, `docs/cli.md` and the Python book's `[gui]` table (`--msaa`), the Python book's style section, `docs/architecture.md` (a `paint.rs` row it never had), a `docs/decisions.md` entry for the batch criterion and the two bounds, and both builders (`opacity`/`radius` in Python's generic props, in TypeScript's `WidgetOptions`). By eye through `gui_style.py`, which grew the row the milestone is about — a rounded panel whose controls stay square, a faded one whose controls fade with it — and boots its host with `--msaa 4`, since the arcs are what shows it.

### Part C — making it someone else's

- ✅ **K13 — The crate's features: what a build pays for.** It had `midi`, `standalone`, `standalone-faust` and — since K10 — `font-atlas`, and no axis at all through the *catalogue*: a program linking this crate for its controls compiled the engraver, `lyon` and the patcher whether or not any def ever names one. It now has `notation` (the `score` element and `lyon`) and `patcher` (the `plane`-with-boxes graph view), both **on by default**, because a feature exists so a build can *drop* a family and never so it has to ask for one — which is also why `font-atlas` and `standalone` default the other way.

  **The protocol invariant is what made this three files rather than a refactor**, exactly as the entry predicted: a family compiled out drops its rows from `elements::builtin`, a lookup that misses both the table and the registry is `Unknown`, and `Unknown` is laid out and not painted. A slim build therefore degrades the way a host older than the def already does, with no new rule and no new failure mode. `patcher` has the one exception worth naming, and it is a property of the wire rather than of the feature: `plane` is the patcher's name *and* the workspace's, told apart by the presence of `boxes`/`cords`, so without the family that fork is not compiled and a patcher node reads as the workspace it also is — degrading to something real rather than to nothing.

  **What the milestone found is why it exists.** `--no-default-features` did not compile, and had not for some time (its own commit, and its entry under "Found by use"): three `use` lines carried a `midi` gate that belonged to none of them. Then the first matrix run failed twice more — six *tests* that exercise a dropped family, which clippy cannot catch because it type-checks test code and never runs it, and one rustdoc link into a module a configuration compiles out. That third one is the reason the doc build walks the matrix at all, and it is the same lesson the root crate already wrote down: a doc comment naming an item across a feature seam names it in **backticks**, never as a link.

  **`clients/gui/check-features.sh`** is the runner, the sibling of `.claude/skills/feature-matrix/check.sh`: seventeen configurations — clippy over the floor, each family alone, `font-atlas` both ways and both `standalone`s; the wasm gate over the floor, the default set and the profile `clients/web/build.sh` ships; rustdoc over the default set and the floor; and **the suite** over both ends, which is the row the server's matrix does not have and the one that caught the six tests. The server's matrix now runs it as a single row (and dropped the two it used to spend on this crate, which are two of these seventeen), so one command still answers "is the tree committable?". Named in `docs/contributing.md` and in the `clausters-gui` skill beside `check-wasm.sh`.

  **What is not here, and it is not a Cargo.toml line.** The entry also named `timeline` and `signal` as families; they are not, yet, and the measurement is worth keeping rather than re-deriving. `notation` is named outside its own modules in **two** places (a `mod` line and a `builtin` row) and `patcher` in four; `signal` is named in twenty-nine files and the timeline family in nineteen — and not at the edges: `frame/mod.rs` is *built around* `WaveformSlot`/`SpectrogramSlot`, `view::Renderers` holds both heavy pipelines, `bulk.rs` and the server leg resolve into them, and the timeline's navigation groups live on the `Host` while its time containers are placements in `layout.rs`. Gating those means the frame's slot machinery, the bulk path and the navigation model must each compile with no heavy view at all — a structural pass of K7's size, with its own milestone's worth of decisions (what a `clip` is without a take, what `Renderers` is when it holds nothing). Landing it half-way would leave a matrix that does not build, which is worse than an axis that does not exist. So the two families stay compiled in, the seam that would carry them (`elements::builtin` → `Unknown`) is already proven by the two that landed, and the work is recorded under "Future directions" instead of pretended here.

- ✅ **K14 — Where a leaf's two halves live.** The port gave every leaf an `impl Element`, and left the other half of it where it already was: a flat module in `host/`. So `host/` is thirty-nine loose files that are four different things — the protocol and its state, the drawing toolkit every widget reads, the passes, and the **models** an element draws through — with the platform seam (agnostic core / native shell / wasm) as the only grouping `mod.rs` states. Nothing is wrong; nothing says where a new file goes either, which is how `elements/patch.rs` and `host/patch.rs` come to sit two levels apart with no line naming the difference between them.

  **The line exists and the code already says it**, in `elements/score.rs`: *"the page itself and all of that reading stay in `host::score`, which is the model; this file is only how the passes reach it."* An **element** is the surface the passes see — one method per question the frame, the gesture machine or a `/gui_query` puts to it. A **model** is what that element knows: the data shape, the geometry, the drawing over a `Draw`, the hit-test primitives that invert it. The boundary is stated negatively, and that is what makes it worth having: a model names no `Host`, no trait, no props map, no OSC and no device, which is why every one of them unit-tests without a window.

  **Why a parallel tree and not a package per element.** The obvious move — a leaf's two halves in one directory, the shape `signal/` already had — does not generalize, and the counting is the argument rather than a preference: `controls` draws eight elements, `pianoroll` draws the `notes` leaf *and* a clip's roll body, `bpf` the `curve` leaf *and* a clip's automation body, `textedit` serves the `text` leaf and `controls`, and `track`/`timeline` serve containers and no element at all. A model with several owners has no directory to live in. So elements and models are two trees, the dependency runs one way (an element reads its model, never the reverse), and an element is a **file, or a directory when it needs one** — which is what `signal/` turns out to be rather than an inconsistency.

  The name is `graphics/`: `views/` collides with a live type (`timeline::View`, `ScrollView`, `crate::view`), and three of these modules already open on the word — *"the piano keyboard's graphic primitives"*, *"the piano-roll graphic primitives"*, *"the multitrack graphic unit"*.

  - ✅ **K14a — The signal element, split by the rule it prompted.** `host/signal/` was ten files and read as the one leaf that had already been packaged; measured against the rule it is the opposite — eight of the ten answer trait questions and are the *element*, and only the drawing is the model. So `elements/signal/` keeps `apply`/`bulk`/`slot`/`live`/`freq`/`chrome`/`body`/`element` and the taxonomy the wire is read into, and `graphics/signal/` takes the pictures: `trace` (the one min/max-per-column answer every signal drawing reads), and `plot`, `spectrum`, `phasescope` and `waterfall`, which were four top-level modules in `host/` and are consumed by nothing but this element — 1847 lines that had no owner named anywhere. The seventeen-configuration matrix is clean and the 686 tests pass; `docs/architecture.md`, both GUI skills and `test_gui_props.py`'s element-directory table follow the move in the same commit.
  - ✅ **K14b — The remaining models into `graphics/`.** `controls`, `meters`, `textedit`, `nodetree`, `piano`, `pianoroll`, `bpf`, `patch`, `track` and `score/`, all ten by the same rule. Mechanical but wide — thirty-odd files change an import — and the payoff is what `host/mod.rs` can now say: the agnostic core is fifteen modules instead of thirty, and they group into four families the file names for the first time (the protocol and its tree, the models, the drawing vocabulary, the passes), with the placement rule written beside them — a module stays flat when the *whole host* reads it and goes in a directory when only its own consumers do, which is why `paint` and `metrics` are files while the models are a tree.
  - ✅ **K14c — `host/canvas.rs` is not a model.** It is the WGSL pipeline a script's shader runs on, consumed by `frame`, `gui/` and `web/` — the third of the crate's heavy renderers, and the other two (`crate::waveform`, `crate::spectrogram`) already lived at the crate root. It moved beside them, so the three pipelines are one row of the crate and `host/` holds none.

- ✅ **K15 — What still reaches a model without going through the element.** The entry opened on a wrong premise, and correcting it is the first thing the milestone found: **`gestures/` is clean.** The calls it was accused of — `patch::obj_rect`, the keyboard's geometry — are in `gestures/tests.rs`, and a test asserting a model's geometry directly is the model doing its job, not a leak. The audit that opened this had grepped the directory without separating the test file from the machine.

  What the measurement did turn up is four things, and only one of them is the shape the entry guessed:

  - **A model that names the wire.** `graphics/piano.rs` carried `voice_on_msg`/`voice_off_msg` — it *built OSC*, which is the one thing the models' own negative test forbids. And they were never the keyboard's: a voice is declared by any element (`Element::voice`) and allocated, sent, remembered and released by the `Host`, which already held the map, the counter and the id window. They are `host/voices.rs` now, with those two constants, and the keyboard is geometry again.
  - **Four parse helpers with one caller each.** `parse_notes`/`parse_osc` (only `elements/notes.rs`) and `parse_patch`/`parse_ports` (only `elements/patch.rs`) sat in the schema's shared `widget/parse.rs` and returned `graphics::` types, so the schema declared a dependency on three models it does not use. They moved into their elements. `widget/parse.rs` also stops holding the crate's one `#[cfg(feature = "patcher")]` island.
  - **A control's size lived apart from its drawing.** `slider_thick`, `slider_across`, `knob_h` and `field_h` were in `widget/size.rs` and read `controls::readout_h` from there — which is why a *pass* imported a model at all. How tall a slider wants to be and how a slider is drawn are one fact (the read-out strip is the same number twice), so they are one section of `graphics/controls.rs` now and an element's `natural` calls straight into it. `widget/size.rs` keeps the vocabulary every widget composes from (`control_box`, `label_strip`, `text_box`, `body_inset`), which is what it is for.
  - **A corner shared by four callers, filed under whoever wrote it first.** `meters::value_text` draws a short string in a body's top-right — the scope's `lock`/`free`, the spectral views' scale tag, the frame's overlay. It is `graphics::corner_text`, at the models' root, so the frame's draw pass stops naming the meter's module for a generic helper.

  **And the rest is a rule, not a debt.** Every edge left runs into `graphics::track` (from `layout`, `interact`, `frame`, `timeline`, `widget`) or into `graphics::nodetree::NodeTree` (from `world`), and both are the boundary working as designed: `Track` and `Clip` are **containers**, kept in `WidgetKind` precisely because the layout pass has to know every coordinate system it places into, so there is no element to route through; and the world holds a document the client leg queried, whose type has to live with its model. `Header` stays in `graphics/track.rs` for the same reason and one more — it carries `width`, its own natural size, and splitting the data from the geometry to satisfy a tree would cost more than it states.

  Seventeen configurations clean, 686 tests pass. `docs/architecture.md` gains the `voices.rs` row and the `keys` row says whose the voices are.

- ⬜ **K16 — The host's own documentation.** With Part A done the extension recipe stops being lore in the `gui-widgets` skill and `docs/architecture.md` and becomes a **public API** — which is more than the chapter this milestone originally named. Today the host's documentation does not live anywhere as a whole: the wire is a page of the *server*'s book (`docs/gui-protocol.md`, `docs/gui-props.md`), how a script drives one is a chapter of the *Python* book, the component is a chapter of the *web* book, and how the host itself is built is `docs/architecture.md`, a development doc. A reader who wants to *write an element* has no book to open, and after K13 a program that merely links the crate is a supported user with no entry point at all.
  So the milestone is split, and **the first part is the planning**, because the rest of the split follows from its answer and cannot be decided before it.

  - ⬜ **K16a — Decide whether the GUI host earns a book of its own, and what is in it.** The project rule today is **three mdBooks, one per platform** (server/workspace, Python client, web client), stated in `CLAUDE.md` and in the `documentation` skill; a fourth is a structural change to both, not a file. Four questions it has to answer, and none of them is obvious:
    - **Is the host a platform?** It is not a language client, which is what the three-book split is organized by — but it *is* a crate a third party links, with an API of its own (the trait, the registry, the Rust tree builder) and rustdoc as its reference generator, which is the same shape the other three have.
    - **What moves, and what must not.** The wire reference is read by *client* authors and sits with the rest of the OSC reference; moving it would split one protocol across two books, and leaving it means the new book cross-references rather than contains it. The Python and web chapters are those clients' usage and stay where they are. What is genuinely homeless is the **widget author's** material and the host's own operation (transports, standalone bundles, the theme and metrics files) — the question is whether that is a book or a section.
    - **What it costs.** A fourth `book.toml`, a fourth ReadTheDocs deploy, a fourth generated API reference, and "keep all three current" becomes four. A thin book is worse than a good chapter.
    - **What it is called and who it is for.** The three books are named by platform; this one would be named by *role* (the host, its authors, its embedders), which is the first place the existing scheme stops fitting.
    Closing K16a means the decision is written in `docs/decisions.md` (it is exactly a non-obvious structural call), `CLAUDE.md` and the `documentation` skill say the new number, and **this entry is rewritten with the parts the answer implies** — K16b/K16c below are what is already clear whichever way it goes.
  - ⬜ **K16b — The widget author's guide.** What an element is; the trait's methods and which pass each one replaces; what the world context carries and what it deliberately does not; how a drag is owned by the element and sequenced by the machine; how an element fills a container's body role and is handed that container's axis; how an edit travels back in the owner's terms; how a registration is named on the wire; and where the line between an element and a container is drawn and why. Its home is K16a's answer; its content is not.
  - ⬜ **K16c — The smallest complete proof that a third party can do it.** `examples/custom_element.rs` in the crate: registers one element, builds a window with K2's builder and opens it — no Python, no OSC, no JSON. It is also the manual test of the public door, which nothing else exercises end to end.

### What stays out of the track

Recorded so no milestone reopens it: a styling language, selectors or a cascade (the role overlay is the only cascade, and it cascades role tables, never per-part rules); a constraint solver; an accessibility tree (K6); a third-party **container** (a coordinate system is a much larger promise than a drawing — K8 draws that line and tests it); and the catalog growing a new name where a product of the existing ones would do — the `signal` rule, which is the only thing that has kept the enum from doubling and which Part A makes structural rather than habitual.

## A track — what a signal measures, and the layers that show it

*Section added 2026-08-12, from the user's brief: true peak drawn at sample zoom, the loudness descriptors as a general library (so a file can be analyzed headlessly, not only looked at), and a system of **layers** — synchronized visualizations drawn over one another, a waveform over a spectrogram, an RMS envelope over a waveform with a configurable alpha, the way Sonic Visualiser stacks layers in a pane.* Two subjects, one track, because they are one sentence: a measurement that cannot be *drawn over the thing it measures* is a number in a log, and a layer stack with nothing to stack is a container that already exists. The track ships the measurements as a **library first** and the drawing as its consumer, which is also the order that keeps the API honest — nothing in `clausters-core` may be shaped by what a pixel needs.

**The whole track is bound by one constraint the user stated: the implementation is general where it can be, behind a clear API that is easy to maintain and to use.** Concretely, three rules every milestone below is checked against. **One algorithm, two faces** — every descriptor is a one-shot function over a slice *and* a streaming meter fed block by block, with the streaming one the implementation and the one-shot a loop over it, so an offline analysis of a file and a live meter on a bus can never disagree and there is no second code path to maintain. **The face is uniform** — the descriptors take the same shape (samples, channel count, sample rate) and return the same shape, so learning one is learning all of them, and a new descriptor is a new type in one table rather than a new spelling. **Nothing enters the API for the drawing's sake** — a display concern (a per-pixel column, a colour, an alpha) stays gui-side, and the test of a placement is still the E-track's: would anything outside a window ever call it.

### The descriptors, and which ones ship

Named here so the track is not re-derived from memory, and because the user asked which measurements matter. All of them are over interleaved samples with a channel count and a rate; all of them are `clausters-core`.

| Descriptor | What it is | Where it is defined |
|---|---|---|
| **Sample peak** | the largest absolute sample over the span, in dBFS | already the pyramid's min/max |
| **True peak** | the peak of the *reconstructed* continuous signal, which a sample peak underestimates by up to ~3 dB — attenuate 12.04 dB, oversample ≥4× through the specified polyphase FIR, take the maximum, restore the gain; reported in **dBTP** | ITU-R BS.1770-4, Annex 2 |
| **RMS** | root mean square over a span or a bucket; the perceived-level body an editor draws inside the peak envelope | classic; A1 stores its **mean square** |
| **Loudness (LUFS/LKFS)** | K-weighted mean square summed over channels with their weights: `-0.691 + 10·log₁₀(Σ Gᵢ·zᵢ)`. **Momentary** = a 400 ms window, **short-term** = 3 s, **integrated** = the gated mean of 400 ms blocks (75 % overlap) with an absolute gate at −70 LUFS and a relative gate 10 LU below the ungated mean | ITU-R BS.1770-4 + EBU R128 |
| **Loudness range (LRA)** | the spread of the short-term loudness: 3 s blocks, absolute gate −70 LUFS, relative gate −20 LU, the 10th-to-95th percentile in LU | EBU Tech 3342 |
| **Crest factor / PLR** | peak-to-RMS and true-peak-to-integrated-loudness; one division over descriptors already computed, and the number a reader actually asks for when they say "how compressed is this" | derived |
| **DC offset** | the mean; a defect detector, free beside the mean square | derived |

What deliberately stays out: DR14 and the other publisher-specific scores (they are a policy over these numbers, not a measurement), and psychoacoustic sharpness/roughness (a different literature, and nothing in the system asks for them). Correlation is already `measure::correlation` and joins the table by reference, not by reimplementation.

### Decisions taken before opening the track

- **A measurement is a derivation of a *source*, not a new element.** The signal element is a presentation × a source × capabilities (E6/E9), and a measured curve is that product with one more factor: a `measure` prop naming a descriptor. That is what makes the feature general for free — the same prop over a bus is a live loudness meter, over a file an offline analysis, inside a clip a body, and under a `field`'s axes a layer; the catalog does not grow a name, which is the `signal` rule the K track made structural. It also settles where the measure's own parameters live: with it, on the element, beside `fft_size` and the rest.
- **A layer is a child of a container, and the container is already there.** A `field` places several elements on its two axes (E7 layered a clip's bodies by doing nothing but placing them), and `opacity` is already a style (K). So the layer system is **not new machinery** — what is missing is what a *stack* has to say and today cannot: the order it is drawn in, which layer the y ruler and the cursor read-out follow, and whether a layer maps through the container's y axis or normalizes into its box. A6 is those three questions and nothing else; if it turns into a new container type, it is wrong.
- **A layer that normalizes is stated, not inferred.** A waveform over a spectrogram share a time axis and share **no** value axis — one is amplitude, the other frequency — which is exactly the case Sonic Visualiser answers by letting one layer own the pane's vertical scale. So a layer declares `y: "axis"` (it measures in the container's y domain) or `y: "box"` (it fills the box it is given, its own domain normalized), and the ruler follows the one layer that says `axis`. Two layers claiming the axis is a script error the host reports rather than resolves.
- **Precision at sample zoom is a resampler, and a resampler needs guard samples.** The user named the artifact: an interpolation filter reading up to the edge of a window sees zeros beyond it and rings. Every reconstruction in this track therefore takes a span *plus* the filter's context on both sides (`guard = (taps-1)/2` at the working rate) and the core API takes the guard **explicitly** rather than hiding it — a caller that cannot supply context (the first samples of a file) gets the documented edge policy, not a silent lie.
- **Placement, once for the whole track** (the G7b rule): every descriptor and the reconstruction filter are `clausters-core`, FFI-exported, because the Python client analyzing a rendered file headlessly is a real consumer today and a server-side analysis command is a plausible one tomorrow. What stays gui-side: the column layer's geometry, the colour and alpha roles, the crossfade weights, the over-0 dBTP marking, and the layer stack's ordering rules.

### The milestones

- ⬜ **A1 — Mean square in the pyramid** *(this is G20e's analysis half; that section is superseded by A1+A2)*. `clausters_core::peaks` grows a per-bucket **mean square** beside min/max — stored as energy, not as RMS, so higher levels combine exactly (the mean of the means) and the LOD crossfade blends without bias, which is the decision G20e recorded and A1 keeps. CLPK v2 → v3, with v1/v2 still parsing; `cache_size`/`multi_cache_size` follow; the FFI exports and `CORE_ABI_VERSION` bump so `clausters.gui.peaks_cache_file` builds the identical cache the host maps. **Acceptance:** core RMS matches brute force on random chunks; a level-2 bucket equals the direct RMS of its samples; a Python-built v3 cache and a host-built pyramid are byte-identical; v1/v2 caches still load.
- ⬜ **A2 — The RMS layer, and the `measure` prop** *(G20e's drawing half, generalized)*. The signal element takes `measure` (`peak` default, `rms` here), and an `rms` trace draws the symmetric body inside the min/max envelope with its own colour role and the element's `opacity`, LOD-crossfaded exactly like min/max. Below `base_bucket` the body follows the crossfade weight out rather than switching off (G20e's call, kept). **This is the first layer**, so it is also the proof that a measure needs no container work: the same element with `measure:"rms"` placed over a plain one in a `field` is the overlay, and A6 only makes the stack's rules explicit. Python: `measure=` on the builder plus the descriptor through `_native` for headless use; the web builder ports in the same commit. Example: `gui_bulk.py` grows the layer.
- ⬜ **A3 — Band-limited reconstruction and true peak**. Core gains the oversampling filter (`clausters_core::resample`: a polyphase FIR with a declared guard, 4× by default and 8× available, the BS.1770 Annex 2 coefficients at the standard rates and a documented design for the others) and `true_peak` over it. Two consumers land together, which is why they are one milestone: **the drawing** — past the polyline threshold the trace draws the reconstructed curve between the sample dots instead of the straight segments it invents today, read with guard samples so the ends do not ring, the dots still marking the data (E-track's rule: the line is an interpolation, the dot is the sample) — and **the number**, the read-out reporting dBTP with the inter-sample peaks marked where they exceed 0 dBTP, which is the one thing a sample-peak view structurally cannot show. **Acceptance:** the classic worst case (a full-scale sine at fs/4 phase-shifted, whose samples read below full scale) measures above 0 dBTP and draws a curve that leaves the ±1 band; the reconstruction against a straight 4× resample of the whole file agrees within the filter's stated error; a span read without guard and with guard differ only at the edges, and the test says by how much.
- ⬜ **A4 — K-weighting and the loudness family**. `clausters_core::loudness`: the two K-weighting biquads (the high shelf and the RLB high-pass), **rate-generic by bilinear redesign** from the 48 kHz reference rather than by resampling, with the redesign tested against the published coefficients at 48 kHz; the channel weights (L/R/C 1.0, surrounds 1.41, LFE excluded); momentary, short-term, gated integrated, and LRA. The API is the track's rule made concrete: a `LoudnessMeter` fed blocks (no allocation in `feed`, so the same type is usable from a live path) and thin one-shot functions over a slice that drive it. FFI + the Python wrappers, since analyzing a rendered file is the immediate use. **Acceptance:** the EBU Tech 3341 compliance cases that do not need a decoder — the −23 LUFS and −33 LUFS tones, the gating cases, the short-term/momentary step responses — pass within the tolerance the document states, and the test names each case.
- ⬜ **A5 — The loudness layer and its read-out**. `measure:"momentary"`/`"short"` draws the loudness curve over the waveform on a **LU axis** (its own domain, so it is the first layer that must declare `y:"box"` or bring its own ruler — the A6 question, met concretely before it is generalized), with the R128 target and the −18/−9 LU guides as optional lines. Beside it the numbers: integrated LUFS, LRA, true peak and PLR, over a span or over the current **selection**, which is the first place the A and D tracks meet. Live acceptance: the same `measure` over a bus draws a moving momentary curve, from the streaming meter, at no extra API.
- ⬜ **A6 — The layer stack becomes explicit**. The three questions, and nothing more: **order** (children order, back to front, stated on the wire and queryable, with a `visible` and a `solo` per layer so a stack is readable while it is being built), **the vertical scale** (`y: "axis" | "box"`, one layer owning the container's y ruler and the cursor read-out, a second claim reported as an error), and **the alpha rule** (a layer's `opacity` is the style that already exists; what A6 adds is that the *stack* has a defined compositing order and that a layer over a texture gets the plate treatment the captions already have where it draws text). **The acceptance is the user's case**: a spectrogram with a waveform drawn over it and an RMS band over that, one time axis, one selection, one playhead, each layer's alpha live via `/gui_set`, and the y ruler following the layer that claims it.
- ⬜ **A7 — The stack from the clients, and the books**. Python: a layer-stack builder over the `field` (`layers=[…]` composing signal elements, not a new object model) and the `Editor` able to open an element with a chosen stack; the web builder ports in the same commit — the packages move together. Docs: `docs/gui-protocol.md` for `measure`, the layer keys and the axis-claim rule; the client book's composition/analysis pages for the descriptors; `docs/decisions.md` for the two calls worth recording (a measure is a factor of the signal element, not a widget; a layer declares its vertical scale instead of the host inferring it). Example: `gui_layers.py`, the stack the A6 acceptance describes, held open until it is closed.

**What stays out of the track**, recorded so no milestone reopens it: a layer *library* of arbitrary annotations (Sonic Visualiser's time-instants and time-values layers are `notes` and `curve` elements placed on the same `field` — if they need anything, it is A6's rules and not a new type); loudness normalization or gain application (a measurement track measures; applying gain is an edit, and edits are the D track); and per-layer blend *modes* beyond alpha, which nothing in the brief asks for and which would put a compositing model into a mesh renderer that has none.

## D track — editing the data: samples, spectra, and what a selection is

*Section added 2026-08-12, from the user's brief: selection and editing per sample (zoom in, grab one sample, drag it up or down; a **draw mode** distinct from the selection mode for a run of them), spectral selection by bin and column with the overlap factor understood, a spectral draw mode whose inverse FFT reconstructs a signal, and — the sentence that ties them — selections whose data can be **saved**, to copy and paste and to hand ranges to editing algorithms, with a lasso for regions that are not rectangles.* It folds in the "A sample as a grabbable point" future direction — *named 2026-08-12 by the user on the `gui_window` pass that checked the round hit shapes ("the samples, the round points, cannot be selected individually and dragged — that could be implemented"), and removed from that list here* — which had already reached the same conclusion from the other side: the hit-test is in place and what is missing is the edit's route. Its three open questions are D1's, answered once for the whole track: an intent per **stroke** rather than a payload per sample, a **span** rebuild in the core rather than a re-summarized file, and a **marked pending** overlay rather than either lying about the data or waiting out the round trip.

**The track's premise is the host's oldest one: it owns no data.** A note, a break-point and a clip are the host's to move because the host holds them; a sample belongs to a buffer, a file or a client's array, and a spectral frame belongs to an analysis of one of those. So every milestone here is the **edit-back pattern** at a new granularity, and the questions it raises are its own — what a payload of ten thousand samples rides as, whether the host may show an edit before the owner confirms it, and what a redraw costs when the summary is a pyramid that has to be rebuilt over the touched span. The track answers those once, in D1, and spends the rest of itself applying the answer.

### Decisions taken before opening the track

- **A selection is a value, not a highlight.** Today it is a time span on the navigation group, drawn and emitted; the brief asks it to be *saved*, passed to algorithms and pasted — which means it must be a typed thing a script can read back. So `Selection` becomes a small tagged model owned by the group (the only place it can live, since linked views read one selection — the rule the "a selection counts samples" entry already fixed): a **time span** on the x axis, optionally restricted by a **value rect** on a y axis that measures something, optionally a **spectral region** (frames × bins), optionally a **mask** for a lasso. It is queryable through `/gui_query` and settable through `/gui_set`, and it obeys K5's rule — what a query gives back is what a set would take.
- **The clipboard is one document, typed.** The host already has a host-wide clipboard, and it is one `String` (K6, the notes block travels through it). That is not a limitation to remove but the shape to keep: the clipboard becomes a **JSON document with a `kind`** — notes, samples, a spectral region — so one mechanism carries every payload, it crosses windows and defs unchanged, and a client can read or write it without a second carrier being invented. Bulk sample data inside it rides the existing blob layout, not a re-encoded array of numbers.
- **A mode is a gesture plan, not a new state machine.** E5/E10 gave every container an ordered plan of steps per modifier, declared on the wire and live via `/gui_set`. **Draw is a new step in that table**, so "selection mode" and "draw mode" are two plans, and which one is bound is a script's or a toolbar's decision rather than a mode enum inside the host. That is what keeps the feature general: the same step gives a curve a pencil and a spectral plane a brush, and a script that wants pencil-on-Ctrl gets it without asking anyone.
- **An edit is an intent, and the host may show it before it is confirmed — as a pending overlay, marked.** A dragged sample leaves as `/gui_event <id> "samples" <start> <v…>` (flat primitives, a payload not an address, the fifth use of the pattern) and the owner applies it and hands the data back. Between those two moments the host draws what the hand did, in a state that is visibly *pending*, and it drops that overlay when fresh data arrives or when the owner refuses. The alternative — draw nothing until the round trip closes — makes a pencil unusable over a network leg, and the alternative to *marking* it is a host that lies about what the data is.
- **A redraw is a span, not a file.** Re-summarizing a minutes-long take because eight samples moved is the one thing that would make this unusable, so `clausters_core::peaks` grows an in-place `update_range` (rebuild the buckets a sample span touches, up the levels, the only general piece the whole editing half needs) — placed in the core because a client that owns the data has to rebuild the identical cache.
- **Spectral editing needs phase, which the display analysis does not keep.** The `Stft` behind the spectrogram stores magnitudes; an edit that must resynthesize needs complex frames, which is twice the memory for the same picture. So an `editable` spectral element analyzes complex — **opt-in, per element, stated on the wire** — and a read-only spectrogram is unchanged. Resynthesis is weighted overlap-add over `clausters_core::fft::irfft_into` (which already exists, and is the same door the server's `FFT`→`PV_*`→`IFFT` chain uses); a magnitude-only edit keeps the original phase, and material that has no original phase is the experimental case D8 names.
- **Bin × frame is not a sample range, and the view must not pretend it is.** The hop sets the column grid; the **window** sets each column's support, so a one-column edit touches `window_size` samples and neighbouring columns share them — which is exactly the overlap factor the brief points at. A spectral selection therefore reports **both**: the frames × bins rectangle it is, and the sample span it actually touches. The cell is drawn hop-wide (that is where the data sits) and the footprint is shown when a selection is made, because a user who edits a column and hears a change a window wide has been told nothing.

### The milestones

- ⬜ **D1 — A sample is a grabbable point** *(folds the "A sample as a grabbable point" future direction)*. Past the polyline threshold the trace already marks each sample with a disc sized as a curve's break-point, and `Element::hit_area` already answers on discs (E22) — so D1 is the route, not the hit-test: press a sample, drag it vertically, the host draws the pending value, and `"samples" <index> <value>` leaves for the owner. With it the three general pieces the rest of the track reuses — `peaks::update_range` in the core, the pending overlay, and the confirm/refuse path (`/gui_set` of the data, or a refusal that drops the overlay). Python: the `Editor`/`Session` side applies a sample edit to a buffer (`/buffer_setRange`) or to a client array and returns the fresh span. Example: `gui_bulk.py` zoomed to samples, edited by hand and re-read.
- ⬜ **D2 — The draw mode**. `draw` joins the gesture plan table: a press-drag over a signal element writes the value under the pointer for every sample the pointer passes, interpolating between motion events so a fast stroke leaves no holes, and emitting one `"samples" <start> <v…>` run per stroke rather than one per sample. It is **refused, visibly, where a pixel is more than one sample** — a stroke at that zoom would write a value the reader cannot see, and refusing is the honest answer (the same call the grip made about an off-screen edge). Python/web builders take the plan; the example shows both modes on one element with the plan switched live.
- ⬜ **D3 — The selection becomes a value**. The typed `Selection` above: the value rect on axes that measure something (the roll's marquee already is one, so this is a generalization rather than a new gesture), the query and set round trip, and the `"selection"` payload extended to carry what it now is — without breaking the two-number form scripts already read, since a payload grows by appending. The sample-axis snapping rule stays exactly as the Found-by-use entry fixed it, and the value axis takes the same treatment on its own domain.
- ⬜ **D4 — Copy, cut and paste of a sample range**. The typed clipboard, and the three verbs over a selection: copy reads what the host has mapped (which is what the host may honestly copy — a source it cannot read declines and says so), cut and paste leave as intents (`"samples"` for the write, plus the length change a cut implies, which is the one case where an edit is not value-for-value and therefore the one that needs its own payload). A block travels between windows and between elements of different sample rates, which forces the one decision to record: a pasted block is **not** resampled by the host — it carries its rate and the owner decides, because resampling is an edit the data's owner performs, not a side effect of a paste.
- ⬜ **D5 — Ranges to algorithms, client-side**. The brief's other half, and it is deliberately **not** the host's: `clausters.form`/the Python client gain a selection object that maps a view's selection back onto the element it came from (a buffer range, a file span, a rendered element's samples), so the ordinary editing operations — gain, normalize, fade, reverse, silence, and any function the user writes — apply to it and the element re-renders. The host's part is only that the selection it emits carries enough to be resolved that way. Example: select a region in a view, call a normalize over it from the notebook, watch the drawing follow.
- ⬜ **D6 — Spectral selection**. A drag over a spectral presentation selects frames × bins, drawn as the cells it holds; the read-out gives the time span, the frequency band and the **window-wide footprint** the overlap implies, plus the region's energy through the A-track descriptors. It joins the same `Selection` value (so a spectral region is copyable and queryable by D3/D4's machinery, and a linked pair of views shares one selection whose time span both understand). The one gui-side geometry piece is the display→(frame, bin) inversion, which the frequency ruler already owns.
- ⬜ **D7 — The lasso**. A free-hand region over a spectral plane, kept as a **mask** rather than as a polygon (the mask is what every consumer wants, and a run-length encoding of it is what a payload can carry); the same gesture is available wherever a container has two measuring axes, so a value plane gets it for free without a second implementation. Its payload and its clipboard form are the mask; the drawn edge is display-only.
- ⬜ **D8 — Spectral drawing and resynthesis** — **experimental**, in the G20f sense: it ships behind the `editable` opt-in, is evaluated on what it sounds like, and is promoted or dropped on that evidence. The complex analysis, a brush over the magnitude plane (attenuate, boost, erase — the operations whose phase is already there), the weighted overlap-add back to samples through `irfft_into`, and the edit leaving as an intent over the **sample** span it reconstructs, since samples are what the owner holds. Material with no original phase (drawing energy where there was none) is where phase reconstruction — Griffin–Lim iterations, or a documented refusal — is decided **with the evidence recorded**, not before. **Acceptance (experimental bar):** erasing a partial from a two-partial tone leaves the other partial audibly clean, the round trip through analysis and resynthesis with no edit is transparent to within a stated error, and the cost on an editor-length file is measured and written down.

**What stays out of the track**: an **undo history** — not because the host may not have one, which is the H track below and its whole subject, but because inverting an edit is a different question from making one, and answering both in the same milestone is what would have turned the host into a second owner; D's part is that every intent it defines is **invertible**, and H is what stacks them. Also out: a file format for selections (the clipboard document is the format); and destructive editing verbs in the host, which is the track's premise again — the host reports intents and draws what came back, with the one exception H names, a `standalone` host applying an inverse it logged because there is no other owner to send it to.

## H track — the edit log: a gesture is undoable, and the data's owner applies it

*Section added 2026-08-13, out of a discussion that began at the D track's exclusion of an undo history "because it belongs to whoever owns the data". That premise is right and it does **not** exclude the host: under `standalone` the host **is** that owner, and in every other case it is still the only thing that saw the gesture. What the exclusion was really protecting against is two histories of the same data, and the shape below avoids that without giving up undo.*

**The rule the whole track rests on, in one sentence: the host keeps the log of its own gestures, and the inverse is applied by whoever holds the data** — by the host itself under `standalone`, by the client as an ordinary intent when one is attached. It is bounded by a property rather than by a policy: **the host can invert exactly what it can draw**, since it had to hold the previous value in order to draw it. An edit it never saw is not in its log and is not its to undo, which is the two-owner problem answered by construction instead of by rule.

### Decisions taken before opening the track

- **Logical material has no undo, and that is not a gap.** A generator is code: going back is rewriting the structure and evaluating it again, and the unit of change there is the source file, which git already versions better than we would. So the log holds only actions on **concrete** material — samples, placements, break-points, a rendered event score. Editing a generator's *output* is not a special case at all: once rendered it is data like any other, and the algorithm that produced it is referenced, never logged.
- **The log is the host's outbox, not a second document.** It holds what the host itself emitted, in order, with the previous value; it is not a copy of anyone's data and it never becomes one. That is why it can coexist with a client that owns everything: the two are not the same kind of thing.
- **A gesture is the transaction.** `gestures` already has press→drag→release, and that boundary is free and correct: a pencil stroke is one entry, not four thousand. Each entry carries a label ("move clip", "draw samples") because a menu has to say what it will undo.
- **What comes back from the owner is not an edit.** Only gestures enter the log; the data that arrives through `/gui_set` after an intent was applied does not. Without that distinction an undo either doubles or bites its own tail, and it is the one invariant most likely to be got wrong in implementation.
- **The log is per session and does not persist.** It dies with the window under `standalone` and is cleared when a client detaches — there is no history file, which is also why none of the concurrency questions a shared file would raise apply here. The one refinement that does not violate it: a destructive edit spills its **replaced span** to a temporary directory deleted on close, under a budget that evicts oldest-first, because holding a normalized twenty-minute take in RAM is what would otherwise make the depth useless.
- **A free or a redefine prunes the log**, exactly as it already prunes bindings (G6). Deleting is a forward operation and recreating mints a new id, so an entry naming a freed id has nothing left to invert.
- **The pending overlay is not the log.** D1's marked-pending drawing is what the host shows between a gesture and its confirmation; the log is what it can replay afterwards. They are adjacent and must never be merged — one is about a round trip in flight, the other about a round trip that closed.
- **Placement (the G7b rule):** the log is **gui-side**, and by the E-track test rather than by convenience — nothing outside a window would call it, because a client that owns its data and wants its own undo owns its own history too. So `host::history` is pure core (entries, cursor, inversion, coalescing, budget: no I/O, wasm-clean, unit-tested without a window) and the spill store is I/O **behind a trait**, the sibling of `DefStore` and `BulkLoader`, with a temporary directory natively and memory in the browser.
- **The session document is not this track's.** It belongs to `clausters.form` with the `standalone` host as the other writer of the same format (`clients/python/PLAN.md`, C38). The two only meet at one point, stated so no milestone confuses them: a save is not an undo boundary, and the log is never written into the document.

### The milestones

- ⬜ **H1 — An intent is invertible, and `host::history` is the log.** Every edit-back payload the host emits gains the previous value (or a reference to it), and the pure log lands beside it: entries, the gesture as transaction, the label, the cursor, the prune on free/redefine, the budget. No UI and no owner yet — **acceptance:** a scripted run of gestures over a headless host inverts back to exactly the starting tree, and an intent re-emitted after an undo is byte-identical to the one the gesture first sent.
- ⬜ **H2 — Undo with an owner: the inverse goes back out.** With a client attached the host applies nothing — it re-emits the inverse as the ordinary edit-back payload, and the data that comes back is *not* logged. The position in the log is **state**, queryable and settable on the root (K5's rule: what a query gives back is what a set takes), rather than two new addresses; undo/redo also join the gesture-plan table so a key or a toolbar can drive them. Python: the `Editor` routes the intent onto the arrangement objects it already indexes by widget id — and because that map is the whole mechanism, **an editable widget is not closed until its id→object route is covered**, which is the requirement this milestone adds to every later one.
- ⬜ **H3 — The spill store: undoing an edit whose inverse is data.** The trait and its two implementations, content-addressed so an undo/redo pair stores one copy and a deterministic operation (gain, fade, reverse, normalize) stores only the span it replaced and recomputes the redo. The budget evicts oldest-first and says so — a depth that silently stops being a depth is worse than a shallow one.
- ⬜ **H4 — Undo without an owner: the standalone editor.** Under `standalone` the host holds the data, so the same log applies its own inverses through the embedded server's buffer commands (`/buffer_setRange` and its family), with `peaks::update_range` redrawing only the touched span. This is what makes the audio-editor case real — a host with predefined operations, a session and an undo stack, no language anywhere — and it is one milestone rather than a track because H1-H3 already built all of it but the application step.
- ⬜ **H5 — The clients and the books.** The Python and web builders for the log's state and the plan steps, in the same commit; `docs/gui-protocol.md` for the queryable position; `docs/decisions.md` for the two calls worth recording (the log is the host's outbox rather than a second document; logical material has no undo, and why that is a property of code and not a limitation of ours); and the example — a tree edited by hand, undone and redone, held open until it is closed.

**What stays out of the track**: undo of node-tree commands (playing a synth is not an edit) and of navigation (a view is not data); a history that outlives its session; a history for logical material, which is the first decision above and the reason the track is small; and any merge or conflict machinery, which only a persisted, two-writer history would need — this one has neither.

## ✅ In-browser audio engine (Web Audio / AudioWorklet) — shipped as the server's B track

**This track converged and is numbered on the server side: `PLAN.md` (root), "B track — the engine in the browser" (B0–B4).** The engine port, the AudioWorklet backend and the wasm-`Embed` link landed there because the engine is the server's; the GUI-host leg landed as B3 (`ServerLink::Page` + `GuiBridge.connect_page`/`server_reply`, the `host::bundle` boot replay, the standalone page), and the web-component packaging landed as B4 (the `clausters` package seeded in `clients/web/`). **Consolidation (with W0):** this crate no longer carries a `web/` directory — the harness/parity/standalone pages and the bundle tooling that G12/G16/B3 grew here live in the web package (`clients/web/examples`, `tests`, `tools`), which stages this crate's wasm-bindgen bundle into its `dist/gui-host/`; the milestone entries keep the original paths as the record of what shipped where. The design notes below are the original record that shaped it — note the SharedArrayBuffer route they sketch was *not* taken initially (no COOP/COEP on arbitrary pages; the MessagePort ring landed instead, with SAB kept open as a later optimization — see `docs/decisions.md`).

Not part of G11-G16; recorded here because the G11 seam was deliberately shaped to accept it, and the G11 decision ("no in-process engine in the browser") is a scope boundary that this track relaxes. Through G16 the browser GUI host drives a *separate* audio server over WebSocket. The parallel, larger piece of work is to compile `clausters-server` itself to `wasm32` with a **Web Audio backend**, so the engine runs **in the browser** - the wasm analogue of the native `standalone`/`embed` mode.

- **A new audio backend behind the existing engine seam, not a DSP rewrite.** The engine core (`Engine::process_block`) is already decoupled from the device: it feeds the real-time cpal callback and the offline `render()`/NRT path from the same block function (FTZ armed in both, NRT sample-identical to RT). A browser backend is a *third* driver - an **AudioWorklet** output whose process callback pulls blocks from the engine. cpal does not target Web Audio, so this is a genuine backend addition, which is exactly why it is its own track rather than a step inside G11-G16.
- **An in-process browser link, the wasm `Embed`.** With the engine in the page, the GUI host reaches it through a new `ServerLink` variant (the wasm counterpart of `Embed`): OSC over an in-process channel, **not** WebSocket. This is the same second link kind the native host already has (`Udp` vs `Embed`); the `Transport` trait and the cfg-gated `ServerLink` from G11 take the variant without a protocol change. WebSocket stays the carrier for a *remote* server, so it never disappears - it stops being the browser's only option.
- **The shared-memory paths return inside the browser.** An AudioWorklet runs on its own thread and shares state with the main thread through `SharedArrayBuffer` (which needs cross-origin isolation, COOP/COEP). That is the browser's shared-memory primitive: it can carry the zero-message `BusSource` (control buses read each frame) and the bulk audio path **inside** the page - the same roles `host::shm`/`mapfile` play natively - instead of the WS/`fetch` fallback G14/G15 build for the remote case. So a browser host paired with an in-page engine looks more like the native host than like the remote-server browser.
- **RT-safety carries to the worklet.** The audio thread's no-alloc/no-lock discipline applies to the AudioWorklet thread, and the lock-free command/garbage FIFOs map onto `SharedArrayBuffer` ring buffers. A standalone-style bundle (a GuiDef + GraphDefs) could then boot entirely in a browser tab with no server process at all - the browser twin of `--standalone`.

This intersected the server track and not only the GUI one, which is why it was numbered there. **The one piece of the sketch above that did not ship is the third bullet** — the in-page shared-memory paths — and it is recorded as a track to open under Future directions rather than left here reading as done. The product TypeScript client (`clients/web`, see the G13 note) is still a separate concern from both.

## Definition of done (per milestone)

Following the project rule: code + tests, a clear commit message (the record of *what* shipped) and this file's checkbox updated; developer/user docs where the feature touches them; a commented example when the feature is user-facing — the example is also how new human-visible behavior gets checked by eye; and a `docs/decisions.md` note only when a choice has non-obvious context.

## Future directions (to fold into milestones as they firm up)

Captured here so the depth the editor-grade vision needs is not lost; each becomes a milestone — or a track — when its design converges. **The convention, stated because the list only works if it holds:** every entry carries a `⬜`, and one that converges into numbered milestones **leaves this list** — it is not marked `✅` here, because converging is not shipping and a `✅` beside an unstarted milestone says the opposite of what is true. The milestone that absorbed it is the record, and it says where it came from, so nothing needs a pointer that would go stale. Gone that way so far: scopes, the editor-grade views, edit-back-to-data and the BPF view (G18-G21), and — 2026-08-13 — the **DAW / timeline view**, which became the whole of G22 with its dedicated note view following as G24 — while the free arrangement plane it named alongside the lane stack **stays here**, since it is a second kind of multitrack whose model differs structurally from the one that shipped and has still to be defined, not an increment on it; and **a sample as a grabbable point**, folded into the D track (D1, D2), which is where the questions it raised are answered.

**Ordering — what is left of it.** The original decision staged the widget-deepening arc first, the timeline and notation views after it, and packaging plus the in-browser audio engine deliberately last. Three quarters of that is spent: the deepening arc landed (G18-G21), the timeline converged as G22 and notation as G31, and the engine shipped as the server's B track — which also settled the `Transport`/`ServerLink` seam the ordering was keeping open, since `ServerLink::Page` is that variant. What still binds is the other half of the same sentence: **packaging stays last**, because it changes how the system ships and not what the system can show, and it keeps constraining design in the meantime — the web frontend must stay Tauri-wrappable.

- ⬜ **The free arrangement plane (the blueprint view)** — *a second kind of multitrack, not a milestone of the one that shipped: it shares characteristics with the lane stack and its model differs from it structurally, so it is named here and still to be defined and planned.* A second projection of the same arrangement: the L3 workspace with its x `View` joined to the window's navigation group, so the plane's x axis *is* the composition's time axis (ruler, selection, the playhead sweeping the whole plane as a vertical line — all existing group machinery), while y is **free spatial organization** — clips and visual elements placed like an architectural plan, not locked to lanes. The model (`clausters.form`) is untouched: `offset`/`dur` stay the temporal truth, and y is presentation geometry owned by the driver and persisted by the `Editor` beside the tree, exactly the P1 `"move"` pattern. A concrete `Group` draws as a bounding **zone** (visual containment as the group's projection) instead of a lane; the classic lane-stack multitrack becomes the *constrained* case of the free plane, the same general-to-constrained pattern L3 fixes for scrolling — which is the direction of the relation and the reason this cannot be an increment on G22: the general case is the one that is missing. One boundary to hold when this opens: **y means nothing to the render** — not pitch, not bus, not bounce order; presentation only, stated so the view never implies semantics the engine does not have (any later mapped-y is an explicit decision, not drift). The honest precedent is the graphic score — **a UPIC whose material is the multitrack's editing structures**: not freehand curves but clips, groups, snap, selection and the edit-back payloads, placed on a free plane (IanniX is the other kin), and the whole of what "free" means there is what a design has to settle. **What it was waiting on has landed** — G22's clip bodies, L2's placement props and L3's workspace — so what stands between it and a track of its own is that definition, not a dependency.
- ⬜ **A steady goniometer, and what a phase view should read** *(the design half left standing by the ✅ "The goniometer's figure jumped, and the signal was the staircase" fix below — smoothing the source removed the staircase and left the view's own question exposed)*. With the signal smoothed the figure sweeps, and what is left is the view's own: it draws the **newest** window of two taps with no alignment, so consecutive ticks read overlapping stretches and the trace's ends need not meet. Three shapes, and the one that is chosen decides what a phase view *is*. **Align it** — reuse the oscilloscope's trigger on the left channel, which costs nothing new and stands a periodic pair still, at the price of no longer showing where in the signal it is. **Persist it** — the classic hardware look: several windows drawn with a decay, which the mesh's per-vertex alpha already allows and which turns instability into the glow it is on a scope. Or **take a positioned window** (`read_bus_at` reports where a window ends, which is what the retained history already appends by), so consecutive ticks read *consecutive* samples and the phase advances continuously instead of overlapping. The third is the only one that changes what the view reads rather than how it draws, and it is the one that would also survive a slower frame rate.

  **Where it shows is the evidence**: with the signal smoothed, what remains is visible at the *ends* of a sweep, where the figure degenerates into a straight line (mono is one diagonal, anti-phase the other). A line is exactly where a phase error is most legible — any of it opens the line into a thin ellipse — so the residue is the unaligned window and not the drawing's resolution, which is what points at the positioned read rather than at drawing more of it.
- ⬜ **The heavy families as features** *(what K13 measured and did not take)*. `timeline` and `signal` were named as element families beside `notation` and `patcher`; they are not families in the same sense yet, and the difference is structural rather than a matter of effort. The two that landed are named outside their own modules in two and four places; `signal` is named in **twenty-nine** files and the timeline family in **nineteen**, and not at the edges — `frame/mod.rs` is built around `WaveformSlot`/`SpectrogramSlot`, `view::Renderers` holds both heavy pipelines, `bulk.rs` and the server leg resolve into them, the navigation groups live on the `Host` and the time containers are placements in `layout.rs`. So gating them is not a `Cargo.toml` line: it means the frame's slot machinery, the bulk path and the navigation model each compile with **no heavy view at all**, which needs its own decisions (what a `clip` is with no take; what `Renderers` is when it holds nothing; whether a host with no timeline still has navigation groups) and a milestone of K7's size to carry them. The seam itself is not in doubt — `elements::builtin` → `Unknown` is proven by the two families that did land — and `check-features.sh` is already the runner such a milestone would report through. What it buys is the browser bundle above all, which is where a dropped family is worth real kilobytes. The A and D tracks make it heavier, not lighter: both grow the signal element, so this is measured again when they close.
- ⬜ **The in-page shared-memory path — a track to open.** The browser engine shipped (the server's B track) and it talks over a **MessagePort**: OSC bytes both ways, commands crossing into the engine through the same in-memory ring the native embed uses. What did *not* ship is the half that would make a browser host look like the native one — a **`SharedArrayBuffer`** carrying the zero-message `BusSource` (control buses read each frame) and the bulk audio path *inside* the page, the roles `host::shm`/`mapfile` play natively. In a tab those still take the WS/`fetch` fallback built for a *remote* server, even when the engine is in the same document. It was left out **by requirement, not by omission**: SAB needs COOP/COEP isolation headers, and an embeddable component cannot demand those of a page it does not control (`docs/decisions.md`). That is also what makes it a **track rather than a milestone** — it is an optimization behind a build: a page that can isolate itself opts in, every other page keeps the MessagePort path, and both have to stay correct, which is a second backend for one seam and not a redesign (the ring seam was shaped to accept it). Opens when a page profile asks for it, and the number to have first is what a frame of `/bus_stream` costs on a page holding forty canvases — that is the whole argument.
- ⬜ **Composed text (IME) cannot be typed into a field.** K6 reads keys and nothing else: the native front takes winit's resolved characters, and in a page a `<canvas>` cannot host an input method at all — so a field takes Latin text and refuses everything a reader of Japanese, Chinese, Korean or a Latin script with dead keys actually types. It is recorded as a limit in `docs/decisions.md` rather than left to be found, and it is a *design* rather than a fix because the answer is not in the host: the page owns composition (an off-screen `<input>` or `contenteditable` fed through `beforeinput`, forwarded to the host as finished text), and the native front owns winit's own IME events — two different mechanisms filling one hole, which is exactly the shape the platform seam exists for. It becomes a milestone the day a field has to hold a name that is not ASCII. **The other half of the sentence has since closed**: text the host cannot *draw* is no better than text it cannot receive, and K9 gave the embedded face lowercase and accents while K10 put any real typeface behind `font-atlas` — so the drawing half is a matter of loading a face that carries the script, and the input half is the whole of what is left here.
- ⬜ **Packaging: an optional Tauri desktop wrapper** reusing the web frontend. The rest of what this entry once listed has shipped — the GUI chapter of the client's book (`gui.md` and its pages) and the worked `gui_*` examples — so the wrapper alone is what remains, and it stays **last** by the ordering rule above: it changes how the system ships, not what it can show.

## Found by use: the running list of fixes

These are not milestones and they are not future directions. They are what
**using the thing** turns up — an eye pass over an example, a path read while
doing something else, a behaviour that is correct and unclear — recorded the day
they are found so they are not rediscovered, and kept here rather than inside
whichever milestone happened to be open at the time.

Two conventions, because the section only works if both hold. Every entry is a
**checkbox**, so what is open reads as open at a glance and nothing has to be
inferred from where it sits. And a fixed one **stays**, with the record of what
was wrong and why the fix is the shape it is — that is what makes the list worth
reading rather than a queue that empties.

Anything unresolved lives here or under "Future directions", both **after** the
tracks: never inside the milestone that happened to be open, and never among
finished work, where a pending item reads as done.

- ✅ **A clip could be resized out of existence** *(found 2026-08-13 by the user, right after the grip fix below; fixed the same day)*. Drag an edge past the other one and `dur` reached zero — and a clip of no duration draws no rectangle, so there was nothing left to press, at any zoom. The clip stayed in the tree, still addressable by `/gui_set`, invisible and unreachable: a state the user could enter with one gesture and could not leave with any.

  **The usual answer is a floor, and it is a floor in three places rather than one**, which is why the fix is not a single `max`. (1) The **data** floor: a drag stops one `snap` step short of the other edge, or one sample where there is no grid — the shortest clip that can exist on the grid the edge already moves in, which is what a DAW does, and a **length in the axis' own units, never a count of pixels**, so the same drag stops at the same place at every zoom (the rule the time selection already follows). A clip a *script* made shorter than the floor is not grown to it: a drag moves the edge it was given hold of, and stretching the far end out to a minimum nobody asked for is an edit of its own — it simply cannot shrink further. (2) The **drawing** floor: a clip on screen is drawn no thinner than `grip_w`, because the data floor alone does not answer the report — with `snap = 0` (which is what `gui_multitrack` sets, deliberately) one sample is far under a pixel at any musical zoom, so the clip would still vanish. Every DAW rounds a short item up the same way and for the same reason; what is *not* rounded up is a clip off the window entirely, since a sliver at the edge would claim a clip is there. (3) The **affordance** floor: a clip too narrow for two grips used to get none — "all body", movable and never growable — and now keeps one, its end (the start when the end is the one off screen), which is the edge that gets the reader out of the corner.

  The three are one property stated at three layers: **every state a gesture can reach, it can leave by the same gesture.** The regression test is that round trip end to end — shrink a clip against its floor, check it is still drawn and still at least a grip wide, then grab it and grow it back.

- ✅ **The grip was lit half a clip away from where it acts, and the press on it went to the notes** *(found 2026-08-13 by the user, on the roll clips of `gui_composer` and `gui_multitrack`, immediately after the note-clamp fix below; fixed the same day)*. Two halves of one rule, which is why they are one entry. The **press** on a clip's grip fell through to the clip's bodies — they were offered every press first — so those dozen pixels went to whatever the body found under them: a note at the end of a roll clip moved instead of the clip's edge, with the cursor sitting on the arrow that promised the opposite. And the **drawing** lit the grip of whichever *half* of the clip the pointer was in, so a strip `grip_w` wide announced itself from the middle of the material, where there is no resize at all.

  The two are the same defect seen from either end: **an affordance is drawn where it acts, and the thing that is drawn is the thing that acts.** Lit by half, the mark was a promise the press could not keep, and both possible fixes on their own make it worse — widening the grab zone to the half would swallow half the roll, and leaving the press with the bodies while lighting only the strip would mean the arrow never does anything. So the strip is the unit on both sides: `clip_grip_at` lights the grip the pointer is **on** (`clip_grips`, the same function `interact::clip_part` hit-tests through, so the pixels cannot disagree), and `gestures::press` offers the bodies everything *except* a grip. A pointer over the material lights nothing, because nothing there resizes; the body keeps the whole rectangle whenever the clip's ends are off screen or it is too narrow to carry a grip, which is the case that already existed and is unchanged.

  One behaviour changed on the way and it is deliberate: a clip being **moved** now lights no grip. The held-clip rule exists because a clip moves in `snap` steps and the pointer does not, so a resize keeps the edge it holds wherever the pointer drifts — but a *move* holds no edge, and the grip it used to light under the drifting pointer offered a gesture that was not the one happening. The frame test that pins the topmost-clip rule was rebuilt around the only place two clips can now both claim the pointer — where one's end strip and the other's start strip land on the same pixels — which is a sharper statement of the same invariant than the half-and-half version it replaces.

- ✅ **A note could be dragged out of the clip that draws it** *(found 2026-08-13 by the user, on the roll clips of `gui_composer` and `gui_multitrack`; fixed the same day)*. Drag a note rightwards past the clip's end and it kept going: still in the list, still played by whatever reads the edit-back, and drawn on no pixel the clip owns — a body is clipped to its clip's rectangle. It could be found again only by resizing the clip by hand, which is the report's own wording for it and the reason this is a bug rather than a quirk: the editor's whole claim is that what you drag *is* the music, and a note nobody can see is a note nobody can drag back.

  **The choice was between two habits, and the DAWs settle it in a third place neither option had.** Auto-growing the clip to take the note was the other candidate; it is what a DAW does *not* do, because a clip's length is set by its own edge, and content that lengthened its container would make one gesture — nudge a note — also move the end of the piece and the scroll under it. What real editors actually do is escape the dilemma: notes are edited in a separate piano-roll window that **draws the space past the clip's end**, shaded, so a note out there stays visible and simply does not sound. That third way is not available here — the roll *is* the clip's body on the lane, so "outside the clip" has nowhere to be drawn — and once it is off the table, stopping the note at the edge is the only option that keeps what is edited visible. It also matches what the arrangement already assumed: `gui_multitrack`'s lead cuts its notes to the clip's length when it builds the pass, so the clip was already the authority on what plays.

  **The rule is the placement's, not the roll's**, which is what makes it one fix for both examples and for the raw `clip(notes=...)` alike. `Notes::edit_limit` reads the seam that already exists — a clip's body is handed its container's axis with no gutter (`indent == 0`), the roll's own view has a keyboard gutter and navigates — and hands a `pianoroll::Limit` to the pure edit functions. A body clamps into `[0, dur]`; the roll standing alone gets `None` and is **deliberately** left unbounded, because its view spans its own content: the note lands where it was dropped, the span grows, and the axis scrolls to it. Bounding that one would take away the one thing the standalone view can do (the user named this in the same pass, on `gui_pianoroll`).

  Two details the clamp turns on. A note is clamped **whole** — its tail parks on the far edge, not its onset — since the part that would vanish is exactly the part being dragged; a note longer than the clip pins to zero instead, the near edge being the one that can be honoured. And a **block** move stops on its last tail with the spread intact, the same way it already stopped on time zero and on the pitch window. The same edge holds a resize's end and an `osc` marker. Shortening a clip over notes still deletes nothing: they stay in the list and come back when it is stretched again. Covered at both levels — the pure functions in `pianoroll`, the seam in `notes` (both placements in one test, since the bug was one of them behaving like the other), and a real press-and-drag on a clip through the gesture machine.

  **Open, and it is the client's half** *(named by the user in the same message)*: the host clamps a gesture, and the arrangement model behind it (`clausters.form`) holds a structure that is only partially evaluated — a generator element is bounced to be drawn — so what a clamped note means for an element that has not been rendered, and for one whose notes came from a pattern rather than from a list, is a question this fix does not answer. It is filed on the Python roadmap.

- ✅ **A round thing was hit-tested as its bounding box** *(found 2026-08-12, named by the user — the bounds of a circular figure, computed with Pythagoras squared, and generalized across the elements; fixed the same day)*. Everything the host draws answered the pointer through `Rect::contains`, which is right for the rectangles and the strokes and wrong for the round ones: a knob's dial is a disc inside a cell that also holds a label strip and a read-out (and whatever width the row spread it to), so pressing the name, the number or the paper in a corner grabbed the value and turned it; a notehead is an oval whose box has a quarter more area than the glyph, and on a dense page that quarter is where the stem, the beam and the note on the next line live.

  The same audit, by eye on `gui_panel`, found two more and they are the same bug without a circle in them: a **slider**'s track area is the cell minus the label and the read-out, and what is drawn in it is a groove a few pixels thick with a short grip riding it, so a press anywhere in a band several times that thick jumped the value; a **toggle** stretched across a row is a small box with a word beside it and a great deal of air after that, and the air flipped the value. So the entry is not about circles: **a hit-test is the shape that was drawn**, whatever that shape is. The knob, the slider and the toggle are verified by eye — the toggle in two passes, since the first bounded its width and left the cell's full height, and in a row of mixed controls that height is the tallest sibling's: a shape is two axes, and bounding one of them reads exactly like bounding neither. The rest of the chrome has not been done at all, which is E23, and why this entry closes on the seam rather than on the catalog.

  Which is why it ends at the trait rather than as a guard per leaf — the user asked the question directly, and it is the right one. `Element::hit_area` returns a `HitArea` (`Rect` by default, `Disc`, `Ellipse`), and `gestures::element::press` is the **one** place it is applied, adding the metrics' `hit_slop`: an element states its shape once, never writes the filter, and a point off it reads exactly as a decline, so the press falls back to the chain the way any declined press does. A leaf that says nothing keeps the whole rectangle it always had, so nothing else in the catalog moved. Each shape is read off the same function the drawing places it with (`knob_disc`, `slider_groove`, `toggle_hit`), so the two cannot disagree. The one deliberate exception is the **overlay** route: a modal is offered the press precisely *because* it is outside as often as because it is inside (a click on the window closes a menu's list), so it goes through the unfiltered door.

  `host::graphics::shape` is the shared answer — `in_disc`, `in_ellipse`, `dist2` — and it is **squared throughout**: a distance in a hit-test is compared (against a radius, against the best so far) and never reported, and squaring preserves the order, so the square root is work with no reader. The ellipse is the same test normalized per axis. Three call sites so far: the knob presses only on the disc the drawing places (`controls::knob_disc` is now one function for both, so they cannot disagree) plus the `hit_slop` every small target gets, declining otherwise so those pixels keep meaning whatever the window means by them; the break-point grab drops its `sqrt`; and the score's hit index carries a `HitShape` per entry, `Ellipse` for a glyph in the SMuFL notehead range and `Rect` for everything else — per entry because only the indexer knows what it measured. What stays rectangular is what *is* rectangular: patcher boxes and ports, roll notes, keys, lanes, clips.

- ✅ **A selection was a band of pixels, so it could select where there are no samples** *(found 2026-08-12, by eye on `gui_bulk` at sample-level zoom, and stated as a rule by the user; fixed the same day)*. Zoomed in until consecutive samples stand apart with a dot on each, a sweep drew its band starting and ending **between** two dots: a region holding no data, which can be neither played nor cut nor read back as anything. The sweep was in pixel coordinates all the way down — `sample_at` gives a fractional position and nothing rounded it — so the number the `"selection"` event reported was a length in fractions of a sample.

  What the selection *means* is a count of samples, so the axis it is measured on is the sample: every write goes through `timeline::snap_selection` (the sweep) or `.round()` (the `/gui_set` keys, the def-time seed). Two calls inside that, both the user's, and both are about the selection agreeing with the hand rather than with the pixels. **Which samples**: the ones the sweep *passed over* — `ceil`/`floor` — not the ones it came nearest. Rounding to the nearest takes a sample in half a sample-width before the cursor arrives and drops one it has already passed, so at sample zoom the band leads and lags the hand; a sweep that has reached no sample selects none, which is the click it started as. **Where the band's edges are**: halfway between the last selected sample and the first one left out (`[start - 0.5, start + len - 0.5]`), because each sample owns the half-width on either side of it. Drawn edge-to-edge the band ends *on* the last selected sample and reads as excluding it — the ambiguity only a sample-level zoom makes visible.

  The rule is the **navigation group's**, not the waveform's, which is what makes it hold for the spectrogram the user named in the same breath: an editor lays the two views over one timeline and they read one selection, so a rule living in either view would have to be written twice and would disagree at the edges. `docs/gui-protocol.md` and both clients' builders say it now.

- ✅ **The trace drew outside its lane, again — so the drawing bounds itself now** *(found 2026-08-12, by eye on `gui_bulk` right after the two renderers became one; named by the user as a recurrence of the clip fix, and as a behavior that should be general; fixed the same day)*. Zoomed into the polyline regime, the sample discs and the stroke around them sat **on the vertical ruler beside the view** and over the labels under it. It is the same overshoot a clip was taught to mask a commit earlier: a trace resolved per visible pixel reads the sample before the left edge and the one after the right — it must, or the line would start and end inside the box — and a value outside the vertical window lands outside the lane the same way.

  What the recurrence says is that the mask was in the wrong place. A clip masks its bodies because a clip is a coordinate system, and that is right; but a free-standing `waveform` is **nobody's content**, so no container mask is in force and the overshoot goes wherever it goes. Fixing it per placement means every future container that holds a trace re-learns this. So the answer is the drawing's: `trace::draw_channel` narrows the mesh's clip to the lane it was handed — never widens it, and puts back whatever it found — which bounds *every* destination at once, on both axes, including the vertical one whose doc comment already claimed a clip rectangle was cutting it. Two consequences worth stating: a column widened to the trace weight at either end of the rect is shaved by the lane rather than hanging over it, and the tests that counted vertices through the edges now count them inside. The regression test draws a signal that leaves the window on both axes and asserts no vertex outside the lane.

- ✅ **Two renderers drew a navigable waveform, and only one of them was right** *(found 2026-08-12, by eye on `gui_bulk` closing the visual pass, and named by the user: “why are there different ways of drawing a waveform with zoom that do the same thing? there should be only one”; fixed the same day)*. Zoomed past the polyline threshold, the trace **arrived at a sample and stopped**: a line came in from the left edge, reached the one visible sample, and nothing left it. The signal continues on both sides, so the segment to the sample past the right edge was simply never built — the pipeline took `floor(start)` and `ceil(start + len)`, exclusive, which is one sample short on the right.

  The off-by-one is not the entry. The entry is that there were **two implementations of one picture**: the mesh renderer (`signal::trace::draw_channel` — a clip's take, a plot, a meter's history) and the navigable view's own `wgpu` pipeline, which built its own vertex buffer through its own WGSL. The previous entry below folded the *live* renderers into the mesh one and left the pipeline standing, on the strength of `tests/gpu_slot_cost.rs` — which, read again, says the opposite of what it was taken to say: the per-column **computation** dominates so completely that where the vertices land is noise. The pipeline bought nothing and cost a second place to write the same drawing, so it drifted in four ways at once: the missing right-hand neighbour, sample dots as **squares** where the mesh draws discs, the regime split on the other side of the threshold (`<=` against `<`), and a minimum ink of one physical pixel against the mesh's trace weight.

  So the pipeline is gone — `WaveformRenderer`, `WaveformGeometry`, `Mode`, `waveform.wgsl`, the vertex buffer, the per-lane `Framing` and the whole upload/draw walk in `frame::render`. `waveform.rs` is now the **data** (raw samples plus a peak pyramid per channel) and a navigable view's **vertical state**; `waveform::draw_lane` places it on its two axes and hands it to `draw_channel`, which every signal against time now reaches the screen through. `Renderers` holds one pipeline, the spectrogram's, and that asymmetry is the point: a spectrogram's picture is a texture sampled once per pixel, which is work triangles cannot do, while a waveform's columns were folded on the CPU either way. The regression tests are the two behaviours that differed: a window narrower than one sample **enters and leaves** the rect, and a cache-only source (a pyramid with no samples) never enters the line regime at all — which is also a real fix, since asking it for a sample reads an empty buffer and the wave vanishes exactly where the reader zoomed in to look at it. `docs/decisions.md` carries the rationale and what was given up with the pipeline (`Framing` is the spectrogram's alone now); `docs/architecture.md`, the skill and the slot-cost test's own narrative are updated.

- ✅ **A take drew nothing in its clip: an element drawn as a body reached its own drawing** *(found 2026-08-12, by eye on `gui_multitrack` opening the visual pass; fixed the same day)*. The three lanes of files came up as empty frames with a line across the middle. The line is what named the cause: it is the baseline `draw_take` puts down before the trace, so the data was there and the trace was not — the element was drawing, and drawing the wrong picture. A clip's body is an element like any other, and the frame hands it its container's axis on `Ctx::time`; the roll and the curve read it, and the signal element did not — so it fell through to `plot::draw`, which reads the **inline samples** and rules its own axes. A bulk take has no inline samples (it has its pyramid), so the plot had nothing to read, and `SignalElement::draw_body` — the body drawing the model documents, with the trace against the clip's span and no chrome of its own — was reachable from nobody. The fix is the missing fork, ahead of the presentations: placed on somebody else's axis, a stored non-navigable signal draws as a body. What let it survive is that the two shapes agree wherever a take is a sketch — an inline `data` clip drew *something* through the plot, which is what the tests and the older examples carry — so only a mapped take, the case the wire exists for, showed it. The regression test is exactly that case: a pyramid, zero samples in the tree, and one column per pixel of the clip demanded (36 vertices without the fix — the baseline alone — over 704 pixels).

- ⬜ **An element's look does not answer for the space it is given** *(found 2026-08-12, by eye on `gui_multitrack`; the rule stated by the user in the same pass)*. Three things the multitrack shows, and they are one entry because they are one rule: **the visual properties of an element belong to the element, and each one answers what it does when its size and its visible space change — including when part of it leaves the screen.** Today that answer is the container's or nobody's, so each of the three is decided at a different place and none of them by the thing being drawn.

  - **A clip's name follows the clamped rectangle instead of the clip.** `clip_rect` is the *visible* part of the clip (`clip_x_range` clamps the span to the lane body), and `draw_clip_label` writes at `cr.x + m.pad` — so a clip scrolled half off the left has its name at the screen edge rather than at its own start, and every clip that runs off the left stacks its name in the same place (two names overlap in the screenshot). It is also written with plain `font::text`: no width, so it spills out of a box narrower than the string, where the lane's *header* right beside it truncates with an ellipsis (`font::text_ellipsis`, the light widgets' rule). The roll's cursor readout does the same thing in the same clip.
  - **The clip's border reads as a grip, at whichever pixel the clamp landed on.** The border is drawn on the clamped rectangle too, so a clip half off-screen gets a vertical edge *in the middle of its material* that looks exactly like the resize affordance the open entry below wants to draw — and where two clips overlap, one's fill buries the other's, so the edge disappears. Whatever shape the grip takes, it must be drawn where the clip's **end** is and be absent when that end is not on screen; and its vertical extent has to mean something, which today it does not (a lane's height is the track's, not the clip's).
  - **The transport's read-out is lost as the window narrows.** The counter beside the buttons is cut and then gone, with no rule about what goes first — a strip that cannot fit its contents has to say what it drops (a shorter unit, fewer forms, an ellipsis), the way a lane header already drops its controls before its name.

  It is an L-track question (what the look says a part is) and it overlaps the affordances entry below: answering either alone would give the two a different vocabulary, so they converge into one milestone. What this entry adds is the general half — the rule is not "clamp the label too", it is that an element carries its own answer to *less room than I want* and the container asks it, rather than each drawing site improvising a clamp.

  **What shipped the same day, and what deliberately did not.** The two texts now stay in the box that holds them, each dropping the end that matters least: a clip's name truncates with the ellipsis to its own visible rectangle (`font::text_ellipsis`, the rule the lane header beside it already followed) and draws nothing at all when there is no room for a glyph, so a sliver of a clip no longer writes over the neighbour that starts there; the roll's cursor read-out is right-aligned **inside** the grid and gives up its tail — the time — keeping the note name, which is the half a pointer on a pitch is asking for (unclamped it started 18 px left of a 40 px body, which is what the regression test pins). The transport read-out was the **example's**, not the host's: the buttons were elastic and split the row four ways, so the counter — the one thing there whose width is its content — got a quarter of the row and came back ellipsized. The buttons take a fixed width now (chrome is a fixed strip; a caption's budget is its box divided by the character cell), and the host's ellipsis stands as the honest answer to a window narrower than the text. **Left open, and it is the milestone**: the two fixes are each a clamp at a drawing site, which is exactly what the rule above says not to settle for — an element still cannot be *asked* what it does with less room than it wants. And the grip stays undrawn (below).

  **A fourth thing the same pass asked for, and it is the general half arriving early: the text plate.** With the two texts inside their boxes they were still *illegible*, because a caption over a picture is written in one color and the picture draws in another — wherever the two meet the line dissolves into the material, and the denser the take the less of the name survives. So a line drawn over a picture now sits on a ground of its own: `graphics::plate_text` — a translucent, rounded box under the glyphs, truncated to the room it was given, returning the plate it laid down so a caller stacking two knows where the first ended. It is **one piece in the models' shared vocabulary** (beside `corner_text`, the same reach) rather than a clip feature: its signature names no clip, no lane and no axis, so the roll's read-out took it unchanged, and the two properties that make it a *ground* are what the test pins — it is as wide as the line it grounds (a truncated caption takes a truncated plate, never the plate of the string it would have drawn) and where not even the ellipsis fits there is no plate at all. Both of its numbers are **roles**, not literals at the drawing site: `plate` (the color, whose alpha *is* the role — an opaque plate would be a label with a background, a different thing) and `plate_radius` (the corner), so the density scale and a window's `ui_scale` carry them like everything else. Documented in the client book's color and size tables and in `docs/gui-protocol.md`'s text paragraph.

- ⬜ **A take is drawn in amplitude and heard in decibels** *(found 2026-08-12, by ear on `gui_multitrack` after the envelope fix below; deferred by decision — it belongs with the revision of multitrack playback and the arrangement model)*. With the picture and the voice finally built from one description, the sound of an audio clip still stops about a **quarter of the clip** before the playhead reaches its end. What is left is not a timing error: it is that the trace is drawn **linearly in amplitude** and heard **logarithmically**. The example's fastest take (`exp(-5t)`) is at 0.024 of full scale three quarters through — **−33 dB**, silence to the ear — while the drawing still puts a line there, and the minimum-ink floor *guarantees* it does: a column is inked at least one pixel, which is exactly the rule that keeps a flat stretch visible. The two rules are right on their own and disagree here.

  The measurements, so the next pass does not re-derive them: at three quarters of its span the example's three takes sit at −32.6, −7.8 and −13.0 dB (decays 5.0, 1.2, 2.0), and at the very end at −43.4, −10.4 and −17.4. The slow ones are plainly audible to the end, so what the ear generalizes from is the fast one — which is also the one whose picture is emptiest.

  Two answers, and the choice is a design rather than a fix. **Draw the body on a dB scale** — the amplitude axis already *labels* in `db`/`bits`/`percent` (the ruler's units of full scale), but the geometry is linear, so this means a display mapping a take body can carry, and a decision about its floor. Or **accept the divergence and say so**: an editor's waveform is amplitude, and a reader learns to read a decay. Whichever wins, it wants the same call the trace renderers took — one rule for every view of a signal, not one for a clip body.

- ✅ **The take sounded a different envelope than the one it drew** *(found 2026-08-12, by ear on `gui_multitrack`: the sound stopped a little before the playhead reached the end of a clip; fixed the same day)*. The first suspicion was the playhead, and it was wrong — worth recording, because the measurements are what turned it around. `Transport.anchor` already compensates the latency (the line marks where the audio is *heard*, not where it was scheduled), the query it takes after starting the pass costs **1.9 ms**, and the engine's sample clock runs ~1 % behind wall time, which over a three-second clip is 30 ms: none of it is a quarter of a beat, and none of it explains a gap that shows on a clean play.

  What the reader was seeing is that **the picture and the sound were two different envelopes**. The take drawn on the lane is a file written with `exp(-decay·t/secs)`; the instrument playing it was a ready-made percussive envelope (`Env.perc`, curve −4). Rendered offline and measured at the same instants, a four-beat take at decay 1.2 is at **0.301** in the picture and **0.001** in the sound at the clip's end — inaudible from about three-quarters through, where the drawing still shows material. Hence *a little before*, hence every audio clip and not the roll (whose notes are drawn as bars, so nothing contradicts them), and hence the spectral clip being fine: the sweep's file and its synth already share one envelope, which the same measurement confirms end to end.

  The fix is in the example, and it is the example's own headline claim — *what sounds is what is drawn* — finally holding for the takes: `multi_tone` takes `decay` as a control beside `secs` and plays the file's curve (a ramp over the clip's span, exponentiated, the ramp being what frees the synth at the end). The three takes now match their files to within 0.008 at every position. **The general lesson is worth more than the fix**: an example that draws one signal and sounds another reads as a *timing* defect, and sends the reader after the clock. When a view and a voice are meant to be the same material, they have to be built from the same description — the arrangement's rule, one level down.

- ✅ **A clip did not bound what it drew** *(found 2026-08-12, by eye on `gui_multitrack` zooming into a take; fixed the same day)*. Zoomed past the polyline threshold, the sample dots at both ends of a clip were drawn **outside** it, on the lane beside it. The drawing is right to read them: a trace resolved per visible pixel takes the sample before the left edge and the one after the right, or the line would start and end inside the box — but once each sample is also *marked*, that overshoot stops being a line running off the edge and becomes a pair of discs sitting where no clip is.

  Fixed as containment rather than as a trim, and that is the whole of it: a clip hands its bodies a **mask of its own box** (`layout::place_on_time`, intersected with whatever mask it inherited), so every drawing placed inside one is bounded by it and no renderer has to know where the edges are. A clip is a coordinate system; one that does not bound its contents is a rectangle they happen to start in. The regression test draws twelve samples over a 400-pixel clip — far enough apart that the polyline and every dot are drawn — and asserts not one vertex of the bodies lands outside the clip's rectangle.

- ⬜ **The other text over pictures has no plate yet** *(found 2026-08-12, answering where `plate_text` belongs after it shipped)*. The plate is in the models' shared vocabulary and two callers use it — a clip's name and the roll's cursor read-out. Five more sites draw text over a *picture* with a bare `font::text` and want the same ground, and they are one change rather than five, since each is one call: the heavy views' cursor read-out (`frame/draw.rs`, time + value over the waveform or the spectrogram), the `plot`'s (`signal/plot.rs`, `440 HZ  -12.3 DB`), the goniometer's and the spectrum's and the scope's labels (`signal/phasescope.rs`, `meters::label_strip`), and `corner_text` itself, which is the same problem in the opposite corner. What deliberately stays **unplated** is text on **chrome** — ruler labels, a lane's header, every control — because there the ground is already the widget's own; the plate is for where the background is *material*. Not done with the multitrack pass because adopting them is an eye pass over the live views (four of the five are in `gui_analyzer`), and that is a session of its own rather than a change to wave through.

- ✅ **The patcher's model is not behind the `patcher` feature** *(found 2026-08-12, auditing where the host's files sit for K14)*. K13 gave the two element families a feature each, and `notation` gates both halves — `host::score` (the model) and `elements/score.rs` (the leaf). `patcher` gates only the leaf: `elements/patch.rs` and `widget::parse::parse_patch`/`parse_ports` carry the attribute, and `host/graphics/patch.rs` — 1570 lines of boxes, cords, hit-testing and geometry — does not, so a build that dropped the family still compiles all of it and nothing can reach it. It is not a break (the matrix is clean either way, and a `pub mod` warns about nothing), which is exactly why it survived: the asymmetry is invisible to every check that exists, and only shows up reading the two families side by side. *(fixed 2026-08-12, in its own commit)*. One `#[cfg]` line in `host/graphics/mod.rs`, and the floor build reported nothing: every caller of the model was already gated, which is the confirmation that only the module declaration had been missed. The seventeen configurations stay clean and a build without the family now drops the whole 1570 lines instead of compiling them for nobody.

- ✅ **One signal, three inkings: the trace renderers agree on the columns and on nothing else** *(found 2026-08-11, fixed the same day, reading the crate's drawing paths after an eye pass noticed a waveform filled at one zoom and outlined at another)*. E6 says the three min/max implementations collapsed into `signal::trace`, and they did — **the stored ones**. The live pair was never folded in, so a signal against time is drawn by three renderers today: the GPU one (`waveform.rs`'s `Mode::{Columns, Line}`), the mesh one (`signal::trace::draw_channel`, the plot and a clip's take) and `meters::trace_lane` (the audio-rate oscilloscope), with `meters::draw_scope` a fourth polyline for a control bus's history. They share the *column source* (`Trace`/`WaveformData::column`) and nothing about how a column is inked.

  **What the reader sees, and it is four disagreements rather than one.** *(1)* Only the GPU path extends a column to **zero** (`lo.min(0.0)`/`hi.max(0.0)`): for bipolar audio the two agree by accident, and anything offset from zero — an envelope, an automation, a unipolar take, a DC-bearing buffer — is a body filled from the baseline in a navigable view and a floating band in the clip of the same samples. *(2)* Only the GPU path ignores `min`/`max`: `amp_to_clip` is pinned to ±1 through `AMP_MARGIN`, and the slot is handed a `WaveformData` and never the element's value bounds, so a navigable view of data outside ±1 leaves clip space while the mesh paths map it. *(3)* Only the mesh path guarantees a **minimum ink** (`max(cw, style.width)`, with a test), which is the fix a flat stretch of an envelope already needed once — the GPU path and `trace_lane` can still collapse one to sub-pixel. *(4)* The optical weight is three numbers: a hardware `LineStrip`, `trace_w`, and `divider_w` for `trace_lane`'s columns. On top of it `LINE_THRESHOLD` is written twice (`waveform.rs`, `signal/trace.rs`) with a comment asking the two not to drift, and `trace_lane` decides the regime by a third rule entirely (`frames > columns * 2`).

  **The fill itself is not the bug** and should be said plainly, since that is what an eye pass reports first: a filled band above the threshold and a polyline below it is the three-regime design, and all three renderers do it. What is a defect is that they disagree about *what* they fill.

  Two decisions to make before touching anything, because either one alone would leave the divergence. **Does a column extend to zero?** It is a property of the *element* — what the signal means — not of the destination that happens to draw it, so whichever way it goes it goes for all three. And **does the GPU path take the value bounds?** It is the one that would move a public surface (the slot is filled with a `WaveformData`; the bounds would ride with it, the way the texture slot's analysis parameters already do). Then `trace_lane` and `draw_scope` fold into `draw_channel`, which is the copy E6 left outside and the only one with a regime rule of its own, and the two `LINE_THRESHOLD`s become one.

  **What shipped, and the first decision was answered by an eye pass rather than by the argument above.** It landed as written — one shared fill rule, "a column reaches the baseline when the domain straddles zero" — and a screenshot at a zoom where cycles are legible killed it on sight: there a column covers two or three samples that all sit near +0.6, and filling to zero **inks a band the signal was never in**. The question that survives is *at which zoom a fill belongs*, and it has no answer, which the **subsonic** case proves outright: a 1 Hz curve has far more samples than the screen has pixels at any zoom showing a cycle, so every "fill once the samples stop fitting" rule fills it, and a cycle a second is exactly the curve a fill destroys. So the rule is that there is no rule — **a column is drawn as it is measured, everywhere** — and the solid body of a zoomed-out waveform is the data filling it (audio crosses zero many times per column), not something the drawing adds. The mesh renderers were right all along; the pipeline's clamp was the anomaly, and closing the divergence the other way would have propagated it. `docs/decisions.md` carries both this and the domain decision.

  The other three closed as planned. **The domain** (`min`/`max`, `[-1, 1]` by default) reaches the pipeline through `SlotFrame::Waveform`, and the **ruler and the cursor readout follow the same pair** — where an element names a domain of its own the strip rules a plain value axis, since `db`/`bits`/`percent` are units of full scale; `Vertical` replaced the `Option<(f64, FreqScale, f64)>` whose `None` silently meant "a waveform, in amplitude". **The minimum ink** is now the pipeline's too (`lane_h_px` reaches `upload_geometry` for it). And **`meters::trace_lane` and `draw_scope` fold into `draw_channel`**, so the live views summarize instead of drawing a segment per sample — the copy E6 left outside is gone, with its third regime rule, and `LINE_THRESHOLD` is one `pub const` the mesh path re-exports.

  **One capability fell out of the same pass, and it is an affordance more than a drawing.** Zoomed past the polyline threshold, each sample is now **marked with a dot** once consecutive samples stand three `point_radius` apart (`trace::dots_fit`, the collision rule the ruler's labels already use): the line between samples is an interpolation the drawing invents, and the dot says which points of it are data. It is sized as a **curve's break-point** on purpose — that is what sample-level editing will grab, so the two read as the same kind of target the day the second becomes draggable, and it connects to the open "the editing gestures want affordances" entry below. Both renderers draw them; on the GPU they are their own vertex range through the column pipeline, since a `LineStrip` cannot draw a quad.

  Ported in the same change: `docs/gui-protocol.md`, both `waveform` builders (which grew `min`/`max`, until now reachable only through the escape hatch), and `docs/decisions.md`. Green on 686 tests, the 17-configuration matrix, `pyright` at zero and the web suite; checked by eye on `gui_rulers`.

- ✅ **The crate did not build without its default feature** *(found 2026-08-11 running the def-family matrix while closing K12; fixed the same day, in its own commit)*. `cargo build --no-default-features` failed with 19 errors, and had for some time: three `use` lines in `host/gui/app.rs` carried a `#[cfg(feature = "midi")]` that belongs to none of them — the live-tick path (`live::retention_window`, `tree_animates`, `tree_has_live_widget`), the key vocabulary (`element::Key`) and the live element context (`element::Live`) are what a *window* reads every frame, MIDI or no MIDI. The attribute was almost certainly added to silence an unused-import warning in a no-MIDI build; only `timeline::group_key` was genuinely MIDI-only, and it keeps its cfg. Nothing but the imports moved.

  What made it survive is what K13 is about: **nothing ever built this crate any other way.** CI lints the default set, `check-wasm.sh` builds the default set for another target, and no habit reaches a configuration where a feature is *off*. The root crate learned this with its def families and answered it with a matrix; this crate had a feature matrix and no matrix runner, which is precisely the gap `clients/gui/check-features.sh` closes — and the fix is a good measure of the cost of not having one, since the break is four lines and had been unbuildable indefinitely.

- ✅ **A `standalone` build could not load a single persisted SynthDef** *(found 2026-08-11 running `gui_standalone`'s own launch line while closing K11; fixed the same day, in its own commit)*. The bundle booted, the window opened and the knob turned — and nothing sounded, with three warnings the example never mentions: `persisted SynthDefs found but this build lacks the 'synth' feature`, then the boot's `/synth_new: SynthDef not found`, then one `/node_set: node 1000 not found` per turn of the knob. The cause is one line of `Cargo.toml`: the server is linked `default-features = false` (so a standalone build does not also pull MIDI and PipeWire, which is right) and `standalone` never added a def family back, so **the feature the example documents could not run the example**. `synth` is in the feature now; `faust` stays the separate `standalone-faust` because it needs libfaust at build time, and `synth` needs nothing. What made it survive this long is exactly what `CLAUDE.md` says about the examples: nothing runs them, and this one's real test is a `cargo run` line printed for a human to paste.

- ✅ **Ctrl+Backspace was missing from the field's editing alphabet** *(found 2026-08-11 while reading K10's text pass; fixed the same day)*. The `text` field walked words with Ctrl+arrow and selected them with Shift, but **deleted only one character at a time** — so removing a typed word meant holding Backspace, in a field whose whole point is that a typed string is delivered live. It is the plainest gap a keyboard has: every editor on either platform has it, and it is the one an unfamiliar user reaches for first.

  The unit is **one run per press**, and that was the second reading rather than the first: `"a, b"` loses `b`, then `", "`, then `a` — where the *motion* crosses separators and the word behind them in a single step. They differ on purpose. A caret that overshoots costs a keystroke to come back; a delete that overshoots costs text, so the destructive one takes the smaller step, which is what every editor a user arrives from does.

  Both are built on the same scan (`run_start_left`/`run_end_right`, one run; `word_start_left`/`word_end_right`, the same scan applied twice when the first run is separators), so the two can never drift into disagreeing about where a word begins. Ctrl+Delete lands with Ctrl+Backspace because it is the same arm mirrored, and a selection still wins over the run in both, the rule the plain Backspace/Delete already follow. No wire change and no front change: the modifiers already ride beside the key.

- ✅ **A by-eye example must not time out** *(found 2026-08-11 while closing K9; fixed the same day)*. `gui_text.py` script-run replayed its timed changes and then **closed itself at thirty seconds**, which is exactly long enough to miss what you opened it for: the example is the manual test surface, and a window that ends on a clock ends the test before the reader has finished reading. Script-run it now holds until the window is closed (the `gui_patch1.py` shape); the `seconds` bound stays for a cell run, where a notebook wants the loop to give the prompt back. Worth stating as a rule rather than a fix: **an example that exists to be looked at ends when the person looking at it says so.**

- ✅ **A query answers what a widget is, not what it was defined as** *(found 2026-08-10 while closing K5; fixed the same day)*. Two surfaces say what a widget holds and a gesture writes only one of them: the **registry** keeps the document (what the script sent, kept current by every `/gui_set`), the **render tree** keeps the widget as the user has since left it — so `/gui_query` reported a dragged control's def-time value forever, a moved clip's original placement, an edited curve's original points. It is the one answer a script cannot get any other way, since the `/gui_event` that announced the edit is a stream you had to be listening to.

  `Widget::info()` is the overlay, and it is what finally gives `Element::info` the consumer it has lacked since K1: an element answers for itself, a built-in is an arm in the query pass whose row disappears as the leaf ports (the shape `needs()` already has), and a **clip answers for its bodies too**, since a body carries no id and the routing is the one a `/gui_set` of a body prop already takes. Two rules keep it from becoming a second source of truth: only what a **gesture** can change belongs there, named by the prop that sets it; and a non-scalar rides as the JSON string its own `/gui_set` accepts (`points`, `notes`, `osc`), so **what a query gives back is what a set would take** — `pianoroll::notes_json`/`osc_json` and `ScrollView::info` were the missing inverses.

- ✅ **A held drag that reports nothing did not repaint** *(found 2026-08-10 while porting `score` in K7; fixed the same day, and it is a K6 regression)*. The machine repainted an element's drag **only when it reported** — the redraw rode along with the delivery — which is right for every control (a value moved, so it is both drawn and sent) and wrong for the two drags whose whole effect is a picture: extending a `text` selection, and a `score`'s element crossing a diatonic step. K6 introduced it by retiring `Drag::TextSelect`, whose arm pushed the repaint by hand; the port would have shipped a score whose displacement only appeared on windows that animate for some other reason. The fix states the rule where the phases already agree on it — **a held drag repaints, whether or not it reported** — and does it in the machine rather than asking every element to remember, which is the same reason `Claim::Take` redraws on the press.

- ✅ **An example wrote captions longer than the boxes it put them in** *(found 2026-08-10, by eye on `gui_analyzer.py` while checking the tick seam; fixed the same day)*. The spectrum's label asked for 480 px of the ~374 its half of the row had, so it came back cut. The **host is right**: a single line that does not fit is truncated with an ellipsis rather than bleeding into the neighbour, which is the rule the light widgets have always followed. What was wrong is the example — a caption written for a full-width view and then placed in a half-width one — and the fix is the labels plus the comment saying why, since the next reader will otherwise read the ellipsis as a bug in the widget. Worth one line here because it is the second time an example's *text* has been the finding of an eye pass, and the measurement is cheap: at `text_scale` 2.0 a character is 12 physical pixels wide, so a caption's budget is the box's width divided by twelve.

- ✅ **The goniometer's figure jumped, and the signal was the staircase** *(found 2026-08-10, by eye on `gui_analyzer.py` while checking the tick seam; confirmed against the pre-tick build — it predates the seam — and fixed the same day)*. The image snapped rather than swept while the script moved it, and the first guess was the drawing: the phase view has no trigger, so its window holds a non-integer number of periods and the trace's ends need not meet. The cause was upstream and simpler. A control is applied **in steps at block boundaries**, and the sweep wrote `wm`/`ws`/`freq` about thirty times a second — so the stereo weights moved in a staircase, and a stepped `freq` breaks the sine's phase, which a goniometer shows as the figure jumping. The example lags the three controls (`control(..., lag=0.05)`, the server's own smoother), so what the views analyze is the continuous signal the script means rather than the staircase the messages arrive as. What the phase view *itself* still has no answer for is stated in "Future directions" below, since it is a design and not this fix: a window with no alignment.

  Worth keeping because of the reflex it corrects: **a live view is a measurement, so an unsteady picture is a claim about the signal before it is a complaint about the drawing.** The A/B against the previous host is what settled it, and the same question is worth asking of the next one.

- ⬜ **Persistence saves the document, not what the user did to it** *(found 2026-08-10, reading the query path while fixing the entry above)*. `def_json` is the bytes the `/gui_def` carried, and neither a `/gui_set` nor a gesture rewrites it — so a named def reloads exactly as it was sent, and a standalone bundle a user has spent an afternoon arranging reopens on the arrangement the script wrote. It is the sibling of the query gap and it is **not** the same fix: making a query honest is a read, and this is a decision about what a persisted def *is*. Two defensible answers, and the entry exists so the choice is made rather than defaulted into — a def is a **document** (it reloads as authored; a session is a separate thing the host does not have) or a def is a **state** (it reloads as left, which is what a user of a standalone bundle will expect and what makes the bundle an instrument rather than a demo). If the second wins, the mechanism is already there: `Widget::info()` says what diverged, so persisting is overlaying it on the document before the store writes — the same overlay the query does, at a different moment.

- ⬜ **The editing gestures want affordances** *(found 2026-08-10, by eye on `gui_bpf.py` and `gui_multitrack.py` after the K5 port)*. Two gestures that work correctly and that nothing on screen announces. They are one entry because they are one decision — what a *grabbable part* looks like in this host — and answering either alone would give the two a different vocabulary.

  - **Bending a segment of a curve.** Press the drawn line between two break-points and drag vertically. The curve is one trace of one weight end to end, so nothing distinguishes "a place you can bend" from "a place Ctrl adds a point to" or from the container underneath. Three shapes to choose between: a **hover state** (the segment under the pointer lit, the way a handle lights), a **midpoint handle** (a small mark at the segment's middle that is what you actually grab, which makes the target explicit and leaves plain-drag-anywhere free), or **leaving the gesture and teaching it** in the widget's documentation. The midpoint handle is the one that also answers what the current gesture cannot: on a clip body the segment drag is deliberately *off* (a body claims its points and leaves the rest to the clip — K5), so an automation clip cannot bend anything at all, and a handle is a point-like target a body could legitimately claim.
  - **Resizing a clip.** `interact::coords::clip_part` gives each end of a clip a six-pixel zone: a press there drags `dur` instead of `offset`. `track::draw_clip` paints a fill and a border, so the zone is invisible and a user finds it by accident — and below twelve pixels wide the clip is all body and there is no resize at all, which is also unannounced. What the ruler and the lane fader already do is the precedent to reuse rather than invent against: a *drawn* grip, a metrics role wide, at each end. **Decided 2026-08-12, from the multitrack pass:** a grip **at each side of the clip, revealed on hover** — so a lane of clips is not a row of permanent handles, and the affordance appears where the pointer already is. Two things it inherits from the entry above: it is drawn where the clip's **end** is (never at the pixel a clamped rectangle landed on, which is what read as a grip before there was one), and it is absent when that end is off screen — the element answering for its own visible space.

  **One real bug to fix with whichever shape wins**, found while reading this: `CLIP_EDGE_PX` is a literal `6.0` in **device** pixels, which is exactly what L6 forbids (no hit site names a number; `hit_slop` is the role) and what L8 makes visible — on a 2x display the grab zone is half the physical width it means, so a clip is hardest to resize on the screen where its edge is thinnest. Drawing the grip forces the number into the metrics table on the way, which is the right order: the affordance and the role are the same change.

    **What shipped (2026-08-12).** The grip is drawn, and the two rules it inherited from the entry above are what make it more than a rectangle: it stands at each end **of the clip**, never at the pixel the window's clamp landed on, and it is **absent when that end is off screen** — `track::clip_ends_on_screen` reads the clip's own axis (the slice of `[0, dur]` its rectangle shows) and answers for each side. It appears **on hover**, so a lane of clips is material rather than a row of handles, and it goes into the overlay mesh with the name (a take's trace would bury it). `track::clip_grips` is the one geometry the renderer and the hit-test both read, so the strip that lights up is the strip that resizes — including the narrow case, where a clip too small for two grips is all body in both. And `CLIP_EDGE_PX` is gone: the width is the **`grip_w` role**, so it is logical and resolved per window, where the literal was device pixels and halved the grab zone on a HiDPI screen. What is still open in this bullet is the **curve's** affordance — a segment you can bend still looks like a line.

    **A second pass the same day, by eye, and four of its five points were the look rather than the rule.** The grip is a **plate** now — the translucent rounded ground the captions sit on, with an **arrow** centred on its height — because an opaque strip cuts a hole in the take instead of marking an edge of it; it is `grip_w` **12**, since 6 was a target you aim at rather than one you land on; and **one** is drawn, the one on the side the pointer is on, because both ends lit is a claim about where the pointer is going that the pointer has not made. The fourth is a z-order rule and the only defect among them: clips overlap, the overlay is painted after every clip's box, so a **covered** clip lighting its edge drew its affordance *over* the clip covering it — announcing a grab the press, which takes the topmost, would never give. Only the topmost clip under the pointer carries a grip now, the rule the hit-test already followed.

    The fifth is not a defect and is worth writing down as the answer it is: **a grip does not appear when the zoom has pushed the clip's end off screen**, which reads as the gesture breaking on zoom-in. It is the rule working — there is no edge on screen to grab, and the alternative (resize from whatever pixel the window cut) is precisely the lie the grip exists to stop telling. What a zoomed-in editor owes the reader instead is a way to *reach* the end: scroll to it, or an edge-scroll while dragging, which is a separate gesture and not this affordance's job.

    **A third pass, and the one rule the first two were missing: an affordance belongs to what is *held*, not to where the pointer is.** Mid-drag the two part company, because a clip moves in `snap` steps and the pointer does not — so the held clip's grip blinked out between steps, and whatever the pointer drifted over lit up instead, including the clip being dragged *across* (whose grab was not on the table at all). The frame now takes a `Grab` beside the world: nothing held (the pointer's rules), something else held (no clip lights up), or *this* clip held by *this* side. Both fronts fill it from the same gesture machine, so the page draws what the desktop draws.

    Found with it, and **not** a grip defect: dragging a clip felt broken under zoom because the lane's `snap` is a fixed length in samples, so a zoomed-in drag moves the pointer far and the clip not at all. The example sets `snap=0` now (a clip moves by whole samples, which is what a pass over the *drawing* wants); a piece being assembled wants its grid back. What the multitrack still owes is the DAW answer — **a grid that subdivides with the zoom** rather than a constant the reader has to change by hand.

    **And what the arrow will have to say.** An edge drag means **trim** on one arrangement and **stretch** on another, and neither is implemented for audio — the current behaviour is a resize of the clip's span with the material unchanged. The symbol is where that difference becomes visible, so it is a parameter of the *gesture*: `draw_clip_grip` takes the side today and takes the meaning the day a lane declares one.
  Becomes a milestone when the vocabulary is chosen — it is an L-track question (what the look says a part is) sitting on gestures the K track has already settled.
