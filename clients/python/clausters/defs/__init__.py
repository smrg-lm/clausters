"""Definitions and server resources (port of ``sc3/synth``, Faust-first).

Placeholder — implemented in milestone C3: ``signals`` (lowercase callables
mapping Faust's Signal API, composed into the JSON graph), ``faustdef``, and
the ``node``/``bus``/``buffer``/``server`` resource allocators. SynthDef-based
definitions (``synthdef``/``ugens``) come later.
"""
