"""Base layer (port of ``sc3/base``).

C1 ships only the minimal OSC wire encoder (:mod:`clausters.base._osclib`).
The rest — ``absobject``, ``builtins``, ``stream``, ``clock``, ``main``,
``netaddr``, ``_oscinterface``, ``_midiinterface`` — arrives in C2.
"""
