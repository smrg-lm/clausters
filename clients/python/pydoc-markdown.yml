# pydoc-markdown configuration for the Clausters Python client API reference.
#
# It reads the package source *statically* (docspec parses the AST; it does not
# import the modules), so the API pages are generated without building or
# loading any native cdylib.
#
# The modules are not listed here: docs/gen_api.py finds them by walking the
# package, drives pydoc-markdown as a library over this file, and writes one
# page per module to docs/src/api/. Run that script, not pydoc-markdown alone.
# The output is plain Markdown -- no Sphinx directives.
loaders:
  - type: python
    search_path: ["."]
processors:
  # The expression, not `exclude_private`: that option is checked only after
  # "has members -> keep", so a private class with methods was published whole,
  # and it lets every dunder through (`__call__`, `__init__`). A leading
  # underscore is private here, dunders included. Modules are kept even when
  # they have nothing documented but their docstring: each one is a page.
  - type: filter
    expression: "not name.startswith('_') and default()"
    documented_only: true
    skip_empty_modules: false
  - type: smart
# No `crossref` processor, deliberately. It rewrites `#name` into a cross
# reference, and these docstrings are plain Markdown with no cross-reference
# syntax at all (see CLAUDE.md), so it had nothing to resolve -- while it did
# corrupt every hex color literal: `"#rrggbb[aa]"` came out as ``"`rrggbb`[aa]"``,
# dropping the `#` and injecting backticks inside an existing code span, which
# left the published reference documenting the wrong color format. There is no
# way to escape a `#` for it (the pattern is `\B\#`, exempting only Markdown
# anchor links). Removing it changed nothing else in the generated page.
renderer:
  type: markdown
  # No filename and no table of contents: gen_api.py writes each page and puts
  # the page's contents list after the module's docstring.
  insert_header_anchors: true
  descriptive_class_title: false
  add_method_class_prefix: true
