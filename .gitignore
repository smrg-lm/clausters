/target
/book
/third_party/faust
/third_party/verovio
.claude/settings.local.json
# Persistent per-session memory (local only, never versioned).
.claude/projects/
__pycache__/
.pytest_cache/
# Packaging metadata regenerated by every editable install.
*.egg-info/
.venv
uv.lock
# Persisted defs left on disk by examples/persistent_graphdef.py (explore, then rm -rf)
/examples/defs_store
# Where scripts/render-all.sh drops its WAVs by default (a bench, not a fixture)
/render
# Personal scratch files (nombre.notrack.md): never committed.
*.notrack/
*.notrack.md
# Working design notes and implementation plans (local only). What shipped is
# the git history and the PLAN.md roadmaps; a spec is scaffolding, not a record.
/specs/
# Subagent-driven-development scratch (ledgers, briefs, review packages).
/.superpowers/

# Artifacts the session example hands to the standalone host and reads back.
clients/python/examples/editors/session.json
clients/python/examples/editors/session-edited.json
clients/python/examples/editors/session-take.wav

# The demo renders the client examples write to the working directory
# (`python clients/python/examples/basics/envelope.py` leaves envelope.wav
# where it was run). Named one by one, so a wav meant to be committed still
# shows up.
/automation_lane.wav
/boxes_library.wav
/chain.wav
/convolution.wav
/cross.wav
/envelope.wav
/graph_maths.wav
/kernel.wav
/multichannel.wav
/offline_render.wav
/pause_resume.wav
/typed_controls.wav
/wavetables.wav
